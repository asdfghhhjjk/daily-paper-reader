---
title: Reducing Redundancy in Whole-Slide Image Patching for Scalable Indexing and Retrieval
title_zh: 减少全切片图像分块冗余以实现可扩展索引与检索
authors: "Jialiang Geng, Ghazal Alabtah, Saghir Alfasly, Wataru Uegami, H. R. Tizhoosh"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.26157v1"
tags: ["query:wsi-mil"]
score: 8.0
evidence: 提出 ARReST 减少数字病理学中全切片图像分块冗余，以实现可扩展索引和检索。
tldr: "随着数字病理学的爆发式增长及检索增强生成(RAG)等临床AI工作流的兴起，全切片图像(WSI)的高效索引与检索面临高昂存储成本的严峻挑战。本文提出ARReST框架，利用不同组织类间的冗余，剪枝对跨类区分贡献最小的“对立patch”，大幅压缩需存储的patch数量。在TCGA多器官数据集上，实现了3%~60%的索引压缩（平均14%），且检索性能无显著下降。该工作为大规模病理数据库提供了可扩展、低成本的索引方案，助力下一代检索驱动临床决策系统。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26157v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1355, \"height\": 812, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26157v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1675, \"height\": 966, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26157v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1674, \"height\": 966, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26157v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1702, \"height\": 600, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26157v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1661, \"height\": 1120, \"label\": \"Table\"}]"
motivation: WSI索引的高存储需求与有限预算间的矛盾，推动了无需牺牲检索精度的索引压缩方法研究。
method: ARReST通过识别并剪枝那些对跨类区分贡献最小的“对立patch”，大幅减少WSI需索引的patch数量。
result: "在TCGA 21个器官数据集上，索引存储节省3%~60%（平均14%±13%），检索性能无显著下降。"
conclusion: ARReST实现可扩展、成本高效的WSI索引，适用于检索增强的临床AI系统。
---

## 摘要
数字病理学的快速发展催生了对全切片图像（WSIs）高效索引与检索的迫切需求。新兴的生成式AI工作流，尤其是检索增强生成（RAG），进一步加剧了这一需求，因其依赖可靠的相似性搜索来支持高风险的临床决策。然而，高性能存储的高昂成本限制了许多医疗机构对WSI索引的可扩展性与可及性。因此，能够降低存储需求同时保持检索准确性的方法已成为关键研究重点。我们提出ARReST（对立冗余削减策略），一种原则性的对立框架，利用不同组织类别之间的冗余来显著减少每张WSI所需索引的块数量。ARReST并非仅消除类内重复块，而是识别并剪除对类间区分贡献最小的对立块，从而压缩可搜索的存档。这种有针对性的缩减在保持形态多样性和检索忠实度的前提下大幅压缩索引。通过最小化多余的块表示，ARReST降低了存储占用、减少了计算开销，并加速了大规模病理库中的相似性搜索。在TCGA（癌症基因组图谱，涵盖21个器官）上的广泛实验表明，ARReST实现了显著的索引压缩，同时保持了具有竞争力的检索性能。观察到的存储节省从3%到60%（14%±13%），可在不损害多个器官检索性能的情况下可靠实现。所提出的策略支持可扩展、成本高效的WSI索引，非常适合下一代检索驱动的临床AI系统。

## Abstract
The rapid growth of digital pathology has created an urgent need for efficient indexing and retrieval of whole slide images (WSIs). This need is intensified by emerging generative AI workflows, particularly retrieval-augmented generation (RAG), which require dependable similarity search to support high-stakes clinical decision-making. Yet the substantial cost of high-performance storage limits the scalability and accessibility of WSI indexing for many healthcare institutions. Consequently, methods that can reduce storage demands while preserving retrieval accuracy have become a critical research priority. We propose ARReST (Antithetical Redundancy Reduction Strategy), a principled oppositional framework that leverages redundancy across dissimilar tissue classes to markedly decrease the number of patches that must be indexed from each WSI. Instead of eliminating only within-class duplicates, ARReST identifies antithetical patches-those whose representations contribute minimally to cross-class discrimination-and prunes them from the searchable archive. This targeted reduction substantially compresses the index without sacrificing morphological diversity or retrieval fidelity. By minimizing superfluous patch representations, ARReST reduces storage footprint, lowers computational overhead, and accelerates similarity search across large pathology repositories. Extensive experiments on TCGA repository (The Cancer Genome Atlas with 21 organs) demonstrate that ARReST achieves significant index compression while maintaining competitive retrieval performance. The observed storage savings of 3% to 60% (14%$\pm$13%) can be reliably achieved without compromising retrieval performance for many organs. The proposed strategy enables scalable, cost-efficient WSI indexing and is well-suited for next-generation retrieval-driven clinical AI systems.

---

## 论文详细总结（自动生成）

### 1. 研究动机与核心问题

- **背景**：全切片图像（WSI）在数字病理学中数量激增，可靠索引与检索对临床AI（尤其是检索增强生成）至关重要。
- **核心矛盾**：高性能存储成本极高，WSI索引常需5%~10%的原始数据量，但总存储需求仍可达5~15 PB，对许多机构构成沉重负担。因此，在不牺牲检索精度的前提下进一步压缩索引成为迫切需求。
- **整体含义**：论文提出ARReST框架，**利用不同组织类别之间的冗余信息**（多为正常组织），从每张WSI中安全地剪除“对立”块，从而显著降低存储占用与计算开销，实现可伸缩、低成本的大规模病理图像检索。

### 2. 方法论：ARReST 对映冗余削减策略

- **核心思想**：在不同癌症类别的两张WSI间，正常/非区分性组织会表现出较高的跨类相似性。ARReST识别并移除这些**对立块（antithetical patches）**——对跨类区分贡献极小的块，以压缩索引规模。
- **关键技术流程**（见 Algorithm 1 & 2）：
  1. **构建冗余知识库**：对已标记的WSI集合，使用Yottixel等算法选取代表性块并提取特征嵌入。将来自不同类别的块嵌入两两计算相似度；若某对块的相似度低于全局相似度分布的 \(q\) 分位数（即极为相似），则将这对块加入冗余库，并从原WSI的块集合中删除。
  2. **查询时冗余修剪**：对于新输入的WSI，同样选取块并提取嵌入。逐一将每个嵌入与冗余库中的所有嵌入比较；若与库中任一嵌入的相似度低于上述阈值，则该块被移除。
  3. **编码与检索**：剩余非冗余嵌入转换为BOB二元条码，利用汉明距离在条码库中进行快速Top-k相似搜索，最后通过多数投票预测标签。

- **算法伪码摘要**：
  - **Algorithm 1**：循环所有不同类别的WSI块对，统计相似度并计算阈值 \(S_q\)；将低于 \(S_q\) 的块对存入冗余库并更新WSI块集合。
  - **Algorithm 2**：入站WSI的每个块与冗余库比较，移除匹配成功的块；对剩余块进行BOB编码，执行Top-k搜索（排除同一患者），输出预测标签。

### 3. 实验设计

- **数据集**：TCGA（The Cancer Genome Atlas）包含21个器官的11,679张WSI，按器官单独评估。
- **基准与baseline**：基础pipeline使用**Yottixel**作为块选择算法（20×放大，224×224块大小），**UNI**基础模型（在1亿张病理图像上预训练）提取特征。对比条件为**应用ARReST前后**的检索性能。
- **评估指标**：Top-1、Top-3多数投票（MV@3）、Top-5多数投票（MV@5）的**准确率**和**宏平均F1分数**，同时记录**存储节省百分比**。
- **验证方案**：5折分层分组交叉验证（Stratified Group K-Fold），严格留出患者，确保同一患者的WSI不跨训练/测试集。

### 4. 资源与算力

- 论文**未明确说明**所用的GPU型号、数量、训练时长或整体计算开销。仅提及使用公开的UNI预训练模型进行特征提取，未报告资源消耗细节。

### 5. 实验数量与充分性

- **实验数量**：针对21个器官分别独立评估，每个器官记录3种Top-k设置的准确率和F1（共6项指标）及存储节省，合计产生约126组主要结果。此外还有按组织特性分组的定性分析。
- **充分性与公平性**：
  - **优点**：覆盖多类器官，采用严格留患者交叉验证，基线清晰（前/后自身对比），分析维度较全面。
  - **局限**：仅测试了**Yottixel一种块选择算法**，未与SPLICE、SDM等其他算法组合验证；未对不同相似度分位数 \(q\) 做消融；未与其他索引压缩方法（如直接降维、采样）对比。对不平衡器官的F1下降分析较为初步，缺乏定量补救策略验证。

### 6. 主要结论与发现

- **存储压缩效果**：平均节省**14%** 的索引存储（范围3%～60%），标准差±13%，在不损害平衡类器官检索准确率的前提下显著降低存储需求。
- **性能维持情况**：
  - **准确率**：26例上升、20例不变、17例下降，**总体无显著差异**（p值高），说明存储节省可安全获得。
  - **F1分数**：16例上升、9例不变、**38例下降**，差异具有统计显著性（p<0.05），表明在**类别不平衡严重**的器官中，剪枝会损害少数类检索性能。
- **器官依赖性**：形态均质、类间易区分的器官（如宫颈、直肠、脑）性能维持最佳；高度异质或存在稀有亚型的器官（如肾上腺、软组织、头颈）F1下降明显。ARReST在多数中等变异器官上仍可保持准确率不变或提升。

### 7. 优点

- **新颖性**：首次提出利用**类间对立冗余**（而非类内重复）进行块级剪枝，思路巧妙。
- **无监督友好**：建库和剪枝仅需类别标签，不依赖像素级标注，易于推广。
- **系统兼容性**：可插入现有Yottixel、BOB条码等流程，不改变基础模型和检索架构。
- **实验规模**：在21个器官上详细评估，揭示了方法有效性和局限性，分析细致。
- **实际价值**：同时降低存储和搜索计算量（\(m \times n\) 比较次数减少），对临床部署有直接意义。

### 8. 不足与局限

- **块选择算法单一**：仅在Yottixel上验证，对SPLICE、SDM等其他算法的泛化能力未知。
- **阈值敏感且固定**：全局分位数 \(q\) 未针对器官优化，可能在异质组织中过度修剪。
- **不平衡类风险**：对稀有亚型保护不足，F1分数下降问题未给出自适应解决方案。
- **对比缺失**：未与简单的随机丢弃、PCA压缩或基于多样性的剪枝等基线对比。
- **算力未公开**：无法评估建库和推理的实际计算成本增量。
- **扩展性限制**：冗余库依赖多类对比，若引入新类别可能需重建库。

（完）
