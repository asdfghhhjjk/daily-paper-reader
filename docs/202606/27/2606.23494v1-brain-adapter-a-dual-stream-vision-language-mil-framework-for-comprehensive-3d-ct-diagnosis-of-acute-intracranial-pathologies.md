---
title: "Brain-Adapter: A Dual-Stream Vision-Language MIL Framework for Comprehensive 3D CT Diagnosis of Acute Intracranial Pathologies"
title_zh: Brain-Adapter：一种用于急性颅内病变综合3D CT诊断的双流视觉-语言MIL框架
authors: "Zhenyu Yi, Zhiyun Song, Yusong Sun, Zelin Liu, Manman Fei, Zhenhao Li, Jiaxuan Zhao, Xu Han, Lichi Zhang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23494v1"
tags: ["query:wsi-mil"]
score: 8.0
evidence: 利用原始诊断报告进行扫描级多标签分类的双流MIL框架
tldr: 针对3D脑CT急性颅内病变诊断严重依赖手动标注、传统模型语义理解不足的挑战，该文提出Brain-Adapter双流视觉语言MIL框架。核心创新在于利用预训练2D VLM和原始报告，通过文本条件注意力机制以诊断语句为语义查询对齐视觉特征，同时构建并行视觉MIL流捕捉全局扫描特性，并通过一致性约束强制双流协同。推理时采用不确定性感知优化动态校准融合预测。大量实验证明，该方法无需密集标注即显著超越最新3D模型和标准MIL方法，为3D脑CT自动化诊断提供高度可扩展且临床可行的方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23494v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1139, \"height\": 929, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23494v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 884, \"height\": 633, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23494v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1254, \"height\": 469, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23494v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1197, \"height\": 615, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23494v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1274, \"height\": 328, \"label\": \"Table\"}]"
motivation: 3D脑CT急性病变自动诊断依赖密集标注且模型语义理解有限，如何将2D VLM表征能力有效迁移至3D体数据仍是开放挑战。
method: 提出Brain-Adapter，利用预训练2D生物医学VLM和诊断报告，通过文本条件注意力动态对齐视觉与疾病概念，并行视觉MIL流捕获全局特征，一致性约束协同双流，不确定性感知模块融合预测。
result: 实验表明Brain-Adapter在3D脑CT多标签分类上显著优于现有3D模型和MIL方法，消除了密集标注需求。
conclusion: Brain-Adapter去除密集标注依赖，为急性颅内病理分析提供高性能、可扩展且临床可行的双流MIL解决方案。
---

## 摘要
3D脑部CT扫描的自动化诊断对重症监护至关重要，但由于严重依赖人工标注以及传统模型语义理解能力有限，其仍具挑战性。尽管2D基础视觉-语言模型（VLMs）已展现出卓越的泛化能力，但将其表示能力有效迁移至3D体积仍是一个开放问题。在本文中，我们提出Brain-Adapter，一种新颖的双流多实例学习（MIL）框架，利用预训练的2D生物医学VLM和原始诊断报告，实现稳健的扫描级多标签分类。具体来说，我们引入了文本条件注意力（TCA）机制，利用原始诊断语句作为语义查询，动态地将视觉线索与特定疾病概念对齐。同时，一个并行的视觉MIL流捕获全局扫描特征，并由通过大型语言模型（LLM）提取的结构化标签进行监督。为确保表示一致性，我们施加了一个一致性约束以促进两个流之间的协同。在推理过程中，一个不确定性感知精炼（UAR）模块动态校准并融合这些双流预测，以解决模糊病例。大量实验表明，我们的方法显著优于最先进的3D模型和标准MIL方法。通过消除对密集标注的依赖，Brain-Adapter为3D急性颅内病变分析提供了一种高度可扩展且临床可行的解决方案。

## Abstract
Automated diagnosis of 3D brain CT scans is essential for critical care, yet it remains challenging due to the heavy reliance on manual annotations and the limited semantic understanding of conventional models. While 2D foundation vision-language models (VLMs) have shown remarkable generalization, effectively transferring their representational power to 3D volumes remains an open problem. In this paper, we propose Brain-Adapter, a novel dual-stream multiple instance learning (MIL) framework that leverages pre-trained 2D biomedical VLMs and raw diagnostic reports for robust scan-level multi-label classification. Specifically, we introduce a Text-Conditioned Attention (TCA) mechanism, utilizing raw diagnostic sentences as semantic queries to dynamically align visual cues with specific disease concepts. Concurrently, a parallel visual MIL stream captures global scan characteristics, supervised by structured labels extracted via a Large Language Model (LLM). To ensure representation coherence, a consistency constraint enforces synergy between the two streams. During inference, an Uncertainty-Aware Refinement (UAR) module dynamically calibrates and fuses these dual-stream predictions to resolve ambiguous cases. Extensive experiments demonstrate that our method significantly outperforms state-of-the-art 3D models and standard MIL approaches. By eliminating the reliance on dense annotations, Brain-Adapter provides a highly scalable and clinically viable solution for 3D acute intracranial pathology analysis.

---

## 论文详细总结（自动生成）

### 1. 论文核心问题与整体含义
- **研究动机**：急性颅内病变（如出血、创伤等）需要紧急分诊，3D脑CT是其一线影像手段。然而，现有自动诊断面临两大瓶颈：
  - **标注成本极高**：密集的体素级或切片级标注费时且依赖专家，阻碍规模化。
  - **语义理解不足**：传统单模态深度学习模型缺乏对开放式诊断语义的泛化能力，难以处理复杂多样的病变。
- **核心问题**：如何将2D视觉-语言基础模型（VLMs）强大的跨模态语义对齐与零样本泛化能力，有效迁移到3D体积数据中，实现无需密集标注的扫描级多标签诊断。
- **整体含义**：该工作提出Brain-Adapter，通过双流多实例学习（MIL）框架，仅利用原始诊断报告作为弱监督，实现高精度、可解释的3D脑CT急性病变自动诊断，为临床提供可扩展的解决方案。

### 2. 方法论
- **整体框架**：训练阶段包含两条并行路径——文本条件注意力的“开放集对齐”分支与逻辑集监督的“视觉MIL”分支，并通过一致性损失协同；推理阶段引入不确定性感知精炼（UAR）动态融合双流输出。
- **关键技术细节**：
  - **LLM驱动的标签提取**：利用大语言模型（LLM）将非结构化诊断报告 `R` 转换为结构化多标签逻辑集 `Y ∈ {0,1}^21`（涵盖出血性发现、其他病变、病因背景）。
  - **特征编码**：使用预训练BiomedCLIP，在视觉编码器自注意力块上采用LoRA微调（rank=16），文本编码器冻结。生成切片视觉嵌入 `V`、全局报告嵌入 `t0` 及细粒度病理句子嵌入 `{tk}`。
  - **文本条件注意力（TCA）**：对每个病理描述 `k`，以其文本嵌入 `tk` 作为Query，视觉嵌入 `V` 作为Key和Value，通过Softmax注意力得到病理特异性视觉表征 `hk`。随后用InfoNCE损失 `L_align` 强制 `hk` 与对应文本嵌入 `tk` 对齐，并推远其他病理文本。
  - **逻辑集监督分支**：采用门控注意力MIL（ABMIL）聚合所有切片嵌入，得到全局扫描表征 `z`，经过线性分类头预测多标签概率。使用非对称损失（ASL） `L_logic` 缓解严重类别不平衡。
  - **一致性约束**：将全局报告嵌入 `t0` 也输入TCA得到 `h0`，通过余弦相似度损失 `L_cons = 1 - cos(z, h0)` 同步双流表征。
  - **不确定性感知精炼（UAR）**：推理时，对于某类 `c`，MIL分支预测概率 `Pc`，计算不确定性得分 `uc = 1 - |2Pc - 1|^α`。同时，将疾病标签填入提示模板得到文本嵌入 `tc`，经TCA获取视觉表征 `hc`，计算语义得分 `S_text(c) = σ(cos(hc, tc)/τ)`。最终动态融合：`P_refined(c) = (1 - λ·uc)Pc + λ·uc·S_text(c)`，仅在模型视觉预测模糊时才借助文本语义校准。

### 3. 实验设计
- **数据集**：
  - **主数据集**：真实世界临床数据集，852例非增强头部CT（NCCT），训练集682例，测试集170例。扫描预处理为轴向对齐、重采样至1×1×5 mm、224×224×32大小，HU裁剪[0,80]并归一化。
  - **外部零样本检测数据集**：CQ500公开数据集，包含491例CT和10种异常标签。
- **对比方法**：
  - 3D体积模型：ViT-B、Swin-B。
  - 2D MIL聚合器：平均池化、ABMIL、TransMIL，均分别用随机初始化与BiomedCLIP预训练两种设置。
  - Brain-Adapter变体：使用标准BCE损失（而非ASL）的版本。
- **评估指标**：Micro AUC、Macro AUC、Hamming Loss，以及Micro敏感度（SEN）、特异度（SPE）等。
- **其他实验**：消融实验验证各模块贡献；零样本异常检测评估跨域迁移能力；TCA注意力权重可视化分析可解释性。

### 4. 资源与算力
- **GPU**：2块NVIDIA GeForce RTX 4090。
- **训练配置**：总批次大小为4，训练100个轮次。
- **优化器**：AdamW，学习率1e-5，β1=0.9，权重衰减0.2。
- **其他**：视觉编码器使用LoRA低秩适配，文本编码器冻结，显著降低可训练参数量。

### 5. 实验数量与充分性
- **实验组数**：约10组对比实验（表1）、4种消融设置（表2）、外部零样本评估（图3a）、可解释性分析（图3b）。
- **充分性评估**：
  - 对比对象全面，覆盖3D模型、2D MIL基准及预训练版本，公平性较好。
  - 消融实验逐步添加组件，清晰展示每个模块的贡献。
  - 零样本实验验证了跨域语义迁移能力，增强了结论的泛化性。
  - 可视化分析提供了定性证据，表明模型捕捉到临床相关模式。
  - 不足：仅在单一私有数据集上训练测试，未进行多中心交叉验证；外部零样本数据量中等，但未进一步探索微调性能。

### 6. 主要结论与发现
- **性能突破**：Brain-Adapter在测试集上达到Micro AUC 0.887、Macro AUC 0.778、Hamming Loss 0.079，显著优于所有对比的3D模型及MIL基线。
- **模块有效性**：
  - ASL损失大幅提升敏感度（从0.285到0.740），有效应对类别不平衡。
  - TCA对齐、一致性约束和UAR融合均带来增益，三者结合最优。
- **零样本泛化**：在CQ500数据集上，利用疾病提示词的TCA语义得分始终优于BiomedCLIP平均池化基线，证明该方法保留了2D VLM的零样本能力。
- **可解释性**：TCA注意力分布能自动聚焦于病变相关切片，并准确响应不同病理查询，与临床阅片逻辑一致。

### 7. 优点
- **标注效率**：彻底摆脱密集标注，仅用原始诊断报告即可训练，临床可及性极强。
- **双流设计**：结合显式语义引导（TCA开放集对齐）与隐式视觉证据（MIL逻辑集监督），并通过一致性约束统一，兼顾准确性与鲁棒性。
- **动态推理校准**：UAR模块利用不确定性感知机制仅在视觉分支置信度低时引入文本知识，避免过度依赖单一路径。
- **可解释输出**：TCA提供的切片级注意力权重可直观展示模型决策依据，提升临床信任度。
- **灵活适配**：LoRA微调和冻结文本编码器的设计，参数效率高且便于在不同部位或模态复用。

### 8. 不足与局限
- **数据规模与代表性**：训练集仅682例、单中心数据，可能引入中心特异性偏差，泛化至其他人群或扫描协议需进一步验证。
- **LLM依赖性**：标签提取依赖LLM，若报告术语不规范或LLM抽取错误，可能扭曲监督信号，文中未讨论其鲁棒性。
- **超参数敏感度**：UAR中α、λ等超参数可能需要根据数据调整，且未进行敏感性分析。
- **任务覆盖**：仅针对扫描级多标签分类，未探索更精细的病灶定位或分割任务，临床辅助能力受限。
- **计算与效率**：虽然使用LoRA，但训练100个epoch仅2块4090、batch size 4，实际训练时间可能较长，未给出具体时长或吞吐量。
- **对比局限性**：因无密集标注，无法与全监督3D模型进行体素级敏感度比较，但论文在扫描级指标上已做了充分对比。

（完）
