---
title: Enhancing Pathological VLMs with Cross-scale Reasoning
title_zh: 增强病理视觉语言模型的跨尺度推理
authors: "Chi Phan, Tianyi Zhang, Qiaochu Xue, Yufeng Wu, Dan Hu, Zeyu Liu, Sudong Wang, Yueming Jin"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17412v2"
tags: ["query:pathfeat"]
score: 8.0
evidence: 跨尺度训练范式从病理模型提取多放大率图块表示
tldr: "病理图像的多尺度特性要求模型具备跨尺度推理能力，但现有视觉语言模型缺乏显式跨尺度训练。本文提出首个跨尺度推理范式，并设计泄漏感知数据构建流程，通过对抗性文本筛选和约束问题设计消除文本捷径。基于此构建Scale-VQA基准（4,685题），并训练ScaleReasoner-R1模型，在跨尺度及单尺度基准上均达到最优性能，验证了有限跨尺度监督的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17412v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1258, \"height\": 642, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17412v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1249, \"height\": 1277, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17412v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1026, \"height\": 788, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17412v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1268, \"height\": 842, \"label\": \"Table\"}]"
motivation: 病理VLM缺乏跨尺度推理目标，无法学习多尺度证据关联，且多图像VQA易受文本捷径干扰。
method: 提出泄漏感知数据构建流程（对抗性文本筛选+约束问题设计），构建Scale-VQA基准；使用强化学习训练ScaleReasoner-R1。
result: ScaleReasoner-R1在跨尺度推理基准上达到SOTA，并泛化至单尺度基准取得最优结果。
conclusion: 有限的跨尺度监督能显著提升病理图像理解，跨尺度推理是病理VLM的关键方向。
---

## 摘要
病理图像本质上是多尺度的，要求病理学家整合从低放大倍率的整体组织结构到高放大倍率的细胞形态学证据，以实现准确诊断。尽管现有用于视觉语言模型（VLM）的病理数据集包含多种尺度，但它们通常缺乏明确的跨尺度推理目标。这一局限性阻碍了VLM捕捉关键的跨尺度表示并学习基于证据的推理。为弥补这一空白，我们首次引入了跨尺度训练与评估范式，将病理解释表述为多放大倍率推理。然而，创建这样的任务揭示了一个关键挑战：多图像视觉问答（VQA）容易受到纯文本捷径的影响，这允许模型利用放大倍率相关的伪影而非视觉证据来猜测答案。为解决此问题，我们提出了一种泄漏感知的数据筛选流程，将对抗性的纯文本筛查与约束引导的问题设计相结合。利用此流程，我们构建了Scale-VQA，一个高质量基准测试集，包含来自多个放大倍率的2,537张病理图像上的4,685道选择题。最后，我们提出了ScaleReasoner-R1，一个通过强化学习训练以优化跨尺度VQA任务性能的模型。ScaleReasoner-R1在我们的跨尺度推理基准上达到了最先进的性能，并在已有的单尺度基准上泛化至最先进水平。研究结果表明，即使是有限的跨尺度监督也能显著提升病理理解能力。代码和演示将开源。

## Abstract
Pathological images are inherently multi-scale, requiring pathologists to integrate evidence from global tissue architecture at low magnification to cellular morphology at higher magnification for accurate diagnosis. While existing pathological datasets for vision-language model (VLM) include various scales, they often lack an explicit cross-scale reasoning objective. This limitation prevents VLMs from capturing essential cross-scale representations and learning evidence-based reasoning. To bridge this gap, we introduce the first cross-scale training and evaluation paradigm that formulates pathology interpretation as multi-magnification reasoning. However, creating such a task reveals a critical challenge: multi-image visual question answering (VQA) is prone to text-only shortcuts, which allow models to guess answers using magnification-dependent artifacts rather than visual evidence. To address this, we propose a leakage-aware curation pipeline that combines adversarial text-only screening with constraint-guided question design. Using this pipeline, we construct Scale-VQA, a high-quality benchmark with 4,685 multiple-choice questions grounded in 2,537 pathology images across multiple magnification levels. Finally, we present ScaleReasoner-R1, a model trained via reinforcement learning to optimize performance on the cross-scale VQA task. ScaleReasoner-R1 achieves state-of-the-art performance on our cross-scale reasoning benchmark and generalizes to SOTA performance on established single-scale benchmarks. Findings suggest that even the limited cross-scale supervision can significantly improve pathological understanding. The code and demos will be open-sourced.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：病理图像分析需要跨尺度（低倍→高倍）整合证据，但现有病理视觉语言模型（VLM）主要处理单尺度任务，缺乏显式跨尺度的推理目标。直接构建多图像VQA时，模型容易利用“文本捷径”（即从问题和选项直接推断答案，而不依赖所有放大视野的图像证据）。
- **整体含义**：为解决上述问题，本文提出第一个跨尺度训练与评估范式，将病理解释形式化为多放大倍率的协同推理。通过设计泄漏感知的数据构建流程，抑制文本捷径，构建高质量基准数据集Scale-VQA，并训练ScaleReasoner-R1模型。工作表明，即使有限跨尺度监督也能显著提升病理理解能力，为更贴近临床实践的VLM发展提供新方向。

## 2. 论文提出的方法论

### 核心思想
- 将病理诊断建模为多放大倍率的视觉推理任务，强制模型在同一案例的不同放大倍数之间建立证据联系，并通过强化学习的反馈优化，迫使模型基于视觉证据作出决策，而非依赖文本捷径。

### 关键技术细节
1. **高质量临床标注**：
   - 15名初级病理学家（>5年经验）和5名高级病理学家（>20年经验）标注177个公开TCGA全切片图像。
   - 选取代表性子区域（ROI）在10×、40×、200×放大倍数，并生成每张ROI的详细文字描述。
   - 利用GPT-5.2将缩放过程和描述合成为跨尺度临床总结，最后经病理学家验证。

2. **泄漏感知VQA构建流程**：
   - **步骤1（尺度特征分解）**：将专家注释分解为按尺度的证据集合，定义5个推理维度（对应、确认、定位、解释、诊断），附加视觉-依赖、尺度-依赖和维度-特定约束。
   - **步骤2（对抗性文本筛选）**：对每个候选选择题，用Gemini 3 Pro和Qwen3-Max仅基于文本推断答案。若它们正确回答，则分析泄漏原因（语言线索、标签先验等），更新约束并重新生成题目，迭代直到文本对抗无法正确推断。
   - **步骤3（MCQ构建与临床验证）**：基于最新约束生成最终选择题，每道题配有多尺度图像、正确答案和简洁推理过程。平衡选项位置，最后由病理学家验证视觉支撑和干扰项合理性。

3. **强化学习训练ScaleReasoner-R1**：
   - 基于Patho-R1-7B（专注单尺度的病理VLM），采用Group Relative Policy Optimization (GRPO) 优化。
   - 奖励函数：\( R = \lambda R_{\text{acc}} + (1-\lambda)R_{\text{form}} \)，其中 \(\lambda=0.8\)，\(R_{\text{acc}}\)为选对得1分/错为0，\(R_{\text{form}}\)强制输出格式合规。
   - 对每个问题采样多个回答，计算组内相对优势，避免显式critic。

## 3. 实验设计

- **数据集**：
  - **跨尺度基准**：Scale-VQA（共4,685道选择题，来自177个WSI的2,537张ROI），按患者划分训练/验证/测试（3,230/450/1,005）。
  - **单尺度基准**：PathMMU（10,387道题，包含5个子集：Atlas、EduContent、PathCLS、PubMed、SocialPath）。
- **基准**：
  - 跨尺度推理准确率（5个维度及平均）。
  - 单尺度PathMMU各子集准确率。
- **对比方法**：
  - 通用VLM：Qwen2.5VL-7B、Gemini 3 Flash、GPT-5.2。
  - 医学VLM：LLaVA-Med-7B、HuatuoGPT-7B、Lingshu-7B。
  - 病理VLM：Quilt-LLaVA、CLOVER、Patho-R1-7B。
  - 消融实验：SFT-only（用4,475个跨尺度VQA样本做监督微调）、SFT+RL。
- **额外对比**：PathMMU原文中的随机选择、频繁选择、专家表现等。

## 4. 资源与算力

- **明确说明**：在8张H100 GPU上训练500步（基于UnPuzzle Pipeline）。
- **未说明**：训练总用时（小时）、单步时间、数据预处理开销、外部API调用次数等。

## 5. 实验数量与充分性

- **实验数量**：
  - 跨尺度基准：表格1列出14个模型+2个消融，共16行结果。
  - 单尺度基准：表格2列出约20行（含基线、对比方法及消融）。
  - 消融对比：SFT、SFT+RL、RL-only共三种训练策略。
- **充分性评价**：
  - 实验覆盖了多个域（通用、医疗、病理）的代表性模型，且采用公开基准PathMMU，对比客观。
  - 消融清楚验证了RL优于SFT范式，并展示了跨尺度训练对单尺度任务的正向迁移。
  - 但未报告多次运行的标准差，可能存在随机性；也未在更多（不同医院或染色）数据集上验证泛化性。整体公平但细节可更丰富。

## 6. 论文的主要结论与发现

1. ScaleReasoner-R1在跨尺度推理基准（Scale-VQA-Test）上平均准确率达到82.89%，远超第二名（GPT-5.2为58.51%），提升超过24个百分点。
2. 在单尺度PathMMU基准上，尽管只使用跨尺度数据训练，ScaleReasoner-R1在所有子集上的性能均超越原模型Patho-R1，并在多个子集（如EduContent、Atlas）接近或超过专家水平。
3. **RL-only显著优于SFT+RL和SFT**：SFT导致单尺度任务性能骤降（灾难性遗忘），SFT+RL有所缓解但仍不如纯RL；这说明纯RL避免了模仿示踪的过拟合，直接优化最终答案选择，泛化更好。
4. 跨尺度监督可有效提升单尺度病理感知，验证了多尺度信息整合的实践价值。

## 7. 优点

- **问题定义新颖**：首次明确将病理VLM的研究焦点从单尺度扩展到跨尺度推理，更贴近临床实际。
- **泄漏感知流程严谨**：通过对抗性文本筛选和迭代约束更新，有效去除文本捷径，确保数据集只依赖视觉证据。
- **高质量专家标注**：多级病理医生参与标注、审核，保证数据临床正确性。
- **强化学习导向优化**：采用GRPO避免对人工推理链条的模仿，以最终答案正确率作为奖励，在低数据场景下效果显著。
- **开源贡献**：代码、数据集和构建流程全部开源，便于社区复现和进一步发展。
- **实验设计比较全面**：覆盖多领域SOTA模型，并包含有价值的消融分析（SFT vs RL）。

## 8. 不足与局限

- **数据规模较小**：仅177个TCGA WSI，器官/癌症类型分布不均（子宫/脑/乳腺较多，结直肠/软组织较少），可能带来选择偏差。
- **放大倍数受限**：只使用10×、40×、200×三个固定倍数，未涵盖临床常用其他倍数（如5×、100×等），也未考虑中间倍数动态切换。
- **训练步数较少**：仅500步GRPO，可能未充分收敛；更长的训练或更复杂的奖励设计可能进一步提升性能。
- **纯文本对抗筛选不完全**：仅使用两种强文本模型检测泄漏，可能存在未发现的更隐蔽捷径（如分布偏向、选项长度等）。
- **缺乏统计不确定性分析**：未报告多次实验均值和方差，无法评估模型稳定性。
- **实际部署限制**：模型处理多图像（三张组合）增加推理成本；在真实低资源环境（如不同扫描仪或染色）下的鲁棒性未验证。

（完）
