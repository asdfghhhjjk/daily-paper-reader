---
title: Enhancing Pathological VLMs with Cross-scale Reasoning
title_zh: 通过跨尺度推理增强病理视觉语言模型
authors: "Chi Phan, Tianyi Zhang, Qiaochu Xue, Yufeng Wu, Dan Hu, Zeyu Liu, Sudong Wang, Yueming Jin"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17412v1"
tags: ["query:cellseg"]
score: 8.0
evidence: 病理图像跨尺度推理
tldr: 病理图像诊断依赖多尺度信息，而现有视觉语言模型缺乏跨尺度推理目标。为此，提出首个跨尺度训练与评估范式，并设计泄漏感知数据筛选管道，构建Scale-VQA多选问答基准。基于强化学习训练的ScaleReasoner-R1在跨尺度推理基准上达到SOTA，并泛化到单尺度基准。研究表明，有限的跨尺度监督即可显著提升病理理解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17412v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1260, \"height\": 643, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17412v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1249, \"height\": 1277, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17412v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1026, \"height\": 788, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17412v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1268, \"height\": 842, \"label\": \"Table\"}]"
motivation: 病理视觉语言模型需跨尺度推理，但现有数据集缺乏显式跨尺度目标，导致模型无法学习基于多尺度证据的推理。
method: 提出泄漏感知数据筛选管道，结合对抗性文本过滤和约束引导问题设计，构建Scale-VQA基准；用强化学习训练ScaleReasoner-R1优化跨尺度VQA。
result: ScaleReasoner-R1在跨尺度推理基准上达到SOTA，并在单尺度基准上同样取得最优性能。
conclusion: 即使有限的跨尺度监督也能显著提升病理视觉语言模型的多尺度理解能力。
---

## 摘要
病理图像本质上是多尺度的，要求病理学家整合从低倍放大下的全局组织结构到高倍放大下的细胞形态等不同尺度的证据，以实现准确诊断。尽管现有的用于视觉语言模型（VLM）的病理数据集包含多种尺度，但它们通常缺乏明确的跨尺度推理目标。这一限制使得VLM无法捕捉关键的跨尺度表征并学习基于证据的推理。为弥补这一差距，我们引入了首个跨尺度训练与评估范式，将病理解释表述为多放大倍率推理任务。然而，创建此类任务揭示了一个关键挑战：多图像视觉问答（VQA）容易陷入仅依赖文本的捷径，使模型能够利用放大倍率相关的伪影而非视觉证据来猜测答案。为解决此问题，我们提出了一种泄漏感知的策展流程，结合了对抗性纯文本筛选与约束引导的问题设计。利用此流程，我们构建了Scale-VQA，这是一个高质量基准测试，包含基于2537张多放大倍率病理图像的4685道多项选择题。最后，我们提出了ScaleReasoner-R1，这是一个通过强化学习训练的模型，旨在优化跨尺度VQA任务的性能。ScaleReasoner-R1在我们的跨尺度推理基准测试上达到了最先进的性能，并泛化至现有单尺度基准测试上的SOTA表现。研究结果表明，即便有限的跨尺度监督也能显著提升病理理解。代码和演示将开源。

## Abstract
Pathological images are inherently multi-scale, requiring pathologists to integrate evidence from global tissue architecture at low magnification to cellular morphology at higher magnification for accurate diagnosis. While existing pathological datasets for vision-language model (VLM) include various scales, they often lack an explicit cross-scale reasoning objective. This limitation prevents VLMs from capturing essential cross-scale representations and learning evidence-based reasoning. To bridge this gap, we introduce the first cross-scale training and evaluation paradigm that formulates pathology interpretation as multi-magnification reasoning. However, creating such a task reveals a critical challenge: multi-image visual question answering (VQA) is prone to text-only shortcuts, which allow models to guess answers using magnification-dependent artifacts rather than visual evidence. To address this, we propose a leakage-aware curation pipeline that combines adversarial text-only screening with constraint-guided question design. Using this pipeline, we construct Scale-VQA, a high-quality benchmark with 4,685 multiple-choice questions grounded in 2,537 pathology images across multiple magnification levels. Finally, we present ScaleReasoner-R1, a model trained via reinforcement learning to optimize performance on the cross-scale VQA task. ScaleReasoner-R1 achieves state-of-the-art performance on our cross-scale reasoning benchmark and generalizes to SOTA performance on established single-scale benchmarks. Findings suggest that even the limited cross-scale supervision can significantly improve pathological understanding. The code and demos will be open-sourced.

---

## 论文详细总结（自动生成）

# 1. 论文的核心问题与整体含义（研究动机和背景）

病理图像的分析需要整合从低倍放大（全局组织结构）到高倍放大（细胞形态）的多尺度信息，这对应着病理学家在临床实践中的跨尺度推理过程。然而，现有的病理视觉语言模型（VLM）大多只针对**单一尺度**的单个图像进行识别或问答，缺乏显式的**跨尺度推理目标**。这导致模型无法学习在不同放大倍数之间协调证据、解释层级关系的能力，从而与实际诊断流程脱节。本文旨在解决这一缺口，提出首个面向跨尺度推理的病理VLM训练与评估范式。

---

# 2. 论文提出的方法论

**核心思想：** 将病理解释形式化为基于多放大倍数图像的协同推理任务，并通过专门设计的**泄漏感知策展管道**构建出高可靠的跨尺度VQA基准，再采用**强化学习**（GRPO）训练模型实现跨尺度推理能力。

**关键技术细节：**

- **临床标注与数据构建**  
  - 使用177张TCGA公开全切片图像（WSI），由15名病理医生（>5年经验）和5名高级病理医生（>20年经验）协作标注。  
  - 每张WSI选取低倍（10×）、中倍（40×）、高倍（200×）三个感兴趣区域（ROI），并生成对应尺度的文字描述和跨尺度综合描述。共得到2,537个ROI描述和937个跨尺度描述。

- **泄漏感知VQA策展管道（图2a）**  
  - **步骤1：尺度特征分解** – 将专家标注分解为各尺度独立的证据集，避免完整描述带来的文本泄漏；定义五个跨尺度推理维度（对应、确认、定位、解释、诊断）。  
  - **步骤2：对抗性纯文本筛选** – 对候选多项选择题（MCQ），使用Gemini 3 Pro和Qwen3-Max作为“纯文本对手”（只给题目和选项，不给图像）。若对手能正确回答，则分析泄漏源并迭代修改问题与约束，直到对手无法依靠文本推断答案。  
  - **步骤3：MCQ生成与临床验证** – 通过最终约束生成MCQ，每个问题附带低/中/高三倍图像、正确答案和简洁推理过程；最后由病理医生验证视觉支持和干扰项合理性。

- **ScaleReasoner-R1模型训练**  
  - 基座模型：Patho-R1-7B（已在单尺度病理数据上预训练）。  
  - 采用GRPO（Group Relative Policy Optimization）进行强化学习。  
  - 奖励函数：R = λ*R_acc + (1-λ)*R_form，其中R_acc为答案正确性（0/1），R_form为格式合规，λ=0.8。  
  - 训练时对每个问题采样多个回答，计算组内相对优势，无需显式critic网络。

---

# 3. 实验设计

- **使用的数据集**  
  - **训练集**：来自93张WSI的4,475个跨尺度VQA样本（从177张WSI中划分出训练部分）。  
  - **验证/测试集**：Scale-VQA共4,685个MCQ，按病例划分train/val/test为3,230/450/1,005个样本。  
  - **单尺度基准**：PathMMU（包含5个子集：Atlas, EduContent, PathCLS, PubMed, SocialPath，共10,387个VQA样本），用于评估模型在常规单尺度任务上的泛化能力。

- **对比方法**  
  - 通用VLM：Qwen2.5VL-7B, Gemini 3 Flash, GPT-5.2  
  - 医学VLM：LLaVA-Med, HuatuoGPT, Lingshu  
  - 病理VLM：Quilt-LLaVA, CLOVER, Patho-R1（本文基座模型）

- **评估指标**：多项选择准确率（Accuracy），分别在五个推理维度和整体平均上报告。

---

# 4. 资源与算力

文中明确说明：  
- **硬件**：8块NVIDIA H100 GPU  
- **训练框架**：基于UnPuzzle Pipeline  
- **训练步数**：GRPO训练500步  
- **基座模型**：Patho-R1-7B（7B参数）  
未提及具体训练耗时，但500步在8×H100上通常为数小时级别。

---

# 5. 实验数量与充分性

- **主要对比实验**：  
  - 跨尺度基准（表1）对比了9个模型（含消融中的SFT、SFT+RL），给出每个推理维度的准确率。  
  - 单尺度基准（表2）对比了12个模型（含随机选择、专家表现、通用/医学/病理VLM），在PathMMU的6个子集上报告结果。

- **消融实验**：  
  - 对比SFT-only、SFT+RL、RL-only三种训练范式，验证RL的必要性。  
  - 结果显示RL-only在跨尺度任务上（82.89%）显著优于SFT（70.25%）和SFT+RL（72.84%），且在单尺度任务上未出现退化，而SFT导致单尺度性能下降。

- **充分性评价**：  
  - 实验覆盖了**跨尺度**和**单尺度**两类场景，对比方法多样、类别全面。  
  - 消融设计清晰，揭示了RL在提升泛化性上的优势。  
  - **不足之处**：仅使用了177张WSI，数据量较小；未在更多外部数据集（如其他器官、不同染色方式）上验证；未与基于全切片级别的方法（如Slidechat、WSI-LLaVA）进行对比（这些方法也涉及多尺度但机制不同）。

---

# 6. 论文的主要结论与发现

1. **跨尺度推理范式有效**：ScaleReasoner-R1在自建跨尺度基准上达到82.89%的准确率，远超所有对比模型（第二名Gemini 3 Flash仅60.90%）。
2. **跨尺度监督有助于单尺度理解**：训练于跨尺度任务上的模型，在单尺度PathMMU基准上同样达到或接近专家水平（如EduContent和Atlas子集），表明跨尺度推理促进了更鲁棒的病理表征。
3. **RL优于SFT**：纯强化学习训练避免了SFT对演示样本的过拟合和灾难性遗忘，在低数据量下仍能实现强泛化。
4. **泄漏感知策展必要**：通过对抗性文本筛选去除文本捷径，迫使模型真正依赖多尺度视觉证据，保证了基准的可靠性。

---

# 7. 优点

- **问题定义新颖**：首个明确提出病理跨尺度推理目标的VLM工作，填补了现有数据集和模型的关键空白。  
- **数据质量控制高**：采用多层级病理医生协作标注，并设计泄漏感知策展流程，确保了基准的信度和效度。  
- **训练方法高效**：仅用177张WSI、500步RL便取得显著提升，展现了强化学习在小数据场景下的潜力。  
- **泛化性强**：跨尺度训练同时改善了单尺度任务性能，表明模型学到了更通用的病理知识，而非仅适应特定任务。  
- **开源承诺**：代码、数据集和管线将开放，有利于社区复现和进一步研究。

---

# 8. 不足与局限

- **数据规模有限**：仅基于177张TCGA WSI构建，器官覆盖15种，但每种器官样本量不均衡，可能限制模型的泛化性和多样性。  
- **评估形式单一**：所有问题均为多项选择（MCQ），未涉及开放生成或自由文本推理，与临床中更灵活的问答场景存在差距。  
- **对比基线不够丰富**：缺少与基于全切片推理的VLM（如WSI-LLaVA、Slidechat）或跨尺度编码器方法的比较，这些方法也处理多尺度但方式不同。  
- **RL训练步数较少**：仅500步GRPO，未探索更长的训练是否带来更多提升或过拟合风险。  
- **潜在泄漏风险**：尽管设计了迭代筛选，但完全消除文本泄漏是困难的；更强大的文本对手可能仍能找到捷径，需持续更新。  
- **结果解释性**：虽然模型输出<think>推理过程，但文中未分析推理质量，存在模型“伪推理”的可能性。

（完）
