---
title: Controllable Histopathology Image Synthesis with Training-free Structural Initialization and Textural Modulation
title_zh: 基于免训练结构初始化与纹理调制的可控组织病理学图像合成
authors: "Yuheng Qiu, Jingyi Luo, Chenfei Ye, Ting Ma, Jianfeng Cao"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.27935v1"
tags: ["query:cellseg"]
score: 9.0
evidence: 直接合成组织病理学图像，缓解数字病理数据稀缺问题
tldr: 组织病理图像合成中，高质量标注数据获取困难且成本高昂，现有方法仍需少量标注进行训练约束。本文提出即插即用的CHIS框架，无需额外训练，通过小波域纹理调制和频域结构初始化，引导仅由无标签数据预训练的扩散模型，依据输入的结构掩码生成逼真组织图像。频域中融合掩码相位与高斯噪声幅值获得空间结构先验，反向扩散时在不同小波分解层级自适应调节纹理细节，确保生成图像保持组织形态与参考风格。实验证实CHIS生成质量优于对比方法，并显著增强下游组织分割性能，为弱标注病理图像合成开辟新路径，该训练无关框架为数据稀缺的病理分析任务提供了高效合成方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27935v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1258, \"height\": 645, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27935v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1260, \"height\": 492, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27935v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1260, \"height\": 471, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27935v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1274, \"height\": 609, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27935v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1273, \"height\": 468, \"label\": \"Table\"}]"
motivation: 针对病理图像合成中高质量标注稀缺且现有方法仍依赖部分标注的难题，需要一种无需额外标注训练即可利用结构掩码控制生成的方法。
method: 提出CHIS框架，采用频域相位振幅融合进行结构初始化，以及小波域多尺度纹理调制，引导无标签预训练扩散模型的采样过程。
result: CHIS生成图像在视觉保真度上优于现有合成方法，并在组织分割任务中带来显著的性能提升。
conclusion: 该训练无关的即插即用框架有效降低了合成对标注的依赖，为无监督病理图像可控生成提供了新方向。
---

## 摘要
深度学习在高通量组织病理学图像分析中取得了显著成功。然而，基于学习的模型性能关键取决于专家病理学家标注的质量和规模，这是一个资源密集且耗时的过程。为解决数据稀缺和标注负担的局限性，已有多种方法被提出用于合成成对的组织病理学数据。尽管如此，这些框架通常仍需要标注数据，尽管数量减少，以在训练期间施加结构约束。在本文中，我们提出CHIS，一个即插即用的框架，通过两个关键阶段引导预训练扩散模型的采样轨迹：起始时的结构初始化和生成过程中的纹理调制。初始噪声状态通过在频域中融合来自先验掩码的相位信息与高斯噪声的幅度得以细化，产生一个结构信息丰富的起点。在逆向扩散过程中，我们在不同的小波分解级别自适应地调制粗粒度和细粒度纹理。这使得仅使用无标签图像预训练的扩散模型能够生成与先验结构掩码对齐的输出，同时保留参考组织风格。我们进行了大量实验，展示了CHIS在生成保真度上的优越性及其对下游分割任务的显著益处。代码已开源：https://github.com/IBIL-Code/CHIS。

## Abstract
Deep learning has demonstrated remarkable success in high-throughput histopathology image analysis. However, the performance of learning-based models critically depends on the quality and size of annotations by expert pathologists, which is a resource-intensive and time-consuming process. To address the limitations of data scarcity and annotation burden, several methods have been proposed to synthesize paired histopathology data. Nevertheless, these frameworks typically still require annotation data, albeit in reduced quantities, to impose structural constraints during training.   In this work, we present CHIS, a plug-in framework that guides the sampling trajectory of a pretrained diffusion model through two key stages: structural initialization at the start and textural modulation during generation. The initial noise state is refined by fusing the phase information from a prior mask with the amplitude of Gaussian noise in the frequency domain, yielding a structurally informed starting point. During the reverse diffusion process, we adaptively modulate both coarse-grained and fine-grained textures at different wavelet decomposition levels. This enables a diffusion model pretrained solely on unlabeled images to generate outputs that align with prior structural masks while preserving the reference tissue style.   We conducted extensive experiments demonstrating the superiority of CHIS in generation fidelity and its substantial benefits for downstream segmentation tasks. Code is available at https://github.com/IBIL-Code/CHIS.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：组织病理学图像分析是癌症诊断的金标准，深度学习模型性能严重依赖专家标注数据的数量与质量，但高质量标注的获取成本极高。
- **现有瓶颈**：已有的数据合成方法（如基于GAN或条件扩散模型）虽能缓解数据稀缺，但多数仍需要配对标注进行训练，无法充分利用仅由无标签图像预训练的生成模型；无监督方法（如CycleDiff）又面临域不匹配和结构不可控问题。
- **本文目标**：提出一种训练无关（training‑free）的即插即用框架 CHIS，在无需任何额外训练或配对标注的条件下，引导一个只由无标签组织病理图像预训练的扩散模型，按照用户提供的结构掩码生成逼真、组织纹理一致的病理图像。

### 2. 方法论
CHIS 通过在扩散模型的逆采样过程中加入两个控制阶段实现结构对齐与纹理保真，整个流程不更新模型参数。

- **骨架模型**：采用预训练的潜在扩散模型（Latent Diffusion Model），使用 VAE 编码图像至潜在空间，DDIM 逆采样生成。模型参数完全冻结。
- **先验掩码生成**（§2.2）：
  - 从单细胞掩码集合出发，依据用户指定或从集合统计得到的细胞数量、面积和间距三个几何先验，将细胞分为簇团和孤立细胞，以聚类方式组织成组织掩码 y。
- **结构初始化（频域相位融合）**（§2.3）：
  - 为避免掩码 y 因分布偏移导致编码不佳，先利用一张参考图像 \(x^{ref}\) 的颜色信息（Otsu 阈值分割出的背景平均色、前景边界距离平均色）对掩码进行着色，得到 \(\hat{y}\)，再编码为潜在特征 \(\hat{z}^{ref}_0\)。
  - 对 \(\hat{z}^{ref}_0\) 和高斯噪声 \(z_T\) 分别进行 FFT，提取相位 \(\phi\) 与幅度 \(A\)。
  - 融合策略：保留噪声的幅度，掩码的相位经低通滤波后与噪声的相位混合，得到结构对齐的初始噪声 \(z^{mix}_T = \mathcal{F}^{-1}(A_{noise} e^{j\phi_{mix}})\)。
- **纹理调制（小波域自适应融合）**（§2.4）：
  - 在逆扩散的前 40 步，同步将当前采样状态 \(z^{mix}_t\) 和从参考图像得到的 \( \hat{z}^{ref}_t \) 进行平稳小波变换（SWT），分解为低频粗略纹理 \(L\) 和三个高频细节纹理 \(H\)。
  - 对低频成分 \(L^{mix}_t\) 进行统计分布偏移，对齐 \(L^{ref}_t\) 的均值与标准差方向。
  - 利用预定义的权重 \(\gamma, \chi, \zeta\) 分别控制背景、前景低频与高频的调制强度：低频采用强约束，高频采用弱约束，背景相对宽松。最终通过加权求和融合调制后的成分：
    \[
    \tilde{L}^{mix}_t = \omega_L \cdot \hat{L}^{mix}_t + (1-\omega_L) \cdot L^{mix}_t,\quad
    \tilde{H}^{mix}_t = \omega_H \cdot H^{ref}_t + (1-\omega_H) \cdot H^{mix}_t
    \]
  - 经逆 SWT 得到更新后的 \(z^{mix}_t\)，继续扩散采样，最后 10 步完全自由生成以补充细节。

### 3. 实验设计
- **数据集**：MoNuSAC、Kumar、PanNuke 三个组织病理学细胞核数据集，按 80/20 划分训练集与测试集，所有合成图像数量与训练集原始样本量一致。
- **对比方法**：
  - 全监督：SDM、NuDiff（需要配对标注）。
  - 弱监督 / 无监督：SynDiff（GAN）、CycleDiff（GAN 循环）、UGDM、ADMMDiff（训练无关引导扩散）。
  - 数据增强基线：CutOut、CutMix。
- **评估维度**：
  - 掩码‑图像一致性：用零样本 nnU‑Net 分割合成图像，计算与输入掩码的 Dice 系数（FS1）和 HD95 距离。
  - 下游分割增益：将合成数据加入训练集，用 Hover‑Net 测 Dice 和 AJI。
  - 生成质量：计算 FID 和 IS 与真实数据的分布相似度。

### 4. 资源与算力
- 论文明确指出 CHIS 本身是 training‑free，仅在预训练模型上操作，未报告 CHIS 运行所需的 GPU 型号、数量或时间。
- 其背干模型 PixCell 的预训练配置在原文中未详细说明，只提及模型参数保持冻结。因此，**具体的算力消耗在文中未被明确记录**。

### 5. 实验数量与充分性
- 共在 **3 个数据集**上进行评估，每个数据集都完成了 **3 类评估**（结构对齐、分割提升、图像质量）。
- 对比方法涵盖 **2 种全监督、2 种 GAN‑based、2 种训练无关引导方法及 2 种数据增强**，比较范围较广且多层次。
- 虽未专门列出消融实验表，但文中对频率融合半径 r、纹理调制权重 \(\gamma, \chi, \zeta\) 等关键超参数给出了明确设定，并在文本中说明了调制规则的设计动机。
- 总体而言，实验覆盖了主要评估指标和代表性基线，设计较为充分、公平。但缺少对超参数敏感性的系统消融，以及对不同骨干模型、不同组织类型的泛化实验。

### 6. 主要结论与发现
- CHIS 在无需任何标注和额外训练的前提下，能够使预训练扩散模型生成与输入结构掩码高度一致的病理图像，**结构一致性（FS1）接近甚至超越部分全监督方法**。
- 合成的数据能显著提升下游实例分割任务的性能，且增益幅度与全监督合成方法相当，同时避免了因纹理偏差引入的分布偏移。
- CHIS 在 FID 和 IS 指标上表现出明显优势，生成图像的视觉风格更贴近真实数据，证实了通过参考图像与频域‑小波域联合引导的有效性。

### 7. 优点
- **零训练成本**：仅利用预训练模型，无需任何额外训练或微调，显著降低标注与计算开销。
- **即插即用**：框架与特定骨干解耦，可适配多种扩散模型。
- **耦合解耦策略**：巧妙利用频域相位编码结构、小波域分层调制纹理，实现了结构‑纹理的精细、独立控制。
- **实验全面**：从结构一致性、图像质量和下游任务增益多角度验证，对比方法全面，代码开源。

### 8. 不足与局限
- **依赖预训练质量**：效果受限于骨干模型（PixCell）的生成能力，若预训练模型本身对某种组织表现不佳，合成质量可能下降。
- **超参数需手动调节**：纹理调制权重、频率融合半径等虽给出参考值，但换用其他数据集或风格时可能需要重新调整。
- **评估限于细胞级分割场景**：未探讨腺体、间质等更复杂组织结构合成，以及分类等其他下游任务。
- **缺乏系统性消融**：未单独验证各模块的贡献（如无小波调制的效果），也未展示对噪声相位混合比例、参考图像选择敏感性的测试。
- **推理时间开销**：因需要在小波域反复变换与调制，可能比标准扩散采样慢，但论文未给出速度对比。

（完）
