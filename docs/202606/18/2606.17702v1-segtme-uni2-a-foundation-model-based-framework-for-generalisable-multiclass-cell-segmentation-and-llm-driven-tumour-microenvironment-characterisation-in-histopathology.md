---
title: "SegTME-UNI2: A Foundation Model-Based Framework for Generalisable Multiclass Cell Segmentation and LLM-Driven Tumour Microenvironment Characterisation in Histopathology"
title_zh: SegTME-UNI2：一种基于基础模型的可泛化多类细胞分割框架及LLM驱动的组织病理学肿瘤微环境表征
authors: "Wan Siti Halimatul Munirah Wan Ahmad, Faris Syahmi Samidi, Mohammad Badal Ahmmed, Vimal Angela Thiviyanathan, Selvam James Thavaraj, Anwar P. P. Abdul Majeed"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17702v1"
tags: ["query:cellseg"]
score: 9.0
evidence: 组织病理学中多类细胞分割与核分类双头解码器
tldr: 肿瘤微环境表征需要细胞分割、特征提取与可解释报告。本文提出SEGTME-UNI2框架，基于UNI2-H基础模型与双头UperNet解码器实现六类语义分割及实例分离，通过三阶段伪标签课程提升泛化性。分割输出经结构化特征提取后由微调GPT生成临床叙述。在PanNuke和TCGA-UT上验证可行性，公开数据集与模型检查点以支持大规模研究。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17702v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1661, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2606.17702v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1652, \"height\": 829}, {\"url\": \"assets/figures/arxiv/2606.17702v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1680, \"height\": 650}, {\"url\": \"assets/figures/arxiv/2606.17702v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 932, \"height\": 1981}, {\"url\": \"assets/figures/arxiv/2606.17702v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1654, \"height\": 876}, {\"url\": \"assets/figures/arxiv/2606.17702v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1628, \"height\": 1055}, {\"url\": \"assets/figures/arxiv/2606.17702v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1698, \"height\": 691}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17702v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1233, \"height\": 399}, {\"url\": \"assets/tables/arxiv/2606.17702v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1113, \"height\": 262}, {\"url\": \"assets/tables/arxiv/2606.17702v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1384, \"height\": 693}, {\"url\": \"assets/tables/arxiv/2606.17702v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1712, \"height\": 788}, {\"url\": \"assets/tables/arxiv/2606.17702v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1711, \"height\": 1579}]"
motivation: 缺乏像素级标注限制了肿瘤微环境的大规模定量分析，需要可泛化的细胞分割与自动报告生成方法。
method: 使用UNI2-UPERHOVER双头分割模型，结合三阶段伪标签课程（PanNuke→TCGA-UT单尺度→多尺度），提取20+指标后由BioNeMo GPT生成TME叙述。
result: 初步验证显示框架在PanNuke和TCGA-UT上具有可行性与内部一致性，分割和报告质量可信。
conclusion: 公开了伪标签数据集和模型检查点，为大规模肿瘤微环境表征与空间生物学研究提供基础。
---

## 摘要
从常规H&E染色组织学图像中表征肿瘤微环境（TME）需要同时进行细胞分割、特征提取和可解释的临床报告。我们提出了SEGTME-UNI2，一个统一框架来满足这些需求。其核心是UNI2-UPERHOVER，一个双头分割模型，将UNI2-H病理基础模型（ViT-Giant，在来自10万张切片的1亿张图块上预训练）与两个并行的UperNet解码器配对：一个用于六类语义分割，另一个用于水平-垂直梯度回归，从而实现基于分水岭的细胞核实例分离。为了解决大型真实世界数据集中像素级标注的缺失，UNI2-UPERHOVER经历了一个三阶段渐进式伪标签课程。每个阶段都训练一个全新的模型（不进行权重迁移），完全通过提高伪标签质量来推动改进：阶段1：使用人工标注的PanNuke（7901张图像，189744个细胞核，0.25微米/像素）。阶段2：使用阶段1模型在271,711个TCGA-UT scale-0图块（0.5微米/像素）上生成的熵过滤伪标签。阶段3：使用阶段2模型在全部1,608,060个TCGA-UT图块（六个分辨率尺度，0.5-1.0微米/像素）上生成的伪标签。分割输出馈入一个结构化的TME特征提取流程，计算20多个每个图块的组成、形态、空间熵和细胞间距离指标。这些指标编码为JSON并传递给微调的NVIDIA BioNeMo GPT模型，以生成临床可解释的TME叙述。在保留的PanNuke和TCGA-UT分区上的初步验证证明了框架的可行性和内部一致性。伪标签的TCGA-UT数据集和UNI2-UPERHOVER检查点已公开发布，以支持大规模TME分析和空间生物学研究。

## Abstract
Characterising the tumour microenvironment (TME) from routine H&E-stained histology images requires simultaneous cell segmentation, feature extraction, and interpretable clinical reporting. We present SEGTME-UNI2, a unified framework addressing these requirements. Its core is UNI2-UPERHOVER, a dual-head segmentation model pairing the UNI2-H pathology foundation model (ViT-Giant, pretrained on >100M tiles from 100K slides) with two parallel UperNet decoders: one for six-class semantic segmentation and one for horizontal-vertical gradient regression enabling watershed-based nuclear instance separation. To address the lack of pixel-level annotations in large real-world repositories, UNI2-UPERHOVER undergoes a three-stage progressive pseudo-label curriculum. Each stage trains a fresh model without weight transfer, driving improvement entirely via increased pseudo-label quality: Stage 1: Uses human-annotated PanNuke (7,901 images, 189,744 nuclei, 0.25 um/pixel). Stage 2: Uses entropy-filtered pseudo-labels from the Stage 1 model on 271,711 TCGA-UT scale-0 patches (0.5 um/pixel). Stage 3: Uses pseudo-labels from the Stage 2 model on all 1,608,060 TCGA-UT patches across six resolution scales (0.5-1.0 um/pixel). Segmentation outputs feed a structured TME feature extraction pipeline computing 20+ per-patch compositional, morphological, spatial entropy, and intercellular distance metrics. These are encoded as JSON and passed to a fine-tuned NVIDIA BioNeMo GPT model to generate clinically interpretable TME narratives. Preliminary validation on held-out PanNuke and TCGA-UT partitions demonstrates framework feasibility and internal consistency. The pseudo-labelled TCGA-UT dataset and UNI2-UPERHOVER checkpoint are publicly released to support large-scale TME profiling and spatial biology research.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：组织病理学H&E图像中肿瘤微环境（TME）的定量分析需要同时解决细胞分割（语义分类+实例分离）、特征提取和可解释报告生成三个任务，但现有方法存在三大瓶颈：
  - 语义分割与实例分割通常分离，且缺乏跨域泛化能力；
  - 大规模真实数据（如TCGA库）缺乏像素级标注，难以直接训练；
  - 量化TME特征（如细胞计数、空间距离）对临床医生不直观，需要转化为临床可读的叙述。
- **研究动机**：利用近年出现的病理基础模型（如UNI2）和大语言模型（如BioNeMo GPT），构建一个统一、可扩展、临床可解释的TME分析流水线，填补从标注稀缺性到实用报告生成的鸿沟。
- **整体含义**：提出SEGTME-UNI2框架，通过渐进式伪标签课程扩展监督信号到百万级无标注TCGA图像，并借助LLM生成结构化临床报告，旨在推动大规模空间生物学和AI辅助病理诊断的研究。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  - 使用UNI2-H（ViT-Giant）作为共享特征提取器，搭配双头UperNet解码器（语义头+HV回归头）实现紧密耦合的语义分割和实例分离。
  - 设计三阶段渐进式伪标签课程，从少量人工标注（PanNuke）逐步扩展到全尺度TCGA数据，每个阶段训练全新模型，仅依赖上一阶段生成的伪标签质量提升。
  - 分割后提取20+个每个图块的TME指标（组成、形态、空间熵、免疫-肿瘤距离等），通过规则映射为生物学Token，再由微调的BioNeMo GPT生成临床描述。

- **关键技术细节**：
  - **UNI2-UPERHOVER架构**：UNI2-H在Blocks 5,11,17,23处提取四尺度特征（FPN），分别投影至256/512/1024/2048通道；两个独立的UperNet解码器分别输出6类语义logits和2通道HV梯度图。
  - **动态HV目标合成**：仅需语义标注即可通过连通组分分析、质心偏移计算近似HV目标，使伪标签训练无需实例级标注。
  - **损失函数**：
    - 语义交叉熵（忽略边界像素255）；
    - HV回归损失 = MSE + 2×MSGE（梯度匹配项，强调边界斜率准确性）；
    - 总损失 L_total = L_sem + λ L_hv，λ=1.0。
  - **渐进式课程**：
    - Stage 1：在PanNuke（7901图，0.25μm/pix）上训练，监督学习。
    - Stage 2：Stage 1模型生成TCGA-UT Scale 0（271,711图，0.5μm/pix）的伪标签，熵过滤（保留前70%低熵图块），从头训练新模型。
    - Stage 3：Stage 2模型生成全部6个尺度（1,608,060图，0.5~1.0μm/pix）的伪标签，再次从头训练，得到最终模型。
  - **TME特征提取**：
    - 组成特征：每类细胞计数、比例、平均面积；临床比率：Tumor-Stroma分数、Immune Infiltration分数。
    - 空间特征：通过cKDTree计算免疫-肿瘤最小/平均距离、50像素内接触免疫细胞数；5近邻细胞类型分布的香农熵作为细胞混合指数。
  - **BioNeMo SFT**：将结构化JSON（raw_data, interpretation, generated_tokens）作为提示，微调NVIDIA BioNeMo GPT，生成临床叙述。

### 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法

- **数据集**：
  - **PanNuke**：7,901张256×256图像，5类细胞（肿瘤、炎性、结缔、死亡、上皮）+背景，0.25μm/pix，人类标注实例级分割。用于Stage 1监督训练和所有阶段的验证（保留20%测试集）。
  - **TCGA-UT Restructured**：来自TCGA的32种实体癌，经质量控制后共8,736张诊断切片（7,175患者），提取1,608,060个256×256图块，6个分辨率尺度（0.5~1.0μm/pix）。无人工像素级标注，用于Stage 2和3的伪标签训练。
  - **HNSCC外部验证**：4例头颈部鳞状细胞癌切片（独立机构），覆盖不同染色条件和TME表型，用于定性评估。

- **Benchmark**（文中提及的对比方法及计划，但当前论文仅提供内部验证）：
  - **语义分割**：单头UperNet（无HV头）+ UNI2-H backbone在PanNuke上作为基线。
  - **实例分割**：计划与HoVer-Net、CellPose、StarDist在MoNuSeg、CryoNuSeg、CoNSeP上比较，本论文尚未执行。当前仅报告了UNI2-UPERHOVER在内部验证上的mIoU。
  - **LLM部分**：无对比方法，仅展示生成示例。

- **实验场景**：
  - 内部语义分割：在PanNuke 20%测试集上评估所有模型（Stage1/2/3）的mIoU（跨域泛化性）；在各自TCGA-UT伪标签分区上报自洽性。
  - 训练动态：三个阶段的mIoU和loss曲线。
  - 定性案例：HNSCC 4个典型TME表型的分割+Token输出。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力

- **GPU型号与数量**：8×NVIDIA A100 GPU（由NVIDIA学术资助计划提供）。Stage 1和2使用8个GPU，Stage 3使用6个GPU。
- **训练时长**：
  - Stage 1：250个epochs（24,651个梯度步）。
  - Stage 2：250个epochs（212,500个梯度步）。
  - Stage 3：100个epochs，但每个epoch处理1,608,060 patches，总共335,100个梯度步。
- **批量大小**：每个GPU batch size=8；梯度累积：Stage1=1, Stage2=4, Stage3=8。混合精度bfloat16，torch.compile。DDP分布式训练。
- 总体算力需求较大，但文中未给出总计时长。

### 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、是否客观、公平

- **实验数量**：
  - 语义分割内部验证：3个模型（Stage1/2/3）在PanNuke测试集上各一个结果，TCGA-UT伪标签自洽性各一个结果，共6个数值。
  - 训练动态曲线：3个模型的mIoU和loss随时间变化。
  - 定性案例：4个HNSCC图块的完整分割+TME Token输出。
  - BioNeMo生成示例：1个表展示完整输出。
  - 无定量消融实验（如移除HV头、不同熵阈值、不同层数等）。
  - 无外部benchmark对比（计划中但未执行）。

- **充分性评价**：
  - **不充分**。作者明确表示本论文为“初步验证”，重点展示框架可行性；全面的外部对比、消融、统计显著性、临床下游分析均计划在后续Nature Methods companion paper中完成。
  - 实验设计存在风险：未提供与SOTA方法的定量比较，无法判断UNI2-UPERHOVER相对现有方法的优势；无重复种子评估（仅单次）。自我验证（自洽性）不能代替真实地面真值评价。
  - 优点在于公开了伪标签数据集和模型权重，便于第三方复现和后续研究。

### 6. 论文的主要结论与发现

- 框架可行性：UNI2-UPERHOVER在PanNuke测试集上达到0.931 mIoU（Stage1），Stage2/3在TCGA-UT伪标签上分别达到0.820和0.772 mIoU。尽管伪标签质量低于人类标注，但三阶段课程展示了伪标签质量提升（初始mIoU逐阶段提高：0.41→0.47→0.53）。
- 渐进式课程有效：不依赖权重迁移，仅靠更高置信度的伪标签驱动模型改善，避免了单步自训练的误差积累。
- TME流水线输出合理：四个HNSCC外部案例的分割和Token输出与视觉表型一致，证明跨染色、跨组织的泛化能力。
- BioNeMo GPT能生成结构化、基于数值的临床叙述，潜在降低幻觉风险。

### 7. 优点：方法或实验设计上的亮点

1. **创新架构**：UNI2-UPERHOVER将双头UperNet解码器组合于病理基础模型上，同时处理语义和HV回归，且支持动态HV目标合成（无需实例标注），极具实用性。
2. **渐进式伪标签课程**：三阶段、无权重继承的从头训练策略，有效扩展了标注规模至160万图块，且每个阶段通过熵过滤保证伪标签质量，是一种新颖的半监督域适应方案。
3. **结构化TME特征+LLM生成报告**：先提取量化指标，再通过规则转换为生物学Token，最后微调LLM生成临床叙述。这种“数值→Token→文本”的分层设计可审计、可解释，明确降低了幻觉风险。
4. **公开资源**：发布最大规模含细胞级伪标签的TCGA-UT数据集（1,608,060图块）和三个模型检查点，推动空间生物学社区研究。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验覆盖不足**：
  - 缺乏外部基准对比（vs HoVer-Net, CellPose, StarDist等）。
  - 缺少消融研究（如HV头贡献、FPN层数、熵阈值影响、课程阶段数）。
  - 缺少统计显著性（无置信区间、无重复种子的P值）。
  - 缺少伪标签质量的人工验证（仅计划200张病理学家标注样本）。
  - 缺少临床下游任务验证（如生存分析、免疫组化相关性）。
  - BioNeMo叙述质量未定量评估（BLEU, ROUGE-L, Cohen's Kappa, 幻觉率均推迟）。
- **技术局限**：
  - 动态HV目标合成对同类型相邻细胞可能引入近似误差；文中提到计划加入形态预分离或学习型细化模块。
  - 图块拼接时last-writer HV映射可能产生梯度不连续；计划使用高斯羽化混合。
  - 规则化Token阈值当前基于文献，未来可替换为结局驱动的分类器。
  - 模型训练仅使用单一种子，泛化稳定性未知。
- **偏差风险**：
  - 训练数据TCGA-UT主要来自欧美医院，可能存在种族/机构偏差。
  - 伪标签过程可能放大初始模型（PanNuke）的偏差（如特定癌种或染色偏好）。
  - 仅报告内部验证，选择性报告可能偏向积极结果。
- **应用限制**：
  - 当前框架仅输出每个图块级别描述，未集成全切片WSI级聚合（如肿瘤区域汇总）。
  - LLM部分依赖预定义Token和文本模板，灵活性有限；且需要大量人工撰写训练对。
  - 计算资源需求高（8×A100），不易在资源有限环境中部署。

**（完）**
