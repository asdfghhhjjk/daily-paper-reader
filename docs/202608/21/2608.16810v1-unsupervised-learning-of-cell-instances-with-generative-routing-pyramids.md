---
title: Unsupervised Learning of Cell Instances with Generative Routing Pyramids
title_zh: 基于生成式路由金字塔的细胞实例无监督学习
authors: "Ziwen Liu, Martin Weigert"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16810v1"
tags: ["query:cell-path"]
score: 8.0
evidence: 从显微图像中进行无监督细胞实例分割和表型分类
tldr: 显微镜图像中细胞实例分割和表型分类通常依赖监督学习和人工标注，且分割与特征表示分离。本文提出一种无监督生成式路由金字塔方法，通过粗到细重建将像素与稀疏潜在源关联，同时生成实例掩码和细胞形态编码。该方法在多种细胞形态和成像模态上取得有竞争力的实例分割性能，并展示了扰动下表型的生成式建模。该工作无需标注，统一了实例分割与表型表示，为无监督细胞分析提供了新思路，代码已开源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.16810v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1236, \"height\": 699}, {\"url\": \"assets/figures/arxiv/2608.16810v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 1006}, {\"url\": \"assets/figures/arxiv/2608.16810v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1255, \"height\": 1043}, {\"url\": \"assets/figures/arxiv/2608.16810v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1247, \"height\": 639}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.16810v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1223, \"height\": 424}, {\"url\": \"assets/tables/arxiv/2608.16810v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1243, \"height\": 265}, {\"url\": \"assets/tables/arxiv/2608.16810v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1197, \"height\": 621}]"
motivation: 现有细胞实例分割依赖监督标注，且分割与表型表示分离，难以扩展到无标注场景。
method: 提出无监督生成式路由金字塔，通过粗到细重建将像素关联到稀疏潜在源，同时产生实例掩码和形态编码。
result: 在多种细胞形态和成像模态上实现有竞争力的实例分割，并展示扰动下表型的生成式建模能力。
conclusion: 该方法无需标注，统一实例分割与表型表示，为无监督细胞分析提供新途径，代码已公开。
---

## 摘要
在显微镜图像分析中，识别并表征细胞或细胞核等对象实例是一项常见任务。现有的机器学习流程通常采用有监督检测或分割，再进行特征提取或分类，这需要人工标注，并将实例分割与细胞表征视为独立阶段。我们提出一种新的无监督方法，用于从未标注的显微镜图像中进行细胞实例分割和表型分类。该方法基于重建每幅图像，利用由粗到精的路由金字塔将像素与空间稀疏的潜在源相关联。由此产生的像素-潜在源关联得到实例掩码，而源潜在变量编码细胞形态。我们在多种细胞形态和成像模态下展示了具有竞争力的实例分割性能，以及扰动条件下细胞表型的生成建模。源代码和检查点可在 https://github.com/weigertlab/routing-pyramids 获取。

## Abstract
Identifying and representing object instances such as cells or nuclei is a common task in microscopy image analysis. Established machine learning workflows typically use supervised detection or segmentation followed by feature extraction or classification, which requires manual annotations and treats instance segmentation and cell representation as separate stages. We describe a new unsupervised method for cell instance segmentation and phenotypic classification from unlabeled microscopy images. Our method is based on reconstructing each image using a coarse-to-fine routing pyramid that associates pixels with spatially sparse latent sources. The resulting pixel-to-latent associations yield instance masks, while the source latents encode cell morphology. We demonstrate competitive performance in instance segmentation across diverse cell morphologies and imaging modalities, as well as generative modeling of cellular phenotypes under perturbations. Source code and checkpoints are available at https://github.com/weigertlab/routing-pyramids.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- 显微图像分析中，细胞或细胞核等对象实例的识别与表征是常见任务，广泛用于细胞计数、形态学分析和追踪。
- 现有主流方法通常依赖**有监督**检测或分割，需要大量人工标注；并且实例分割与细胞表征往往被当作两个独立阶段处理，导致数据标注和训练成本重复。
- 本文提出一种**无监督**方法，从无标注显微图像中同时学习细胞实例分割和表型表征，将分割与表示统一到同一个生成式重建框架中。
- 核心思想是：利用粗到细的“路由金字塔”重建图像，将每个像素与空间稀疏的潜在源关联起来，从而既得到实例掩码，又得到编码细胞形态的潜在向量。

## 2. 论文提出的方法论

### 2.1 总体框架
- 模型由**编码器**和**路由金字塔解码器**组成。
- 编码器从输入图像预测**前景潜在场**和**背景潜在场**（均为高斯后验，空间步长 δ=8，潜在维度 d=64）。
- 前景候选通过 sigmoid 门控产生**软存在图（presence map）** s_u，表示前景候选在空间位置 u 的激活程度。
- 解码器使用**由粗到精的金字塔结构**，每一层在局部 3×3 邻域内用**软注意力路由**选择父位置，逐步将粗粒度信息扩散到全分辨率。

### 2.2 路由金字塔与像素-源关联
- 每层解码器输出局部路由权重 \(T^\ell_{ji}\)，表示目标位置 j 从上一层邻域位置 i 获取信息的概率。
- 各层路由构成一个有向无环图（DAG），从输出像素指向潜在种子网格。
- 将各层转移概率沿路径相乘，得到**像素到种子的关联矩阵 A**，其中 \(A_{nu}\) 表示像素 n 分配到种子 u 的概率，且每行之和为 1。
- 最终实例掩码由种子组件扩展得到：对每个连通种子组件 \(C_k\)，计算像素处的前景质量 \(m_k(n)=\sum_{u\in C_k} A_{nu}s_u\)，将像素分配给质量最大的组件（超过阈值 \(\tau_m=0.1\)）。

### 2.3 训练目标
- **重建损失**：L1 损失，即重建图像与原始图像的逐像素绝对误差。
- **KL 正则项**：分别对前景和背景潜在后验施加标准正态先验，鼓励潜在空间正则化。
- **稀疏惩罚**：使用凹函数 \(((s_u+\epsilon)^\alpha-\epsilon^\alpha)\)，\(\alpha=0.5\)，促使前景容量集中到少量种子区域。
- **流动正则化**：惩罚每层路由的期望局部几何代价（平方步长），防止无意义的远程跳跃。

总损失为上述各项的加权和，权重在所有实验中固定（如 \(\lambda_{rec}=1.0\)、\(\lambda_{fg}=0.01\)、\(\lambda_{bg}=0.05\)、\(\lambda_{flow}=0.005\)，稀疏项权重按数据集选择 0.2 或 0.5）。

### 2.4 推理与表征提取
- 推理时使用潜在场的后验均值，不再采样。
- 对存在图进行阈值化（\(\tau_s=0.5\)），用 8-连通性得到种子组件。
- 每个组件的对象表征 \(e_k\) 定义为该组件内前景后验均值的 presence 加权平均。
- 该表征可用于下游表型分析、聚类、生成和检索。

## 3. 实验设计

### 3.1 数据集
- **实例分割数据集**：
  - **Allen 核形态数据集**：人诱导多能干细胞（hiPSCs），核膜荧光（Lamin B1-mEGFP），可见核膜结构。
  - **Fluo-N2DL-HeLa**：HeLa 细胞，细胞核荧光（H2B-GFP），来自 Cell Tracking Challenge。
  - **PhC-C2DL-PSC**：大鼠胰腺干细胞，相差成像，分割整个细胞体。
- **表型表征数据集**：
  - **BBBC013**：U2OS 细胞双通道荧光图像，使用 LY294002 和 wortmannin 两种药物扰动，包含稀释系列和对照。

### 3.2 基准与对比方法
- **有监督参考**：Cellpose-SAM（冻结权重）。
- **无监督 baseline**：Cellulus。
- **非参数 baseline**：Otsu 阈值 + 连通域。
- 评估指标：实例级 F1（IoU 阈值 0.5、0.7、0.9）和全景质量（PQ）。

### 3.3 训练细节
- 单通道图像，256×256 裁剪，batch size 64，训练 200 epochs。
- 所有数据集使用相同模型架构（潜在维度 d=64）。
- PhC-PSC 图像双线性上采样 2 倍；Allen 图像最大强度投影后 2 倍平均池化下采样。

## 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量或总训练时长。
- 仅给出了训练超参数：batch size 64，200 epochs，256×256 裁剪。
- 因此无法从文中推断具体算力消耗。

## 5. 实验数量与充分性

- 共包含 **3 个实例分割数据集**和 **1 个药物扰动表征数据集**，覆盖荧光、相差、核与全细胞等不同条件。
- 对比了有监督参考、无监督 baseline 和非参数 baseline 三种类型，比较较全面。
- 评估指标包含不同 IoU 阈值下的 F1 和 PQ，能反映不同严格程度下的性能。
- 但论文**未报告消融实验**，例如损失项权重、潜在维度、金字塔层数等关键设计选择的影响。
- 未报告多次运行的方差或统计显著性检验，结果的稳健性有待进一步验证。
- 公平性方面：Baseline（Cellulus 和 Otsu）使用了在测试集上选择的后处理参数，而本文方法使用固定后处理参数，因此对比对本文方法更不利，结果仍更优，说明方法具有一定优势。

## 6. 主要结论与发现

- 生成式路由金字塔在**无标注**条件下能够学习细胞实例分割和形态表征。
- 在三个数据集上，该方法在无监督方法中取得最佳表现，PQ 分别达到 0.867、0.800、0.518，优于 Cellulus 和 Otsu。
- 在 Allen 数据集上，其 PQ 和严格 IoU 0.9 下的 F1 甚至**超过有监督的 Cellpose-SAM**（PQ 0.867 vs 0.859；F1@0.9 0.649 vs 0.256），表明边界更接近真实标注。
- 在 PhC-PSC 相差图像上，所有方法在 IoU 0.9 时表现都很差（<0.1），反映低分辨率相差图像边界精确分割的困难。
- 在药物扰动实验中，对象潜在编码能按剂量形成簇，GMM 可区分阳性和阴性对照，达到 100% 准确率；生成和最近邻检索能恢复相应表型。

## 7. 优点

- **无需标注**：完全无监督，降低数据成本。
- **统一框架**：同时输出实例分割和对象表征，避免两阶段流程。
- **端到端可微**：通过软路由而非不可微的硬分配，训练稳定。
- **灵活的实例表示**：潜在编码可直接用于下游聚类、生成和检索。
- **跨数据集后处理一致**：本文方法使用固定阈值和最小面积等参数，而 baselines 需要数据集特定调参，仍在多数指标上更优。
- **生成式可解释性**：潜在空间能反映扰动诱导的表型变化，具有较好的可解释性。

## 8. 不足与局限

- **每数据集单独训练**：模型不能跨数据集泛化，面对新样本需重新训练。
- **假设较强**：假设对象紧凑、外观相似、背景平滑，对复杂组织成像等场景可能不适用。
- **算力未报告**：无法评估训练成本。
- **缺少消融实验**：损失项权重、潜在维度、金字塔深度等关键设计未系统研究。
- **缺少多次运行统计**：未报告方差或置信区间，结果稳定性未知。
- **相差图像边界精度差**：在 PhC-PSC 上严格 IoU 下表现不佳，说明该方法在低对比度、低分辨率图像上仍有挑战。
- **数据集覆盖有限**：仅在少数细胞类型和成像模态上验证，可能存在偏差风险。

（完）
