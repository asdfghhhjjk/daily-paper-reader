---
title: "MambaBack: Bridging Local Features and Global Contexts in Whole Slide Image Analysis"
title_zh: MambaBack：在全视野切片图像分析中连接局部特征与全局上下文
authors: "Sicheng Chen, Chad Wong, Tianyi Zhang, Enhui Chai, Zeyu Liu, Fei Xia"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15729v1"
tags: ["query:cellrep"]
score: 10.0
evidence: 使用基于Mamba的多实例学习和局部细胞结构进行全切片图像分析
tldr: 本文针对全视野病理图像分析中局部信息丢失和内存开销大等问题，提出结合Mamba和MambaOut优点的MambaBack架构，通过Hilbert采样保持空间感知，层次化设计融合局部Gated CNN与全局BiMamba2特征，并采用非对称分块策略优化推理效率，实验结果显示性能优于多种先进模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15729v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of MambaBack. a. MIL pipeline with MambaBack. b. MambaBack structure. c. Heatmap visualization. d. Inference memory usage comparison.\", \"page\": 2, \"index\": 1, \"width\": 1068, \"height\": 802}, {\"url\": \"assets/figures/arxiv/2604.15729v1/fig-002.webp\", \"caption\": \"Table 1: The performance comparison with SOTA methods.\", \"page\": 5, \"index\": 2, \"width\": 1064, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2604.15729v1/fig-003.webp\", \"caption\": \"Figure 2: Analysis of WSI tile distributions and ablation studies on key components. a. Distribution of tile counts across datasets. b. Ablation study on sampling strategies on PANDA dataset. c. Ablation study on model architectures on PANDA dataset.\", \"page\": 5, \"index\": 3, \"width\": 1068, \"height\": 463}]"
motivation: 现有Mamba在WSI分析中难以同时保持2D空间信息、局部结构建模和内存效率。
method: 提出MambaBack混合架构，结合Gated CNN和BiMamba2模块，并采用Hilbert采样和非对称分块实现局部与全局特征的高效融合。
result: 在五个数据集上优于七种最新方法，在精度和内存效率上均表现突出。
conclusion: MambaBack能高效结合局部细粒度特征和全局上下文信息，为病理图像分析提供更优性能和可部署性。
---

## 摘要
全视野切片图像（Whole Slide Image，WSI）分析在计算病理学中至关重要，它通过整合不同放大倍数下的形态学与结构特征，实现癌症诊断。多实例学习（Multiple Instance Learning，MIL）是WSI分析的标准框架。近年来，Mamba作为MIL的有前景骨干网络，凭借其源自自然语言处理（NLP）的高效性与全局上下文建模能力，正在超越Transformer。然而，现有基于Mamba的MIL方法面临三大挑战：（1）在将二维空间展平成一维序列时破坏了二维空间局部性；（2）对精细的细胞局部结构建模不足；（3）在资源受限的边缘设备上推理时出现峰值内存消耗高的问题。已有研究（如MambaOut）表明，Mamba的SSM组件在局部特征提取中是冗余的，Gated CNN已足够胜任。鉴于WSI分析既需要类似自然图像的细粒度局部特征提取，又需要类似NLP的全局上下文建模，我们提出MambaBack，这是一种融合Mamba与MambaOut优势的新型混合架构。首先，我们提出Hilbert采样策略，在一维序列中保留图块的二维空间局部性，从而增强模型的空间感知。其次，我们设计了一个分层结构，其中基于MambaOut的一维Gated CNN模块用于捕获局部细胞特征，而BiMamba2模块用于聚合全局上下文，从而共同提升多尺度表示能力。最后，我们引入非对称分块设计，使训练阶段可并行处理，而推理阶段通过分块-流式累积机制显著降低峰值内存占用，便于模型部署。基于五个数据集的实验结果表明，MambaBack在性能上优于七种最新的先进方法。源代码与数据集已公开。

## Abstract
Whole Slide Image (WSI) analysis is pivotal in computational pathology, enabling cancer diagnosis by integrating morphological and architectural cues across magnifications. Multiple Instance Learning (MIL) serves as the standard framework for WSI analysis. Recently, Mamba has become a promising backbone for MIL, overtaking Transformers due to its efficiency and global context modeling capabilities originating from Natural Language Processing (NLP). However, existing Mamba-based MIL approaches face three critical challenges: (1) disruption of 2D spatial locality during 1D sequence flattening; (2) sub-optimal modeling of fine-grained local cellular structures; and (3) high memory peaks during inference on resource-constrained edge devices. Studies like MambaOut reveal that Mamba's SSM component is redundant for local feature extraction, where Gated CNNs suffice. Recognizing that WSI analysis demands both fine-grained local feature extraction akin to natural images, and global context modeling akin to NLP, we propose MambaBack, a novel hybrid architecture that harmonizes the strengths of Mamba and MambaOut. First, we propose the Hilbert sampling strategy to preserve the 2D spatial locality of tiles within 1D sequences, enhancing the model's spatial perception. Second, we design a hierarchical structure comprising a 1D Gated CNN block based on MambaOut to capture local cellular features, and a BiMamba2 block to aggregate global context, jointly enhancing multi-scale representation. Finally, we implement an asymmetric chunking design, allowing parallel processing during training and chunking-streaming accumulation during inference, minimizing peak memory usage for deployment. Experimental results on five datasets demonstrate that MambaBack outperforms seven state-of-the-art methods. Source code and datasets are publicly available.

---

## 论文详细总结（自动生成）

# MambaBack：在全视野切片图像分析中连接局部特征与全局上下文  

---

## 一、研究背景与核心问题

- **研究动机**：  
  全视野切片图像（Whole Slide Image, WSI）在病理学中用于癌症诊断，需要在极高分辨率下整合细胞级和组织级特征。然而，传统的多实例学习（Multiple Instance Learning, MIL）框架在分析WSI时面临以下困难：  
  1. **2D空间局部性丧失**：WSI在分析时通常被展平为一维序列，破坏了原始空间邻接关系。  
  2. **局部细粒度建模不足**：现有基于状态空间模型（SSM，尤其是Mamba）的方法在捕捉细胞结构特征上不如卷积网络。  
  3. **高内存占用问题**：在资源受限的边缘设备上进行推理时，模型易出现内存峰值过高而难以部署。  

- **研究目标**：  
  作者希望提出一个既能保持空间局部感知、又能高效融合局部与全局特征，同时在推理阶段具备高资源利用率的WSI分析模型。  

---

## 二、方法论与技术方案

### 1. 核心思想
提出 **MambaBack**，一种结合 Mamba 与 MambaOut 优点的 **混合架构**，同时处理局部细胞特征和全局组织上下文信息。

### 2. 关键设计与结构

- **Hilbert采样策略**  
  - 利用 Hilbert 空间填充曲线，在将二维图像块（tile）映射到一维序列时保持空间邻域关系。  
  - 步骤包括坐标稠密化、递归Hilbert序列生成及连续分块采样。  
  - 引入随机连续块采样作为训练阶段的数据增强，提升空间连续性学习。

- **分层架构设计**  
  - **局部特征提取阶段**：基于 *MambaOut* 的 **一维Gated CNN块**，负责捕捉细胞级细粒度结构。  
  - **全局特征聚合阶段**：基于 *BiMamba2* 的 **双向状态空间模块**，用于融合滑片总体上下文信息。  
  - 层之间通过局部门控注意力和全局门控注意力机制连接。

- **非对称分块（Asymmetric Chunking）设计**  
  - **训练阶段**：并行处理所有分块，提高 GPU 吞吐量与稳定性。  
  - **推理阶段**：采用分块流式累积（Chunk-and-Accumulate）机制，将各块逐次加载并释放中间特征图，从而将峰值内存限制为常数级，与WSI大小无关。  

### 3. 模型流程概述
1. WSI → 分块（Tile） → 冻结特征提取器（如 Prov-GigaPath） → 获得Tile特征。  
2. 采用 Hilbert 序列重排 → 形成空间感知的一维输入序列。  
3. 局部阶段（Gated CNN）提取细粒度特征 → 压缩为代表token。  
4. 全局阶段（BiMamba2）在双向上下文中建模整体组织结构。  
5. 通过全球门控注意力生成最终 slide-level embedding → 分类或生存分析输出。  

---

## 三、实验设计

- **使用数据集（共 5 个）与任务（4 种）**：  
  1. **CAMELYON16** — 乳腺癌淋巴结转移二分类。  
  2. **CAMELYON17** — 乳腺癌转移多分类。  
  3. **PANDA** — 前列腺癌ISUP等级评估（多分类）。  
  4. **TCGA-NSCLC** — 肺癌亚型分类。  
  5. **TCGA-BRCA** — 乳腺癌患者生存期预测（回归/生存任务）。

- **对比基线（7种先进方法）**：  
  - ABMIL  
  - CLAM  
  - DTFD-MIL  
  - TransMIL  
  - Prov-GigaPath  
  - MambaMIL  
  - PathRWKV  

- **评估指标**：  
  - 分类任务：Accuracy、AUC、F1-score  
  - 生存分析任务：C-Index  

---

## 四、资源与算力使用

- 模型训练环境说明：  
  - 使用 **4 张 NVIDIA RTX 4090 GPU**。  
  - 框架：Python 3.12 + PyTorch 2.9 + CUDA 12.8。  
  - 优化器：AdamW，并采用 Cosine 学习率衰减。  
  - 未具体给出训练时间，但采用早停（patience=10 epoch）策略。  
  - 可推断资源充足，实验在高性能GPU环境完成。  

---

## 五、实验数量与充分性

- **核心评估实验**：在五个公开数据集上与七个SOTA方法对比。  
- **消融实验**：  
  1. **采样策略对比**（Hilbert、Z-order、随机、顺序）。  
  2. **架构对比**（纯Gated CNN、纯BiMamba2、替换型MambaBack(R)）。  
  3. **内存分析**：不同结构在推理阶段的峰值内存比较。  

- **实验充分性与公平性**：  
  - 每个数据集均采用5折交叉验证。  
  - 同一Hilbert采样策略在所有模型上统一使用以保证公平对比。  
  - 指标全面（AUC、Acc、F1、C-Index），覆盖分类与生存预测任务。  
  ➜ 评估设计相对客观且充分。  

---

## 六、主要结论与发现

- MambaBack在所有五个数据集上均取得最佳性能：  
  - 高于七种SOTA方法的平均AUC、F1得分；  
  - 示例：在 CAMELYON17 上 F1 提升至 0.513，在 PANDA 上达 0.733。  
- 模型在保持高精度的同时显著降低推理内存，占用接近轻量模型（CLAM）。  
- 证明 Hilbert 采样显著提升空间一致性，层次混合结构能同时捕捉微观与宏观病理特征。  
- 通过非对称分块技术，使模型具备 **临床部署可行性**，适用于边缘设备实时病理分析。  

---

## 七、方法与设计亮点

- **创新性高**：提出Hilbert采样和混合SSM+CNN架构的整合方法。  
- **训练推理双优化**：同时提升并行训练效率与推理时内存稳定性。  
- **空间感知能力强**：通过Hilbert曲线保持了WSI的空间连续性。  
- **模型通用性与可扩展性好**：适用于多任务、多数据集。  
- **部署友好**：有效应对临床边缘设备的内存约束。  

---

## 八、不足与潜在局限

- **训练开销较大**：虽然推理阶段高效，但训练仍需大型GPU集群。  
- **Hilbert采样依赖坐标稠密化过程**：对极度稀疏且异构的组织区域可能不适用。  
- **跨模态表现未验证**：当前仅在图像特征层面验证，未涉及文本或分子数据融合。  
- **临床验证有限**：结果虽优于SOTA，但缺乏真实临床应用场景的验证实验。  

---

**（完）**
