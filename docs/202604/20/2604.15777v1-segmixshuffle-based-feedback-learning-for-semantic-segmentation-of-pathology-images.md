---
title: "SegMix:Shuffle-based Feedback Learning for Semantic Segmentation of Pathology Images"
title_zh: SegMix：基于打乱反馈学习的病理图像语义分割方法
authors: "Zhiling Yan, Sicheng Chen, Tianyi Zhang, Nan Ying, Yanli Lei, Guanglei Zhang"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15777v1"
tags: ["query:pathseg"]
score: 10.0
evidence: 利用基于洗牌的反馈学习和图像级标签进行病理图像语义分割
tldr: 本文针对病理图像语义分割标注昂贵的问题，提出SegMix方法，通过基于课程学习的patch级洗牌反馈机制生成高质量伪分割掩码，仅需图像级标签。实验表明该方法在多个数据集上性能显著优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15777v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the proposed method. The model adaptively adjusts the schedulers in the left blue box based on the feedback from previous learning. The patch size scheduler and shuffle ratio scheduler regulate the patch size and shuffle ratio. Then, relation patches are in-place shuffled across the same batch, maintaining their relative positions the same in images. Lastly, the mixed and original images are fed into the Weekly Supervised Segmentation Network for twins’ steps training in the green boxes.\", \"page\": 2, \"index\": 1, \"width\": 1068, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2604.15777v1/fig-002.webp\", \"caption\": \"Table 1: Dice score (%) and IoU (%) of methods for weakly supervised semantic segmentation. Ours w/o FL denotes the proposed method without the feedback learning. The best results are in bold.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 394}]"
motivation: 由于获取高质量像素级病理分割标注代价高昂，研究旨在利用图像级标签实现弱监督分割。
method: 提出一种基于课程学习思想的洗牌反馈学习方法，通过病理图像的patch级洗牌并动态调整策略生成伪分割掩码。
result: 在三个不同数据集上，该方法优于现有最先进的弱监督分割方法。
conclusion: 该方法证明了在病理图像语义分割任务中，通过基于反馈的洗牌学习可以在弱监督条件下获得高质量的伪标注。
---

## 摘要
在计算病理学中，分割是一项关键任务，因为它可以识别疾病或异常增生所影响的区域，对于诊断和治疗至关重要。然而，获取高质量的像素级监督分割数据需要有经验的病理学家投入大量工作，这限制了深度学习的应用。为应对这一挑战，通过将标签条件放宽至图像级分类标签，可以使用更多数据并启用更多场景。一种可行的方法是利用类激活图（Class Activation Map, CAM）在仅有图像级标签的情况下生成伪像素级语义分割标注。然而，该方法未能充分探索病理图像的关键特征，因此通常只能识别出较小区域，难以生成足够的伪掩膜。为此，本文提出了一种受课程学习启发的基于打乱的反馈学习方法，用于生成更高质量的伪语义分割掩膜。具体而言，我们对病理图像执行图块级打乱操作，模型根据先前学习的反馈自适应地调整打乱策略。实验结果表明，在三个不同数据集上，我们提出的方法优于现有的最新方法。

## Abstract
Segmentation is a critical task in computational pathology, as it identifies areas affected by disease or abnormal growth and is essential for diagnosis and treatment. However, acquiring high-quality pixel-level supervised segmentation data requires significant workload demands from experienced pathologists, limiting the application of deep learning. To overcome this challenge, relaxing the label conditions to image-level classification labels allows for more data to be used and more scenarios to be enabled. One approach is to leverage Class Activation Map (CAM) to generate pseudo pixel-level annotations for semantic segmentation with only image-level labels. However, this method fails to thoroughly explore the essential characteristics of pathology images, thus identifying only small areas that are insufficient for pseudo masking. In this paper, we propose a novel shuffle-based feedback learning method inspired by curriculum learning to generate higher-quality pseudo-semantic segmentation masks. Specifically, we perform patch level shuffle of pathology images, with the model adaptively adjusting the shuffle strategy based on feedback from previous learning. Experimental results demonstrate that our proposed approach outperforms state-of-the-arts on three different datasets.

---

## 论文详细总结（自动生成）

# SegMix：基于打乱反馈学习的病理图像语义分割方法 — 深度总结

---

## 一、研究背景与核心问题

- **研究动机：**  
  病理图像语义分割是计算病理学中的关键任务，它帮助识别组织中异常或病变区域，是辅助诊断的重要工具。  
  当前主流深度学习分割模型依赖于**高质量像素级标注**，而这些标注通常需要经验丰富的病理学家手工完成，成本极高且效率低。

- **核心问题：**  
  在仅有**图像级标签（weak supervision）**的情况下，如何实现高精度的像素级语义分割？  
  现有基于 Class Activation Map (CAM) 的方法往往只能定位病变区域的一小部分，缺乏对病理图像的多尺度特征与实例关系的充分探索。

- **论文目标：**  
  作者希望通过**基于打乱的反馈学习机制**生成更高质量的伪标注（pseudo masks），从而在弱监督条件下获得接近全监督的方法性能。

---

## 二、方法论：SegMix 的核心思想与技术细节

### 1. 总体框架

SegMix 整体框架包含三个主要模块：

1. **反馈学习模块 (Feedback Learning Module)：**  
   - 引入两个动态调度器（Schedulers）：
     - **Patch-size Scheduler**：用于控制打乱的块（patch）大小。
     - **Shuffle-ratio Scheduler**：用于控制不同图像间混合的比例。  
   - 模型在训练过程中根据**前一阶段的学习反馈（loss 值与阈值 T）**自适应调整这两个参数，实现由**粗到细**的学习过程，体现出类课程学习（Curriculum Learning）的思想。

2. **打乱拼接模块 (Shuffle Patches)：**  
   - 将病理图像切分为大小为 `p × p` 的小块。  
   - 从同一批次中不同图像的对应位置随机选取一部分块进行“**in-place shuffle**”，其余块保持不变，形成混合图像。  
   - 公式化表达：  
     \( P_{mix,i} = M \odot P_{F,i} + (1-M) \odot P_{R,i} \)  
     其中 \( M \) 为二值掩码，指示是否混合该 patch。  
   - 这种混合操作允许模型同时观察**局部与全局特征**并捕捉**跨样本的实例关系**。

3. **像素相关模块 (Pixel Correlation Module, PCM)：**  
   - 对基础 CAM 特征进行增强，计算像素间的余弦相似度来衡量空间一致性。  
   - 更新后的 CAM 公式：  
     \( Y_p = \frac{1}{P} \sum f(x_i, x_j) \sigma(\theta(X^T)\theta(X))Y \)  
   - 有助于提高预测的平滑性与连贯性。

---

## 三、实验设计与数据集

### 1. 使用的数据集

论文在三个公开病理影像数据集上进行了验证：

1. **ROSE**：胰腺快速评估（Rapid On-Site Evaluation）图像，共 1773 癌症样本与 3315 正常样本。  
2. **WBC**：白细胞图像，包括多种类型（嗜酸性粒细胞、单核细胞、淋巴细胞等）。  
3. **MARS**：胃组织样本，包含正常与两种腺癌类型（管状腺癌、黏液腺癌），共 1770 张图像。

### 2. 对比方法

对比的弱监督分割方法包括：
- CAM（Class Activation Map 基线）
- SEAM（Self-supervised Equivariant Attention Mechanism）
- CPN（Complementary Patch Network）  
此外还设置**“Ours w/o FL”**（去除反馈学习模块）作为内部消融。

### 3. 实验指标

使用标准分割评估指标：
- Dice Score (DSC)
- Intersection over Union (IoU)

---

## 四、算力与资源配置

- 实验环境：Ubuntu + PyTorch 1.10 + CUDA 11.3 + Python 3.8。  
- 硬件资源：**Intel Xeon Platinum 8350C CPU + 2 × NVIDIA RTX 3090 GPU**，但每次训练仅使用**单张 GPU**。  
- 未明确报告训练时长，仅说明迭代 8 次（batch size = 4）。  
👉 **整体算力信息有限，但足以判断实验属于中等规模 GPU 训练。**

---

## 五、实验数量与充分性

- 总体包括：
  1. **三个公开数据集主实验**（ROSE, WBC, MARS）。  
  2. **消融实验（Ablation Study）**：验证反馈学习模块与不同打乱策略的贡献。  
     - “Group”、“Back”、“Ours” 三种打乱和反馈变体。  
  3. **可视化分析**：展示不同方法的分割掩码对比图。

- **实验充分性：**
  - 涵盖多种细胞与组织尺度场景。  
  - 对比方法均为弱监督分割领域的代表性模型。  
  - 消融结果定量且解释合理，具备较强客观性。  
  - 但未涉及跨模态或三维数据，验证范围仍局限于二维病理图像。

---

## 六、主要结论与发现

- SegMix 在三项 benchmark 上都取得**显著优于现有方法**的结果：
  - ROSE：DSC 提升至 60.7%，IoU 提升至 50.2%。  
  - WBC：DSC 提升至 41.5%，IoU 提升至 29.4%。  
  - MARS：DSC 提升至 40.6%，IoU 提升至 31.3%。  
- 加入**反馈学习模块**后性能提升更为明显，验证了模型自适应由粗到细调整策略的有效性。  
- 可视化结果显示该方法能更准确地定位病变区、减少背景噪声干扰。

---

## 七、优点与亮点

- **弱监督性强：**仅需要图像级标签即可实现像素级分割。  
- **课程学习式反馈机制：**模型能自调节学习阶段，从粗粒度到细粒度，提升多尺度特征捕捉能力。  
- **跨样本信息融合：**通过patch级打乱引入不同个体间的结构关系，提高泛化能力。  
- **模块组合简洁：**核心组件易于与其他分割网络结合，具备较高可扩展性。  
- **实验结果一致性好：**在不同数据集和类别上均展示稳定优势。

---

## 八、不足与局限

- **资源信息不完整：**未给出训练时间与显存需求等细节。  
- **应用范围限制：**目前仅验证二维病理切片，未扩展至三维或多模态医疗影像。  
- **伪标签的质量依赖模型反馈稳定性：**若早期学习反馈不准确，可能导致打乱策略选取不理想。  
- **数据集相对有限：**虽有三组，但均为中等规模公开数据集，缺乏更复杂的真实临床场景验证。  
- **跨病种泛化尚待验证：**尤其在不同器官与染色条件下是否仍有效未被探讨。

---

**（完）**
