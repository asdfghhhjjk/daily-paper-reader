---
title: "HistoSeg++: Delving deeper with attention and multiscale feature fusion for biomarker segmentation"
title_zh: HistoSeg++：利用注意力和多尺度特征融合深入进行生物标志物分割
authors: "Saad Wazir, Rao Faizan, Daeyoung Kim"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01675v1"
tags: ["query:cellseg"]
score: 8.0
evidence: 提出HistoSeg++用于医学图像生物标志物分割，使用注意力和多尺度特征融合。
tldr: 医学图像中生物标志物的精准分割是后续分析的基础，但现有方法难以有效捕获多尺度上下文信息，且跨数据集泛化能力不足。为此，本文提出HistoSeg++，一种改进的Nested-UNet架构，通过嵌入内外注意力单元和挤压-激励模块，在上采样过程中增强特征聚焦和通道重标定，并采用边缘感知损失强化边界对齐。在三个公开数据集上的评测显示，HistoSeg++的分割精度和泛化效果均显著优于现有Nested-UNet方法，尤其在边界细节上表现突出。该工作为病理图像中生物标志物的自动化高精度分割提供了新思路，有望推动相关生物医学研究和临床应用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01675v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1808, \"height\": 1186, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01675v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 932, \"height\": 697, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01675v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 894, \"height\": 1345, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01675v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 849, \"height\": 317, \"label\": \"Table\"}]"
motivation: 生物标志物分割任务中，现有方法常忽略多尺度上下文与边界细节，导致跨数据集泛化能力受限，影响后续分析准确性。
method: 在Nested-UNet基础上，融合内外注意力单元增强上采样特征，利用SE模块进行通道重标定，并结合边缘感知损失强化边界。
result: 在三个公开基准数据集上测试，HistoSeg++取得了更高的分割精度和更强的跨数据集泛化能力，验证了其有效性。
conclusion: 该工作表明，融合注意力与多尺度特征可有效提升分割精度和泛化性，对病理图像自动分析具有重要价值。
---

## 摘要
生物标志物在医学图像中的分割通常被视为任何生物信息学或生物医学应用中医学图像分析的第一步。尽管取得了一些进展，现有方法仍难以捕获多尺度信息，并跨不同数据集有效进行上采样，这些缺点往往导致泛化能力不佳。近年来，属于 Nested-UNet 家族的架构擅长捕获多尺度上下文信息并进行有效的上采样。在本工作中，我们提出了一种新颖的 Nested-UNet 架构，该架构能有效捕获多尺度上下文信息。它包含内部和外部注意力单元以增强上采样过程中的关注度，并结合挤压-激励模块进行通道级特征重校准，从而提升分割性能。此外，该架构整合了边缘感知损失，通过为边缘区域赋予更大重要性来强调边界准确性。在三个公开基准数据集上进行广泛测试，我们的方法展现出优于现有 Nested-UNet 方法的泛化性能。代码：https://github.com/saadwazir/histosegplusplus

## Abstract
Segmentation of biomarkers in medical images is frequently viewed as a first step towards medical image analysis in any bioinformatics or biomedical application. Despite progress, existing methods still struggle to capture information at multiple scales and to perform upsampling effectively across different datasets. These shortcomings often result in suboptimal generalization capabilities. Recently, architectures belonging to the Nested-UNet family excel in capturing multiscale contextual information and upsample them effectively. In this work, We propose a novel Nested-UNet architecture that effectively captures multi-scale contextual information. It includes inner and outer attention units to enhance focus during upsampling, along with channel-wise feature recalibration using squeeze-and-excitation modules, leading to improved segmentation performance. Additionally, the architecture integrates an edge-aware loss to emphasize boundary accuracy by assigning greater importance to edge regions. Tested extensively on three publicly available benchmark datasets. Our method demonstrates a generalization performance superior to existing Nested-UNet methods. Code: https://github.com/saadwazir/histosegplusplus

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- 医学图像中的生物标志物分割是病理分析、生物信息学等下游任务的关键预处理步骤，但该任务长期面临挑战：
  - 目标尺寸、形态差异大，边界模糊。
  - 现有方法（包括 Nested‑UNet 家族）难以同时捕获多尺度上下文信息并进行有效的上采样。
  - 跨数据集的泛化能力不足，在不同成像条件、细胞类型下性能下降明显。
- 该项工作旨在设计一种新的 Nested‑UNet 架构，通过强化注意力机制与多尺度融合，提升分割精度和泛化能力，为生物标志物分割提供更鲁棒的基线。

### 2. 论文提出的方法论
- **整体架构**：`HistoSeg++` 是一个由 **Outer‑UNet** 外部骨架和多个 **Inner‑UNet** 内部模块组成的嵌套 U 形网络。
- **Inner‑UNet (I‑UNet)**：
  - 每个 I‑UNet 模块内部是一个小的 U‑Net（编码器-解码器），采用参数 `k` 控制深度（如图中 I‑UNet 7/6/5/4）。
  - 编码器侧使用卷积块 + 最大池化；解码器侧使用上采样 + **注意力门（Attention Gate）** 过滤跳跃连接中的噪声。
  - 最后通过残差连接融合原始局部特征和聚合的多尺度长程特征。
- **关键组件**：
  - **Squeeze‑and‑Excitation (SE) 块**：全局平均池化 → 全连接层 → sigmoid 激活，得到通道缩放因子，实现通道级特征重标定。
  - **卷积块**：深度可分离卷积 + 批归一化 + LeakyReLU，用于特征提取和非线性变换。
  - **注意力门（Attention Gate）**：在跳跃连接处施加空间注意力，突出相关区域，抑制无关激活。
  - **ASPP（空洞空间金字塔池化）**：使用深度可分离空洞卷积，在不改变空间尺寸的前提下捕获多尺度高语义特征。
  - **桥接块**：在深层的小分辨率层采用空洞卷积，保持语义信息密度。
- **损失函数**：
  - 提出 **边缘感知损失**，在二元交叉熵的基础上，对真实边缘像素（通过 Sobel 滤波和梯度阈值检测）的损失赋予更高权重 `w`。
  - 定义为：
    \[
    L(y, \hat{y}) = \frac{1}{N} \sum_{i=1}^{N} \big(1 + (em_i \times (w-1))\big) \times \text{BCE}(y_i, \hat{y}_i)
    \]
    其中 `em` 是边缘掩码。

### 3. 实验设计
- **数据集**：
  - **MoNuSeg**（多器官核分割）：H&E 染色病理图像，30 张训练，14 张测试，尺寸 1000×1000。
  - **DSB (2018 Data Science Bowl)**：不同条件和生物源下的细胞核图像，尺寸 128×128 到 512×512。仅使用 stage1 训练集，自行 9:1 划分训练/评估。
  - **Electron Microscopy (EM)**：线粒体分割，CA1 海马区，训练和测试各 165 张，尺寸 768×1024。
- **数据增强**：离线增强（亮度对比度、网格扭曲、转置、弹性变换等）生成大量样本（MoNuSeg 8820，DSB 13710，EM 9852），在线增强包括旋转、平移、剪切、缩放、翻转。
- **基准方法与评估指标**：
  - 对比方法：UNet、HoVer‑Net、nnU‑Net、UNet++、UNet 3+、U2‑Net、Swin‑UNet、TransUNet。
  - 指标：IoU、Dice、Precision、Recall、HD95（95% 豪斯多夫距离）。
- **训练设置**：Adam 优化器，学习率 1e‑4，batch size 8，He Uniform 初始化，训练 200 epoch，用验证集选择最佳检查点，所有对比方法均从其公开代码复现并使用报告的最优设置。

### 4. 资源与算力
- 论文未明确说明使用的 GPU 型号、数量、训练时长或总计算量。
- 仅给出训练超参数（学习率、batch size、epoch 数），从 patch 生成和增强后的数据规模推断训练需要一定算力，但缺乏可重复的具体算力信息。

### 5. 实验数量与充分性
- **实验组数**：
  - 在三个数据集上分别进行了 8 种方法（含本文方法）的完整对比，每个数据集提供 5 项指标。
  - 在 MoNuSeg 上进行了 6 组消融实验（逐步添加组件）。
- **充分性与公平性**：
  - 对比方法覆盖 CNN 基线、Nested‑UNet 变体、Transformer 方法，覆盖面广。
  - 使用统一数据预处理和评估流程复现基线，结果客观。
  - 消融实验清楚展示了各模块的增益，表明设计贡献非偶然。
  - 不足：DSB 数据集采用自行划分，可能与标准基准不完全一致；未在更大规模或完全外部的数据集上进行泛化测试；缺少统计显著性检验。

### 6. 论文的主要结论与发现
- HistoSeg++ 在三个分割数据集上均取得了最优或次优结果，尤其在 IoU 和 Dice 上显著超过 UNet++、nnU‑Net 等强基线。
- 内外注意力、SE 重标定以及 ASPP 的组合有效提升了特征表示和上采样质量。
- 边缘感知损失能强化边界准确性，带来更清晰的分割边界。
- 整体框架相比纯 Transformer 方法（Swin‑UNet、TransUNet）在标注样本有限时泛化性更优。

### 7. 优点
- **多尺度特征融合设计**：外层 U‑Net 与内层 I‑UNet 嵌套，自然实现多级上下文聚合。
- **注意力机制系统整合**：空间注意力门 + 通道 SE 注意力，在编码器和解码器上均发挥作用。
- **边界精细化**：边缘感知损失直接优化边界区域，提升了分割的几何精度。
- **全面的实验验证**：在多个模态和任务上评估，对比方法丰富，消融实验扎实。
- **可复现性**：代码开源，训练细节清晰。

### 8. 不足与局限
- **算力与效率**：作者指出模型改进会带来更大计算开销，但未量化推理速度或参数规模。
- **数据集规模与多样性有限**：三个数据集总体样本量不大，且均来自特定医学图像领域，跨机构、跨染色协议的鲁棒性未验证。
- **仅支持二值分割**：当前设计针对单目标分割，未扩展到多类场景。
- **边缘感知损失超参数敏感**：权重 `w` 的选择可能影响性能，论文未给出调试分析。
- **DSB 划分非官方**：自行划分可能引入评估偏差。
- **缺乏与最新 Transformer 方法的对比**（如 SegFormer、U‑Former 等仅关联工作中提及但未纳入实验），也未测试更现代的自监督预训练策略。

（完）
