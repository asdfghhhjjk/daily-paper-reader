---
title: "TaxoMIL: Taxonomy-Constrained Learning for Hierarchical Whole Slide Image Analysis"
title_zh: "TaxoMIL: 面向层次化全切片图像分析的分类法约束学习"
authors: "Chaeyeon Lee, Khang Nguyen Quoc, Jinsol Song, Yosep Chong, Kwangil Yim, Jin Tae Kwak"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31100v1"
tags: ["query:wsi-mil"]
score: 10.0
evidence: 提出TaxoMIL，一种基于分类约束的MIL方法，用于分层全切片图像分类
tldr: 现有的全切片图像诊断大多采用多实例学习将其简化为扁平分类，无法体现临床诊断中从器官、癌症大类到亚型的层次推理；近期基于视觉语言模型的方法虽尝试生成层次化文本，但缺乏分类学约束，常出现跨层次幻觉和父子标签矛盾。TaxoMIL 将 WSIs 诊断重新定义为分类学约束的多粒度文本生成任务：采用双头 Transformer 解码器同时解码粗粒度和细粒度诊断描述，并通过分类学引导的嵌入空间对齐与层次目标，显式地将视觉特征映射到树形标签结构。在三个公开病理数据集上，TaxoMIL 全面超过 SOTA 的 MIL 分类器和 VLM 生成模型，实现了准确且严格遵循分类学层级关系的诊断预测，为病理人工智能提供了结构化的可解释推理范式。代码已开源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31100v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1267, \"height\": 415, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31100v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1254, \"height\": 528, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31100v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1241, \"height\": 1002, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31100v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1264, \"height\": 466, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31100v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1265, \"height\": 800, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31100v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1270, \"height\": 394, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31100v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1266, \"height\": 396, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31100v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 923, \"height\": 430, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31100v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1007, \"height\": 1168, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31100v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1280, \"height\": 658, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31100v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 779, \"height\": 440, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31100v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 858, \"height\": 497, \"label\": \"Table\"}]"
motivation: 传统WSI诊断将层次临床推理简化为扁平分类，而视觉语言模型常产生分类学幻觉和父子标签违规。
method: 提出分类学约束的多粒度文本生成框架，用双头Transformer解码器同时生成粗/细诊断，并引入分类学引导的嵌入对齐和层次目标。
result: 在三个WSI数据集上，TaxoMIL一致超越SOTA MIL分类器和VLM生成方法，实现层次感知的精准诊断。
conclusion: TaxoMIL以分类学约束生成实现精准层次化WSI诊断，减少幻觉，为计算病理提供可解释的结构化推理路径。
---

## 摘要
全切片图像分析是计算病理学的核心，多实例学习已成为切片级诊断的标准框架。然而，传统方法将WSI诊断形式化为离散标签上的平坦分类任务，这与临床推理固有的层次化、由粗到细的特性相矛盾。尽管最近的层次化分类器和视觉语言模型试图弥补这一结构差距，但它们要么未能捕获相关诊断之间的语义连续性，要么因无约束的文本生成而产生分类幻觉和父子标签违规。为了解决这些局限性，我们提出了TaxoMIL，一个分类法约束框架，将WSI诊断重新表述为多粒度文本生成任务。TaxoMIL采用双头Transformer解码器生成粗粒度和细粒度的诊断文本，并引入分类法引导的目标，明确地构建标签嵌入空间，并严格地将切片级视觉表征锚定在临床分类法内。在三个不同的WSI数据集上的广泛实验表明，TaxoMIL一致优于最先进的MIL分类器和基于VLM的生成方法，产生了准确且层次感知的诊断预测。代码发布于https://github.com/QuIIL/TaxoMIL。

## Abstract
Whole slide image (WSI) analysis is central to computational pathology, with multiple instance learning (MIL) emerging as the standard pipeline for slide-level diagnosis. However, conventional approaches formulate WSI diagnosis as a flat classification task over discrete labels, contradicting the inherently hierarchical, coarse-to-fine nature of clinical reasoning. Although recent hierarchical classifiers and vision-language models (VLMs) have sought to address this structural gap, they either fail to capture semantic continuity between related diagnoses or suffer from unconstrained text generation that produces taxonomic hallucinations and parent-child label violations. To address these limitations, we propose TaxoMIL, a taxonomy-constrained framework that reformulates WSI diagnosis as a multi-granularity text generation task. TaxoMIL utilizes a dual-head Transformer decoder to generate coarse- and fine-level diagnostic text, and introduces taxonomy-guided objectives that explicitly structure the label embedding space and strictly ground slide-level visual representations within the clinical taxonomy. Extensive experiments across three diverse WSI datasets demonstrate that TaxoMIL consistently outperforms state-of-the-art MIL classifiers and VLM-based generative methods, yielding accurate and hierarchy-aware diagnostic predictions. The code is released at https://github.com/QuIIL/TaxoMIL

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **背景**：计算病理学中，WSI 分析依赖多实例学习（MIL）将整张切片视为包，但传统 MIL 将切片级诊断看作扁平分类任务，忽略了临床诊断固有的层次化、由粗到细的推理过程（例如先判“恶性”，再细分亚型）。
- **问题**：扁平分类不强制保持父类–子类一致性，易造成语义断裂；近期层次化分类器仍使用离散标签索引，未能捕捉语义连续性；而视觉语言模型（VLM）虽能生成文本，却缺乏显式分类法约束，常出现分类幻觉或父子标签违反。
- **整体含义**：期望将切片诊断重新构建为受临床分类法约束的多粒度文本生成任务，从而同时实现结构化推理、分类忠诚度和可解释文本输出。

### 2. 论文提出的方法论
- **核心思想**：TaxoMIL 将 WSI 诊断建模为分类学约束的层次文本生成。基于 MIL 骨干提取切片整体表征，通过双分支多模态条件模块产生粗/细粒度的解码条件序列，再由双头文本解码器生成对应的诊断标签文本。
- **关键技术细节**：
  - **双分支条件模块**：将 MIL 输出 z 沿特征维度拆分为 zc 和 zf，分别经线性投影+层归一化+GELU 得到粗/细粒度视觉标记嵌入 v(c)、v(f)。利用标签名构建基嵌入并通过共享投影头获得标签文本嵌入 t(c)、t(f)。构建解码器前缀：粗细两个分支分别拼接 v、可学习的图像标记 e_img、粒度特定的全局语义标记 e_ctx（同一层级所有标签基嵌入的平均）以及提示模板嵌入，形成条件序列 E(c)、E(f)。
  - **双头解码器**：共享 GPT-2 小模型（12 层）作为主干，在其上挂接两个轻量头（一个 Transformer 解码块+反嵌入）分别生成粗粒度和细粒度标签文本，采用自回归贪婪解码。
  - **训练目标（公式说明）**：
    - 主损失 `L_GEN`：两个分支的逐 token 交叉熵之和。
    - 标签层次损失 `L_HIER`：由层次对齐损失 `L_HAL`（拉开不同粗类、对齐父类与子类嵌入、分散兄弟类）和兄弟边缘损失 `L_SML`（基于欧氏距离的合页损失强制兄弟类嵌入保持一定间距）组成。
    - 图像–文本对齐损失 `L_ITAL`：将视觉嵌入 v 与对应真实标签文本嵌入做对比对齐（类似 InfoNCE）。
    - 监督对比损失 `L_CL`：在分支内对视觉嵌入做类别监督对比学习。
    - 循环损失调度：使用归一化进度 u 和余弦函数动态调整权重，让结构损失（HIER、CL）和对齐损失（ITAL）交替主导，避免优化不稳定。
- **整体流程**：算法在论文补充材料以伪代码给出，包括标签嵌入初始化、MIL 前向、前缀构造、多目标联合训练等步骤。

### 3. 实验设计
- **数据集/场景**：
  - **GastWSI**：私有数据集，7228 张胃部 WSI，4 个粗类，23 个细类。
  - **BRACS**：公开乳腺病理数据，545 张 WSI，3 个粗类，7 个细类。
  - **PANDA**：公开前列腺数据，10614 张 WSI，将 ISUP 分级聚合为 3 个粗类（风险层），6 个细类，保留所有样本（含模糊案例），实验设定更具挑战。
- **Benchmark 与方法对比**：
  - **MIL 扁平分类基线**：TransMIL、CLAM-SB、ABMIL、MambaMIL、S4MIL。
  - **MIL 层次分类基线**：HMIL、Chang et al.、ViLa-MIL（改编为双粒度）、HiClass。
  - **VLM 文本生成基线**：WSI-VQA、SlideChat、CAMP。
  - 所有基线在原实现基础上用同一数据分割、冻结的 UNI 特征和评估协议重新训练，确保公平。
- **评估协议**：生成方法采用严格精确匹配，输出字符串移除特殊标记后须与真实标签字符串完全一致，任何部分匹配或语义相似但不同均算错误。MIL 方法用 top-1 准确率。

### 4. 资源与算力
- 文中明确给出：所有实验在 **NVIDIA A6000 GPU** 上完成。
- 训练参数：冻结 UNI 作为特征提取器，GPT-2 small（124M）作为解码器骨干。学习率针对 GastWSI 设 1e-5，BRACS 和 PANDA 设 1e-4，批大小 64，使用 AdamW 优化器、余弦退火调度，最多训练 100 个 epoch 并早停。其他参数（τ=0.07, margin=1.5, 循环损失超参等）均有说明。
- 虽未报告总训练时长，但效率对比表中给出了各模型的延迟、显存占用和 GFLOPs，显示 TaxoMIL 的 GFLOPs 远低于多数 VLM 基线，延迟与其他 VLM 相当。

### 5. 实验数量与充分性
- **主实验**：在三个数据集上从整体级（粗细均需正确）、粗级、细级三个维度对比了 12 个基线，共产生 3×3×12 组主要结果表，汇报了准确率、加权 F1、Cohen’s κ。
- **消融研究**：
  - 训练策略逐项添加：`L_GEN` → +`L_CL` → +`L_HIER`+`L_ITAL` → +循环调度，每个变体在三个数据集均有完整的三粒度结果。
  - 解码器前缀消融：移除 `e_ctx`、移除 `<|img|>` 标记，同样三数据集三粒度。
  - 额外消融：将生成头换为分类头（TaxoMIL-Cls）、使用不同的解码器（BioGPT、GPT-2 medium），以及层次一致性分析（父子违规率）。
- **充分性与公平性**：实验覆盖了多种架构类型（扁平MIL、层次MIL、VLM生成），统一使用相同特征提取器、数据划分和评估标准，并重复训练，确保了公平；消融实验系统分解了各损失项和设计组件的影响，数量充足，结论可靠。

### 6. 论文的主要结论与发现
- TaxoMIL 在所有数据集和评估粒度上均优于现有 MIL 和 VLM 方法，尤其在全局同时正确和细粒度准确率上优势显著（例如 BRACS 整体准确率提升 7% 以上）。
- 分类学引导的层次损失与图像–文本对齐有效塑造了标签嵌入空间，使粗类标签居于子类中心，兄弟类分散，视觉嵌入也呈现对应结构，降低了诊断混淆。
- 生成文本严格遵循分类法，产生的父子违规率保持在较低水平，同时实现了可解释、自然语言输出。
- 循环损失调度有助于稳定多目标训练，带来一致的性能提升。
- 非生成对照显示，即使换成分类头，框架依然优于多数基线，说明增益主要来自分类学约束的表示学习，而非解码器容量。

### 7. 优点
- **结构化创新**：首次将 WSI 诊断形式化为显式分类法约束的层次文本生成，贴合临床推理流程。
- **多目标互补设计**：层次结构损失、跨模态对齐与对比损失分工明确，循环调度避免冲突，设计精巧。
- **严格评估**：采用精确字符匹配和整体双粒度正确评价，对生成方法施加极高要求，结果更具临床参考价值。
- **可解释性**：输出自然语言诊断文本，且通过嵌入可视化证明了内部表征的层次组织，增强了模型透明性。
- **代码开源，复现性强**：提供了实现细节和超参。

### 8. 不足与局限
- **固定分类法假设**：当前仅支持预定义的二层树结构，扩展到更深层次或动态更新的分类学需要重新设计。
- **无法完全消除父子违规**：论文在补充材料中报告的违规率仍有部分存在（平均 4.09%），虽优于多数基线但未彻底解决。
- **计算开销**：引入自回归文本解码器增加了参数和内存，较纯 MIL 分类器训练和推理更重（尽管相比大型 VLM 已较轻）。
- **数据集规模与分布**：GastWSI 为私有数据，公开数据集中 BRACS 较小，可能影响泛化结论；PANDA 保留所有样本导致所有模型性能较低，任务困难度高，但也反映了真实场景的挑战。
- **生成能力受限**：仅生成封闭集合的标签字符串，未涉及描述性报告或开放词汇诊断，应用范围相对狭窄。

（完）
