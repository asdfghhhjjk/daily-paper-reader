---
title: "CGRL: Concept-Guided Pruning and Representation Learning for Whole-Slide Image Classification"
title_zh: CGRL：概念引导的剪枝与表示学习用于全切片图像分类
authors: "Thuc Huynh, Tuan Le, Doanh C. Bui"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12556v1"
tags: ["query:path-xai-sel"]
score: 9.0
evidence: 概念相关性剪枝通过类别概念相似度对补丁排序，选择最具信息量的补丁
tldr: 弱监督全切片图像分类常用多实例学习聚合大量图像块，但视觉线索主导易保留非信息图像块，导致特征与类别语义对齐较弱。为此，本研究提出CGRL，引入类别概念原型进行概念相关性修剪，仅保留top-K相关图像块，并设计概念引导对比学习优化表示。在TCGA-BRCA和NSCLC数据集上，CGRL改进了多种MIL方法，明显提升准确率和宏F1，同时降低计算开销。研究表明，类别语义概念为弱监督计算病理的实例选择和表示学习提供了有效先验。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12556v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 854, \"height\": 547}, {\"url\": \"assets/figures/arxiv/2607.12556v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 868, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2607.12556v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1849, \"height\": 899}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12556v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1880, \"height\": 698}, {\"url\": \"assets/tables/arxiv/2607.12556v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 914, \"height\": 323}]"
motivation: 现有弱监督方法在WSI分类中依赖视觉线索聚合实例，难以筛选语义相关的关键实例，导致特征与类别语义对齐弱。
method: CGRL框架引入疾病概念原型，通过概念相关性修剪保留top-K相关实例，并利用概念引导对比学习优化表示空间。
result: 在TCGA-BRCA和NSCLC上，CGRL有效提升多种MIL方法的准确率和宏F1，同时降低计算开销。
conclusion: 证明类别级语义概念为弱监督WSI分类提供了有效的实例选择与特征学习先验。
---

## 摘要
弱监督全切片图像(WSI)分类在计算病理学中应用广泛，因为切片级标签比密集区域标注更易获取。现有大多数多实例学习(MIL)方法主要基于视觉线索聚合大量图像块嵌入，这往往会保留许多非信息性图像块，且实例特征与类别级疾病语义之间对齐较弱。我们提出概念引导的剪枝与表示学习(CGRL)，一个简洁的框架，将源自疾病提示的类别级概念原型引入MIL流程。首先，概念相关度剪枝根据图像块与类别概念的相似度对图像块实例排序，保留前K个概念相关图像块用于下游MIL聚合。其次，概念引导的对比表示学习从同一相似度矩阵中构建类别级的正负图像块集，优化目标类别、对称辅助及跨类分离目标，从而正则化投影概念空间。我们在TCGA-BRCA和TCGA-NSCLC数据集上使用多种代表性MIL方法评估CGRL。实验结果表明，CGRL改善了若干模型-数据集组合，其增益取决于下游MIL模型和数据集。它在准确率和宏观F1值上取得了尤为明显的提升，同时通过概念相关剪枝降低了计算成本。这些发现表明，类别级语义概念为弱监督计算病理学中的图像块选择和表示学习提供了有效且实用的先验。

## Abstract
Weakly supervised whole-slide image (WSI) classification is widely used in computational pathology because slide-level labels are easier to obtain than dense region annotations. Existing multiple instance learning (MIL) methods often aggregate large bags of patch embeddings using mainly visual cues, which can retain many non-informative patches and provide weak alignment between instance features and class-level disease semantics. We propose Concept-Guided Pruning and Representation Learning (CGRL), a simple framework that introduces class-level concept prototypes derived from disease prompts into the MIL pipeline. First, concept-relevance pruning ranks patch instances by their similarity to class concepts and retains the top-K concept-relevant patches for downstream MIL aggregation. Second, concept-guided contrastive representation learning constructs class-wise positive and negative patch sets from the same similarity matrix and optimizes target-class, symmetric auxiliary, and cross-class separation objectives, thereby regularizing the projected concept space. We evaluate CGRL on TCGA-BRCA and TCGA-NSCLC using multiple representative MIL methods. Experimental results show that CGRL improves several model-dataset combinations, with gains depending on the downstream MIL model and dataset. It achieves particularly clear improvements in accuracy and macro-F1 while reducing computational cost through concept-relevance pruning. These findings demonstrate that class-level semantic concepts provide an effective and practical prior for patch selection and representation learning in weakly supervised computational pathology.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：在计算病理学中，全切片图像(WSI)分类对癌症诊断和亚型识别至关重要。由于像素级标注极其昂贵，弱监督学习（仅需切片级标签）成为主流。
- **核心问题**：目前主流的弱监督多实例学习(MIL)方法主要基于视觉线索聚合巨量图像块，导致两个明显局限：
  - 大量非信息性或冗余的背景块被保留，既稀释了诊断信号，又增加了计算开销。
  - 实例特征空间与高层疾病语义之间缺乏对齐，影响亚型的精细判别。
- **整体含义**：论文提出将可解释的“疾病概念”引入MIL框架，让语义先验同时引导实例选择（剪枝）和表示空间正则化，从而在提升准确率与效率的同时改善可解释性。

### 2. 方法论
- **核心思想**：利用预训练的视觉‑语言基础模型（CONCH图像编码器、TITAN文本编码器）获取图像块嵌入和类别概念原型（由疾病提示文本编码），通过概念‑实例相似度矩阵同时实现概念相关剪枝与概念引导对比学习。
- **关键技术细节**：
  - **投影与相似度计算**：通过一个可学习的投影头 \(g_\phi\) 将图像块嵌入 \(x_i\) 映射到概念空间得到 \(u_i\)，然后与类别概念原型 \(t_c\) 计算余弦相似度矩阵 \(S\)。
  - **概念相关剪枝**：对每个图像块，取其对所有概念原型相似度的最大值作为“概念相关分数” \(r_i = \max_c S_{i,c}\)，保留分数最高的 \(K_p\) 个块用于下游MIL分类，其余丢弃。
  - **概念引导对比学习**：基于同一相似度矩阵，对每个类别构建正样本集（最相关块）和负样本集（最不相关块），并设计三个InfoNCE对比损失：
    - \(\mathcal{L}_y\)：使目标类相关块靠近真实概念原型。
    - \(\mathcal{L}_{\text{sym}}\)：对非目标类，使其相关块靠近各自概念原型并远离不相关块。
    - \(\mathcal{L}_{\text{cross}}\)：将目标类相关块与“对竞争类高度相关的块”进一步推开，增强类间分离。
  - **联合训练**：训练损失为 \(\mathcal{L} = \mathcal{L}_{\text{cls}} + \lambda_{\text{CGRL}}\mathcal{L}_{\text{CGRL}}\)；推理时仅保留剪枝后的块输入MIL模型。
  - **即插即用**：所有视觉和文本编码器保持冻结，仅训练投影头和下游MIL，开销极低。

### 3. 实验设计
- **数据集**：
  - TCGA‑BRCA（乳腺浸润性导管癌 vs. 浸润性小叶癌，871例）
  - TCGA‑NSCLC（肺腺癌 vs. 肺鳞癌，958例）
  - 均采用5折交叉验证。
- **基准对比方法**：
  - 四种代表性MIL模型：ABMIL（注意力池化）、CLAM‑SB（带聚类约束）、TransMIL（Transformer相关建模）、DS‑MIL（双流聚合）。
  - 每个MIL模型均比较四种设置：全量图像块、随机剪枝、K‑means聚类剪枝、CGRL概念剪枝。
- **评价指标**：AUC、准确率(ACC)、宏观F1。

### 4. 资源与算力
- **硬件**：单张NVIDIA GeForce RTX 2080 Ti GPU。
- **软件与优化**：使用Adam优化器，学习率 \(2\times10^{-4}\)，batch size为1。文中未提供单次训练的具体时长，但提到由于推理时不需对比损失，整体推理轻量。

### 5. 实验数量与充分性
- **实验组数**：两个数据集 × 4个MIL模型 × 4种图像块选择方式，共计32组主实验。此外还包含消融实验（对比不同损失组合的效果）。
- **充分性评价**：
  - **较充分**：覆盖了基于注意力、Transformer、双流等不同机制的MIL模型，有助于说明方法的通用性；消融实验验证了各损失分量的贡献。
  - **客观性**：使用相同的预处理、预训练特征、交叉验证，并对比随机和K‑means剪枝，排除“简单剪枝即可提升”的怀疑。
  - **局限性**：仅限二分类任务，且均为TCGA公开数据集，未在更大规模或更多亚型的数据上验证；未与最近的其他剪枝方法（如PAMIL、CICS）做直接对比。

### 6. 主要结论与发现
- 概念引导的剪枝能有效去除冗余块，使下游MIL模型聚焦于疾病相关区域，在多个设置下显著提升ACC和F1（如ABMIL在BRCA上ACC从0.9101→0.9307）。
- 效果与MIL模型及数据集相关，其中注意力类方法受益最明显；在AUC指标上有时与简单采样持平，但在判别性指标上优势更突出。
- 联合剪枝与对比学习能将语义概念同时作为选择线索和训练监督，提供了一种轻量、可解释的WSI分类增强方案。

### 7. 优点
- **统一且轻量**：同一概念相似度矩阵同时驱动实例选择和表征正则化，无需额外复杂模块。
- **即插即用**：冻结视觉‑语言基础模型，仅需训练投影头，可匹配现有各种MIL聚合器。
- **降低计算开销**：剪枝大幅减少送入MIL的块数（如保留300块），推理成本成倍下降。
- **概念可解释性**：所选块直接与类别概念对齐，为模型决策提供直观的语义支撑。

### 8. 不足与局限
- **任务与数据单一**：仅在两个TCGA二分类任务上测试，未覆盖多类分类、不同癌种或实际临床数据，泛化性有待验证。
- **指标提升不均衡**：部分模型‑数据集组合的AUC无明显改进，甚至略降，说明概念剪枝对某些MIL机制可能不兼容。
- **依赖固定概念原型**：疾病提示由规则生成，可能无法覆盖所有病理语义变化，缺乏自适应机制。
- **对比基线有限**：未与已有的更高级剪枝/压缩方法直接比较，削弱了性能优势的说服力。
- **算力信息不完整**：未给出训练耗时、内存占用等具体数据，难以完全评估效率优势。

（完）
