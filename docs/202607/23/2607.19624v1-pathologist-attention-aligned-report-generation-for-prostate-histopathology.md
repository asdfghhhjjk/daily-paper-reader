---
title: Pathologist Attention-Aligned Report Generation for Prostate Histopathology
title_zh: 面向前列腺组织病理学的病理学家注意力对齐报告生成
authors: "Ruoyu Xue, Suryakant Singh, Souradeep Chakraborty, Pierre Marza, Oksana Yaskiv, Constantin Friedman, Natallia Sheuka, Paul Friedman, Bharat Ramlal, Beatrice Knudsen, Rajarsi Gupta, Joel Saltz, Prateek Prasanna, Gregory Zelinsky, Dimitris Samaras"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19624v1"
tags: ["query:path-xai-sel"]
score: 8.0
evidence: 利用病理学家注意力识别WSI中的诊断关键区域
tldr: "病理学家在癌症诊断中的注意力分配高度选择性，能提供关键语义线索用于报告生成。为此，收集了121张前列腺全切片图像的多模态数据集，包含病理学家多尺度注视轨迹、口头描述和光标移动，并标注了五个临床相关组件。通过注意力对齐损失微调两个报告生成模型，使其图像块注意力分布匹配病理学家注意力。实验表明，自然语言处理指标平均提升10.9%，五个临床组件的准确率提升19.3%，且模型注意力图更贴近病理学家关注区域，为生成报告提供了更强的视觉可解释性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19624v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1270, \"height\": 368, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19624v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1218, \"height\": 356, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19624v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 330, \"height\": 208, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19624v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 343, \"height\": 285, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19624v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1273, \"height\": 394, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19624v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19624v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1209, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19624v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1010, \"height\": 191, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19624v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1010, \"height\": 192, \"label\": \"Table\"}]"
motivation: 引入病理学家注意力作为语义线索，提升前列腺病理报告生成质量和临床可解释性。
method: 收集多模态注意力数据集，使用注意力对齐损失微调两类报告生成模型，对齐模型与病理学家的图像块注意力分布。
result: "NLP指标平均提升10.9%，五个临床相关报告组件准确率提升19.3%，模型注意力图与病理学家注意力更一致。"
conclusion: 利用人类注意力训练可有效改善前列腺病理报告生成性能，并增强视觉支持的可解释性。
---

## 摘要
病理学家在癌症诊断过程中的视觉注意力分配是一个高度选择性的过程，它极大地塑造了从全切片图像中提取的信息。人类注意力有助于医学成像任务（如分类和分割），并成为识别诊断信息区域以生成报告的强语义线索。本文我们将人类注意力引入病理学家报告生成模型的训练中。为此，我们收集了一个多模态人类注意力数据集，包含121个前列腺全切片图像，标注了病理学家的多尺度视口轨迹，并与他们的口头描述和光标移动同步，针对五个临床相关成分（如Gleason模式）。利用该数据集，我们通过注意力对齐损失微调了两个报告生成模型，该损失约束模型在图像块上的注意力以匹配病理学家的注意力分布。我们使用两种具有不同内部注意力机制（即图像令牌如何融入语言解码器）的模型，在前列腺癌报告生成和视觉问答上评估了我们的方法。实验表明，在基于NLP的指标上平均提升10.9%，在五个临床相关报告成分上的准确率提升19.3%。此外，在推理时提取的模型注意力图（计算开销极小）与病理学家的注意力更加一致，通过突出对输出影响最大的区域，为生成的报告提供了更强的视觉支持。

## Abstract
The allocation of visual attention by pathologists during cancer diagnosis is a highly selective process that critically shapes the information extracted from whole-slide images (WSIs). Human attention helps medical imaging tasks such as classification and segmentation, and becomes a strong semantic cue for identifying diagnostically informative regions for report generation. In this paper, we introduce human attention into the training of pathologist report generation models. To this end, we collected a multimodal human-attention dataset of 121 prostate WSIs annotated with pathologists' multi-scale viewport trajectories synchronized with the pathologists' verbal descriptions and cursor movements for five clinically relevant components (e.g., Gleason patterns). Using this dataset, we finetune two report generation models with an attention-alignment loss that regularizes the model attention over image patches to match the distribution of pathologist attention. We evaluate our approach on prostate cancer report generation and visual question answering using two models with different internal attention mechanisms (i.e., how image tokens are integrated into the language decoder). Experiments show average gains of 10.9% on NLP-based metrics and 19.3% in accuracy across five clinically relevant report components. Further, model attention maps extracted at inference time, with minimal computational overhead, align more closely with pathologist attention, providing stronger visual support for the generated reports by highlighting the regions that most influence the output.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将使用中文，以 Markdown 形式，对所提供的论文进行结构化、深入、客观的总结。

***

### 论文标题：《面向前列腺组织病理学的病理学家注意力对齐报告生成》

---

### 1. 论文核心问题与研究动机

*   **核心问题**：当前病理学报告生成模型（如 HistGen、WSI-LLaVA）在生成诊断文本时，缺乏对病理学家真实视觉注意力过程的显式建模，导致模型决策的视觉依据不透明，且生成的报告在临床关键信息上可能存在偏差。
*   **研究动机**：
    *   **临床价值**：病理学家在诊断全切片图像（WSI）时，其视觉注意力高度选择性地集中于诊断关键区域，这不仅是诊断的核心过程，也是训练住院医师和评估专家水平的重要依据。
    *   **技术启发**：认知科学和计算机视觉领域研究表明，人类的注视行为能显著影响其对视觉内容的描述。因此，将专家注意力作为辅助信号，有望让模型“学会”像病理学家一样观察，从而生成更准确、可解释性更强的报告。
*   **整体含义**：本研究旨在填补病理报告生成领域中“利用人类注意力作为监督信号”的空白，通过引入病理学家的真实注视数据来引导模型训练，实现报告质量的提升和模型决策视觉证据的可视化。

---

### 2. 方法论

*   **核心思想**：提出一种即插即用的“注意力对齐”框架。在报告生成模型的训练过程中，引入一个额外的损失函数，该函数约束模型对图像块的注意力分布与病理学家的真实注意力分布保持一致。
*   **关键技术细节**:
    1.  **数据集构建**:
        *   **数据采集**：收集了6位病理学家审阅121张前列腺WSI的多模态数据，包括多尺度视口轨迹、同步口头描述和光标移动。
        *   **注意力图生成**：将采集到的视口中心点转化为注视路径（Scanpath），再通过高斯平滑为每个倍数（2x, 4x, 10x, 20x）生成一个空间显著性图，最终融合成一张多尺度的病理学家显著图。
    2.  **病理学家注意力编码**:
        *   将生成的病理学家显著性图映射到图像块（Token）层级，为每个图像块分配一个来自病理学家的注意力值，形成一个人类注意力向量 $\mathbf{a}_{human} \in \mathbb{R}^I$。
    3.  **模型架构与注意力对齐**:
        *   **基础模型**：使用两种不同注意力融合机制的报告生成模型——**WSI-LLaVA**(自注意力融合)和 **HistGen**(交叉注意力融合)。
        *   **模型注意力提取**：从模型解码器的最后一层提取注意力权重。
            *   对于 **WSI-LLaVA**，提取自注意力权重中“答案Token查询图像Token”的部分 $A_{ans \rightarrow img}$，并在所有答案Token上平均，得到模型注意力向量 $\mathbf{a}_{model}$。
            *   对于 **HistGen**，直接提取其交叉注意力权重作为 $\mathbf{a}_{model}$。
        *   **对齐损失函数**：计算模型注意力分布与人类注意力分布之间的 KL 散度（Kullback-Leibler Divergence），作为注意力对齐损失 $L_{KL} = D_{KL}(\text{softmax}(a_{human}) \| \text{softmax}(a_{model}))$。
    4.  **训练目标**：将标准的文本生成交叉熵损失 $L_{CE}$ 与注意力对齐损失 $L_{KL}$ 结合，构成最终损失函数 $L = L_{CE} + \lambda L_{KL}$。训练时权重 $\lambda$ 设置为1.0。
    5.  **推理可视化**：在推理阶段，无需任何额外输入，直接提取最后的模型注意力向量，反向投影到原图空间，生成高亮诊断关键区域的视觉证据热力图。

---

### 3. 实验设计

*   **数据集与场景**:
    *   **训练集（用于注意力对齐）**: 基于自采集的多模态数据集中 81 张有病理学家注意力标注的 WSI。
    *   **报告生成任务（HistGen）**: 遵循 HistGen 的官方数据集划分，在验证集（86张）和测试集（76张）上进行评估。
    *   **报告生成任务（WSI-LLaVA）**: 遵循 WSI-LLaVA 的官方数据集，在训练集(107例)和测试集（11/15例）上评估。
*   **Benchmark 方法对比**:
    *   **Zero-shot (零样本)**：直接使用预训练好的 HistGen 和 WSI-LLaVA 模型进行测试，不进行任何微调。
    *   **w/o Alignment (无对齐)**：仅使用报告的文本标签对基础模型进行 LoRA 微调，不加入病理学家注意力对齐损失。
    *   **Ours (本文方法)**：在 LoRA 微调的基础上，加入病理学家注意力对齐损失进行训练。
*   **评价指标**:
    *   **报告质量**: 传统 NLP 指标 (BLEU-1~4, METEOR, ROUGE-L) 和针对 4 个临床关键组件（Gleason模式, 神经周围侵犯, 前列腺外扩散, 手术切缘）的准确率。
    *   **视觉支持度**: 归一化扫描路径显著性 (NSS)、KL 散度 (KL)（对比模型注意力图与病理学家注意力图）、ROC-AUC（对比模型注意力图与Gleason区域分割标签）。

---

### 4. 资源与算力

*   **GPU 型号与数量**: 论文明确提到，所有实验均在**单块 RTX 6000 GPU** 上运行。
*   **数据集采集时间**: 记录了 6 位病理学家审阅 121 张 WSI 的过程，总计约**7小时**的标注工作量。
*   **训练时长**: 论文未明确提及具体的训练总时长。

---

### 5. 实验数量与充分性

*   **实验覆盖度**:
    1.  **跨模型验证**：在两个主流但结构不同的报告生成模型（HistGen 和 WSI-LLaVA）上验证了方法的有效性，证明了其作为即插即用模块的通用性。
    2.  **多维度评估**：从报告文本质量、临床关键信息准确率和模型注意力图可解释性三个维度全面评估。
    3.  **消融实验**: 进行了一项关键消融实验，探究“在哪一层施加注意力对齐损失”的影响，通过对比“所有层”和“最后一层”，验证了仅对齐最后一层注意力的最优性。
*   **实验充分性与公平性**:
    *   **充分性**：实验设计层次递进，从文本质量到临床价值再到可解释性，形成了完整的证据链。跨模型验证增强了结论的鲁棒性。
    *   **公平性**：对比基线设置公平，考虑了直接零样本和使用相同文本数据微调（无注意力）两种情况，排除了单纯由微调带来的性能提升。
    *   **局限性**：消融实验仅针对“对齐层的选择”，未对损失权重 $\lambda$、注意力图构建方式（如高斯平滑半径）等超参数进行充分探索。

---

### 6. 主要结论与发现

*   **报告质量提升**: 加入病理学家注意力对齐后，HistGen 和 WSI-LLaVA 在 NLP 指标（如 BLEU-4, METEOR）上均有显著提升（平均提升 10.9%）。
*   **临床信息准确性提高**: 模型在五个临床相关报告组件（特别是 Gleason 模式）上的生成准确率获得了更大幅度的提升（平均提升 19.3%），表明注意力信号有效引导模型关注了诊断关键信息。
*   **增强了可解释性**: 经过对齐的模型，其推理时产生的注意力热力图与病理学家的真实注视区域更加一致（NSS 更高，KL 散度更低），并与真实的 Gleason 分割区域有更高的重叠度（ROC-AUC 更高），为生成的诊断陈述提供了更可信的视觉依据。
*   **方法有效性**：仅使用约7小时的专家标注数据和简单的损失函数，即可实现对模型的有效引导，证明了该方法效率高且实用。

---

### 7. 优点

*   **创新性强**: 首次将病理学家的真实视觉注意力作为辅助监督信号引入组织病理学报告生成任务，开辟了新方向。
*   **方法即插即用**: 提出的注意力对齐损失作为一个独立模块，能够在不改变原模型结构的前提下，轻松集成到不同注意力机制的模型（自注意力/交叉注意力）中。
*   **数据有效性**: 证明了利用少量（7小时）的专家注视数据即可显著提升模型性能，极大地降低了应用成本。
*   **提升可解释性**: 不仅提升了文本指标，更重要的是提供了与专家认知过程一致的视觉解释，增强了模型在临床高风险场景下的可信度。

---

### 8. 不足与局限

*   **数据规模与来源单一**: 数据集仅限于前列腺癌（TCGA-PRAD），且来自本研究收集的少量样本。模型泛化到其他癌种或不同来源数据上的能力有待验证。
*   **临床组件覆盖不全**：在评估中，由于数据稀疏性，排除了淋巴血管侵犯、精囊腺侵犯，同时由于测试集报告缺失，未评估WSI-LLaVA的手术切缘准确率，使得临床评估不够完整。
*   **模型范围局限**：虽然在两种模型上验证，但仍然仅为视觉语言模型领域中的一小部分架构。其方法在更大规模的最新模型上的效果尚不明确。
*   **超参数研究不足**: 论文仅进行了对齐层选择的消融实验，对损失权重 $\lambda$ 等关键超参数缺乏敏感性分析，可能并非最优设置。
*   **注视数据偏差**: 病理学家的注意力模式受经验、疲劳度和个人习惯影响，仅利用6位专家的注视数据，可能引入专家个体偏差，未必能代表普适的“最佳”病理学诊断模式。

（完）
