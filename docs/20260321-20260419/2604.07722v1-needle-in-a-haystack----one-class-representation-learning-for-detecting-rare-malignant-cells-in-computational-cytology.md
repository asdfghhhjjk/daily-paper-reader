---
title: Needle in a Haystack -- One-Class Representation Learning for Detecting Rare Malignant Cells in Computational Cytology
title_zh: 大海捞针——用于检测计算细胞学中罕见恶性细胞的单类表示学习
authors: "Swarnadip Chatterjee, Vladimir Basic, Arrigo Capitanio, Orcun Goksel, Joakim Lindblad"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07722v1"
tags: ["query:cellrep"]
score: 9.0
evidence: 用于检测细胞学中罕见恶性细胞的一类表征学习
tldr: 本研究针对细胞病理图像中极少量恶性细胞的检测问题，提出采用单类表示学习方法，只利用正常细胞训练模型，通过检测与正常分布偏离来识别恶性细胞，在多个数据集上取得在超低恶性细胞比例下的领先性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07722v1/fig-001.webp\", \"caption\": \"Figure 4: Mean AUTK@400 on the bone marrow dataset across\", \"page\": 11, \"index\": 1, \"width\": 498, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.07722v1/fig-002.webp\", \"caption\": \"Figure 5: Mean normalized AUFROC on the bone marrow\", \"page\": 11, \"index\": 2, \"width\": 498, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.07722v1/fig-003.webp\", \"caption\": \"Figure 3: Mean nDCG@400 on the bone marrow dataset across\", \"page\": 11, \"index\": 3, \"width\": 498, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.07722v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of the proposed pipeline for rare abnormal cell detection in whole-slide cytology images. For training the\", \"page\": 2, \"index\": 4, \"width\": 1029, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2604.07722v1/fig-005.webp\", \"caption\": \"Fig. 7b.\", \"page\": 7, \"index\": 5, \"width\": 1029, \"height\": 528}, {\"url\": \"assets/figures/arxiv/2604.07722v1/fig-006.webp\", \"caption\": \"Figure 2: Mean Recall@400 on the bone marrow dataset across\", \"page\": 10, \"index\": 6, \"width\": 498, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.07722v1/fig-007.webp\", \"caption\": \"Figure 6: Example mosaic for the bone marrow dataset at 10%\", \"page\": 12, \"index\": 7, \"width\": 498, \"height\": 498}]"
motivation: 传统弱监督方法在恶性细胞极少的情境下难以泛化，需要更有效的模型来应对稀有病变检测挑战。
method: 采用DSVDD和DROC两种单类表示学习方法，仅用阴性样本进行训练并检测异常偏离。
result: 在两个细胞学数据集上，DSVDD在极低恶性细胞比例下实现了最佳实例级检测性能，部分实验甚至优于完全监督方法。
conclusion: 单类表示学习在极端稀有情况下比弱监督多实例学习更稳健且可解释，适用于实际细胞学病变检测。
---

## 摘要
在计算细胞学中，由于恶性细胞在形态上具有高度多样性且在大量正常细胞背景中极为稀少，因此在全视野切片图像上检测恶性细胞具有相当大的困难。由于类别极度不平衡和标注数据有限，准确检测这些极少数的恶性细胞仍然是一大挑战。传统的弱监督方法，例如多实例学习（MIL），在实例层面往往无法很好地泛化，尤其当恶性细胞比例（witness rate）极低时。本研究探讨了在低 witness rate 情景下，基于单类表示学习（one-class representation learning）的方法在恶性细胞检测中的应用。这类方法仅使用阴性切片的图像块进行训练，无需任何实例级监督。具体而言，我们评估了两种单类分类（OCC）方法：DSVDD 和 DROC，并将其与 FS-SIL、WS-SIL 以及最新的 ItS2CLR 方法进行了比较。这些单类方法学习正常样本的紧致表示，并在测试时检测偏离正常的样本。基于公开的骨髓细胞形态学数据集（TCIA）和内部的口腔癌细胞学数据集的实验表明，DSVDD 在实例级异常性排序中达到了当前最优性能，特别是在极低 witness rate（≤1%）情况下，甚至在某些情形下超越了完全监督学习的表现——而完全监督学习在全视野细胞学中通常并不可行，因为无法获得详尽的实例级标注。DROC 在极端稀有条件下同样表现出竞争力，得益于其分布增强的对比学习。这些研究结果表明，单类表示学习在极度稀有条件下是比 MIL 更加稳健且具可解释性的优越选择，用于恶性细胞检测。

## Abstract
In computational cytology, detecting malignancy on whole-slide images is difficult because malignant cells are morphologically diverse yet vanishingly rare amid a vast background of normal cells. Accurate detection of these extremely rare malignant cells remains challenging due to large class imbalance and limited annotations. Conventional weakly supervised approaches, such as multiple instance learning (MIL), often fail to generalize at the instance level, especially when the fraction of malignant cells (witness rate) is exceedingly low. In this study, we explore the use of one-class representation learning techniques for detecting malignant cells in low-witness-rate scenarios. These methods are trained exclusively on slide-negative patches, without requiring any instance-level supervision. Specifically, we evaluate two OCC approaches, DSVDD and DROC, and compare them with FS-SIL, WS-SIL, and the recent ItS2CLR method. The one-class methods learn compact representations of normality and detect deviations at test time. Experiments on a publicly available bone marrow cytomorphology dataset (TCIA) and an in-house oral cancer cytology dataset show that DSVDD achieves state-of-the-art performance in instance-level abnormality ranking, particularly in ultra-low witness-rate regimes ($\leq 1\%$) and, in some cases, even outperforming fully supervised learning, which is typically not a practical option in whole-slide cytology due to the infeasibility of exhaustive instance-level annotations. DROC is also competitive under extreme rarity, benefiting from distribution-augmented contrastive learning. These findings highlight one-class representation learning as a robust and interpretable superior choice to MIL for malignant cell detection under extreme rarity.

---

## 论文详细总结（自动生成）

# 《Needle in a Haystack – One-Class Representation Learning for Detecting Rare Malignant Cells in Computational Cytology》论文总结

---

## 一、研究背景与核心问题

- **研究动机：**  
  计算细胞学（Computational Cytology）通过全视野切片（Whole-Slide Images, WSI）的数字化扫描实现细胞形态的自动分析。然而，恶性细胞在大量正常细胞中极其稀少（witness rate 可低至 0.01%），形态又高度多样，使得检测极少量恶性细胞成为计算病理中最艰难的任务之一。  

- **核心问题：**  
  - 在极端类别不平衡条件下，弱监督多实例学习（MIL）等方法失效，难以定位具体的异常细胞。  
  - 完全监督学习需要昂贵的实例级标注，在全切片场景下不可行。  
  - 需发展不依赖正样本监督、能鲁棒表示“正常性分布”并检测偏离的模型。  

- **研究目标：**  
  本文提出并系统评估**单类表示学习（One-Class Representation Learning, OCC）**方法用于检测极低比例的恶性细胞，以实现无需异常样本监督的实例级异常检索。

---

## 二、方法论：核心思想与关键算法

### 1. 总体思路

- 仅使用**阴性切片（slide-negative patches）**训练模型，学习“正常细胞”的紧致特征分布。
- 在测试阶段，通过测量与正常分布的“距离”来判定细胞是否为异常。
- 评估两种主流的单类学习方法：
  - **DSVDD（Deep Support Vector Data Description）**
  - **DROC（Deep Representation One-Class Classification）**

### 2. DSVDD 方法流程

- **核心思想：** 将正常细胞样本嵌入到潜在空间中，使它们集中于一个超球中心，以最小化样本到中心的距离。异常样本将远离该中心。
- **训练流程（文本化公式说明）：**
  1. 预训练自动编码器 \( f_{enc}, f_{dec} \)，通过重建损失学习一般性的特征表示：
     \[
     \mathcal{L}_{AE} = \frac{1}{n}\sum_i \| f_{dec}(f_{enc}(a(x_i))) - a(x_i) \|^2
     \]
  2. 固定中心 \( c \) 为正常样本特征的均值，并使用下式约束样本聚合至该中心：
     \[
     \mathcal{L}_{DSVDD} = \frac{1}{n}\sum_i \| \phi(x_i;\theta) - c \|^2 + \lambda \|\theta\|^2
     \]
  3. 测试时通过距离 \( s(x) = \| \phi(x) - c \|^2 \) 衡量异常度。
- **增强机制：**
  - 多随机种子（multi-seed ensemble）训练降低方差；
  - 测试时数据增强（TTA）与多视角融合，提高鲁棒性。

### 3. DROC 方法流程

- **思想：** 基于 SimCLR 框架的**对比自监督学习**，只用正常样本，通过构造强增强的“伪异常”（pseudo abnormal）样本来拉开类间差距。
- **关键步骤：**
  1. 生成两个弱增强视图 \(A(x_i), A'(x_i)\)，作为正对。
  2. 生成强增强视图 \(D(x_i)\)，作为“伪异常”负样本。
  3. 优化改进的分布增强对比损失：
     \[
     \mathcal{L}_{DROC} = \mathcal{L}_{clr} + \alpha \mathcal{L}_{DA}
     \]
  4. 训练完 encoder 后，提取特征并用 One-Class SVM（RBF 核）拟合正常分布。
  5. 测试时使用 SVM 的决策函数作为异常分数。

### 4. 对比基线

- **FS-SIL:** 完全监督单实例学习（拥有真实细胞标签，作为上限参考）。  
- **WS-SIL:** 弱监督单实例学习（由切片级标签继承，存在噪声）。  
- **ItS2CLR:** 最新的基于迭代自定步对比学习的弱监督 MIL 模型。

---

## 三、实验设计

### 1. 数据集

1. **骨髓细胞形态数据集（TCIA）**
   - 21 类细胞，由血液病理专家标注。  
   - 仅以 Lymphocyte（LYT）作为正常类，其余7类（如Basophil、Hairy cell等）合并为异常类。
   - 训练仅用正常细胞，评估多种 witness rate 条件 (0.05%–9%)。

2. **口腔癌细胞学（自建 dataset）**
   - 28 张液基细胞学（LBC）切片（10 正常、10 恶性用于训练，8 用于测试）。  
   - 仅有切片级标签，未提供单细胞标注。  
   - 提取约 64 万张阴性 patch 供训练。

### 2. 对比方法

在两个数据集上比较五种方法：
- FS-SIL、WS-SIL、ItS2CLR（监督/弱监督）
- DSVDD、DROC（单类学习）

### 3. 评价标准

- **主要指标：**  
  - Recall@400（在前 400 个候选中召回到的异常细胞数目）  
- **次要指标：**  
  - nDCG@400、AUTK@400、Normalized AUFROC@400  
- **任务设定：**  
  - 骨髓数据：具备完整细胞标注，用于定量指标。  
  - 口腔癌数据：仅做双专家盲评（top-100 patch）。

---

## 四、资源与算力信息

- 文中使用 **ResNet18** 作为 backbone。  
- 训练算法包括 SGD 和 Adam，不同阶段学习率取 \(10^{-3}\) 或 \(10^{-4}\)。  
- 没有明确说明使用的 GPU 型号、显存、或训练时长。  
- 可推测在单卡或小型 GPU 集群上可完成（因 batch size 较小、ResNet18 轻量）。  

> 🔹结论：**论文未明确报告算力配置和训练耗时。**

---

## 五、实验数量与充分性

- **实验设置：**
  - 骨髓数据集中测试 6 种不同 witness rate（0.05%、0.1%、0.5%、1%、5%、9%），并进行 10 次随机试验求平均。  
  - 口腔癌数据进行 4 张恶性切片的专家盲审（两位专家独立标注）。  
  - 还包含多种指标(nDCG, AUTK, AUFROC)的全面定量评估。
- **实验充分性：**
  - 覆盖从高稀有（0.05%）到中等稀有范围，验证了模型在极端数据稀疏下的稳定性。  
  - 同时跨越两个领域（血液细胞、口腔上皮），具备良好的任务多样性。  
  - 对比涵盖监督、弱监督、单类三类范式→公平性较高。

---

## 六、主要结论与发现

1. **在有效低显率（WR ≤ 1%）下，DSVDD 性能最稳健：**
   - 在 Recall@400、nDCG@400等指标上均优于 WS-SIL 和 ItS2CLR。
   - 在部分测试中甚至超越完全监督 FS-SIL（说明弱标注难以泛化）。
2. **DROC 在极端稀有情形下具竞争力：**
   - 其分布增强对比学习使得伪异常训练有助于区分正常与异常样本。
3. **弱监督方法在低 WR 时迅速失效：**
   - 由滑动标签噪声引发的误导学习导致召回率接近随机。
4. **专家盲评结果：**
   - 在口腔癌数据中，DSVDD 提供的候选集中，专家确认的可疑细胞数量最多，显示临床可用性。
5. **单类表示学习可为人机协作诊断提供 interpretable pipeline**，通过 top-K 检索界面辅助病理学家快速筛查。

---

## 七、方法与实验的优点

- **创新视角：** 引入单类表征学习到细胞病理检测任务，是弱监督方案的重要替代。
- **实例级评估设计：** 从 slide-level 分类转向 instance-level ranking，更贴合临床需要。
- **数据高效性：** 仅用正常样本训练，规避正样本极度稀缺的问题。
- **可靠的实验对比：** 系统横向比较了多类标准方法，结论客观。
- **可解释输出：** top-K 可视化网格便于专家直接验证，增强结果可复核性。
- **技术细节扎实：**
  - Autoencoder 预训练 + 中心稳定化；
  - 多种种子和视角融合增强鲁棒性；
  - DROC 架构引入 distribution augmentation。

---

## 八、不足与局限

- **算力与时间信息缺失：** 无 GPU 规格、训练时长等细节，降低 reproducibility。  
- **伪异常增强设计主观：** DROC 的增强策略（颜色抖动、形变）未充分验证是否生物学合理。  
- **数据集规模有限：**
  - 骨髓数据 9,000+ 正常样本；
  - 口腔癌数据仅 28 张切片。
- **口腔癌实验缺乏量化指标：**
  - 仅依赖专家盲评，难以统计性比较。
- **域移问题：** 不同染色方式、扫描仪、噪声可能降低跨域泛化性。  
- **固定 top-K 偏差：** 在病变负荷高的切片中，固定的候选数（如 400）限制召回率上限。

---

## 总结

本文系统地验证了单类表示学习在极度稀有细胞病理检测中的可行性与优势。  
通过 DSVDD 与 DROC 两种方法的比较，论文展示了它们在极低病变比例条件下的优越性能，强调了这种“仅用正常数据学习”策略的临床应用潜力。  
未来工作包括开发更生物学合理的伪异常生成策略、结合专家交互式持续学习，以及在更大规模多中心数据上的验证。

---

（完）
