---
title: "UniSemAlign: Text-Prototype Alignment with a Foundation Encoder for Semi-Supervised Histopathology Segmentation"
title_zh: UniSemAlign：使用基础编码器的文本原型对齐方法实现半监督组织病理学分割
authors: "Le-Van Thai, Tien Dat Nguyen, Hoai Nhan Pham, Lan Anh Dinh Thi, Duy-Dong Nguyen, Ngoc Lam Quang Bui"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09169v1"
tags: ["query:pathseg"]
score: 9.0
evidence: 基于基础编码器的计算病理学半监督语义分割
tldr: 本文提出UniSemAlign框架，通过在病理预训练Transformer中引入原型和文本语义对齐机制，将显式类别结构融入像素级学习，稳定伪标签训练并显著提升半监督病理分割性能，在GlaS与CRAG数据集上取得明显改进。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09169v1/fig-001.webp\", \"caption\": \"Figure 2. Qualitative results for different semi-supervised methods under the 10% labeling setting on GlaS-2017 and CRAG-2019. Pink boxes highlight notable differences between competing methods and our approach.\", \"page\": 7, \"index\": 1, \"width\": 1036, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2604.09169v1/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison of the dual semantic alignment branches on GlaS (10% labeled). From left to right: (a) input image, (b) UniSemAlign (ours), (c) Ground truth, (d) without text branch, (e) without prototype branch. Pink boxes highlight notable differences. The prototype branch primarily improves structural completeness and boundary coherence, while the text branch suppresses ambiguous/background activations.\", \"page\": 7, \"index\": 2, \"width\": 496, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2604.09169v1/fig-003.webp\", \"caption\": \"Table 1. Quantitative results on GlaS-2017 and CRAG-2019 datasets under two labeled ratios. We highlight the best performance for each metric in bold, and the second-best in underlined.\", \"page\": 6, \"index\": 3, \"width\": 956, \"height\": 581}, {\"url\": \"assets/figures/arxiv/2604.09169v1/fig-004.webp\", \"caption\": \"Figure 1. Overview of UniSemAlign. An input image is encoded by UNI ViT-B/16 and decoded by DeepLabV3+ to produce visual logits. In parallel, pixel embeddings are projected into a shared semantic space and aligned with learnable class prototypes and prompt-guided text embeddings generated by a frozen CONCH text encoder with learnable context tokens. Cosine similarity between pixel embeddings and these class representations produces alignment logits, which are fused with the decoder logits to generate pseudo-labels for semi-supervised training.\", \"page\": 3, \"index\": 4, \"width\": 1036, \"height\": 484}]"
motivation: 病理语义分割标注稀缺且伪标签不可靠，亟需融合结构化语义信息增强半监督学习。
method: 该方法在病理预训练Transformer编码器上，结合原型级与文本级对齐分支，通过跨模态嵌入空间对齐提升分割稳定性。
result: "在GlaS和CRAG数据集上，UniSemAlign相较现有基线Dice提升最高达8.6%，仅需10%-20%标注数据。"
conclusion: UniSemAlign显著提高了在有限标注条件下的病理图像半监督分割性能，证明其结构化语义对齐策略有效。
---

## 摘要
由于像素级标注稀缺和伪标签监督不可靠，计算病理学中的半监督语义分割仍然具有挑战性。我们提出了 UniSemAlign，这是一种双模态语义对齐框架，通过向像素级学习注入显式的类别层级结构来增强视觉分割效果。UniSemAlign 基于在病理数据上预训练的 Transformer 编码器，在共享嵌入空间中引入互补的原型级和文本级对齐分支，提供结构化的指导，从而减少类别歧义并稳定伪标签的优化。对齐后的表示与视觉预测融合，为未标注的组织病理图像生成更可靠的监督信息。该框架通过有监督分割、跨视角一致性以及跨模态对齐目标进行端到端训练。在 GlaS 和 CRAG 数据集上的大量实验表明，UniSemAlign 在有限监督下显著优于近期半监督基线方法，在仅有 10% 标注数据时，Dice 系数在 GlaS 上提升至多 2.6%，在 CRAG 上提升至多 8.6%，并在 20% 监督下继续取得显著改进。代码可在以下地址获取：https://github.com/thailevann/UniSemAlign

## Abstract
Semi-supervised semantic segmentation in computational pathology remains challenging due to scarce pixel-level annotations and unreliable pseudo-label supervision. We propose UniSemAlign, a dual-modal semantic alignment framework that enhances visual segmentation by injecting explicit class-level structure into pixel-wise learning. Built upon a pathology-pretrained Transformer encoder, UniSemAlign introduces complementary prototype-level and text-level alignment branches in a shared embedding space, providing structured guidance that reduces class ambiguity and stabilizes pseudo-label refinement. The aligned representations are fused with visual predictions to generate more reliable supervision for unlabeled histopathology images. The framework is trained end-to-end with supervised segmentation, cross-view consistency, and cross-modal alignment objectives. Extensive experiments on the GlaS and CRAG datasets demonstrate that UniSemAlign substantially outperforms recent semi-supervised baselines under limited supervision, achieving Dice improvements of up to 2.6% on GlaS and 8.6% on CRAG with only 10% labeled data, and strong improvements at 20% supervision. Code is available at: https://github.com/thailevann/UniSemAlign

---

## 论文详细总结（自动生成）

# UniSemAlign：使用基础编码器的文本原型对齐方法实现半监督组织病理学分割 — 深度总结

---

## 一、研究背景与核心问题

### 背景动机
- **领域问题**：在计算病理学中，语义分割是自动化病理分析与辅助诊断的核心任务。  
- **现有困境**：  
  - 像素级标注成本高昂且依赖专业医师。  
  - 半监督方法利用伪标签，但伪标签质量不稳定、类边界模糊，影响模型可靠性。  
  - 现有基于一致性或伪标签的半监督算法忽视了**病理特定语义结构**，无法充分区分类内部复杂形态。
- **研究动机**：结合**跨模态语义信息（视觉+文本）**和**病理领域预训练模型**，为半监督训练提供更明确的类别指导，以减少伪标签误差并提高鲁棒性。

---

## 二、方法论：UniSemAlign 框架

### 核心思想
- 基于**病理预训练 Transformer 编码器（UNI ViT-B/16）**，在像素级学习中引入**显式类别语义结构**。
- 通过**原型级（Prototype）对齐**与**文本级（Text）对齐**双分支，建立跨模态嵌入空间，使像素特征与类别语义统一。
- 对齐后产生的语义引导与视觉预测融合，用于生成更加稳定的伪标签。

### 方法结构（总体流程）
1. **编码与解码阶段**：
   - 编码器：UNI ViT-B/16，提取层级特征（block 2 与 block 9）。
   - 解码器：DeepLabV3+，用 ASPP 聚合多尺度特征并输出分割 logits。
2. **双语义对齐分支**：
   - **原型分支**：
     - 维护可学习的类别原型向量 \( P \in \mathbb{R}^{C\times D} \)。
     - 通过像素嵌入与原型的余弦相似度生成原型 logits。
   - **文本分支**：
     - 使用冻结的 CONCH 文本编码器生成带上下文提示（CoOp prompt）的类别文本嵌入。
     - 将类别名称与可学习的上下文 token 拼接，通过文本编码器提取 EOT 表征。
     - 投影至与视觉特征同一语义空间后计算余弦相似度生成文本 logits。
3. **Logits 融合与伪标签生成**：
   - 融合公式：  
     \[
     S_{\text{fuse}} = S_{\text{dl}} + \eta_p S_{\text{zp}} + \eta_t S_{\text{zt}}
     \]
     其中 ηp, ηt 控制原型与文本对齐的贡献。
   - 在弱视图下从融合 logits 生成伪标签，并在强视图上通过一致性和分布约束进行优化。
4. **损失函数设计**：
   - **有监督部分**：
     - 三个分支（decoder、prototype、text）的交叉熵损失。
     - 原型与文本嵌入间的 MSE 对齐损失 \( L_{\text{align}} = MSE(P, T) \)。
   - **无监督部分**：
     - 弱-强一致性框架(CorrMatch)的硬伪标签损失、软一致性(KL)损失、特征扰动一致性损失。
   - 总体损失函数：  
     \[
     L = \frac{1}{2}(L_{\text{sup}} + L_{\text{unsup}})
     \]

---

## 三、实验设计

### 数据集与场景
- **GlaS-2017**：165张结直肠腺癌病理图像，挑战为跨阶段组织差异。
- **CRAG-2019**：213张高分辨率结肠病理图像，组织形态变化大。
- 以**10% 和 20% 有标注数据比例**进行半监督训练，其余图像为无标签。

### Benchmark 与对比方法
- 对比半监督分割基线：UAMT、FixMatch、CPS、CT、XNet、CorrMatch、DuSSS、CSDS 等。
- 衡量指标：**Dice 平均系数（mDice）**和**Jaccard 系数（mJaccard）**。

---

## 四、资源与算力配置

- 实验平台：**单张 NVIDIA TITAN Xp GPU (12 GB)**。
- 框架：PyTorch 实现。
- 训练配置：
  - 学习率 0.001（SGD，momentum 0.9，weight decay 10^-4，poly decay）。
  - 训练时长：80 epoch。
  - 输入尺寸：256×256，随机裁剪与增广（CutMix、弱强视图）。
- 文中未提供具体训练时间，但硬件及单卡条件表明为中等算力可复现级别。

---

## 五、实验数量与充分性

- **总实验内容较丰富**：
  - 2 个真实病理数据集（GlaS、CRAG）。
  - 2 个标注比例（10%、20%）。
  - 8 种基线对比方法。
  - 多维消融实验：
    - 编码器替换（ResNet vs. UNI）。
    - 上下文 token 数量变化（2~5个）。
    - 对齐分支去除（仅原型/仅文本）。
    - 对齐损失类型比较（Cosine/KL/MSE）。
- **实验充分性与公平性**：
  - 所有模型使用相同的训练流程与评估指标。
  - 对比方法来自相关顶级会议（CVPR、AAAI、MICCAI、ICCV）。
  - 实验覆盖结构化和语义化多个角度，评价全面且较为公正。

---

## 六、主要结论与发现

1. **性能提升显著**：
   - 在仅10%标注时，GlaS提升 +2.6% Dice、CRAG提升 +8.6%。  
   - 在20%标注时表现进一步增强，接近全监督性能。
2. **方法有效性**：
   - 原型与文本对齐显著降低伪标签噪声。
   - MSE 对齐损失在语义空间中更能保持一致性。
3. **病理领域预训练模型的优势**：
   - 使用 UNI 编码器比常规 ResNet 提升约 +1.77 mDice。
4. **语义引导作用清晰**：
   - 原型分支改善结构完整性，文本分支减少背景误检。
   - 双分支互补使预测更加平滑且边界更准确。

---

## 七、方法与实验亮点

- ✅ **病理特定基础模型整合**：充分利用大型病理预训练的视觉语义。
- ✅ **双模态语义对齐创新**：结合原型与文本引导，形成类显式约束。
- ✅ **弱-强一致性融合伪标签**：提升未经标注样本的监督质量。
- ✅ **丰富消融分析**：系统揭示不同设计对性能的影响。
- ✅ **轻量可复现实现**：单GPU即可完成训练，具备实际推广潜力。

---

## 八、不足与局限性

- ⚠️ **数据集覆盖有限**：仅在两种腺体类病理任务验证，尚未扩展到器官层级或多类别分割。
- ⚠️ **文本语义依赖静态类别名**：尽管引入上下文 token，但无法捕获复杂组织间关系。
- ⚠️ **计算资源未限定时长**：训练耗时未提供，复现周期略不明确。
- ⚠️ **对齐方式简单**：采用线性映射+MSE，对语义空间非线性关系的表达可能不足。
- ⚠️ **伪标签门限依赖经验**：阈值与 EMA 更新规则需调参，存在潜在敏感性。

---

**（完）**
