---
title: "MambaBack: Bridging Local Features and Global Contexts in Whole Slide Image Analysis"
title_zh: MambaBack：在全幅切片图像分析中连接局部特征与全局上下文
authors: "Sicheng Chen, Chad Wong, Tianyi Zhang, Enhui Chai, Zeyu Liu, Fei Xia"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15729v1"
tags: ["query:cellrep"]
score: 9.0
evidence: 全切片图像分析与细粒度局部细胞结构建模
tldr: 论文旨在改进全视野病理图像分析中的多实例学习。作者提出MambaBack模型，通过Hilbert采样保持空间局部性，利用Gated CNN提取局部细胞特征，并用BiMamba2聚合全局上下文，同时采用非对称分块设计降低推理内存，实验验证其优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15729v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of MambaBack. a. MIL pipeline with MambaBack. b. MambaBack structure. c. Heatmap visualization. d. Inference memory usage comparison.\", \"page\": 2, \"index\": 1, \"width\": 1068, \"height\": 802}, {\"url\": \"assets/figures/arxiv/2604.15729v1/fig-002.webp\", \"caption\": \"Table 1: The performance comparison with SOTA methods.\", \"page\": 5, \"index\": 2, \"width\": 1064, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2604.15729v1/fig-003.webp\", \"caption\": \"Figure 2: Analysis of WSI tile distributions and ablation studies on key components. a. Distribution of tile counts across datasets. b. Ablation study on sampling strategies on PANDA dataset. c. Ablation study on model architectures on PANDA dataset.\", \"page\": 5, \"index\": 3, \"width\": 1068, \"height\": 463}]"
motivation: 现有基于Mamba的WSI分析方法在保持空间局部性、捕捉细粒度结构和降低推理内存方面存在不足。
method: 提出了结合Hilbert采样、1D Gated CNN和BiMamba2的混合架构MambaBack，以增强局部特征和全局上下文建模。
result: 在五个数据集上，MambaBack性能优于七种最新方法，并显著减少模型内存峰值。
conclusion: MambaBack有效融合局部与全局表征，提升模型性能并优化内存效率，可推动计算病理学发展。
---

## 摘要
全幅切片图像（Whole Slide Image，WSI）分析在计算病理学中至关重要，它通过整合不同放大倍率下的形态和结构信息来实现癌症诊断。多实例学习（Multiple Instance Learning，MIL）是WSI分析的标准框架。最近，Mamba作为一种用于MIL的潜力骨干模型脱颖而出，凭借其源于自然语言处理（Natural Language Processing，NLP）的高效性及全局上下文建模能力，已超越Transformers。然而，现有基于Mamba的MIL方法仍面临三项关键挑战：（1）在将二维空间展平为一维序列时破坏了空间局部性；（2）对细粒度的局部细胞结构建模效果不佳；（3）在资源受限的边缘设备上推理时出现高内存峰值。相关研究如MambaOut显示，在局部特征提取中，Mamba的SSM组件是冗余的，Gated CNN即可满足需求。鉴于WSI分析同时需要类似自然图像的细粒度局部特征提取和类似NLP的全局上下文建模，我们提出MambaBack，这是一种融合Mamba与MambaOut优势的新型混合架构。首先，我们提出Hilbert采样策略，以在一维序列中保留切块的二维空间局部性，从而提升模型的空间感知能力。其次，我们设计了一个分层结构：包括基于MambaOut的一维Gated CNN模块，用于捕捉局部细胞特征，以及BiMamba2模块，用于汇聚全局上下文，并共同增强多尺度表示。最后，我们实现了非对称分块设计，在训练期间支持并行处理，而在推理期间通过分块-流式累积方式降低峰值内存消耗，便于部署。五个数据集上的实验结果表明，MambaBack优于七种最新方法。源码和数据集已公开。

## Abstract
Whole Slide Image (WSI) analysis is pivotal in computational pathology, enabling cancer diagnosis by integrating morphological and architectural cues across magnifications. Multiple Instance Learning (MIL) serves as the standard framework for WSI analysis. Recently, Mamba has become a promising backbone for MIL, overtaking Transformers due to its efficiency and global context modeling capabilities originating from Natural Language Processing (NLP). However, existing Mamba-based MIL approaches face three critical challenges: (1) disruption of 2D spatial locality during 1D sequence flattening; (2) sub-optimal modeling of fine-grained local cellular structures; and (3) high memory peaks during inference on resource-constrained edge devices. Studies like MambaOut reveal that Mamba's SSM component is redundant for local feature extraction, where Gated CNNs suffice. Recognizing that WSI analysis demands both fine-grained local feature extraction akin to natural images, and global context modeling akin to NLP, we propose MambaBack, a novel hybrid architecture that harmonizes the strengths of Mamba and MambaOut. First, we propose the Hilbert sampling strategy to preserve the 2D spatial locality of tiles within 1D sequences, enhancing the model's spatial perception. Second, we design a hierarchical structure comprising a 1D Gated CNN block based on MambaOut to capture local cellular features, and a BiMamba2 block to aggregate global context, jointly enhancing multi-scale representation. Finally, we implement an asymmetric chunking design, allowing parallel processing during training and chunking-streaming accumulation during inference, minimizing peak memory usage for deployment. Experimental results on five datasets demonstrate that MambaBack outperforms seven state-of-the-art methods. Source code and datasets are publicly available.

---

## 论文详细总结（自动生成）

# MambaBack：在全幅切片图像分析中连接局部特征与全局上下文

---

## 1. 研究背景与核心问题

### 背景
- **全幅切片图像（Whole Slide Image, WSI）分析**是计算病理学的核心环节，用于自动化癌症诊断与组织病理评估。  
- WSI 通常是**吉像素级**的大规模图像，包含跨尺度的细胞与组织结构信息，需要同时建模局部微观形态与整体空间关系。  
- 当前主流方法采用 **多实例学习（Multiple Instance Learning, MIL）**，即从切块（tile）中提取特征，再通过聚合模型进行全图级预测。

### 研究问题
现有基于 Mamba 的 MIL 模型虽然在大规模长序列建模上优于 Transformer，但存在三大核心问题：
1. **二维空间展开为一维序列后局部性丢失**，破坏了组织结构的空间连续性。  
2. **局部结构表征不足**，难以捕获细胞层级的微观特征。  
3. **推理内存消耗高**，难以在资源受限的边缘设备上部署。

---

## 2. 方法论与技术细节

### 核心思想
作者提出 **MambaBack** —— 一种融合 Mamba 与卷积特性的混合架构，旨在：
- 同时捕获**局部细粒度特征**与**全局上下文关系**；
- 在推理时保持**内存友好**和**硬件可部署性**。

### 关键设计

#### （1）Hilbert 采样策略
- 使用 **Hilbert 空间填充曲线**将二维坐标投影到一维序列，同时最大程度地保持局部空间邻域。
- 采用 **rank 映射**压实稀疏坐标空间：  
  \[
  \tilde{x}_i = Rank(x_i), \quad \tilde{y}_i = Rank(y_i)
  \]
- 计算每个切块的 Hilbert 索引 \( h_i \)，并按该序列排序得到拓扑连续的输入。  
- 训练阶段随机截取连续片段进行学习，相当于空间增强。

#### （2）混合层级架构 MambaBack Structure
- **局部阶段**：基于 MambaOut 的 **一维 Gated CNN** 模块，用于建模邻近 tile 的相关性。
  - 采用深度卷积（DWConv）+ 门控机制（σ 激活与通道拼接）。  
  - 每个局部块产生一个等效 token，显著**降低序列长度与计算量**。
- **全局阶段**：基于 **双向 Mamba2（BiMamba2）** 的 SSM 结构，  
  - 同时从前向与反向序列建模长程依赖：  
    \[
    H_{fwd} = Mamba2(T), \quad H_{bwd} = Mamba2(Flip(T))
    \]
  - 最终融合两向输出做全局特征聚合。
- **聚合阶段**：使用 Gated Attention 将全局 token 汇总为切片级嵌入进行分类预测。

#### （3）非对称分块设计（Asymmetric Chunking）
- **训练阶段**：采用并行批处理，保证 GPU 闭塞度与训练稳定性。  
- **推理阶段**：采用 **Chunk-and-Accumulate** 模式，按块处理并丢弃中间特征图，内存占用随输入大小保持近似常量。  
- 保持高精度注意力聚合的同时显著降低峰值内存。

---

## 3. 实验设计

### 数据集与任务
共在 **5 个数据集/4 个任务** 上进行了评估（共约 17,828 张切片）：
1. **CAMELYON16** — 乳腺转移二分类。  
2. **CAMELYON17** — 乳腺转移多分类。  
3. **PANDA** — 前列腺癌 ISUP 六级分级。  
4. **TCGA-NSCLC** — 肺组织三分类（正常/腺癌/鳞癌）。  
5. **TCGA-BRCA** — 基于组织形态的生存时间预测。

### 对比基线
与 **7 种最新方法** 对比：
- ABMIL、CLAM、DTFD-MIL（基于注意力的 MIL）
- TransMIL、Prov-GigaPath（Transformer 系列）
- MambaMIL（Mamba 基线）
- PathRWKV（SSM-RNN 结合模型）

### 评价指标
- 分类任务：Accuracy、AUC、F1-Score  
- 生存分析任务：C-Index（一致性指数）  

### 实验设置
- **特征提取器**：冻结的 Prov-GigaPath 基础模型。  
- **tile 尺寸**：224×224 像素，0.5 mpp。  
- **优化器**：AdamW + 余弦退火学习率，学习率范围 \(10^{-4}–10^{-2}\)。  
- **训练早停**：10 次无改进停止。  

---

## 4. 资源与算力

- 明确说明在 **四张 NVIDIA RTX 4090 GPU** 上完成训练。  
- 环境：Python 3.12、PyTorch 2.9、CUDA 12.8。  
- 论文未明确训练时长或推理时间，仅说明推理阶段通过非对称分块实现 CPU 级可行性。

---

## 5. 实验数量与充分性

- **主实验**：5 个公开数据集，7 个基线方法，对应 **共 35 组主结果**。  
- **消融实验**（Fig. 2）：
  - 三组关键模块验证：
    1. 采样策略（随机 / 顺序 / Z-order / Hilbert）。  
    2. 模块架构（纯 BiMamba2、纯 GatedCNN、反转配置 MambaBack(R)）。  
    3. 非对称设计对内存峰值影响。  
- 实验全面覆盖算法各组成部分，评估指标多样，**对比公正且复现实验条件一致**，具备较强说服力。

---

## 6. 主要结论与发现

- MambaBack 在 **所有五个数据集上均达到最优或并列最优结果**。  
  - 例如 PANDA 数据集 AUC 达到 **0.948**（超越 MambaMIL、TransMIL 等）。  
  - CAMELYON17 多分类 F1-score **0.513**，超过 CLAM 与 MambaMIL。  
- Hilbert 采样在保持空间连贯性方面最优，显著减少性能方差。  
- 混合层级结构比单独 CNN / SSM 更有效地结合局部与全局特征。  
- 非对称设计降低峰值内存并保持高精度，适合边缘设备部署。

---

## 7. 优点与创新亮点

- **结构创新**：首次将 Mamba (SSM) 与 Gated CNN 混合，用分层方式模拟病理学“由微到宏”的观察逻辑。  
- **空间优化**：Hilbert 采样保留 2D 空间拓扑结构，显著改进切片顺序建模。  
- **资源适配性强**：非对称分块推理大幅降低内存占用，使得在 CPU / FPGA 等平台上可部署。  
- **实验全面且复现性好**：涵盖五大公开 benchmark，公开代码与数据集。  
- **性能与泛化兼顾**：在所有基准上保持高稳定性，Transformer 类模型易过拟合的缺陷得到有效缓解。

---

## 8. 不足与局限

- **训练时长未披露**，无法量化训练效率。  
- **Hilbert 采样**虽保留局部连续性，但仍依赖 2D→1D 展平，复杂非凸组织结构可能仍有信息丢失。  
- **基础特征提取器冻结**，未探索端到端上线方式，对领域自适应能力有限。  
- **任务类型主要聚焦分类与生存预测**，尚未验证在分割、检测或弱监督定位任务上的泛化能力。  
- **Edge 部署仅限内存评估**，未提供真实硬件性能或延迟数据。

---

**（完）**
