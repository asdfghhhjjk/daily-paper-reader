---
title: "MambaBack: Bridging Local Features and Global Contexts in Whole Slide Image Analysis"
title_zh: MambaBack：在全切片图像分析中连接局部特征与全局上下文
authors: "Sicheng Chen, Chad Wong, Tianyi Zhang, Enhui Chai, Zeyu Liu, Fei Xia"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15729v1"
tags: ["query:cellrep"]
score: 10.0
evidence: 基于Mamba的多实例学习用于WSI分析，聚焦局部细胞结构
tldr: 本文提出MambaBack，一种融合Mamba与Gated CNN优势的新型多实例学习架构，用于全切片病理图像分析。通过Hilbert采样保持二维空间局部性、分层结构协调局部特征与全局上下文，并采用不对称分块设计降低推理内存，显著超越现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15729v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of MambaBack. a. MIL pipeline with MambaBack. b. MambaBack structure. c. Heatmap visualization. d. Inference memory usage comparison.\", \"page\": 2, \"index\": 1, \"width\": 1068, \"height\": 802}, {\"url\": \"assets/figures/arxiv/2604.15729v1/fig-002.webp\", \"caption\": \"Table 1: The performance comparison with SOTA methods.\", \"page\": 5, \"index\": 2, \"width\": 1064, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2604.15729v1/fig-003.webp\", \"caption\": \"Figure 2: Analysis of WSI tile distributions and ablation studies on key components. a. Distribution of tile counts across datasets. b. Ablation study on sampling strategies on PANDA dataset. c. Ablation study on model architectures on PANDA dataset.\", \"page\": 5, \"index\": 3, \"width\": 1068, \"height\": 463}]"
motivation: 现有Mamba-based MIL方法在空间局部性保持、细粒度结构建模及内存效率上存在不足。
method: 采用Hilbert采样保留空间关系，结合Gated CNN捕获局部特征与BiMamba2聚合全局上下文，并通过不对称分块优化内存。
result: 在五个数据集上，MambaBack性能优于七种最新方法，验证其卓越的精度与效率。
conclusion: MambaBack有效结合局部与全局建模优势，在全切片图像分析中展现了优异性能与实际部署潜力。
---

## 摘要
全切片图像（Whole Slide Image, WSI）分析在计算病理学中至关重要，通过整合不同放大倍率下的形态学与结构信息来实现癌症诊断。多实例学习（Multiple Instance Learning, MIL）是WSI分析的标准框架。近期，Mamba成为MIL的潜力骨干网络，凭借其源于自然语言处理（NLP）的高效性与全局上下文建模能力，超越了Transformers。然而，现有基于Mamba的MIL方法面临三个关键挑战：（1）在一维序列展开过程中二维空间局部性被破坏；（2）对细粒度的局部细胞结构建模不充分；（3）在资源受限的边缘设备上推理时出现高内存峰值。已有研究如MambaOut指出，Mamba的SSM组件在局部特征提取方面是冗余的，而门控卷积神经网络（Gated CNN）已足够。鉴于WSI分析同时需要类似自然图像的细粒度局部特征提取与类似NLP的全局上下文建模，我们提出MambaBack，这是一种融合Mamba与MambaOut优势的新型混合架构。首先，我们提出Hilbert采样策略，以在一维序列中保留瓦片的二维空间局部性，从而增强模型的空间感知能力。其次，我们设计了一个分层结构，其中包括一个基于MambaOut的1D门控CNN模块用于捕获局部细胞特征，以及一个BiMamba2模块用于聚合全局上下文，共同提升多尺度表征能力。最后，我们实现了不对称分块设计，使训练阶段可进行并行处理，而推理阶段采用分块流式累积，从而将峰值内存使用最小化以便部署。在五个数据集上的实验结果表明，MambaBack的性能优于七种最新的先进方法。源代码和数据集已公开发布。

## Abstract
Whole Slide Image (WSI) analysis is pivotal in computational pathology, enabling cancer diagnosis by integrating morphological and architectural cues across magnifications. Multiple Instance Learning (MIL) serves as the standard framework for WSI analysis. Recently, Mamba has become a promising backbone for MIL, overtaking Transformers due to its efficiency and global context modeling capabilities originating from Natural Language Processing (NLP). However, existing Mamba-based MIL approaches face three critical challenges: (1) disruption of 2D spatial locality during 1D sequence flattening; (2) sub-optimal modeling of fine-grained local cellular structures; and (3) high memory peaks during inference on resource-constrained edge devices. Studies like MambaOut reveal that Mamba's SSM component is redundant for local feature extraction, where Gated CNNs suffice. Recognizing that WSI analysis demands both fine-grained local feature extraction akin to natural images, and global context modeling akin to NLP, we propose MambaBack, a novel hybrid architecture that harmonizes the strengths of Mamba and MambaOut. First, we propose the Hilbert sampling strategy to preserve the 2D spatial locality of tiles within 1D sequences, enhancing the model's spatial perception. Second, we design a hierarchical structure comprising a 1D Gated CNN block based on MambaOut to capture local cellular features, and a BiMamba2 block to aggregate global context, jointly enhancing multi-scale representation. Finally, we implement an asymmetric chunking design, allowing parallel processing during training and chunking-streaming accumulation during inference, minimizing peak memory usage for deployment. Experimental results on five datasets demonstrate that MambaBack outperforms seven state-of-the-art methods. Source code and datasets are publicly available.

---

## 论文详细总结（自动生成）

# MambaBack：在全切片图像分析中连接局部特征与全局上下文  

---

## 一、研究核心问题与背景  

- **研究动机**：  
  全切片图像（Whole Slide Image, WSI）在病理诊断中包含从细胞级到组织级的多尺度信息。这些图像的巨量数据规模与复杂结构使得分析过程费时费力，传统深度学习模型难以高效捕捉既细粒度又全局的特征。  
- **现有难题**：  
  目前基于多实例学习（Multiple Instance Learning, MIL）的WSI分析是主流方法，但在实践中存在三大瓶颈：  
  1. **二维空间局部性破坏**：WSI瓦片被展平为一维序列后，其组织空间关系丢失。  
  2. **局部细胞结构建模不足**：现有模型更强于全局上下文，但忽视了细胞微结构。  
  3. **推理阶段内存峰值高**：难以在临床边缘设备（如FPGA）上部署。  
- **研究意义**：  
  作者旨在设计一种兼顾局部特征提取与全局上下文聚合的高效MIL架构，解决Mamba模型在图像领域的结构性短板，实现更高的性能和更低的计算成本。

---

## 二、方法论：核心思想与关键技术  

### 1. 总体框架  

**MambaBack**是一种由两部分组成的混合模型：  
- **局部特征提取层**：基于1D门控卷积神经网络（Gated CNN），来自“MambaOut”的理念，用于捕获细粒度细胞结构。  
- **全局上下文聚合层**：使用双向Mamba2（BiMamba2）模块以建模长程依赖。  
其设计目标是融合CNN擅长的空间局部建模与Mamba擅长的长序列建模能力。

### 2. Hilbert采样策略  

- **问题解决**：避免传统行列展开导致空间相关性破坏。  
- **核心思想**：利用Hilbert曲线的空间填充特性，将二维坐标映射到一维顺序同时保留邻域信息。  
- **算法流程**（文字化阐述）：  
  1. 对原始瓦片坐标进行稠密映射。  
  2. 依据Hilbert曲线的阶次递归计算每个瓦片的Hilbert索引。  
  3. 按索引排序形成空间连续的1D序列，并在训练阶段进行随机连续片段采样，实现空间保持的数据增强。  

### 3. 分层结构设计  

- **Local Stage（局部阶段）**：  
  将输入序列分块，每块通过Gated CNN模块处理局部邻域特征，再经门控注意力压缩为代表token。  
- **Global Stage（全局阶段）**：  
  所有代表token输入双向Mamba2结构，通过正向和反向状态空间建模捕获完整全局上下文。  
- **公式核心要素**：包含通道拆分、深度可分卷积（DWConv）、门控激活（GELU）、局部注意力压缩与双向状态空间更新。

### 4. 不对称分块设计  

- **目的**：在训练阶段保持并行高吞吐，而在推理阶段显著降低峰值内存。  
- **实现流程**：  
  - 训练时：分块（如每块64长度）并行在GPU上处理，压缩为M个token。  
  - 推理时：采用“Chunk-and-Accumulate”流式算法逐块推理，及时释放中间特征，实现近乎常量内存复杂度。  

---

## 三、实验设计  

- **数据集与任务**：  
  共使用五个公开WSI数据集，覆盖四种任务类型：  
  1. **CAMELYON16**：乳腺癌二分类。  
  2. **CAMELYON17**：乳腺癌多分类。  
  3. **PANDA**：前列腺癌ISUP分级。  
  4. **TCGA-NSCLC**：肺癌亚型分类。  
  5. **TCGA-BRCA**：乳腺癌生存期预测（回归任务）。  

- **对比方法（共七种）**：  
  - ABMIL、CLAM、DTFD-MIL（基于注意机制）  
  - TransMIL、GigaPath（Transformer类方法）  
  - MambaMIL、PathRWKV（基于Mamba的SSM方法）  

- **Benchmark指标**：  
  - 分类任务：Accuracy、AUC、F1-score。  
  - 生存任务：C-Index。  
  - 均采用五折交叉验证。

---

## 四、资源与算力  

- **算力配置**：  
  - 硬件：四张 NVIDIA RTX 4090 GPU。  
  - 软件环境：Python 3.12、PyTorch 2.9、CUDA 12.8。  
  - 未明确说明训练总时长或单模型耗时。  
- **批次与参数**：  
  - 训练批次大小：B=8（PANDA 为 B=64）。  
  - 最多处理 4096 tile features。  
  - 优化器：AdamW + cosine decay，早停策略 10 epochs。  

---

## 五、实验数量与充分性  

- **主要实验类型**：  
  1. **性能对比实验**：五个不同数据集，与七种SOTA方法对比。  
  2. **消融实验**：  
     - Hilbert采样 vs 随机 / 顺序 / Z-order。  
     - 结构对比（纯BiMamba2 / 纯Gated CNN / 反向组合）。  
     - 内存消耗对比（不同模型推理阶段）。  
- **实验充分性与公平性**：  
  - 涵盖多任务（分类 + 生存预测），结果一致。  
  - 指标全面，均采用相同数据划分与预处理。  
  - 数据量庞大（总计17,828张WSI），实验充分且客观性强。

---

## 六、主要结论与发现  

- MambaBack在五个数据集上**全面优于七种最先进方法**。  
- 证明Hilbert采样有效保持空间邻域关系，提高模型稳定性。  
- 混合结构（Gated CNN + BiMamba2）显著提升局部和全局建模能力。  
- 不对称设计显著降低推理内存峰值，实现轻量部署。  
- 其性能与轻量模型（如CLAM）相当，却具备更丰富的多尺度表征，展现良好泛化性与临床可部署潜力。

---

## 七、优点与亮点  

- **结构创新**：首次在WSI分析中结合Mamba与CNN，实现局部与全局的双域建模。  
- **采样创新**：Hilbert曲线保持二维拓扑连续性，提升空间特征保真度。  
- **推理优化**：不对称分块使模型既能高吞吐训练又能低内存推理，兼顾性能与可部署性。  
- **实验全面**：多任务、多数据集、多方法比较，结果稳定一致。  

---

## 八、不足与局限  

- **算力与能耗未详细披露**：训练时长、参数数量缺乏具体说明。  
- **消融实验范围有限**：未深入分析不同分块大小或BiMamba配置的影响。  
- **实际临床部署验证缺乏**：虽强调边缘设备可用性，但无真实部署测试。  
- **模型复杂度仍高**：尽管降内存，但其双阶段设计可能在极端数据规模上效率下降。  

---

（完）
