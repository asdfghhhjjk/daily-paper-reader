---
title: Biological Spatial Priors Regularize Foundation Model Representations for Cross-Site MSI Generalization in Colorectal Cancer
title_zh: 生物空间先验正则化基础模型表示以提升结直肠癌跨地点MSI泛化能力
authors: Dasari Naga Raju
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02660v1"
tags: ["query:cellrep"]
score: 8.0
evidence: "H&E全切片图像中基础模型表征的瓦片级空间先验"
tldr: 本文研究如何利用生物学空间先验改善结直肠癌MSI预测模型的跨机构泛化能力，通过在Transformer聚合器中引入肿瘤边缘距离和免疫邻域编码，使模型关注生物学相关形态特征而非机构特定纹理，并在跨站点测试中显著提升预测表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02660v1/fig-001.webp\", \"caption\": \"Figure 1: Pipeline for MSI prediction from whole slide images (H&E). Tile features are extracted using a foundation model, augmented with a peripheral distance (PD) spatial prior, and aggregated using TransMIL for slide-level prediction.\", \"page\": 5, \"index\": 1, \"width\": 821, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2605.02660v1/fig-002.webp\", \"caption\": \"Table 2: TransMIL with spatial biological priors. Peripheral Distance (PD) encodes margin proximity. Local Immune Neighborhood (LIN) encodes neighborhood LYM/TUM ratio. READ MSS Spec is MSS specificity on TCGA-READ (49 MSS cases).\", \"page\": 7, \"index\": 2, \"width\": 867, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2605.02660v1/fig-003.webp\", \"caption\": \"Table 1: Results for all foundation model and aggregator configurations. COAD MSI AUC is mean ± std across 5 folds. READ MSS Spec is MSS specificity on TCGA-READ (49 MSS cases). Kather 2019 benchmark AUC: 0.840 [4].\", \"page\": 7, \"index\": 3, \"width\": 827, \"height\": 250}]"
motivation: 由于MSI预测模型在不同机构间表现不一致，需探索生物学驱动的方式提升模型的跨站点泛化能力。
method: 通过在TransMIL聚合器中注入基于肿瘤边缘距离和局部免疫邻域的空间编码，将生物学上下文信息融入基础模型特征中。
result: 边缘距离编码在TCGA-COAD上取得AUC 0.959并在TCGA-READ上实现MSS特异性1.000，优于参考配置；局部免疫编码跨站点表现较弱。
conclusion: 研究表明，引入基于生物学空间先验的正则化可显著提升模型跨机构的泛化能力。
---

## 摘要
从常规苏木精和伊红（H&E）全切片图像（WSIs）预测微卫星不稳定性（MSI）状态是一种替代分子检测的实用方法，但在一个机构训练的模型通常在不同地点采集的切片上泛化能力较差。尽管基础模型的表示具有广泛性，仍然会将特定地点的纹理编码到与MSI相关的保守生物形态中。我们研究是否可以通过源自已知MSI组织学的切片级空间先验，引导这些表示倾向于更加跨地点不变的特征。我们提出一种基于生物学动机的空间先验——基于外围距离编码，反映在肿瘤侵袭边缘处类似克罗恩病的外围淋巴细胞反应，并评估一种次级局部免疫邻域编码，反映每个切片的局部空间邻域内的淋巴细胞与肿瘤的比例。两种先验均在自注意力之前被注入到TransMIL聚合器中，使Transformer能够在所有注意层上整合空间生物学上下文与UNI2-h或Virchow2特征。我们以六种基础模型和MIL聚合器的组合作为参考，然后评估每个空间先验的效果。在TCGA-COAD（137张切片）上训练，在不重新训练的情况下在TCGA-READ（50张切片）上进行外部评估，外围距离编码在COAD上实现了MSI AUC为0.959 ± 0.012，在READ上实现了MSS特异性为1.000，相比之下最强参考配置分别为0.957和0.939。局部免疫邻域编码获得了可比的内部AUC，但跨地点的特异性较低，表明边缘邻近性比局部免疫密度更加编码了跨地点不变的生物信号。结果表明，具有生物学基础的空间先验可作为正则化器，减少模型对特定地点成像模式的依赖。

## Abstract
Predicting microsatellite instability (MSI) status from routine hematoxylin and eosin (H&E) whole slide images (WSIs) offers a practical alternative to molecular testing, but models trained at one institution tend to generalize poorly to slides acquired at a different site. Foundation model representations, despite their generality, still encode site-specific texture alongside the conserved biological morphology underlying MSI. We investigate whether tile-level spatial priors derived from known MSI histology can guide these representations toward more site-invariant features. We introduce a biologically motivated spatial prior based on peripheral distance encoding, reflecting the Crohn's-like peripheral lymphocytic reaction at the tumor invasive margin, and evaluate a secondary local immune neighborhood encoding reflecting the lymphocyte-to-tumor ratio in each tile's immediate spatial neighborhood. Both priors are injected into a TransMIL aggregator before self-attention, allowing the transformer to integrate spatial biological context with UNI2-h or Virchow2 features across all attention layers. We evaluate six foundation model and MIL aggregator combinations as a reference, then assess the effect of each spatial prior. Training on TCGA-COAD (137 slides) and evaluating externally on TCGA-READ (50 slides) without retraining, peripheral distance encoding achieves MSI AUC 0.959 +/- 0.012 on COAD and MSS specificity 1.000 on READ, compared to 0.957 and 0.939 for the strongest reference configuration. Local immune neighborhood encoding achieves comparable internal AUC but lower cross-site specificity, suggesting margin proximity encodes a more site-invariant biological signal than local immune density. Results suggest biologically grounded spatial priors act as regularizers that reduce reliance on site-specific imaging patterns.

---

## 论文详细总结（自动生成）

# 生物空间先验正则化基础模型表示以提升结直肠癌跨地点 MSI 泛化能力 — 深度总结

---

## 一、研究核心问题与整体背景

- **研究对象**：结直肠癌（Colorectal Cancer, CRC）的分子分型之一——微卫星不稳定性（Microsatellite Instability, MSI）。
- **临床背景**：MSI-H 肿瘤与免疫检查点抑制剂敏感相关，是目前精准治疗的重要分型指标。
- **问题动机**：
  - 传统分子检测（PCR 或免疫组化）成本高、周期长。
  - H&E 全切片图像（Whole Slide Images, WSIs）提供一种低成本替代方案。
  - 当前模型在单机构训练后跨机构泛化能力明显下降——即“域间漂移”问题。
- **核心假设**：
  - 基础模型（如 UNI2-h、Virchow2）的表示虽具普适性，但仍残留**站点特定纹理与染色差异**。
  - 若引入**生物学空间先验**（例如组织结构位置、免疫分布规律），可显式引导模型关注跨站点稳定的形态学特征，从而提高泛化性能。

---

## 二、方法论：核心思想与关键技术

### 1. 核心思想
> 通过在 Transformer-based 草图聚合器（TransMIL）中注入**生物学动机的空间编码**，将肿瘤免疫反应的空间分布显式作为模型的输入特征，使其成为正则化项以抑制站点特定纹理依赖。

### 2. 空间先验类型
- **（1）外围距离编码（Peripheral Distance Encoding, PD）**
  - 基于几何坐标的单维度编码，用于度量瓦片（tile）相对于组织边缘的归一化距离。
  - 理论依据：MSI-H 组织常在肿瘤侵袭边缘表现出“Crohn’s-like”淋巴细胞浸润。
  - 公式：
    - 计算瓦片四方向的归一化边距，取最小值再线性转换为外围距离分数 `dperiph = 1 − 2 × min(dL, dR, dT, dB)`。
  - 实现方式：在 TransMIL 输入层前追加该维度，与原 tile 特征拼接形成 (1536+1) 或 (2560+1) 维向量。

- **（2）局部免疫邻域编码（Local Immune Neighborhood Encoding, LIN）**
  - 反映局部区域内淋巴细胞与肿瘤组织的空间比例。
  - 基于 NCT-CRC-HE-100K 数据集上训练的线性 probe 提供 LYM（淋巴）与 TUM（肿瘤）类别概率。
  - 在半径 r=0.1 的邻域内计算平均概率比：
    - `sLIN(i) = log( (p̄LYM + ε) / (p̄TUM + ε) )`
  - 特征加入方式：同 PD，在输入层前拼接，维度增加 1。

### 3. 模型架构与集成机制
- 使用 **TransMIL** 聚合器进行全切片级预测。
  - 在多层自注意力机制中，瓦片可相互关联。
  - 生物空间先验嵌入后，Transformer 在注意力层能同时考虑**空间上下文与语义内容**。
- 三头多任务输出：同时预测 MSI、MSS、以及超突变状态（hypermutation）。

---

## 三、实验设计

### 1. 数据集与场景
- **TCGA-COAD（训练/验证）**
  - Colon Adenocarcinoma，共 137 张切片（23 MSI-H，114 MSS）。
  - 5 折分层交叉验证。
- **TCGA-READ（测试）**
  - Rectal Adenocarcinoma，50 张切片（1 MSI-H，49 MSS）。
  - 仅用于跨站点验证，不做再训练。
- **特征提取模型**
  - UNI2-h（1536维）与 Virchow2（2560维）两种基础模型。
  - 无 stain normalization 或数据增强。

### 2. 对比方法
共测试六组基线组合：
- Encoder：UNI2-h / Virchow2  
- Aggregator：ABMIL / CLAM-SB / TransMIL  
- 基线性能：UNI2-h + TransMIL 在 TCGA-COAD AUC 0.957，READ MSS specificity 0.939。

### 3. 空间先验消融实验
- 分别将 PD 与 LIN 加入 UNI2-h + TransMIL、Virchow2 + TransMIL。
- 对比内部（COAD）AUC 与跨站点（READ）MSS specificity。

---

## 四、资源与算力

- 明确说明：
  - 单卡 NVIDIA GPU；
  - 混合精度（bfloat16）训练；
  - 训练 30 epoch；
  - Adam 优化器，学习率 1e−4，权重衰减 1e−5；
  - 余弦退火调度；
  - 梯度裁剪最大范数 1.0；
  - 无多卡训练、无大规模分布式。
- 算力消耗较低，未提及具体 GPU 型号或耗时估计。

---

## 五、实验数量与充分性

- **主要实验类别**：
  - 6组基线模型比较；
  - 2种空间先验 × 2种基础模型组合，共4组先验实验；
  - 5折交叉验证（COAD）；
  - 外部测试（READ）；
  - 可视化分析（attention map）。
- **实验充分性**：
  - 内部验证与外部泛化均有；
  - 结合不同预训练模型和不同聚合器架构，覆盖面广；
  - 均保持训练超参一致，保证公平性；
  - 不足：外部测试数据 MSI-H 样本仅 1例，不可评估敏感性，仅特异性可靠。

---

## 六、主要结论与发现

- **外围距离编码（PD）**：
  - 内部 COAD MSI AUC 提高至 0.959；
  - 外部 READ MSS specificity 从 0.939 → 1.000（零假阳性）。
  - 在 Virchow2 上也有明显提升（AUC 0.915 → 0.941，特异性 0.878 → 0.959）。

- **局部免疫邻域编码（LIN）**：
  - 对 UNI2-h 增益有限；
  - 对 Virchow2 减少内部 AUC但显著提升跨站点特异性；
  - 显示该先验主要起到正则化作用。

- **定性结果（注意力分布）**：
  - MSI-H 的注意力由组织中心转向边缘；
  - MSS 则呈现注意力抑制效应；
  - 验证了先验能够引导模型聚焦于解剖学稳定区域而非纹理模式。

---

## 七、研究优点与创新亮点

- **方法上的创新**：
  - 将病理学知识（肿瘤侵袭边缘免疫反应）编码为几何先验；无需标注或额外数据。
  - 通过简单维度拼接实现，与 Transformer 自注意力兼容、无架构改动。
- **正则化视角**：
  - 先验不提供新特征，而引导注意力机制趋向生物学稳定标志。
- **跨模型一致性**：
  - PD 在两种基础模型上均提升特异性，显示稳定的普适效应。
- **临床意义**：
  - 在 MSS 高占比的临床场景下实现零假阳性，具有潜在应用价值。

---

## 八、不足与局限

- **数据规模与代表性**：
  - TCGA-COAD 样本较少（仅 23 MSI-H），验证统计稳定性不足。
- **外部测试的单样本限制**：
  - TCGA-READ 仅含 1 个 MSI-H样本，无法评估敏感性。
- **空间先验粗粒度**：
  - 外围距离仅为近似几何代理，未能精确定位真正的肿瘤侵袭边缘。
- **未验证多机构外部独立数据**：
  - 泛化的结论仅基于 TCGA 内部跨队列结果。
- **算力细节未披露**：
  - 未说明 GPU 型号及训练时长，难以评估计算资源消耗和可复现性。

---

**（完）**
