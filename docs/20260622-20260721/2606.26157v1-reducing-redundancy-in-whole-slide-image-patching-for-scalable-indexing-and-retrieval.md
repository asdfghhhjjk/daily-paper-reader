---
title: Reducing Redundancy in Whole-Slide Image Patching for Scalable Indexing and Retrieval
title_zh: 减少全切片图像分块冗余以实现可扩展索引与检索
authors: "Jialiang Geng, Ghazal Alabtah, Saghir Alfasly, Wataru Uegami, H. R. Tizhoosh"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.26157v1"
tags: ["query:wsi-recon"]
score: 9.0
evidence: 全切片图像分块冗余降低以实现计算病理学高效索引与检索
tldr: "数字病理学领域的飞速发展和检索增强生成等前沿AI工作流的涌现，对全切片图像的高效索引和检索提出了迫切要求，但高昂的存储成本成为大规模应用的突出障碍。本文提出的ARReST策略通过分析不同组织类别间的冗余，精确定位并裁剪掉对跨类判别贡献极小的“对立补丁”，实现索引高效压缩，同时保持形态多样性。在TCGA数据库21种器官上的评估显示，存储消耗显著削减，节省幅度达3%–60%（平均14%±13%），且检索表现保持强劲。该方法有效降低了存储与计算开销，加快了搜索速度，为构建可扩展、低成本的病理图像检索系统，特别是支持检索增强的临床AI应用，提供了实用途径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26157v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1355, \"height\": 812}, {\"url\": \"assets/figures/arxiv/2606.26157v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1675, \"height\": 966}, {\"url\": \"assets/figures/arxiv/2606.26157v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1674, \"height\": 966}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26157v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1702, \"height\": 600}, {\"url\": \"assets/tables/arxiv/2606.26157v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1661, \"height\": 1120}]"
motivation: 数字病理图像规模激增，高效索引和相似性检索成为瓶颈，而高昂的存储成本限制了大规模部署，迫切需要压缩索引的方法。
method: 提出ARReST框架，利用不同组织类别的冗余，识别并移除对跨类区分贡献微弱的对立补丁，精准压缩索引。
result: "在TCGA 21种器官上，索引存储节省3%至60%（平均14%±13%），同时检索性能保持竞争力。"
conclusion: ARReST有效压缩WSI索引，降低存储和计算开销，加速相似性搜索，为检索增强生成等下一代临床AI工作流提供可扩展、低成本的解决方案。
---

## 摘要
数字病理学的快速发展对全切片图像（WSI）的高效索引与检索提出了迫切需求。新兴的生成式AI工作流，尤其是检索增强生成（RAG），进一步加剧了这一需求，因为它们需要可靠的相似性搜索来支持高风险的临床决策。然而，高性能存储的显著成本限制了许多医疗机构中WSI索引的可扩展性和可访问性。因此，能够在保持检索精度的同时降低存储需求的方法已成为关键研究重点。我们提出ARReST（对立冗余减少策略），这是一种原则性的对立框架，利用相异组织类别间的冗余来显著减少每张WSI必须索引的分块数量。ARReST并非仅消除类内重复，而是识别出那些对类间区分贡献极小的对抗性分块，并将其从可搜索档案中裁剪。这种有针对性的缩减大幅压缩了索引，且不牺牲形态学多样性或检索保真度。通过最小化多余的分块表示，ARReST减少了存储占用、降低了计算开销，并加速了大规模病理库中的相似性搜索。在TCGA库（癌症基因组图谱，涵盖21个器官）上的大量实验表明，ARReST在实现显著索引压缩的同时保持了有竞争力的检索性能。观察到的存储节省幅度为3%至60%（14%±13%），且在许多器官上可可靠地实现而不损害检索性能。所提出的策略使可扩展、成本高效的WSI索引成为可能，非常适合下一代检索驱动的临床AI系统。

## Abstract
The rapid growth of digital pathology has created an urgent need for efficient indexing and retrieval of whole slide images (WSIs). This need is intensified by emerging generative AI workflows, particularly retrieval-augmented generation (RAG), which require dependable similarity search to support high-stakes clinical decision-making. Yet the substantial cost of high-performance storage limits the scalability and accessibility of WSI indexing for many healthcare institutions. Consequently, methods that can reduce storage demands while preserving retrieval accuracy have become a critical research priority. We propose ARReST (Antithetical Redundancy Reduction Strategy), a principled oppositional framework that leverages redundancy across dissimilar tissue classes to markedly decrease the number of patches that must be indexed from each WSI. Instead of eliminating only within-class duplicates, ARReST identifies antithetical patches-those whose representations contribute minimally to cross-class discrimination-and prunes them from the searchable archive. This targeted reduction substantially compresses the index without sacrificing morphological diversity or retrieval fidelity. By minimizing superfluous patch representations, ARReST reduces storage footprint, lowers computational overhead, and accelerates similarity search across large pathology repositories. Extensive experiments on TCGA repository (The Cancer Genome Atlas with 21 organs) demonstrate that ARReST achieves significant index compression while maintaining competitive retrieval performance. The observed storage savings of 3% to 60% (14%$\pm$13%) can be reliably achieved without compromising retrieval performance for many organs. The proposed strategy enables scalable, cost-efficient WSI indexing and is well-suited for next-generation retrieval-driven clinical AI systems.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：数字病理全切片图像（WSI）的索引与检索面临存储成本过高和计算效率低下的瓶颈，制约了大规模临床部署和检索增强生成（RAG）等新一代AI系统的应用。
- **背景**：
  - WSI单张大小通常为0.5–2 GB，大型病理库的索引存储预估可达50–150 PB/年。
  - 现有无监督分块选择方法（如Yottixel、SPLICE等）可将索引压缩至原始大小的5%–10%，但仍有数PB的存储需求，经济压力大。
  - 类间检索时，大量正常或非区分性组织分块（如不同癌症类型间高度相似的区域）成为冗余，增加了存储和比对计算量。
- **目标**：在不牺牲检索精度的前提下，进一步减少需要索引的分块数量，降低存储占用并加速相似性搜索。

## 2. 论文提出的方法论
- **核心思想（ARReST）**：通过跨类别分块嵌入的相似性匹配，系统性地识别并移除“对立冗余分块”（即对不同类别区分贡献极小的正常或非特异性组织区域），从而压缩索引。
- **关键技术细节**：
  - **冗余库构建（Algorithm 1）**：
    - 对每两张不同类别的WSI，提取其代表性分块嵌入，计算两两相似度（如余弦相似度或距离函数 \( f \)）。
    - 将所有跨类相似度值收集，设定全局 \( q \)-分位阈值 \( S_q \)。
    - 对于任意两个不同类别WSI的分块对，若相似度低于 \( S_q \)（即高度相似），则将该对分块加入冗余库 \( \mathcal{R} \)，同时从原WSI中移除。
  - **查询阶段冗余移除与检索（Algorithm 2）**：
    - 对新入WSI，同样提取分块嵌入，并与冗余库 \( \mathcal{R} \) 中所有嵌入逐一比对。
    - 若相似度低于阈值，则丢弃该分块。
    - 保留的分块嵌入生成二进制码（BOB barcode），与存档中的码进行海明距离计算，最后用Top-k多数投票确定标签。
- **流程图示**：图2展示了冗余库创建过程，图3展示了与冗余库协同工作的完整检索引擎结构。

## 3. 实验设计
- **数据集**：
  - 使用**TCGA**（The Cancer Genome Atlas）数据集，共11,679张WSI，覆盖21个器官。
  - 每张WSI以20×倍率、224×224大小的分块处理。
- **基准方法与比对对象**：
  - 以 **Yottixel** 作为基础分块选择算法和检索框架。
  - 比较“应用ARReST前”与“应用ARReST后”的性能，即同框架下去冗余前后的对比。
- **评估设置**：
  - 5折分层组交叉验证，严格按患者ID划分，避免同患者WSI跨训练/测试集。
  - 评估指标：Top-1准确率、多数投票Top-3（MV@3）和Top-5（MV@5）准确率，以及宏平均F1分数。
  - 冗余阈值采用相似度分布的低位分位点 \( S_q \)。
- **特征提取**：使用预训练模型 **UNI**（在1亿张病理图像上训练，覆盖多种组织类型）提取分块嵌入。

## 4. 资源与算力
- 文中**未明确提及**所用GPU型号、数量及实验总运行时间。特征提取依赖预训练UNI模型（推理过程），无额外训练环节，因此未报告训练算力开销。冗余库构建和检索均基于预提取的嵌入向量进行计算，计算成本主要来自嵌入提取和相似度比对，但无具体硬件资源说明。

## 5. 实验数量与充分性
- **实验数量**：
  - 针对21种器官，每种器官分别在Top-1、MV@3、MV@5三种检索场景下比较了应用ARReST前后的**准确率**和**宏平均F1分数**，共计 \( 21 \times 3 \times 2 = 126 \) 个直接对比点（表格1）。
  - 汇总了存储节省百分比及各器官的均值与标准差。
  - 额外对器官进行了分组定性分析（表2），按性能变化特点分类。
- **充分性与客观性**：
  - 覆盖多个器官，测试场景丰富，能反映方法在不同组织类型上的表现。
  - 采用严密的患者级交叉验证，避免数据泄漏，比较公平。
  - **不足**：未进行超参数 \( q \) 的消融实验，也未将ARReST与其他分块选择方法（如SPLICE、SDM）组合验证，广度稍显有限。但整体实验能较好地支撑主要结论。

## 6. 论文的主要结论与发现
- ARReST在TCGA的21个器官上实现了**3%–60%的索引存储节省**，平均为 \( 14\% \pm 13\% \)。
- **平衡数据集**上，检索准确率在统计上等效（\( p \) 值高），说明压缩未损害性能。
- **不平衡数据集**上，F1分数出现统计显著下降（\( p < 0.05 \)），表明少数类判别分块可能被误删，导致稀有亚型检索精度降低。
- 器官特性影响效果：同质形态、类间界限明显的器官（如宫颈、直肠、脑）压缩效果好且保持性能；高异质性器官（如肺、头颈、软组织肿瘤）则对剪枝更敏感。
- ARReST通过减少索引分块数量，降低了存储占用和检索时的比对计算量，适合大规模临床部署。

## 7. 优点
- **方法创新**：首次提出利用跨类别冗余来压缩WSI索引，思路简单有效，无需额外标注。
- **兼容性**：可与现有无监督分块选择和二进制编码框架无缝集成，普适性较好。
- **效率收益**：同时降低存储和检索计算开销，有利于推动低成本病理检索系统的落地。
- **实验评估广泛**：覆盖21个器官，使用严格的交叉验证，结果提供器官级详细分析，可解释性强。

## 8. 不足与局限
- **阈值敏感性**：冗余移除效果依赖全局分位阈值 \( q \)，论文未探讨该超参数的选择策略或自适应方法，可能需针对不同器官定制。
- **依赖类别标签**：构建冗余库需要各类别的分支标签，无法用于纯无监督或无类别信息的场景。
- **基线单一**：实验仅基于Yottixel分块方法，未验证ARReST在其他分块选择器（如SPLICE）上的增益，泛化性待进一步确认。
- **不平衡场景下的退化**：在亚型分布高度不平衡的数据集上，稀有类的判别分块可能被错误删除，损害F1分数，论文未提出缓解措施。
- **未与聚合方法对比**：虽然提及聚合方法仍依赖分块，但未实验直接比较嵌入聚合后的性能。
- **未涉及临床验证**：仅在公开数据集上评估，缺乏真实临床工作流中的验证和计算效率的实测。

（完）
