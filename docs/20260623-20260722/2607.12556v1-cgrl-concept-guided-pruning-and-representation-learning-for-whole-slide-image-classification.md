---
title: "CGRL: Concept-Guided Pruning and Representation Learning for Whole-Slide Image Classification"
title_zh: CGRL：概念引导的剪枝与表示学习用于全切片图像分类
authors: "Thuc Huynh, Tuan Le, Doanh C. Bui"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12556v1"
tags: ["query:path-xai-sel"]
score: 9.0
evidence: 概念引导剪枝以选择Top-K信息性瓦片用于全切片分类
tldr: 弱监督WSI分类中，现有MIL方法依赖视觉线索聚合补丁，易保留非信息补丁且语义对齐弱。CGRL引入类别级概念原型，通过概念相关性剪枝保留top-K相关补丁，并利用概念引导对比学习正则化特征空间。在TCGA-BRCA和NSCLC上，CGRL提升多MIL模型的准确率与宏F1，并降低计算成本。该方法为计算病理学补丁选择和表示学习提供有效语义先验。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12556v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 854, \"height\": 547}, {\"url\": \"assets/figures/arxiv/2607.12556v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 868, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2607.12556v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1849, \"height\": 899}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12556v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1880, \"height\": 698}, {\"url\": \"assets/tables/arxiv/2607.12556v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 914, \"height\": 323}]"
motivation: 现有MIL方法主要依赖视觉线索，保留非信息补丁且语义对齐弱，需要更有效的先验来指导补丁选择和表示学习。
method: 提出CGRL，通过疾病提示生成类别级概念原型，进行概念相关性剪枝保留top-K补丁，并应用概念引导对比学习优化特征空间。
result: 在TCGA-BRCA和NSCLC上，CGRL提升多种MIL模型准确率与宏F1，并显著降低计算成本，增益依赖下游模型和数据集。
conclusion: 类别级语义概念作为先验指导补丁选择和表示学习，能有效提升弱监督WSI分类性能，为计算病理学提供实用方案。
---

## 摘要
弱监督全切片图像（WSI）分类在计算病理学中广泛应用，因为切片级标签比密集区域注释更容易获取。现有的多实例学习（MIL）方法通常主要利用视觉线索聚合大量的补丁嵌入，这可能保留许多非信息性补丁，并提供实例特征与类别级疾病语义之间的弱对齐。我们提出了概念引导的剪枝与表示学习（CGRL），这是一个简单的框架，将源自疾病提示的类别级概念原型引入MIL流程。首先，概念相关剪枝根据补丁实例与类别概念的相似性对其进行排序，并保留前K个概念相关补丁用于下游MIL聚合。其次，概念引导的对比表示学习从相同的相似性矩阵构建类别的正负补丁集，并优化目标类别、对称辅助和跨类分离目标，从而正则化投影的概念空间。我们在TCGA-BRCA和TCGA-NSCLC上使用多种代表性的MIL方法评估了CGRL。实验结果表明，CGRL改善了几种模型-数据集组合，其增益取决于下游MIL模型和数据集。它在准确率和宏F1方面取得了特别明显的提升，同时通过概念相关剪枝降低了计算成本。这些发现表明，类别级语义概念为弱监督计算病理学中的补丁选择和表示学习提供了一种有效且实用的先验。

## Abstract
Weakly supervised whole-slide image (WSI) classification is widely used in computational pathology because slide-level labels are easier to obtain than dense region annotations. Existing multiple instance learning (MIL) methods often aggregate large bags of patch embeddings using mainly visual cues, which can retain many non-informative patches and provide weak alignment between instance features and class-level disease semantics. We propose Concept-Guided Pruning and Representation Learning (CGRL), a simple framework that introduces class-level concept prototypes derived from disease prompts into the MIL pipeline. First, concept-relevance pruning ranks patch instances by their similarity to class concepts and retains the top-K concept-relevant patches for downstream MIL aggregation. Second, concept-guided contrastive representation learning constructs class-wise positive and negative patch sets from the same similarity matrix and optimizes target-class, symmetric auxiliary, and cross-class separation objectives, thereby regularizing the projected concept space. We evaluate CGRL on TCGA-BRCA and TCGA-NSCLC using multiple representative MIL methods. Experimental results show that CGRL improves several model-dataset combinations, with gains depending on the downstream MIL model and dataset. It achieves particularly clear improvements in accuracy and macro-F1 while reducing computational cost through concept-relevance pruning. These findings demonstrate that class-level semantic concepts provide an effective and practical prior for patch selection and representation learning in weakly supervised computational pathology.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究问题**：在弱监督全切片图像（WSI）分类中，当前主流的多实例学习（MIL）方法主要依赖视觉特征聚合大量补丁。这种做法存在两个关键局限：
  - **冗余信息**：原始包中包含大量缺乏诊断价值的背景或噪声补丁，不仅增加计算开销，还会稀释与疾病相关的关键信号。
  - **语义对齐弱**：单纯基于视觉学习的实例表征往往难与高层次的疾病类别语义对齐，尤其当癌症亚型依赖细微的形态学差异时，模型难以捕捉更具判别性的语义线索。
- **研究意义**：探索如何将文本语义（类别级概念）作为先验，既指导补丁的选择（剪除冗余），又作为监督信号正则化特征空间，从而提升弱监督病理分类的性能与效率。

### 2. 论文提出的方法论
- **整体框架**：CGRL（Concept‑Guided Pruning and Representation Learning），一个“即插即用”的模块，嵌入到已有的MIL流程中。它利用疾病文本提示生成类别级概念原型，并以两种互补方式发挥作用：概念相关性剪枝与概念引导对比表示学习。
- **关键技术细节**：
  - **概念原型构建**：对每个类别𝑐，用特定的疾病提示经冻结的文本编码器（TITAN）得到类别原型向量 \(t_c\)。
  - **投影与相似度矩阵**：补丁特征\(x_i\)经可学习的轻量投影头\(g_\phi\)映射到概念空间得到\(u_i\)，计算归一化后的余弦相似度矩阵\(S\) \((S_{i,c} = \frac{u_i^\top t_c}{\|u_i\|\|t_c\|})\)。
  - **概念相关性剪枝**：定义每个补丁的得分为\(\max_c S_{i,c}\)，保留前\(K_p=300\)个最相关的补丁形成精简包\(\tilde{X}\)，送入下游MIL做切片分类。原始补丁嵌入保留，仅通过概念空间筛选。
  - **概念引导表示学习**（仅训练阶段）：从原始包的全部投影特征\(U\)中，对每个类别\(c\)根据\(S_{:,c}\)取前\(K_{\text{pos}}\)和后\(K_{\text{neg}}\)个实例构建正、负集合。设计三个InfoNCE损失：
    1. **ℒ_y（目标类）**：使目标类正样本对齐到真实标签的概念原型\(t_y\)，同时远离目标类负样本。
    2. **ℒ_sym（对称辅助）**：对每个非目标类\(c\neq y\)，让该类正样本对齐到\(t_c\)并远离该类负样本，保持整体概念空间的类别结构。
    3. **ℒ_cross（跨类分离）**：增强类间区分，利用非目标类的负样本构造辅助原型\(p_{y\to c}\)，使目标类正样本靠近该原型（即远离非目标类相关特征），同时远离非目标类正样本。
    总表示学习损失 \(L_{CGRL} = L_y + \lambda_{sym}L_{sym} + \lambda_{cross}L_{cross}\)，与MIL分类损失加权求和。
- **推理阶段**：移除对比学习分支，仅保留概念相关性剪枝和MIL分类，轻量且保持效率。

### 3. 实验设计
- **数据集**：TCGA‑BRCA（871张WSI，IDC vs ILC）和TCGA‑NSCLC（958张WSI，LUAD vs LUSC），均采用5折交叉验证。
- **基准对比方法**：
  - MIL骨干网络：ABMIL（注意力池化）、CLAM‑SB（带聚类约束）、TransMIL（Transformer）、DS‑MIL（双流聚合）。
  - 剪枝基线：每个MIL模型分别使用全部补丁（Full）、随机采样（Random）、基于K‑means聚类的采样（Kmeans）以及本文的CGRL。共4×4=16组对比。
- **评价指标**：AUC、ACC、宏平均F1。
- **消融实验**：在TCGA‑BRCA上对CGRL的三个损失进行组件消融，同时验证剪枝数量\(K_p=300\)的设定。

### 4. 资源与算力
- **硬件**：全部实验在单张NVIDIA GeForce RTX 2080 Ti GPU上完成。
- **训练时长**：论文中未明确提及单次运行的训练时间或总耗时。
- **模型参数**：图像编码器（CONCH）和文本编码器（TITAN）均冻结，仅训练轻量投影头\(g_\phi\)和下游MIL模型，整体计算开销较小。

### 5. 实验数量与充分性
- **实验规模**：约2个数据集×4个MIL模型×4种采样策略=32条主要结果，另加1组消融实验（8个变体）。总体实验量中等。
- **充分性评价**：
  - **对比公平性**：对比了完全不加剪枝、随机剪枝、基于聚类的剪枝，能够有效验证“概念引导”的增量价值。
  - **组件拆解**：消融实验清晰展示了各损失项的贡献，增强结果的可解释性。
  - **局限**：仅在两个TCGA子数据集上评估，缺乏对第三方独立病理数据集或更多癌症类型的测试；未探索不同\(K_p\)值、不同概念原型构造策略的敏感度；推理阶段未与同时使用对比目标的方法比较。

### 6. 论文的主要结论与发现
- 补丁采样的普遍收益：在多数设定中，裁剪冗余补丁后（包括随机或K‑means）已能提升分类性能，说明浓缩信息包有益。
- CGRL的突出贡献：附加概念先验的策略在多项组合中进一步提升准确率和F1（例如ABMIL在BRCA上ACC从0.910升至0.931，DS‑MIL在NSCLC上ACC从0.927升至0.949），并伴有计算量下降（参看FLOPs比较）。
- 方法依赖性：概念引导的有效性与下游MIL模型、数据集相关，并非在所有情况下均最优，但总体倾向于在类别判别力指标上带来更稳定的增益。
- 语义先验的价值：文本概念原型可以作为有效的选择监督信号，辅助补丁子集筛选和特征空间正则，为弱监督病理分析提供了一种轻量且实用的语义增强范式。

### 7. 优点
- **方法简洁且模块化**：利用已有的冻结视觉‑语言基础模型，只需一个轻量投影头即可实现概念空间的构建与引导，可方便地插入多种MIL框架。
- **双重功能设计**：概念原型既用于剪枝（提高效率与信号纯度），又用于对比学习（改善语义对齐），并在推理时只保留高效通路。
- **实验设置合理**：选取主流MIL方法和常用TCGA基准，与随机和聚类剪枝进行公平对比，消融分析完整。
- **效能‑效率平衡**：在提升准确率与F1的同时显著降低浮点运算量（见图1a），对临床应用部署友好。

### 8. 不足与局限
- **数据集泛化性有限**：仅验证了两个TCGA二分类任务，缺少对多分类、不同扫描仪、外部来源数据的评估，跨中心泛化能力未知。
- **概念原型固定**：使用预定义疾病提示，未能自适应地调整概念表示，可能对不典型或罕见亚型覆盖不足。
- **超参数依赖**：\(K_p\)、正负样本数量、损失权重等均固定或经验设定，未展示在不同设置下的鲁棒性，也未与自适应剪枝策略比较。
- **模型依赖性强**：CGRL的增益在某些模型‑数据集组合中不显著甚至略有下降，说明其效果依赖于MIL聚合机制，需谨慎选择下游模型。
- **只利用文本概念**：没有充分利用图像‑文本对齐中的细粒度局部信息，未来可探索多模态交互式概念选择。

（完）
