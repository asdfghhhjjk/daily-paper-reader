---
title: "C2RM-Seg: Causal Counterfactual Reasoning with Structural-Semantic Priors for Weakly Supervised Histopathological Tissue Segmentation"
title_zh: C2RM-Seg：基于结构-语义先验的因果反事实推理用于弱监督组织病理学组织分割
authors: "Hualong Zhang, Siyang Feng, Zihan Huan, Yi Qian, Zhenbing Liu, Rushi Lan, Xipeng Pan"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25508v1"
tags: ["query:path-xai-sel"]
score: 9.0
evidence: 使用因果推理的组织病理学分割，直接属于计算病理学分析
tldr: 弱监督组织分割常因类激活映射聚焦染色外观而非因果形态，导致伪标签噪声大、结构一致性差。提出C2RM-Seg框架，通过因果反事实推理生成形态对齐的激活图，并结合双路径结构-语义架构与不确定性门控损失抑制噪声。在两个公开组织病理数据集上取得最优性能，提升了弱监督分割的精确性和结构连贯性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25508v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1265, \"height\": 584}, {\"url\": \"assets/figures/arxiv/2606.25508v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1263, \"height\": 534}, {\"url\": \"assets/figures/arxiv/2606.25508v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1121, \"height\": 507}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25508v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 540}, {\"url\": \"assets/tables/arxiv/2606.25508v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1238, \"height\": 494}]"
motivation: 现有弱监督方法依赖CAM，易捕捉染色驱动的表面模式，忽略真实组织形态，产生噪声伪标签。
method: 设计因果反事实推理模块生成形态对齐CAM；融合ResNeSt结构特征与DINOV3语义先验的双路径架构，以交叉门控自适应注入语义；提出不确定性门控边缘损失减轻残留噪声。
result: 在两个公开组织病理数据集上，C2RM-Seg实现了弱监督组织分割的最先进性能。
conclusion: C2RM-Seg通过因果推理和结构语义增强，有效抑制虚假定位，提高弱监督组织分割的精度与结构一致性。
---

## 摘要
组织病理学组织分割对于计算机辅助诊断至关重要，然而弱监督方法常常受到类激活映射（CAM）生成的噪声伪标签的困扰。现有的CAM方法倾向于关注染色驱动的外观线索，而非真正的因果组织形态，导致虚假定位和较差的结构一致性。为了解决这个问题，我们提出C2RM-Seg，一个将因果伪标签细化与结构感知语义增强相结合的两阶段框架。在分类阶段，我们引入了一个因果反事实推理模块（C2RM），该模块将特征分解为潜在因子，并通过学习到的因果结构矩阵进行反事实干预，抑制混淆上下文并生成形态对齐的CAM。在分割阶段，我们设计了一个双路径结构-语义架构，将来自ResNeSt的细粒度结构特征与来自冻结的DINOV3基础模型的全局语义先验相结合。跨路径门控机制利用局部结构线索自适应地调节语义注入，以保持边界保真度。为了进一步减轻残留的伪标签噪声，我们提出了不确定性门控边际（UGM）损失，该损失根据预测不确定性动态平衡边际强化和置信度学习。在两个公开组织病理学组织数据集上的大量实验表明，C2RM-Seg达到了最先进的性能。

## Abstract
Histopathological tissue segmentation is essential for computer-aided diagnosis, yet weakly supervised methods often suffer from noisy pseudo-labels generated by Class Activation Mapping (CAM). Existing CAM approaches tend to focus on staining-driven appearance cues rather than true causal tissue morphology, resulting in spurious localization and poor structural consistency. To address this issue, we propose C$^2$RM-Seg, a two-stage framework that integrates causal pseudo-label refinement with structure-aware semantic enhancement. For classification, we introduce a Causal Counterfactual Reasoning Module (C$^2$RM) that decomposes features into latent factors and performs counterfactual intervention via a learned causal structure matrix, suppressing confounding context and producing morphology-aligned CAMs. For segmentation, we design a Dual-Path Structural-Semantic Architecture that combines fine-grained structural features from ResNeSt with global semantic priors from a frozen DINOV3 foundation model. A cross-path gating mechanism adaptively regulates semantic injection using local structural cues to preserve boundary fidelity. To further mitigate residual pseudo-label noise, we propose an Uncertainty-Gated Margin (UGM) loss, which dynamically balances margin enforcement and confidence learning based on prediction uncertainty. Extensive experiments on two public histopathological tissue datasets show that C$^2$RM-Seg achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：弱监督组织病理组织分割（仅用图像级标签）严重依赖类激活图（CAM）生成伪标签，但现有CAM方法易受染色差异、扫描噪声等外观混淆因素影响，捕捉的是“虚假相关”而非真正的组织形态因果特征，导致伪标签存在大量噪声、边界模糊、结构一致性差。
- **整体含义**：论文旨在从根本上纠正CAM的因果偏差，通过引入因果反事实推理，从特征中剥离出染色风格等混淆因子，使激活图聚焦于真实组织形态。同时，结合结构细节与基础模型语义先验，并设计噪声鲁棒优化策略，构建一套高保真、结构连贯的弱监督组织分割框架。

### 2. 论文提出的方法论（核心思想、关键技术细节、公式或算法流程）
- **整体框架**：两阶段管线。
  - **第一阶段（伪标签生成）**：使用带因果反事实推理模块（C²RM）的分类网络生成形态对齐的CAM，经后处理得到高质量伪掩膜。
  - **第二阶段（分割训练）**：采用双路径结构‑语义分割网络，融合ResNeSt局部结构特征与冻结的DINOV3全局语义特征，并用不确定性门控边际损失（UGM）抑制残留噪声。

- **因果反事实推理模块（C²RM）**：
  - **核心思想**：基于结构因果模型（SCM），将编码器特征X分解为K个潜在因子子空间\(z_k\)，并学习因子间有向因果图（邻接矩阵A）。将其他因子对当前因子的加权和视为混淆贡献\(E_i = \sum_{j\neq i} A_{i,j} z_j\)。
  - **反事实干预**：通过减法去除传入混淆边，得到反事实因子\(z^{cf}_i \approx z_i - E_i\)，即\(Z^{cf} = Z - AZ\)。
  - **空间重校准**：将\(Z^{cf}\)广播后经门控残差修正\(G = \sigma(R(Broadcast(Z^{cf})))\)，校正原特征\(\hat{X} = X + \gamma \cdot (G \odot X)\)，产生去混淆的CAM。

- **双路径结构‑语义架构**：
  - **结构分支**：ResNeSt‑200e + 金字塔池化模块提取多尺度局部特征\(F_S\)。
  - **语义分支**：冻结的DINOv3 ViT‑B/16编码器 + 可学习适配器\(A_c\)，输出上采样对齐特征\(F_V\)。
  - **跨路径门控融合**：从\(F_S\)生成空间门控图\(G = \sigma(\Psi(F_S))\)，自适应注入语义信息\(\hat{F} = F_S + G \odot F_V\)，以保留边界细节。

- **不确定性门控边际损失（UGM）**：
  - 定义像素i的边际违反\(M_i = [m - (p_{i,y} - p_{i,s})]_+\)和置信度惩罚\(C_i = -\log(p_{i,y} + \epsilon)\)。
  - 使用预测不确定性\(u_i = 1 - p_{i,y}\)，通过sigmoid门控动态融合两部分：
    \[
    \mathcal{L} = \frac{1}{|\Omega|} \sum_{i\in\Omega} \left[ \frac{1}{1+e^{-\kappa(u_i-0.5)}} C_i + \left(1 - \frac{1}{1+e^{-\kappa(u_i-0.5)}}\right) M_i \right]
    \]
    对低不确定像素强调扩大分类边际，对高不确定像素侧重保守置信度学习，避免过度惩罚噪声标签。

### 3. 实验设计（数据集/场景、benchmark、对比方法）
- **数据集**：
  - **LUAD‑HistoSeg**：肺腺癌，54张WSI（16 678训练块，607测试块，像素级标注），4类组织（TE、TAS、NEC、LYM）。
  - **BCSS**：乳腺癌，151张WSI（23 422训练块，8 404测试块，像素级标注），4类组织（TUM、STR、LYM、NEC）。
- **评价指标**：mIoU、bIoU、HD95，以及计算效率（GFLOPs、FPS）。
- **对比方法**：涵盖10种方法，包括基于SAM/SAM2/MedSAM的提示分割（均用本文第一阶段伪标签提示）、多种弱监督分割方法（TPRO、HAMIL、MLPS、SIPE、ARML、OEEM、ESFAN），对比全面且公平。

### 4. 资源与算力
- **硬件**：单张NVIDIA RTX 3090 GPU。
- **分类阶段**：ResNet38d作为主干，K=8因子，训练20个epoch，batch size 20，PolyOptimizer初始学习率1×10⁻²。
- **分割阶段**：ResNeSt‑200e用SGD，学习率1×10⁻²；DINOV3适配器用AdamW，学习率5×10⁻³；损失超参m=0.3，κ=5。
- **训练时长**：文中未明确给出具体时钟时间，但提供了模型参数量/计算量（191.47 GFLOPs，86.08 FPS），显示端侧推理具有实际可行性。

### 5. 实验数量与充分性
- **主要性能对比**：两项数据集上各对比10种方法，5次独立运行取均值±标准差，统计严谨。
- **消融实验**：以LUAD‑HistoSeg为对象，对C²RM、双路径结构、UGM损失三个模块进行8种组合（含全组合和不使用）消融，证明各模块独立及联合的增益。
- **可视化分析**：提供分割定性对比图、t‑SNE特征分布图，直观展示去混淆效果。
- **不足**：未进行跨癌种或跨染色平台的泛化实验，消融未深入探索因子数量K、边际m等超参的敏感性。

### 6. 论文的主要结论与发现
- C²RM‑Seg在两个公开组织病理数据集上取得最优性能（BCSS mIoU 72.17%，LUAD‑HistoSeg 79.62%），在保持较高推理速度（86 FPS）的同时，大幅领先现有最先进弱监督方法。
- 因果反事实干预能有效解耦染色外观与组织形态，生成更紧凑、类别可分的特征表示，显著提升伪标签质量和后续分割精度。
- 双路径结构‑语义融合策略与不确定性自适应损失协同工作，进一步增强了边界完整性和对伪标签噪声的鲁棒性。

### 7. 优点（方法或实验设计上的亮点）
- **创新性强**：首次在弱监督组织分割中引入结构因果模型与反事实干预，从因果层面解决CAM偏差问题，思路新颖。
- **结构‑语义深度融合**：用结构门控动态注入冻结基础模型的语义先验，既避免过拟合又保持边界保真，设计巧妙。
- **噪声自适应损失**：UGM损失根据预测不确定性动态平衡边际强化与保守学习，有效应对伪标签噪声。
- **实验扎实**：对比方法全面、消融充分、可视化有力，且代码开源，可复现性高。
- **效率出色**：尽管融合了基础模型，仍保持较优的推理速度和合理的计算开销。

### 8. 不足与局限（实验覆盖、偏差风险、应用限制等）
- **外部泛化未经检验**：仅在两个公开机构（或来源）的组织病理数据集上测试，未涉及更多染色方式、扫描仪差异或罕见癌种，泛化性证据不足。
- **因果模型简化**：采用线性加性SCM近似因子交互，可能无法完全捕捉组织形态与染色的复杂非线性关系。
- **基础模型冻结**：DINOV3保持冻结虽有利泛化，但可能限制对特定病理语义的深入适配，适配器容量有限。
- **超参敏感性**：损失中的边际m（0.3）和陡度κ（5）固定，未分析其影响，不同数据集可能需要调整。
- **两阶段非端到端**：伪标签生成与分割分步进行，训练流程相对复杂，且最终性能仍受限于第一阶段CAM质量。

（完）
