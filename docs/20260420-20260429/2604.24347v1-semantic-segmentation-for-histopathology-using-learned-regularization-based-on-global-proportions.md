---
title: Semantic Segmentation for Histopathology using Learned Regularization based on Global Proportions
title_zh: 基于全局比例学习正则化的病理组织语义分割
authors: "Yangping Li, Thomas Pinetz, Michael Hölzel, Marieta Toma, Alexander Effland"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24347v1"
tags: ["query:pathseg"]
score: 9.5
evidence: 利用Transformer和全局标签比例进行组织病理学语义分割
tldr: 本文提出变分分割框架VSLP，通过全局标签比例实现病理图像的语义分割，无需像素级标注。模型分两阶段运行：先利用预训练Transformer生成像素置信度，再通过结合Wasserstein数据项和学习正则化的变分优化生成分割。实验结果在公开和真实病理数据上均超越现有弱监督方法，并具有良好可解释性和实际应用价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24347v1/fig-001.webp\", \"caption\": \"Figure 5: Comparison of our method with LLP-adapted SA-MIL and C2C on the in-house dataset. Red indicates high branching, green indicates low branching, and blue indicates sinusoid. Global class proportions are provided by an experienced pathologist and serve as the ground-truth labels. Predicted class proportions are computed by summing segmented pixels for each class. Notably, all of our models generate meaningful segmentation and accurate class proportions, enhancing trust in the predictions.\", \"page\": 10, \"index\": 1, \"width\": 822, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2604.24347v1/fig-002.webp\", \"caption\": \"Table 4: Effect of patch size and stride on segmentation performance for the first-stage and second-stage VSLP variants.\", \"page\": 8, \"index\": 2, \"width\": 508, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.24347v1/fig-003.webp\", \"caption\": \"Figure 4: Comparison of our method with the state-of-the-art approaches on samples from the BCSS [57] dataset.\", \"page\": 9, \"index\": 3, \"width\": 1029, \"height\": 443}, {\"url\": \"assets/figures/arxiv/2604.24347v1/fig-004.webp\", \"caption\": \"Table 5: Effect of TTA rotation count on segmentation performance. The upper part reports performance as a function of the number of TTA rotations for different second-stage variants, using the best-performing first-stage patch size (224, 150). The lower part compares different first-stage backbone models under the same TTA setting.\", \"page\": 9, \"index\": 4, \"width\": 523, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2604.24347v1/fig-005.webp\", \"caption\": \"Table 6: Performance of VSLP variants under different data conditions.\", \"page\": 9, \"index\": 5, \"width\": 506, \"height\": 389}]"
motivation: 病理组织类型的空间分布和比例易获得但难以转化为像素级分割，现有方法缺乏这一能力。
method: 提出两阶段的VSLP框架，结合预训练的Transformer生成置信度预测，并通过包含Wasserstein项和学习正则项的变分优化融合结果。
result: VSLP在两个公开数据集和一个内部含噪声标注数据集上取得显著优于弱监督和无监督方法的分割效果。
conclusion: VSLP框架在病理图像中实现了从全局比例推断像素级分割的突破，提供了更高性能和更强可解释性。
---

## 摘要
在病理学中，组织类型的空间分布与比例是疾病进展的重要指标，比精细标注更容易获得。然而，这些评估很少被映射到像素级分割。该任务本质上是不确定的，因为在缺乏像素级约束的情况下，许多空间上不同的分割都可以满足相同的总体比例。为了解决这一问题，我们提出了基于标签比例的变分分割（Variational Segmentation from Label Proportions，VSLP），这是一个两阶段框架，可以在没有任何像素级标注的条件下，从全局标签比例中推断密集分割。该框架首先利用预训练的 transformer 模型结合测试时数据增强，生成像素级置信度估计。在第二阶段中，通过求解一个包含 Wasserstein 数据保真项及学习正则项的变分优化问题，将这些估计值融合。与端到端网络不同，我们的变分方法能够可视化保真-正则能量，从而获得更可解释的分割。我们在两个公共数据集上验证了该方法，相较于现有的弱监督与无监督方法表现更优。其中一个数据集的比例由经验丰富的病理学家估计，为学术界提供了一个现实的基准。此外，该方法可扩展到一个包含噪声病理学家标注的内部数据集，在性能上显著超越当前最先进的方法，展示了其实用性。代码与数据将在论文接受后于 https://github.com/xiaoliangpi/VSLP 公开。

## 速览
**TLDR**：本文提出了一种基于全局标签比例的病理图像语义分割框架VSLP，通过无像素级标注推断密集分割。方法结合预训练Transformer生成像素置信度，并以Wasserstein保真项与学习正则项的变分优化融合，提升解释性和性能，在多个公开及自有数据集上取得领先表现。 \
**Motivation**：由于病理图像像素级标注稀缺且获取困难，而总体组织比例评估更易获得，需要一种仅凭全局比例实现精确分割的方法。 \
**Method**：采用两阶段框架：先用预训练Transformer生成像素置信度，再通过变分优化结合Wasserstein保真与学习正则项融合分割结果。 \
**Result**：该方法在两个公共病理数据集上超过了现有的弱监督与无监督分割方法，在带噪声的内部门诊病理数据上也显著优于现有技术。 \
**Conclusion**：VSLP实现了基于全局比例的可解释、高性能病理图像分割，具备显著实际应用价值。

---

## Abstract
In pathology, the spatial distribution and proportions of tissue types are key indicators of disease progression, and are more readily available than fine-grained annotations. However, these assessments are rarely mapped to pixel-wise segmentation. The task is fundamentally underdetermined, as many spatially distinct segmentations can satisfy the same global proportions in the absence of pixel-wise constraints. To address this, we introduce Variational Segmentation from Label Proportions (VSLP), a two-stage framework that infers dense segmentations from global label proportions, without any pixel-level annotations. This framework first leverages a pre-trained transformer model with test-time augmentation to produce a pixel-wise confidence estimate. In the second stage, these estimates are fused by solving a variational optimization problem that incorporates a Wasserstein data fidelity term alongside a learned regularizer. Unlike end-to-end networks, our variational method can visualize the fidelity-regularization energy, resulting in more interpretable segmentation. We validate our approach on two public datasets, achieving superior performance over existing weakly supervised and unsupervised methods. For one of these datasets, proportions have been estimated by an experienced pathologist to provide a realistic benchmark to the community. Furthermore, the method scales to an in-house dataset with noisy pathologist labels, severely outperforming state-of-the-art methods, thereby demonstrating practical applicability. The code and data will be made publicly available upon acceptance at https://github.com/xiaoliangpi/VSLP.

---

## 论文详细总结（自动生成）

# 基于全局比例学习正则化的病理组织语义分割 — 论文总结

---

## 一、研究背景与核心问题

- **研究领域**：病理图像的语义分割，用于揭示组织类型在显微切片中的空间分布。  
- **核心问题**：在临床病理实践中，病理学家常能给出各组织类型的**总体比例（global proportions）**，但很难提供像素级精细标注。要从这些比例信息推断出像素级分割，是一个**数学上不确定的反问题**，因为多种空间分布都可能满足相同的全局比例。  
- **研究动机**：当前弱监督或无监督方法要么忽略比例信息，要么仅输出粗糙掩码，缺乏**全局一致性与不确定性建模**。因此，作者希望构建一种框架，**仅利用全局标签比例即可生成可靠且可解释的像素级分割图**，为病理学提供透明的自动诊断辅助。

---

## 二、方法论：VSLP 两阶段变分分割框架

### 核心思想
提出 **Variational Segmentation from Label Proportions (VSLP)** 框架，将全局比例监督转化为局部像素分割。思想上结合：
- **学习从全局信息到局部结构的映射**；
- **变分优化（variational optimization）**与**学习正则化（learned regularizer）**相结合；
- **概率视角下的不确定性建模**。

### 阶段一：Transformer 生成像素置信度
1. 使用 **ImageNet 预训练的 Swin Transformer** 对病理图像进行细化训练（fine-tuning），输入全局比例标签。  
2. 整片图像被划分为重叠 patch，各 patch 预测局部比例，再加权平均得到整体比例损失：  
   \[
   \ell(x,y)=\frac{1}{N}\sum_i\Big(y-\frac{1}{N_p}\sum_j\epsilon_j f(Px_i)_j\Big)^2
   \]
3. 利用 **测试时数据增强（TTA）**，对图像进行多角度旋转，共生成 Nr 次预测，形成每个像素的概率分布（或直方图表示），用于建模预测不确定性。

### 阶段二：变分优化生成最终分割
1. 以第一阶段生成的像素级预测直方图 \(z_h^i\) 作为输入，建立能量函数：
   \[
   E(u)=D(u,z_h^i)+\lambda R(u;z_h^i,x_i,\theta^*)
   \]
   其中：
   - **数据保真项 D**：使用 **Wasserstein 距离**度量预测结果与数据分布的差异；
   - **正则项 R**：基于学习的 U-Net 模型捕捉空间结构与纹理先验。
2. 优化方法采用 **Unadjusted Langevin Algorithm (ULA)**，引入随机扰动模拟扩散式迭代更新：
   \[
   u^{s+1}=u^s-\tau(\nabla_u D+\lambda\nabla_u R)+\sqrt{2\tau}\,n
   \]
3. 为提升效率，还引入 **高斯混合模型（GMM）变体**，以像素的均值与方差参数化直方图分布，自动实现不确定性感知的自适应平滑。

---

## 三、实验设计与对比方案

### 数据集与场景

1. **RINGS 数据集**  
   - 含 1500 张前列腺组织切片（两类组织：腺体与其他组织）；
   - 同时包含病理学家估计的全局比例；
   - 用于验证模型在现实比例标注下的性能。

2. **BCSS 数据集**  
   - 来源于 TCGA，包含 151 张肿瘤组织切片；
   - 将 22 类组织合并为两类（肿瘤 / 非肿瘤），挑战高变异性场景。

3. **内部自建数据集（Renal Cell Carcinoma）**  
   - 共 325 张 CD31 染色肾癌组织图像，3 类血管形态；
   - 仅有病理医生提供的比例标签；
   - 用于测试多类任务与噪声标注条件。

### 基准与对比方法

- 类别：  
  - **无监督方法**：MedSAM  
  - **MIL 方法**：C2C、SA-MIL、PistoSeg、OEEM、URN  
  - **LLP 方法**：FGNet、FlipCAM、LPLP  
  - **全监督方法**：U-Net、SwinUnet、TransUnet  
- 在每类方法内部对齐监督级别，保持公平性。

### 评估指标  
- Dice、平均交并比（mIoU）、95% Hausdorff 距离（95HD）；  
- 内部数据集采用 Accuracy、Precision、Recall、F1、MAE、MSE。

---

## 四、资源与算力

- **训练硬件**：单台 **NVIDIA A40 GPU**。  
- **训练时长**：第一阶段约 16 小时，第二阶段约 50 小时。  
- **训练参数规模**：
  - 第一阶段 Swin Transformer；
  - 第二阶段 U-Net 正则器参数约 1.37M（150 万）；
- **数据增强与配置**：
  - Patch 大小 224×224，stride 150；
  - 使用 360 度旋转的 TTA；
  - AdamW 优化器，学习率 3×10⁻⁵。

---

## 五、实验数量与充分性

- **主实验**：在两个公开数据集与一个内部数据集上完整测试。  
- **对比实验**：与 10+ 项弱监督 / MIL / LLP / 无监督 / 全监督方法进行比较。  
- **消融实验**：
  - Patch 尺寸与步幅影响；
  - TTA 旋转次数；
  - 去除正则项或随机比例的性能下降；
  - 噪声与粗比例条件下鲁棒性验证。  
- **总体评价**：实验覆盖面广，指标客观，对照充分，标注级别匹配合理，结果具有高度可信度。

---

## 六、主要结论与发现

- VSLP 能在仅有全局比例监督下实现高质量像素级分割。  
- 在弱监督场景下（LLP），明显**超越现有最佳方法（FGNet、FlipCAM 等）**；  
- 在含噪声和粗标注条件下仍保持稳定性能，显示出良好的**鲁棒性与临床可适用性**；  
- 第二阶段的变分优化显著提升边界一致性与结构完整度，并能**可视化能量分布与不确定性**。

---

## 七、方法优点与创新点

- **创新监督范式**：首次将“从全局比例到像素级分割”变分化建模。  
- **可解释性强**：能显式展示数据保真项与正则项能量分布，提供透明的推理依据。  
- **不确定性建模**：通过 TTA + Langevin + GMM 建模实现像素级置信度估计。  
- **可扩展性**：框架可迁移至多类场景或含噪标注数据集。  
- **轻量化设计**：U-Net 正则器参数量低，训练效率较高。  
- **鲁棒性突出**：即使全局比例存在误差或粗糙标注，也能得到稳定分割。

---

## 八、不足与局限

- **假设局限**：假定组织形态具有“规则结构先验”，对形状异常或稀有结构鲁棒性不足。  
- **GMM 模型简单**：单高斯设定无法刻画复杂的多峰不确定性。  
- **无边界细修复**：未进行显式边界优化，输出仍可能呈现局部锯齿。  
- **域适应问题**：不同染色方式（如 H&E 与 CD31）导致的跨域分布差异未解决。  
- **资源依赖**：需较长训练时间（第二阶段 50 小时），不利于大规模部署。

---

（完）
