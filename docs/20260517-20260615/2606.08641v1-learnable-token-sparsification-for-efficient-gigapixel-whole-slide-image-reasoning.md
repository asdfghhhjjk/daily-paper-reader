---
title: Learnable Token Sparsification for Efficient Gigapixel Whole Slide Image Reasoning
title_zh: 可学习的令牌稀疏化用于高效十亿像素全切片图像推理
authors: "Jingzhi Chen, Landi He, Zhuo Chen, Shawn Young, Lijian Xu"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08641v1"
tags: ["query:wsi-mil"]
score: 9.0
evidence: 可学习令牌稀疏化用于WSI，类似于MIL中的top-k图块选择
tldr: "十亿像素全切片图像在视觉语言模型中面临大量视觉token的瓶颈，现有下采样或启发式剪枝方法易丢失细粒度病理特征。本文提出可训练token稀疏化框架，通过SparseLearn组件利用可微分Soft Top-K算子和对角注意力去噪器学习最优选择策略。压缩至仅32个token（原始长度0.78%），在SlideBench (TCGA)上取得73.32%准确率，超越采样基线和通用视觉语言模型。解决了视觉上下文瓶颈，实现高效端到端推理，并展现出强零样本泛化能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08641v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1687, \"height\": 886, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1697, \"height\": 794, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1637, \"height\": 1120, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1674, \"height\": 923, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08641v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1684, \"height\": 612, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08641v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1688, \"height\": 860, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08641v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1715, \"height\": 323, \"label\": \"Table\"}]"
motivation: 现有token剪枝方法依靠固定启发式规则，无法保留稀疏但关键的病理证据，导致诊断信息丢失。
method: 提出可训练token稀疏化框架，包含SparseLearn模块，通过可微分Soft Top-K与注意力去噪器实现梯度传播，推理时采用Hard Top-K保留32个高得分token。
result: "在SlideBench (TCGA)上达73.32%准确率，压缩比99.22%，零样本泛化至BCNB和WSI VQA*数据集，超越所有基线。"
conclusion: 本工作为十亿像素全切片图像推理提供了高效范式，通过可学习稀疏化同时提升效率与诊断准确性，防止关键证据被稀释。
---

## 摘要
在视觉语言模型中处理十亿像素的全切片图像面临一个主要困难，即视觉令牌数量过多。现有解决方案通常依赖于空间下采样或启发式剪枝策略，这些策略在无训练的情况下运行，并且往往丢弃细微但临床上有意义的模式，因为病理证据在组织中不规则分布。为克服这一限制，我们将全切片图像中的令牌缩减重新表述为可训练的稀疏化问题，使模型能够学习最优选择策略，而不是遵循固定启发式方法。我们提出了一种解耦路由架构。为了在训练过程中通过不可微的剪枝操作传播梯度，我们引入了一个名为SparseLearn的组件。该组件使用一种保持方差的噪声门控，通过可微的软Top-K算子调节每个斑块的信息流，并配合一个对角注意力去噪器，在不泄露空间信息的情况下恢复受扰动的表示。在推理时，SparseLearn模块被完全丢弃，训练后的评分器应用确定性的硬Top-K算子，只保留得分最高的32个令牌，不产生额外计算。通过将视觉序列压缩到仅32个令牌的稀疏集合，这仅占原始长度的0.78%，我们的框架在SlideBench（TCGA）上实现了73.32%的总体准确率，持续优于基于采样的基线和通用视觉语言模型。在SlideBench（BCNB）和WSI VQA*上也展示了强大的零样本泛化能力。通过解决视觉上下文瓶颈并防止稀疏诊断证据的稀释，这项工作为端到端十亿像素全切片图像推理提供了一种高效范式。

## Abstract
The processing of gigapixel whole slide images within vision language models faces a major difficulty due to an excessive number of visual tokens. Existing solutions typically rely on spatial downsampling or heuristic pruning strategies that operate without training, and these methods often discard subtle but clinically meaningful patterns because pathological evidence is scattered irregularly across the tissue. To overcome this limitation, we reformulate token reduction in whole slide images as a trainable sparsification problem, allowing the model to learn an optimal selection strategy instead of following fixed heuristics. We propose a decoupled routing architecture. To enable gradient propagation through the nondifferentiable pruning operation during training, we introduce a component called SparseLearn. This component uses a variance-preserving noise gate that regulates the information flow of each patch via a differentiable Soft Top-K operator, together with a diagonal attention denoiser that recovers perturbed representations without leaking spatial information. At inference time, the SparseLearn module is entirely discarded, and the trained scorer applies a deterministic Hard Top-K operator to keep only the highest scoring 32 tokens, incurring no extra computation. By compressing the visual sequence down to a sparse set of just 32 tokens, which represents as little as 0.78% of the original length, our framework achieves 73.32% overall accuracy on SlideBench (TCGA), consistently surpassing sampling-based baselines and general-purpose vision language models. It also demonstrates strong zero shot generalization on SlideBench (BCNB) and WSI VQA*. By resolving the visual context bottleneck and preventing the dilution of sparse diagnostic evidence, this work provides a highly efficient paradigm for end to end gigapixel whole slide image reasoning.

---

## 论文详细总结（自动生成）

# 论文总结：Learnable Token Sparsification for Efficient Gigapixel Whole Slide Image Reasoning

## 一、核心问题与整体含义（研究动机与背景）

- **核心问题**：在视觉语言模型中处理十亿像素全切片图像时，一张WSI可生成超过10万张图块，导致LLM的自注意力复杂度呈二次增长，造成严重的计算和内存瓶颈。现有方法（空间下采样、启发式剪枝）虽能减少token数量，但往往采用无训练的固定规则，容易丢弃<1%面积的关键病理证据（如微转移灶），导致诊断性能下降。
- **整体含义**：本文提出将WSI的token缩减重新定义为**可学习的稀疏化问题**，让模型通过连续优化自主学会保留最关键的视觉信息，而非依赖人工预设的启发式规则，从而在极端压缩下仍保持较高的诊断准确性。

## 二、方法论：核心思想、关键技术细节

- **核心思想**：解耦训练与推理路径，在训练阶段通过可微的软剪枝代理实现梯度流动，在推理阶段直接执行硬剪枝。
- **关键技术细节**：
  - **系统架构**：在视觉编码器与投影层之间插入**SparseLearn**模块（仅训练时使用），包含：
    - **学习型评分器**：轻量Transformer+线性投影，为每个token输出重要性分数 \( s_i \)。
    - **软Top-K操作**：将分数经z-score归一化后，通过温度τ控制的Soft Top-K映射为连续权重α，满足 \(\sum \alpha_i \approx K\)，且输出呈双峰分布（接近0或1）。τ从大到小余弦退火。
    - **方差保持噪声门控**：保持序列长度N不变，对每个token \( x_i \) 注入噪声：\(\tilde{x}_i = \sqrt{\alpha_i} x_i + \sqrt{1-\alpha_i} \epsilon_i\)，其中 \(\epsilon_i \sim \mathcal{N}(0, I)\)。当α接近1时保留原始特征，接近0时特征被噪声覆盖，且方差保持不变。
    - **对角注意力去噪器**：单层Transformer块，但自注意力中仅允许每个token关注自身（对角掩码），防止高重要度token信息泄露给低重要度token，从而严格维持稀疏性约束。该去噪器修复噪声引起的分布偏移。
  - **优化目标**：联合优化评分器参数θ、去噪器参数ω、LLM的LoRA参数φ，通过负对数似然损失进行下一token预测。
  - **推理阶段**：丢弃SparseLearn模块，仅使用训练好的评分器，通过硬Top-K直接保留32个得分最高的token（保持原始空间位置索引用于RoPE），无额外计算开销。

## 三、实验设计

- **数据集与任务**：
  - 主评测：**SlideBench (TCGA)**，含显微镜（Microscopy）和诊断（Diagnosis）两个子集，报告总体准确率。
  - 零样本泛化：**SlideBench (BCNB)**（乳腺癌）和 **WSI-VQA***（视觉问答）。
- **对比方法**：
  - 无压缩上界：SlideChat（保留全部token，最大4096 tokens）。
  - 随机基线：随机选择固定数量token。
  - 通用VLM：GPT-4o、LLaVA-Med、Quilt-LLaVA、MedDr（均使用空间采样，约17.2K tokens）。
- **实现细节**：两阶段训练（第一阶段冻结LLM，训练其他模块；第二阶段训练LLM的LoRA等），使用2块NVIDIA A6000 GPU。

## 四、资源与算力

- **GPU型号与数量**：2块NVIDIA A6000 GPU。
- **训练时长**：论文未明确说明具体训练时间，仅描述了两阶段训练协议。

## 五、实验数量与充分性

- **主要实验**：表1展示在SlideBench (TCGA)上的总体及子类准确率，以及零样本结果。进行了一组对比（5个基线+随机）。
- **消融实验**：表2对SparseLearn的两个关键组件（VP噪声门控 vs 标量门控；对角注意力 vs 全局注意力）进行消融，K=32时各起约2-3%的性能影响。
- **效率分析**：图3展示accuracy vs token数曲线；图4展示不同序列长度下稳定性；图5为UMAP可视化，定性验证所选token位于拓扑极端。
- **充分性评价**：主实验覆盖主要基线，消融针对于核心模块，零样本验证泛化性，但缺少与更多最新可学习剪枝方法（如LearnPruner等）的对比，也缺少在不同K值下的全面实验。

## 六、主要结论与发现

- 在严格32 token预算下，模型达到73.32%准确率，超过所有压缩基线，且在诊断子集上保留无压缩模型96%的推理能力，计算开销仅1.3%。
- 零样本泛化到BCNB和WSI-VQA*均优于对比方法，表明学习到的语义优先级具有域不变性。
- 模型对输入序列长度不敏感，说明已稳定提取不变量语义子空间。
- 消融证实VP噪声门控优于标量门控（-2.92%），对角注意力优于全局注意力（-1.70%），验证了设计必要性。

## 七、优点

- **方法论创新**：首次将WSI token剪枝形式化为可学习稀疏化问题，通过连续噪声门控解决非可微离散剪枝的梯度问题，无需STE或辅助损失。
- **高效推理**：推理时零额外计算，仅32个token使LLM复杂度与原始patch数解耦，动态压缩比可达128倍。
- **性能优越**：在极低计算预算下达到接近无压缩模型的准确性，且泛化性强。
- **架构简洁**：SparseLearn组件仅在训练时启用，不改变预训练LLM的架构，易于集成。

## 八、不足与局限

- **局部评分假设**：评分器仅基于单个patch特征独立预测重要性，缺乏图块间的早期交叉通信，可能遗漏需要全局上下文才能识别的宏观形态模式（如弥散性边缘）。
- **静态稀疏预算**：固定K=32，不同临床任务可能需要不同比例，无法动态调整，论文在局限部分提及未来工作可探索动态预算。
- **实验覆盖有限**：消融实验仅有两个配置变体，未系统分析温度调度、K值变化对结果的影响；对比方法中缺少同类可学习剪枝方法（如基于Gumbel-Softmax或STE的WSI方法）。
- **潜在偏差风险**：训练数据仅来自TCGA（单一来源），零样本泛化到BCNB虽好但跨度有限，在其他组织类型（如肺、前列腺）上的表现未知。
- **可解释性不足**：虽然UMAP可视化显示所选token位于拓扑极端，但未提供病理学专家验证所选区域是否确实包含诊断证据。

（完）
