---
title: Aligning Cellular Sheaves with Classifier Attention for Interpretable Weakly-Supervised Pathology Localization
title_zh: 对齐细胞层与分类器注意力以实现可解释的弱监督病理定位
authors: "Devansh Lalwani, Swapnil Bhat, Maulik Shah"
date: 2026-05-24
pdf: "https://arxiv.org/pdf/2606.00092v1"
tags: ["query:wsi-mil"]
score: 9.0
evidence: 将细胞层与分类器注意力对齐用于可解释弱监督病理定位
tldr: 弱监督病理定位中，注意力图常无法准确聚焦病灶。本文引入细胞层结构，通过图上的向量空间和线性映射检测局部不一致性，并结合注意力机制提出注意力条件一致性训练。该方法使注意力与细胞层分歧场共同指向诊断区域，在Camelyon16上取得patch级AUC 0.940，注意力AUC从0.717提升至0.953。为病理诊断提供两种互补解释，增强模型可信度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00092v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1596, \"height\": 1682, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00092v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1193, \"height\": 240, \"label\": \"Table\"}]"
motivation: 弱监督注意力图定位不精准，模型虽分类正确但未关注病灶，缺乏可解释性。
method: 结合细胞层与注意力机制，提出注意力条件一致性训练，使相似特征根据注意力对齐。
result: Camelyon16上patch级AUC 0.940，注意力AUC从0.717提升至0.953，Camelyon17上迁移性能稳定。
conclusion: 生成注意力图和分歧场两种互补定位，提升弱监督病理定位的可靠性和可解释性。
---

## 摘要
基于基础特征之上使用注意力多实例学习（ABMIL）的全切片图像弱监督分类在Camelyon16切片级性能上已接近饱和，但相应的注意力图并非完美的定位信号：在临床解读中，一个模型即使没有在真实病变区域激活也能正确分类，这难以令人信任。我们通过细胞层（cellular sheaves）来解决这一差距，它为图的每个顶点和边配备有限维向量空间及它们之间一致的线性映射，提供了一种在图形结构数据上检测局部不一致性的原则性方法。我们将细胞层应用于全切片图像的弱监督肿瘤定位，将层不一致场（sheaf disagreement field）与ABMIL相结合。自然的训练目标——鼓励相似特征之间的一致性——会产生追踪组织级纹理而非诊断内容的不一致场。我们提出注意力条件一致性（attention-conditional consistency），利用分类器的注意力来定义哪些相邻图像块应当一致。在此目标下联合训练分类器和层产生的不一致场在Camelyon16上实现了0.940的块级AUC，并将注意力头从仅ABMIL时的0.717提升至0.953。将分类器冻结在其ABMIL值的两阶段消融实验在不一致场上仅达到0.727，注意力停留在0.717，证实增益来自投影仪在两个目标下的共同适应，而非单独损失变化。训练好的模型无需重新训练即可迁移到Camelyon17的标注切片上，保持Delta AUC 0.932 ± 0.083和注意力AUC 0.955 ± 0.099。结果得到一张注意力图和一张层不一致图，两者在同一诊断区域激活，为临床医生每次切片级预测提供两种互补的解释。

## Abstract
Weakly-supervised classification of whole-slide images with attention-based multiple instance learning (ABMIL) on top of foundation features now reaches near-saturation on Camelyon16 slide-level performance, but the corresponding attention maps are an imperfect localization signal: in clinical interpretation, a model that classifies correctly without firing on the actual lesion is hard to trust. We address this gap with cellular sheaves, which equip each vertex and edge of a graph with a finite-dimensional vector space and consistent linear maps between them, providing a principled way to detect local disagreement on graph-structured data. We apply cellular sheaves to weakly-supervised tumour localization on whole-slide images, combining a sheaf disagreement field with ABMIL. The natural training objective, encouraging consistency between similar features, produces a disagreement field that tracks tissue-level texture rather than diagnostic content. We propose attention-conditional consistency, which uses the classifier's attention to define which neighbouring patches should agree. Joint training of the classifier and the sheaf under this objective produces a disagreement field with patch-level AUC 0.940 on Camelyon16 and raises the attention head from its ABMIL-alone level of 0.717 to 0.953. Two-stage ablation with the classifier frozen at its ABMIL values reaches only 0.727 on the disagreement field and leaves attention at 0.717, confirming that the gain comes from the projector co-adapting under both objectives, not from the loss change in isolation. The trained model transfers without retraining to annotated slides from Camelyon17, maintaining Delta AUC 0.932 +/- 0.083 and attention AUC 0.955 +/- 0.099. The result is an attention map and a sheaf-disagreement map that fire on the same diagnostic regions, giving clinicians two complementary explanations for each slide-level prediction.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在弱监督全切片图像（WSI）分类中，基于注意力的多实例学习（ABMIL）虽在切片级分类性能上接近饱和（如Camelyon16上AUC ~0.95），但其产生的注意力图作为定位信号并不完美——模型可能在未正确关注病灶区域的情况下做出正确分类，导致临床上难以信任。
- **整体含义**：为了提升模型的可解释性，需要一种既能指出“哪里被关注”（注意力图）又能指示“组织本身发生转变”（拓扑不一致信号）的互补定位机制，从而帮助病理学家理解每个预测的依据。

## 2. 论文提出的方法论：核心思想、关键技术细节与算法流程

### 核心思想
将**细胞层（cellular sheaves）** 引入WSI的图结构（patch图），通过计算每个顶点上的**不一致性（disagreement）** 作为额外定位信号，并通过**注意力条件一致性（attention-conditional consistency）** 训练目标，使层的不一致场与分类器的注意力对齐。

### 关键技术细节

- **图构造**：每个切片构建图G=(V,E)，顶点为组织块（128μm物理分辨率），边连接8个空间最近邻。
- **细胞层定义**：每个顶点分配特征空间R^d（d=256），每条边分配边空间R^d'（d'=64）。利用超网络（hypernetwork）生成低秩限制映射（low-rank form F_{v⊴e}=AB^T，rank r=8），以减少参数并强制比较方向。
- **层拉普拉斯算子与不一致场**：定义边不一致δ_e = F_{u⊴e} x_u - F_{v⊴e} x_v，层拉普拉斯L_F = δ^Tδ，每顶点不一致性Δ_v = ||(L_F x)_v||^2。
- **注意力条件一致性损失**：分类器生成注意力向量a（detached），计算每条边上的注意力差距|a_u - a_v|。按每张切片百分比阈值，将低差距边（底部30%）视为“一致”，高差距边（顶部30%）视为“不一致”。一致边最小化||δ_e||^2，不一致边鼓励||δ_e||^2超过一个边界。注意力在计算该损失时被detach，梯度不流向分类器，但更新投影仪。
- **联合训练**：同时优化分类交叉熵损失、注意力条件一致性损失、以及低秩因子的弱正交正则项。前5个epochs仅训练分类器（预热），随后40个epochs联合训练。

### 算法流程（文字说明）

1. 输入切片，提取UNI特征，构建patch图。
2. 共享投影仪将特征映射到维度d=256。
3. 分类头：基于投影特征计算门控注意力和切片级logit，得到注意力图a。
4. 层头：使用超网络生成每边的限制映射，计算层拉普拉斯，得到不一致场Δ。
5. 计算分类损失、基于注意力差距的边一致性损失（detach注意力）、正交正则。
6. 反向传播更新投影仪和分类器（分类器仅从分类损失得到梯度，层头从一致性损失得到梯度）。

## 3. 实验设计：数据集、基准与对比方法

- **数据集**：
  - **Camelyon16**：训练270张切片（110肿瘤，160正常），测试129张切片（49肿瘤，80正常）。
  - **Camelyon17**：50张带有像素级标注的切片（来自5个不同医疗中心），用于外部验证（不进行微调）。
- **基准（baseline）**：纯ABMIL（无层分支），使用UNI特征，注意力图作为定位信号。
- **对比方法**：
  - Sheaf-MIL-Loc（联合训练，注意力条件一致性）。
  - 两阶段消融：先训练ABMIL，冻结分类器，仅训练层头（注意力条件一致性）。
  - 特征相似性一致性消融（简单训练使特征相似块一致）。
  - 文中声明未与CLAM、DSMIL、TransMIL等对比（未来工作）。

## 4. 资源与算力

- **文中提及的训练参数**：优化器AdamW，学习率1e-4，权重衰减1e-5，batch size 1 slide，梯度裁剪norm 1.0，40个epochs（前5个epochs分类器预热）。
- **未明确说明的**：具体GPU型号、数量、训练总时长。论文仅提供了超参数设置，但未提供训练硬件细节。因此我们无法据此评估算力需求。

## 5. 实验数量与充分性

- **实验组数**：
  - Camelyon16上定位性能表1（主模型 vs ABMIL vs 两阶段消融）。
  - 特征相似性一致性消融（仅提及退化结果，AUC 0.575）。
  - Camelyon17迁移评估（未微调，report Δ AUC 0.932±0.083，Attn AUC 0.955±0.099）。
  - Spearman相关性分析（Δ与特征范数、余弦相似度等）。
  - 可视化示例图1（4张Camelyon17切片热图）。
- **充分性评估**：
  - **优点**：包含了必要的消融实验（两阶段、特征相似性对比），验证了注意力条件一致性的贡献；在外部数据集上进行了迁移测试。
  - **不足**：
    - 仅报告了单种子（single seed）结果，没有多种子方差估计（作者在Limitations中承认未来工作计划多种子）。
    - 仅对比了ABMIL baseline，未与CLAM、DSMIL、TransMIL等当前主流弱监督定位方法比较，限制了结论的全面性。
    - 超参数（rank r=8，k=8，d'=64）固定，未进行敏感性分析。
    - 未在除Camelyon系列之外的数据集（如TCGA）上验证，泛化性证据有限。
    - 未报告精确计算时间、内存占用等资源指标。

整体上，实验设计合理但不够充分，多基线对比和多数据集验证是缺失的关键部分。

## 6. 论文的主要结论与发现

- **主要发现**：
  - 注意力条件一致性联合训练使得层不一致场（Δ）在Camelyon16上达到patch级AUC 0.940，而纯ABMIL的注意力仅为0.717。
  - 联合训练同时提升了注意力图本身的定位能力：注意力AUC从0.717提升至0.953，增幅达0.236。
  - 两阶段消融（冻结分类器）中Δ AUC仅0.727，注意力0.717，证明增益来自投影仪在两个头间的共同适应，而非损失函数本身。
  - 特征相似性一致性训练导致Δ退化（AUC 0.575），说明对齐目标的选择至关重要。
  - 模型可无微调迁移至Camelyon17，保持与原域相近的性能（Δ AUC 0.932±0.083，Attn AUC 0.955±0.099）。
  - 不一致场倾向于在肿瘤-正常边界处激活（而非肿瘤中心），而注意力聚焦于肿瘤区域，二者互补。
- **临床意义**：提供两种互补的定位图——注意力图（指示分类器关注点）和层不一致图（指示组织拓扑转变点）。在正确预测时二者一致增强信任，在不确定时二者分歧可提供额外校准信号。

## 7. 优点：方法或实验设计上的亮点

- **创新性**：首次将细胞层拓扑应用于WSI弱监督定位，并巧妙设计注意力条件一致性损失，使不一致场主动对齐分类器注意力的边界，而非原始特征相似性。
- **方法优雅**：利用低秩限制映射（AB^T）减少参数，保持数学性质（PSD、规范不变性）；超网络根据端点特征和空间偏移生成映射，具有几何意义。
- **实验设计**：关键消融实验（两阶段、特征相似性）清晰地分离了联合训练与损失函数的贡献；外部迁移检验增强了结果可信度。
- **可解释性产出**：不要求修改现有ABMIL架构，只需添加一个可同时训练的层头，即可输出两种互补解释，临床实用性强。
- **代码和数据可复现**：基于公开数据集和预训练UNI模型，方法组件开放，易于复现。

## 8. 不足与局限

- **基线对比不充分**：仅与ABMIL对比，未与CLAM、DSMIL、TransMIL等当前顶级弱监督方法比较，且未报告多种子方差。
- **超参数未调优**：rank r=8、k=8、d'=64固定，缺乏敏感性分析，可能影响最优性能。
- **数据集局限**：仅使用Camelyon系列（乳腺癌淋巴结转移），未在更多类型病理（如结肠癌、肺癌）上验证泛化性。
- **阈值选择主观**：注意力一致/不一致的边划分阈值（30%固定）可能依赖数据集，未进行系统选择或自适应优化。
- **临床验证缺失**：虽然声称支持临床解释，但未进行与病理学家用户的交互实验或定量评估解释质量。
- **注意力条件一致性损失中注意力detach**：虽然避免了梯度反馈，但可能限制了分类器对层头信号的间接利用（如通过投影仪更新进行帮助）。这种设计的选择理由仅基于防止分类器被层干扰，但可以探讨替代方案。
- **资源分析不透明**：未提供GPU型号、训练时间等，不利于其他研究者复现和评估计算成本。

## （完）
