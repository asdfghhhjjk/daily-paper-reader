---
title: "LRMIL: Efficient Low-Resolution Multiple Instance Learning via High-Resolution Knowledge Distillation for Whole Slide Image Classification"
title_zh: LRMIL：通过高分辨率知识蒸馏实现高效低分辨率多实例学习用于全切片图像分类
authors: "Yonghan Shin, Won-Ki Jeong"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.06864v1"
tags: ["query:wsi-mil"]
score: 9.0
evidence: 低分辨率多实例学习结合知识蒸馏实现高效WSI分类
tldr: 全切片图像分类中，多实例学习常依赖高分辨率补丁，导致计算开销大且难以捕获低倍率全局特征。本文提出LRMIL框架，通过补丁级和切片级两阶段蒸馏，将高分辨率知识迁移至低分辨率表示，推理时仅用低分辨率补丁。在多个基准上，LRMIL性能超越现有方法且推理更高效。该工作为WSI分析提供了实用且可扩展的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06864v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1248, \"height\": 629, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06864v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1141, \"height\": 717, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06864v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1239, \"height\": 501, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06864v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1207, \"height\": 500, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06864v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 931, \"height\": 325, \"label\": \"Table\"}]"
motivation: 现有MIL方法依赖高分辨率补丁，计算开销大且难以捕获低倍率全局视觉线索。
method: 提出LRMIL，采用补丁级跨分辨率蒸馏和切片级知识蒸馏，将高分辨率知识迁移至低分辨率学生模型。
result: 在多个WSI基准上，LRMIL性能超越SOTA方法，且推理时数据预处理与计算成本显著降低。
conclusion: LRMIL为WSI分类提供了高效、可扩展且适合临床病理的实用方案。
---

## 摘要
多实例学习已成为数字病理学中全切片图像分析的标准范式，因为它无需密集标注即可实现切片级预测。现有的MIL方法通常依赖于高分辨率切片的详尽提取和编码。然而，这种做法在实际临床环境中存在两个关键限制：它难以在较低放大倍数下捕获全局视觉线索，并且由于每个切片的海量高分辨率补丁而带来巨大的计算开销。为解决这些限制，我们提出了一种高效的低分辨率多实例学习框架，该框架将高分辨率知识迁移到低分辨率表示。LRMIL采用两阶段蒸馏策略。首先，补丁级跨分辨率蒸馏将低分辨率补丁嵌入与高分辨率表示对齐。其次，切片级知识蒸馏在切片级监督和教师指导下训练低分辨率学生MIL模型。在推理时，LRMIL仅对低分辨率补丁进行操作，显著减少了数据预处理和计算成本。在多个WSI基准测试上的大量实验表明，LRMIL在实现更高效推理的同时始终优于最先进的MIL方法。这些结果突显了LRMIL作为临床病理学中WSI分析的实用且可扩展的解决方案。

## Abstract
Multiple instance learning (MIL) has become a standard paradigm for whole slide image (WSI) analysis in digital pathology, as it enables slide-level prediction without dense annotations. Existing MIL methods typically rely on exhaustive extraction and encoding of high-resolution patches. However, this practice suffers from two critical limitations in real-world clinical settings: it struggles to capture global visual cues at lower magnifications, and incurs substantial computational overhead due to the massive number of high-resolution patches per slide. To address these limitations, we propose an efficient low-resolution multiple instance learning (LRMIL) framework that transfers high-resolution knowledge to low-resolution representations. LRMIL adopts a two-stage distillation strategy. First, patch-level cross-resolution distillation aligns low-resolution patch embeddings with high-resolution representations. Second, slide-level knowledge distillation trains a low-resolution student MIL model under both slide-level supervision and teacher guidance. At inference time, LRMIL operates exclusively on low-resolution patches, substantially reducing data preprocessing and computational cost. Extensive experiments on multiple WSI benchmarks demonstrate that LRMIL consistently outperforms state-of-the-art MIL methods while achieving more efficient inference. These results highlight LRMIL as a practical and scalable solution for WSI analysis in clinical pathology.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：现有全切片图像的多实例学习方法严重依赖高分辨率补丁的详尽提取和编码，导致两大缺陷：
  - 低倍率下的全局视觉线索（如组织架构、空间上下文）难以被建模；
  - 每张WSI包含成千上万高分辨率补丁，预处理和特征编码的计算开销巨大。
- **整体含义**：本文旨在解决高分辨率MIL在临床部署中的效率瓶颈，通过知识蒸馏将高分辨率补丁中的细粒度语义迁移到低分辨率表示，从而在推理时仅使用低分辨率补丁，达到与高分辨率方法相当甚至更优的性能，同时大幅降低计算成本。

## 2. 方法论
- **核心思想**：采用两阶段蒸馏策略，将高分辨率知识从教师模型迁移到低分辨率学生模型。训练阶段使用高分辨率补丁提供监督，推理时仅用低分辨率补丁。
- **关键技术细节**：
  - **Stage 1: 补丁级跨分辨率蒸馏**：
    - 从同一WSI的同一区域分别提取高分辨率（20×）和低分辨率（5×）补丁，低分辨率补丁覆盖的区域对应K=16个高分辨率补丁。
    - 使用预训练且冻结的高分辨率ViT编码器（CONCH）作为教师，训练相同架构的低分辨率编码器。
    - 在多个选定层（如第0、1、11层），将高分辨率补丁的CLS嵌入与对应低分辨率区域token（通过平均池化获得）进行MSE对齐，蒸馏损失 \( \mathcal{L}_{\text{patch}} \) 为各层损失之和。
  - **Stage 2: 切片级知识蒸馏**：
    - **Step 1（教师分支）**：冻结高分辨率编码器，训练基于注意力的MIL头部（如ABMIL），使用交叉熵损失 \( \mathcal{L}_{\text{pre}}^{\text{HR}} \)，仅需极轻量训练。
    - **Step 2（学生分支）**：冻结Stage 1蒸馏得到的低分辨率编码器，训练低分辨率MIL学生模型。总损失包括：
      - 标签监督损失 \( \mathcal{L}_{\text{pre}}^{\text{LR}} \)（CE）
      - 袋级蒸馏损失 \( \mathcal{L}_{\text{bag}} \)：教师与学生袋级logits间的KL散度（温度T1）
      - 实例级软注意力匹配损失 \( \mathcal{L}_{\text{inst}}^{\text{soft}} \)：将教师高分辨率补丁注意力平均后与低分辨率补丁注意力进行KL散度对齐（温度T2）
      - 实例级硬top-k/bottom-k监督损失 \( \mathcal{L}_{\text{inst}}^{\text{hard}} \)：取教师注意力最高/最低的k个区域作为伪正/负例，对学生注意力进行二值交叉熵。超参数k=4（通过敏感性分析选择）。
  - **推理**：仅使用低分辨率编码器和学生MIL头部，无需任何高分辨率处理。
- **公式/算法流程**（文字描述）：
  - 第1阶段：对每个低分辨率补丁及其对应的16个高分辨率补丁，在选定层提取特征，计算MSE损失。
  - 第2阶段：先训练教师MIL模型（固定编码器）；再用蒸馏后的低分辨率编码器训练学生MIL模型，优化总损失 \( \mathcal{L}_{\text{slide}} = \mathcal{L}_{\text{pre}}^{\text{LR}} + \lambda_{\text{bag}} \mathcal{L}_{\text{bag}} + \lambda_{\text{soft}} \mathcal{L}_{\text{inst}}^{\text{soft}} + \lambda_{\text{hard}} \mathcal{L}_{\text{inst}}^{\text{hard}} \)，其中 \( \lambda_{\text{bag}}=0.5, \lambda_{\text{soft}}=0.5, \lambda_{\text{hard}}=0.3 \)。

## 3. 实验设计
- **数据集与场景**：
  - **组织学亚型分类**：TCGA-BRCA（IDC vs ILC）、TCGA-NSCLC（LUAD vs LUSC）、TCGA-RCC（KIRP vs KIRC vs KICH）、BRACS（7类）
  - **分子亚型分类**：TCGA-BRCA（LumA, LumB, Basal, Her2），记为BRCA*
  - **生存预测**：TCGA-BRCA、LUAD、LUSC、KIRP、KIRC（二分类：存活/死亡）
- **Benchmark与评价指标**：Accuracy、AUC、总推理时间（t，含补丁提取、特征编码和MIL聚合，单位×10⁴秒）
- **对比方法**：
  - HR全补丁方法：Max-P, Mean-P, ABMIL, CLAM-MB, CLAM-SB, DSMIL, TransMIL, DTFD-MIL
  - 选择性子集方法（SC）：ZOOMMIL, HDMIL
- **公平性保证**：所有方法均使用相同的CONCH视觉编码器作为特征提取器；5折交叉验证；Adam优化器，学习率1e-4，早停20轮。

## 4. 资源与算力
- **文中未明确说明**所使用的GPU型号、数量及训练总时长。
- 仅提及使用CONCH预训练编码器（可能基于ViT-Base），所有模型在5折交叉验证下训练，每折直到早停（20轮无改善停止）。但无具体计算资源描述。

## 5. 实验数量与充分性
- **实验数量**：共9张表/图（实际显示3张表+1张注意力热图，但表内包含多个数据集和任务）：
  - 表1：亚型分类（5个数据集评估）
  - 表2：生存预测（5个数据集评估）
  - 表3：消融研究（3个代表性数据集，涵盖三个任务，测试各损失组件贡献）
- **补充分析**：
  - 超参数敏感性（top-k）在正文中讨论（k从2到64），未另列表但说明性能稳定。
  - 定性注意力热图对比（图2）。
- **充分性与公平性**：
  - 实验覆盖多种任务（分类、生存）及多种病理类型（乳腺、肺、肾等），较全面。
  - 对比方法充分，包括主流MIL及效率导向方法。
  - 消融清晰展示了各组件贡献（Stage1蒸馏、袋级蒸馏、实例级软/硬对齐）。
  - 但缺少对超参数λ的全面搜索分析（仅固定一组值）；未与更近期的WSI蒸馏方法（如Virchow2等）对比；未报告标准差或置信区间。

## 6. 主要结论与发现
- LRMIL在所有数据集上获得最高或并列最高的Acc/AUC，同时推理时间减少一个数量级以上（例如TCGA-BRCA亚型分类推理时间t=4 vs HR方法49，ZOOMMIL的9）。
- 补丁级跨分辨率蒸馏（Stage1）提供了基础性能提升；添加袋级蒸馏进一步改善；实例级软注意力对齐贡献最大，硬top-k监督互补。
- 注意力热图显示LRMIL能更一致地定位临床相关肿瘤区域，结合了低倍率全局结构和HR细节。

## 7. 优点
- **方法创新**：首次将知识蒸馏应用于MIL中的跨分辨率迁移，以低分辨率推理获得高分辨率知识，兼顾效率与性能。
- **工程实用**：推理阶段仅需低分辨率处理，预处理和编码成本大幅降低，适合临床大规模部署。
- **实验充分**：在多个真实病理数据集上验证，涵盖分类与生存预测任务，消融和定性分析支撑有效。
- **可复现**：代码公开在GitHub，便于后续研究和比较。
- **参数鲁棒**：top-k超参数在较宽范围内性能稳定。

## 8. 不足与局限
- **资源信息缺失**：未报告训练所需GPU型号、数量及时间，难以评估训练阶段的计算消耗。
- **依赖性**：仅使用CONCH作为统一编码器，未探索其他预训练编码器（如Virchow、CTransPath）是否同样有效，可能影响泛化性。
- **任务覆盖局限**：仅评估分类（亚型、生存）任务，未扩展到回归（如肿瘤分级评分）或分割任务。
- **消融组合不足**：消融仅测试了逐步添加损失的效果，未对比不同λ权重的敏感性；Stage1中未分析不同层选择策略的影响。
- **偏差风险**：实验均基于TCGA和BRACS数据集，可能无法完全代表真实临床多样性（如不同染色、扫描仪）。
- **未讨论低倍率图像质量**：当WSI中低倍率图像存在模糊或伪影时，蒸馏效果可能下降。
- **与最新方法对比缺位**：未与2025/2026年更高效的蒸馏MIL方法（如FOCUS、Hierarchical Distillation MIL等）比较。

（完）
