---
title: Cell Instance Segmentation via Multi-Task Image-to-Image Schrödinger Bridge
title_zh: 基于多任务图像到图像薛定谔桥的细胞实例分割
authors: "Hayato Inoue, Shota Harada, Shumpei Takezaki, Ryoma Bise"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12318v1"
tags: ["query:pathseg"]
score: 10.0
evidence: 在PanNuke和MoNuSeg数据集上通过多任务薛定谔桥进行细胞实例分割
tldr: 本文提出一种基于多任务图像到图像薛定谔桥框架的细胞实例分割方法，将分割问题建模为分布式的图像生成任务，并引入反距离图的边界监督和确定性推理，在多个数据集上实现了无需预训练或后处理的优异性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12318v1/fig-001.webp\", \"caption\": \"Fig. 5. Joint distributions of cell size and circularity for the ground truth (GT), the proposed method, and CellViT variants on the PanNuke dataset. The proposed method produces a distribution closer to the GT, suppressing morphologically atypical cell instances.\", \"page\": 6, \"index\": 1, \"width\": 533, \"height\": 232}, {\"url\": \"assets/figures/arxiv/2604.12318v1/fig-002.webp\", \"caption\": \"Fig. 2. Overview of the proposed Multi-Task Image-to-Image SB framework.\", \"page\": 2, \"index\": 2, \"width\": 474, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2604.12318v1/fig-003.webp\", \"caption\": \"Fig. 4. Transformation process at time step.\", \"page\": 5, \"index\": 3, \"width\": 1072, \"height\": 232}]"
motivation: 现有细胞分割方法依赖确定性预测和后处理，缺乏对实例整体结构的显式约束。
method: 作者构建多任务图像到图像薛定谔桥模型，并加入边界感知的反距离监督以实现稳定预测。
result: 在PanNuke和MoNuSeg数据集上，该方法取得了优于或可比的性能，并表现出在数据有限情况下的鲁棒性。
conclusion: 薛定谔桥图像生成框架为细胞实例分割提供了一种高效且稳定的解决思路。
---

## 摘要
现有的细胞实例分割流程通常将确定性的预测与后处理相结合，但这种方式对实例掩膜的全局结构施加的显式约束有限。在本研究中，我们提出了一种多任务图像到图像的薛定谔桥（Schrödinger Bridge）框架，将实例分割表述为基于分布的图像到图像生成问题。通过反距离图（reverse distance map）引入了边界感知监督，并采用确定性推理以产生稳定的预测。在 PanNuke 数据集上的实验结果表明，所提出的方法在无需依赖 SAM 预训练或额外后处理的情况下，达到了具有竞争力或更优的性能。在 MoNuSeg 数据集上的附加实验进一步展示了在训练数据有限条件下的鲁棒性。这些结果表明，基于薛定谔桥的图像到图像生成方法为细胞实例分割提供了一种有效的框架。

## Abstract
Existing cell instance segmentation pipelines typically combine deterministic predictions with post-processing, which imposes limited explicit constraints on the global structure of instance masks. In this work, we propose a multi-task image-to-image Schrödinger Bridge framework that formulates instance segmentation as a distribution-based image-to-image generation problem. Boundary-aware supervision is integrated through a reverse distance map, and deterministic inference is employed to produce stable predictions. Experimental results on the PanNuke dataset demonstrate that the proposed method achieves competitive or superior performance without relying on SAM pre-training or additional post-processing. Additional results on the MoNuSeg dataset show robustness under limited training data. These findings indicate that Schrödinger Bridge-based image-to-image generation provides an effective framework for cell instance segmentation.

---

## 论文详细总结（自动生成）

# 基于多任务图像到图像薛定谔桥的细胞实例分割 — 论文总结

---

## 1. 核心问题与研究动机

- **问题背景**：细胞实例分割是生物医学图像分析的关键任务，涉及单个细胞的精确分割，广泛应用于病理分析、药物筛选、细胞形态学研究等。  
- **现有问题**：大多数现有方法（如 U-Net、Cellpose、StarDist、CellViT）依赖于确定性预测后通过启发式后处理（如 watershed、形态学操作、聚类等）来分离细胞实例。这类后处理：
  - 依赖超参数调整、数据集特性；
  - 缺乏对实例整体结构的显式约束；
  - 容易出现形态异常的分割结果（如粘连细胞、碎片化实例）。
- **研究动机**：作者希望突破“确定性+后处理”的框架，引入**分布式建模**的思想，将分割问题视为一个**概率分布生成问题**，从而隐式地学习形态合理性。他们选择使用**薛定谔桥（Schrödinger Bridge, SB）**来建模图像到掩膜的生成分布。

---

## 2. 方法论：核心思想与技术细节

### （1）总体框架
- **核心思想**：将细胞实例分割表述为一个分布到分布的“图像到图像生成问题”，即学习从输入显微图像分布到输出掩膜分布的最优传输过程（Schrödinger Bridge）。
- **特点**：
  - 保留空间一致性（像素到像素对应）；
  - 生成形态合理的掩膜分布；
  - 不依赖启发式后处理。

### （2）多任务设计
- 为增强边界分割能力，引入**反距离图（reverse distance map）**作为辅助任务：
  - 每个像素的值越靠近边界越高；
  - 用此图作为辅助监督信号，强化模型对边界的敏感性；
  - 推理阶段只输出实例掩膜。

### （3）模型构造与训练流程
- **输入与输出结构**：  
  - 输入图像 \(X_i \in \mathbb{R}^{H\times W\times 3}\)；
  - 输出为实例掩膜 \(M_i\) 与反距离图 \(R_i\)，以通道复制方式对齐维度 (\( \times 6 \)通道)。
- **薛定谔桥连接公式**：  
  训练中定义输入状态 \(X_1\) 和目标状态 \(X_0\)，通过扩散过程连接两者：  
  \( q(X_t | X_0, X_1) = N(X_t; \mu_t, \Sigma_t) \)，其中  
  \(\mu_t, \Sigma_t\) 的表达式保证结构保真性和随机传输平稳。
- **训练机制**：
  - 随机采样时间 \(t \in [0, 1]\)；
  - 训练网络 \( \epsilon_\theta(X_t, t)\) 预测噪声项，驱动过程由输入到输出；
  - 损失函数基于噪声预测误差。
- **推理策略**：
  - **确定性反扩散**（关闭随机噪声）；
  - 沿最可能轨迹从输入状态到输出；
  - 输出平均的掩膜通道后取阈值 0.5，得到最终分割结果，无需后处理。

---

## 3. 实验设计

### （1）数据集与场景
- **PanNuke**：大型组织学细胞分割数据集，包含 19 种组织类型、189,744 个细胞。
  - 用于评估大规模训练条件下的性能。
- **MoNuSeg**：小型核分割数据集（30 张训练图像、14 张测试图像）。
  - 用于测试数据稀缺条件下的鲁棒性。

### （2）对比方法
- **Baseline：**
  - U-Net（传统确定性分割）
  - U-Net+R（引入反距离图监督）
- **SOTA：**
  - CellViT（使用 Segment Anything Model 预训练）
  - CellViT+proc（带启发式后处理）
- 与以上方法比较可评估：
  - 边界监督的效果；
  - 分布式生成的效果；
  - 是否能替代后处理。

### （3）评价指标
- **bPQ（binary Panoptic Quality）**：同时度量分割和检测准确性；
- 辅助指标：Precision、Recall、F1；
- 实例匹配半径：12 像素（近似单细胞半径）。

---

## 4. 资源与算力

- **设备**：NVIDIA RTX A6000 单卡。
- **推理时间**：约 3.5 秒 / patch（T=50）；
  - 相比 CellViT 的 0.35 秒更慢，但不需复杂后处理。
- **训练配置**：
  - Adam 优化器；
  - 学习率 \(5\times10^{-5}\)；
  - 迭代 300,000 次；
  - 使用 EMA（指数滑动平均）参数；
  - Diffusion 步数 T=50，噪声 schedule βmax=0.3。
- **算力信息仅部分描述**，论文未明确列出 GPU 数量或总体训练时长。

---

## 5. 实验数量与充分性

- **主要实验类型**：
  1. PanNuke 大规模训练；
  2. MoNuSeg 小数据集对比实验；
  3. 消融实验（三种任务配置：Mask、Reverse Distance、Multi-Task）；
  4. 形态分析（实例形状分布对比）。
- **实验充分性**：
  - 涵盖不同数据规模；
  - 包含公平对比（相同网络架构和监督条件）；
  - 同时定量+定性评估（含可视化过程、形态分布图）。
- **公平性**：遵守统一的训练策略与评价标准，结论可信。

---

## 6. 主要结论与发现

- **性能方面**：
  - PanNuke 上超越所有基线和 SOTA（bPQ=0.6362，Precision=0.8458）。
  - MoNuSeg 上在无预训练和后处理条件下仍接近或优于 CellViT+proc。
- **关键发现**：
  1. 多任务反距离图监督能显著改善密集区域的实例分离；
  2. 分布式图像生成（SB）能自然避免形态不合理的掩膜；
  3. 确定性推理提供稳定、可重复分割结果；
  4. 对数据规模不敏感，在小样本下仍保持鲁棒性。

---

## 7. 优点与亮点

- **理论创新**：首次将薛定谔桥应用于细胞实例分割，将扩散生成与图像到图像任务结合；
- **不需后处理**：通过概率分布学习直接生成可用掩膜；
- **边界强化**：反距离图显著提升边界检测与分离精度；
- **形态合理性**：生成的掩膜分布在形状和大小的统计分布上更接近真实细胞；
- **跨数据集鲁棒性**：在大数据和小数据场景下均保持优异性能；
- **可解释性强**：通过扩散过程可观察分割形成阶段（从粗到细、边界逐渐清晰）。

---

## 8. 不足与局限

- **推理效率较低**：扩散反演过程较耗时（3.5s 每 patch），不利于大规模实时应用；
- **算力需求较高**：虽仅列单卡，但训练迭代多、参数量大；
- **模型复杂性**：SB 框架数学结构复杂，工程实现门槛高；
- **数据种类**：仅测试两个数据集，缺乏在更多细胞类型或显微成像条件下的验证；
- **缺乏参数敏感性分析**：例如时间步数 T、噪声率 βmax 对性能影响未系统评估；
- **现实部署性**：未讨论临床集成或在线推理的可行性。

---

**（完）**
