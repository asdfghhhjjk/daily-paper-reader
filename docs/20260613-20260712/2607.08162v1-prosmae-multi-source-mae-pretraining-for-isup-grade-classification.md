---
title: "ProsMAE: Multi-Source MAE Pretraining for ISUP Grade Classification"
title_zh: ProsMAE：用于ISUP分级分类的多源MAE预训练
authors: "Anna Jung, Kyeonghun Kim, Youngung Han, Eunseob Choi, Jiwon Yang, Ken Ying-Kai Liao, Hyuk-Jae Lee, Nam-Joon Kim"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08162v1"
tags: ["query:wsi-mil"]
score: 8.0
evidence: 多源MAE预训练用于全切片前列腺癌ISUP分级
tldr: 全切片图像提供丰富诊断信息，但面临大规模、染色差异、扫描仪差异、组织伪影和有限标注等挑战。本文提出多源掩码自编码器框架ProsMAE，利用PANDA、CAMELYON17和BRACS数据集的图块预训练，使编码器接触多样化组织形态和采集条件，随后通过冻结编码器与线性分类头进行ISUP分级。在PANDA分割上，ProsMAE平均验证加权二次卡帕系数优于vanilla MAE线性探针基线，证明了多源预训练的有效性，但需更多分割实验验证稳健性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08162v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1735, \"height\": 814, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08162v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1371, \"height\": 485, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08162v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 843, \"height\": 283, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08162v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 899, \"height\": 796, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08162v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 885, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08162v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 532, \"height\": 335, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08162v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 819, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08162v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 859, \"height\": 224, \"label\": \"Table\"}]"
motivation: 解决WSI中数据异质性和标注稀缺问题，通过多源预训练提升模型泛化能力。
method: 提出ProsMAE框架，在PANDA、CAMELYON17、BRACS多源图块上MAE预训练，迁移至ISUP分级使用冻结编码器线性分类。
result: 在disjoint PANDA分割下，ProsMAE的平均验证QWK超过vanilla MAE线性探针基线。
conclusion: ProsMAE证明多源MAE预训练能改善ISUP分级，但稳健性需进一步验证。
---

## 摘要
全切片图像（WSIs）为计算病理学提供了丰富的诊断信息，但其千兆像素的规模、染色差异、扫描仪差异、组织伪影以及有限的专家标注使得鲁棒的模型训练具有挑战性。本文提出了一个名为ProsMAE的多源掩码自编码器（MAE）框架，用于组织病理学表示学习。来自前列腺癌分级评估（PANDA）、2017年淋巴结转移挑战赛（CAMELYON17）和乳腺癌分子亚型分类（BRACS）的切片被用于ProsMAE预训练，以使编码器接触到多样的组织形态和采集条件。学习到的编码器通过ProsCLS迁移用于国际泌尿病理学会（ISUP）分级分类，使用冻结的编码器和线性分类头。在评估的不相交PANDA划分下，ProsMAE取得了比普通MAE冻结线性探测基线更高的平均验证二次加权卡帕（QWK）。仍需进行多次划分评估，以进一步确立在不同划分组合下的鲁棒性。

## 速览
**TLDR**：为了应对全切片图像在病理分析中的尺度巨大、染色差异、扫描仪差异、伪影及标注稀少等挑战，提出多源掩码自编码器ProsMAE，在PANDA、CAMELYON17和BRACS数据上预训练以学习多样化组织表征。迁移至ISUP分级任务时，采用冻结编码器与线性分类头，在PANDA划分上取得高于基线模型的二次加权Kappa。该框架验证了多源自监督预训练对分级性能的提升，但需进一步重复划分评估以确证鲁棒性。 \
**Motivation**：针对WSIs的多源视觉差异和有限标注问题，提出ProsMAE以学习泛化性强的组织表征。 \
**Method**：在PANDA、CAMELYON17和BRACS上联合预训练ProsMAE，然后在ISUP分级任务中冻结编码器并训练线性分类头。 \
**Result**：在PANDA划分上，ProsMAE的平均验证QWK超越vanilla MAE冻结线性评估基线。 \
**Conclusion**：多源MAE预训练可提升ISUP分级性能，但需重复划分评估以验证鲁棒性，未来可探索更广泛的数据源。

---

## Abstract
Whole slide images (WSIs) provide rich diagnostic information for computational pathology, but their gigapixel scale, stain variation, scanner differences, tissue artifacts, and limited expert annotation make robust model training challenging. This paper presents a multi-source Masked Autoencoder (MAE) framework, named ProsMAE, for histopathology representation learning. Tiles from Prostate cANcer graDe Assessment (PANDA), CAncer MEtastases in LYmph nOdes challeNge 2017 (CAMELYON17), and BReAst Carcinoma Subtyping (BRACS) are used for ProsMAE pretraining to expose the encoder to diverse tissue morphology and acquisition conditions. The learned encoder is transferred for International Society of Urological Pathology (ISUP) grade classification through ProsCLS, using a frozen encoder and a linear classification head. ProsMAE achieved a higher mean validation quadratic weighted kappa (QWK) than the vanilla MAE frozen linear-probe baseline under the evaluated disjoint PANDA split. Repeated-split evaluation remains necessary to further establish robustness across split compositions.

---

## 论文详细总结（自动生成）

# ProsMAE：用于ISUP分级分类的多源MAE预训练

## 1. 论文的核心问题与整体含义
- **研究动机与背景**
  - 全切片图像（WSIs）在计算病理学中蕴含丰富的诊断信息，但面临千兆像素规模、染色协议差异、扫描仪差异、组织伪影以及专家标注稀缺等挑战，直接端到端训练困难。
  - 前列腺癌的 ISUP 分级依赖于腺体结构与组织形态学模式，这些模式可能仅出现在少数活检区域，而弱监督学习下幻灯片级标签与局部形态的对齐性差。
  - 自监督学习（尤其是掩码自编码器 MAE）可从无标注病理图块中学习可迁移表示，但单一来源数据难以覆盖临床中多样的组织形态与采集条件，导致域偏移。
- **核心问题**
  - 如何利用多中心、多器官的组织病理学数据通过 MAE 预训练，学习对域变化更具鲁棒性的特征，从而改善下游 ISUP 分级性能。
- **整体含义**
  - 提出 ProsMAE 框架，通过在 PANDA、CAMELYON17、BRACS 三个异质数据集上进行多源 MAE 预训练，使编码器习得通用组织形态学特征，再以轻量级线性分类头迁移到前列腺癌 ISUP 分级，在低计算资源下提升分级一致性。

## 2. 论文提出的方法论
- **核心思想：ProsMAE 两阶段框架**
  - **第一阶段（ProsMAE 预训练）**：利用多源 WSI 图块，通过高掩码比的 MAE 进行自监督重建，使编码器学习对数据集特有变异不敏感的结构化表示。
  - **第二阶段（ProsCLS 下游分类）**：移除解码器，冻结预训练编码器，对每个幻灯片采样固定数量图块提取特征，平均池化得到幻灯片级表示，再训练一个线性分类头预测 6 类 ISUP 标签。
- **关键技术细节**
  - **掩码重建**：采用 ViT-B/16 为骨干，掩码比设为 0.75。编码器仅处理可见块标记，解码器根据潜在表示与掩码标记重建被遮挡区域。
    - 重建损失：\(L_{MAE} = \frac{1}{|\Omega|} \sum_{i \in \Omega} \|x_i - \hat{x}_i\|^2\)，其中 \(\Omega\) 为掩码块集合。
  - **多源数据**：预训练图块来自 PANDA（前列腺）、CAMELYON17（淋巴结转移）和 BRACS（乳腺癌亚型），按统一流程处理（均为 224×224 大小，不进行染色归一化）。
  - **噪声注入消融**：对输入添加高斯噪声（标准差 \(\sigma \in \{0.02,0.05,0.10,0.20\}\)）测试重建鲁棒性，但主实验未使用噪声。
  - **下游分类**：
    - 每张幻灯片随机采样 \(N=100\) 个 512×512 图块，缩放到 224×224。
    - 编码器提取特征 \(z_i = f_\theta(x_i)\)，幻灯片表示 \(z_{slide} = \frac{1}{N}\sum_{i=1}^N z_i\)。
    - 线性分类头输出 \(\hat{y} = \text{Softmax}(W z_{slide} + b)\)，用加权交叉熵损失 \(L_{cls} = -\sum_{c=1}^6 y_c \log(\hat{y}_c)\) 训练。

## 3. 实验设计
- **数据集与场景**
  - **预训练与下游**：PANDA（前列腺癌，ISMU 级，含良性/无癌），CAMELYON17（淋巴结转移），BRACS（乳腺癌亚型）。
  - **划分方案**：保持不相交 PANDA 分割：241 张幻灯片用于 MAE 预训练，82 张用于下游训练，80 张用于下游验证。所有实验均在此固定分割上进行。
  - **下游类别**：6 类（良性/无癌 + ISUP 1~5 级）。
- **对比方法（Baselines）**
  - **重建质量对比**：与标准自编码器（AE）、变分自编码器（VAE）、单源 MAE 比较 LPIPS、SSIM、PSNR。
  - **下游分类对比**：普通 MAE（单源 PANDA 预训练，相同设置）作为基线，同时对比不同数据源组合（PANDA-only、PANDA+CAMELYON17、PANDA+BRACS、三者联合）。
- **评估指标**
  - **主指标**：二次加权卡帕（QWK），因其更适合序数分级，惩罚大间距错误。
  - **辅助指标**：准确率、宏 F1 分数。
- **消融实验**
  - 掩码比（0.25, 0.50, 0.75）。
  - 噪声注入不同标准差（w/o noise, 0.02, 0.05, 0.10, 0.20）。
  - 基线分割敏感性：对 vanilla MAE 在三个不同独立分割下重复实验（每分割运行 30 轮）。
  - 图块采样敏感性：测试下游采样 50、100、150 个图块时的性能。

## 4. 资源与算力
- **GPU 使用**：文中仅在表 II 注明了“total pretraining GPU hours”，其中 ProsMAE 预训练耗时 10-11 小时，与普通 MAE 相近，快于 AE/VAE，但**未明确报告 GPU 型号、数量或显存**。未提及下游训练或推理的具体算力需求。
- **预训练设置**：最多 5000 步（上限 20 轮），使用 AdamW 优化器，批次大小 64，学习率 5e-5，250 步热身及余弦衰减。整体计算量较低。

## 5. 实验数量与充分性
- **实验组数概览**
  - 重建性能对比：4 个数据集/组合 × 4 个模型，共 16 组实验。
  - 外部数据源结果：4 种数据组合（如表 I）。
  - 掩码比消融：3 种设置。
  - 噪声注入消融：5 种噪声水平。
  - 分割鲁棒性：3 个独立分割，每分割 30 轮，共 90 轮下游评估。
  - 图块采样敏感性：3 种采样数。
- **充分性评估**
  - 实验较为系统，涵盖了数据源组合、超参数消融、噪声鲁棒性、分割敏感性等多个维度。
  - **不足**：下游验证仅限于单个 PANDA 队列（80 张幻灯片），样本较小；未与对比学习等其他自监督方法对比；分割敏感性实验仅针对普通 MAE 基线，ProsMAE 本身仅评估一个主分割；作者也明确指出“需要多次划分评估”和“未宣称统计显著性”，当前结果仅适用于评估的主分割，泛化性证据不足。
  - 实验间对比基本公平（相同框架、相同骨干、相同推理协议），但种子范围未完全配对，可能引入轻微偏差。

## 6. 论文的主要结论与发现
- 多源 MAE 预训练（ProsMAE）在主不相交 PANDA 分割上，平均验证 QWK 为 0.4736，优于普通 MAE 基线的 0.4084（绝对提升 0.0652）。
- 加入噪声的消融并未一致提升性能，最高噪声水平（0.20）性能与无噪声相近，因此无噪声方案被保留为主设置。
- 高掩码比（0.75）在 QWK 上显著优于低掩码比，尽管准确率略低，但更符合序数分级需求。
- 普通 MAE 下游性能对分割组合敏感，说明单一分割结果不能完全代表方法的优越性。
- 图块采样数在 50~150 范围内对 ProsMAE 性能影响较小，方法对采样数不敏感。
- 在相同的低计算预算（5000步）下，ProsMAE 能有效学习多源组织形态学特征，提供了一种部署友好的轻量级病理图像表示学习方案。

## 7. 优点
- **低资源高效**：仅需 5000 步预训练和线性探测，冻结编码器，计算消耗低，便于复现和部署。
- **多源域泛化设计**：首次针对前列腺癌分级整合前列腺、淋巴结、乳腺三类不同器官数据，通过 MAE 学习域不变特征。
- **实验分析细致**：通过掩码比、噪声、分割敏感性、采样数等多角度消融，增强了对方法行为的理解。
- **序数指标导向**：以 QWK 作为主要评判标准，更贴合临床诊断中分级误差的严重程度。
- **强调结果稳健性**：主动指出分割敏感性问题，并建议重复分割评估，态度严谨。

## 8. 不足与局限
- **下游验证单一**：仅在单个 PANDA 内部分割（80 张验证片）上评估，未在独立的外部队列或多中心前列腺癌数据集上测试，泛化性未经验证。
- **小规模验证集**：验证集仅 80 例，可能导致 QWK 估计的方差较大，结果不够稳定。
- **基线对比不全面**：仅与普通 MAE 对比，未纳入 SimCLR、DINO、病理专用基础模型等自监督或弱监督方法。
- **预训练步数较少**：5000 步可能未达到完全收敛，更高步数下的性能未知。
- **聚合策略简单**：使用均值池化和线性分类头，可能丢失图块间空间关系，未探索 Transformer 或多实例学习聚合器。
- **未统计显著性检验**：种子范围未完全配对，且未汇报置信区间/假设检验，不能确认提升的统计显著性。
- **染色归一化未用**：虽然有意保留原始变异，但极端染色差异仍可能影响表示质量，未与归一化方案进行对比。
- **病理任务单一**：下游仅限前列腺 ISUP 分级，未展示方法在其他器官或任务上的迁移能力。

（完）
