---
title: "QG-MIL: A Gated Transformer Aggregator for Domain-Agnostic Multiple Instance Learning in Medical Imaging"
title_zh: QG-MIL：一种用于医学影像领域无关多示例学习的门控Transformer聚合器
authors: "Luca Zedda, Davide Antonio Mura, Cecilia Di Ruberto, Maurizio Atzori, Muhammed Furkan Dasdelen, Carsten Marr, Andrea Loddo"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20027v1"
tags: ["query:wsi-mil"]
score: 9.0
evidence: 用于全切片病理的门控Transformer MIL稳定注意力并识别重要图块。
tldr: 医学影像多实例学习（MIL）中现有注意力聚合器常因注意力集中导致预测过自信且不稳定。本文提出QG-MIL门控变换器聚合器，通过RMSNorm预归一化、每头QK归一化、细粒度注意力门控和SwiGLU前馈四种组件协同，稳定训练并均匀分配注意力，无需辅助损失。在涵盖全切片病理和细胞血液学的六个基准上，最优变体平均宏F1提升6.1分，注意力可视化证实更均匀的实例加权。消融研究表明该设计跨域性能一致性最优、方差最小，代码已开源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20027v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1680, \"height\": 542}, {\"url\": \"assets/figures/arxiv/2606.20027v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1693, \"height\": 1158}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20027v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 673}, {\"url\": \"assets/tables/arxiv/2606.20027v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1695, \"height\": 851}, {\"url\": \"assets/tables/arxiv/2606.20027v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1697, \"height\": 713}]"
motivation: 医学影像MIL注意力聚合器易出现注意力集中，导致预测过自信且不稳定。
method: 提出QG-MIL，一种结合RMSNorm预归一化、每头QK归一化、细粒度注意力输出门控和SwiGLU前馈模块的门控变换器聚合器。
result: 在六个跨尺度基准上，QG-MIL平均宏F1提升6.1分，注意力分布更均匀，跨域性能一致性最优。
conclusion: QG-MIL无需辅助损失即实现均匀注意力和稳定训练，为域无关MIL提供了通用聚合器。
---

## 摘要
医学影像中基于注意力的多示例学习聚合器容易出现注意力集中现象，导致过于自信且不稳定的预测。我们提出了QG-MIL，一种门控Transformer聚合器，通过四个协同的架构组件来解决这一问题：基于RMSNorm的预归一化、逐头QK归一化、细粒度注意力输出门控以及SwiGLU风格的前馈模块。这些设计选择共同稳定了训练过程，并在无需辅助损失、掩码或多阶段正则化的情况下，使注意力在实例间更均匀地分布。我们在涵盖全切片病理和细胞水平血液学的六个基准上评估了QG-MIL，覆盖了两种根本不同的MIL尺度。表现最佳的QG-MIL变体在所有六个基准上均优于领先的基线方法，平均宏F1分数提高了6.1个百分点。注意力叠加和注意力质量分析证实了更分散的实例加权。消融研究表明，虽然单个组件在特定数据集上可以与完整模型相媲美，但与选定的基线相比，QG-MIL设计提供了最一致的跨域性能和最小的方差。我们发布了可配置的实现以支持可重复性：https://github.com/unica-visual-intelligence-lab/QG-MIL

## Abstract
Attention-based Multiple Instance Learning aggregators in medical imaging are prone to attention concentration, producing overconfident and unstable predictions. We introduce QG-MIL, a gated transformer aggregator that addresses this through four synergistic architectural components: RMSNorm-based pre-normalization, per-head QK normalization, fine-grained attention output gating, and SwiGLU-style feed-forward modules. Together, these design choices stabilize training and distribute attention more uniformly across instances without auxiliary losses, masking, or multi-stage regularization. We evaluate QG-MIL across six benchmarks spanning whole-slide pathology and cell-level hematology, covering two fundamentally different MIL scales. The best-performing QG-MIL variants outperform leading baselines on all six benchmarks, with an average improvement of +6.1 mean macro F1 points. Attention overlays and attention mass analysis confirm more distributed instance weighting. Ablation studies show that while individual components can match the full model on specific datasets, the QG-MIL design provides the most consistent cross-domain performance and tightest variance when compared to selected baselines. We release a configurable implementation to support reproducibility at: https://github.com/unica-visual-intelligence-lab/QG-MIL

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对提供的论文进行结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：在医学影像的弱监督多示例学习（MIL）任务中，主流的基于注意力的聚合器容易出现**注意力集中（Attention Concentration）** 现象。即模型的注意力权重会塌缩到少数几个实例（如个别细胞或组织切片中的小块区域）上，形成“注意力汇”（Attention Sinks）。
*   **负面影响**：这种问题会导致模型预测过于自信（overconfident）、对数据噪声敏感、泛化能力差，并且缺乏对整张图像全面、合理的解释性。
*   **现有方案局限**：以往的解决方法多采用训练层面的技巧，如注意力掩码、自监督预训练或师生知识蒸馏。这些方法通常需要引入额外的训练阶段、辅助损失函数或复杂的流程，增加了整体负担。
*   **本研究含义**：论文旨在从根本上解决问题，通过**重新设计MIL聚合器的架构**，**结构性**地抑制注意力集中，而不是在训练过程中进行事后正则化。其最终目标是创建一个即插即用、无需额外训练技巧、在不同医学影像领域中都能稳定工作的通用聚合器。

### 2. 论文提出的方法论

论文提出了 **QG-MIL（Qwen Gated Multiple Instance Learning）**，一种门控Transformer聚合器。其核心思想是借鉴大语言模型中应对信息坍塌的成功经验，通过四个协同的架构组件来稳定训练并分散注意力。

*   **核心架构组件**:
    1.  **RMSNorm 预归一化**: 在输入投影和每个Transformer块内部，使用RMSNorm进行归一化，以稳定深层网络的训练。
    2.  **逐头 QK 归一化**: 在计算注意力分数之前，对每个注意力头的查询（Query）和键（Key）进行归一化，防止数值不稳定，进而促进注意力分布的平滑。
    3.  **细粒度注意力输出门控**: 这是模型的核心创新。在得到注意力输出 `Z` 后、进行输出投影前，引入一个可学习的门控机制 `g`。门控信号由一个专门的线性层从 `Z` 中学习，并通过Sigmoid函数激活，然后与原始输出相乘。这允许模型动态地调节信息流，抑制无关或过强的实例信号。论文探索了**头维度门控**（整个头共享一个标量门）和**元素维度门控**（每个特征维度有独立门）两种形式。
    4.  **SwiGLU 前馈模块**: 将传统Transformer中的标准FFN替换为带有门控机制的SwiGLU模块，以进一步增强非线性和筛选信息的能力。

*   **算法流程（文字描述）**:
    1.  **输入投影**：将包内的所有实例特征 `xi` 通过一个线性层 `Wp` 和RMSNorm+Dropout进行初步处理，映射到 `D` 维空间。
    2.  **层级处理**：将处理后的所有实例特征 `H` 送入 `L` 个堆叠的门控Transformer块。每个块执行以下操作：
        *   **多头注意力计算**：对 `H` 进行线性投影得到 `Q, K, V`。对 `Q` 和 `K` 执行逐头归一化，然后计算缩放点积注意力得分 `A` 和注意力输出 `Z`。
        *   **注意力输出门控**：将 `Z` 送入一个专用的线性门控层，生成门控信号 `g`，并用 `g` 对 `Z` 进行调制得到 `O`。
        *   **输出与残差连接**：将多头门控输出 `O` 拼接后经 `Wo` 投影，再通过残差连接和Dropout。
        *   **前馈与残差连接**：将上一步结果送入SwiGLU前馈模块，再次通过残差连接和Dropout。
    3.  **聚合与分类**：将最终的精炼实例特征送入一个门控注意力池化层，计算出最终的包级别表征 `z`。
    4.  **预测**：将 `z` 送入线性分类头，输出包级别的预测。

### 3. 实验设计

论文的实验设计旨在验证模型的跨域通用性和性能优势。

*   **数据集与场景**: 涵盖了两种尺度迥异的医学影像MIL问题，共六个基准。
    *   **全切片病理图像（WSI）诊断**:
        *   **乳腺癌**: MSK数据集 (130张WSI，78位患者)。
        *   **肺癌**: LungHist700数据集 (691张图像，45位患者)。
    *   **全切片病理图像预后预测**:
        *   **前列腺癌**: Prostate Cancer数据集 (587份活检组织，213位患者，预测未来癌症发展)。
    *   **细胞级血液学**:
        *   **急性髓系白血病（AML）亚型**: AML-Hehr数据集 (129位患者，5类)。
        *   **急性早幼粒细胞白血病（APL）鉴别**: APL-AML数据集 (106位患者，2类)。
        *   **血细胞形态学**: cAItomorph数据集 (2043位患者，8类)。

*   **基准方法**: 与9种主流的MIL聚合方法进行了全面对比：ABMIL, CLAM, DSMIL, ILRA, RRT, Transformer, TransMIL, WIKG, MeanMIL。

*   **特征提取器**: 为评估泛化性，使用了多种冻结的、先进的预训练模型作为静态特征提取器，如UNI2-h, Prov-Gigapath, CONCHv1.5, UNIv1, ResNet50 (病理) 和 DinoBloom-S/L (血液学)。

### 4. 资源与算力

*   论文明确提到了实验所使用的算力资源。
*   **硬件**: 实验均在研究团队的内部服务器上完成，使用的是 **NVIDIA A100 PCIe 80 GB** GPU。
*   **总排放**: 论文估计其所有实验的总碳排放量为 **7.78 kg CO2eq**，体现了对环境影响的关注。

### 5. 实验数量与充分性

*   **实验数量庞大且充分**: 论文进行了海量实验来验证其主张。
    *   **跨域评估**: 在6个性质完全不同的数据集上进行评估，覆盖了诊断和预后，组织病理和细胞病理，强有力地证明了方法论的“域无关”特性。
    *   **多编码器评估**: 在多个数据集上使用了多达5种不同的先进特征提取器，极大排除了编码器偏差，验证了聚合器本身的提升。
    *   **多基线对比**: 对比了9种现有方法，覆盖面广。
    *   **详细消融研究**: 对QG-MIL的四个核心组件进行了细致的消融（去门控、去QKnorm、换LayerNorm、轻量版、深层版、元素级门控），清晰展示了各组件的贡献和适用场景。
    *   **算法稳定性分析**: 使用5折交叉验证，并报告测试集上的标准差，量化了模型性能的稳定性。
*   **实验客观性与公平性**: 实验设计较为公平，所有模型使用相同的训练策略（交叉熵损失、提前停止、相同的层数设定），并尽可能遵循先前研究的患者分层和数据划分。

### 6. 论文的主要结论与发现

*   **性能显著提升**: QG-MIL系列模型在所有六个基准上均优于或匹配领先的基线方法，平均宏F1分数提升达到 **+6.1个百分点**。尤其在挑战性的前列腺癌预后任务中，性能最优的变体显著优于基线。
*   **有效缓解注意力集中**: 视觉分析和量化指标（注意力质量、基尼系数、熵）均证实，QG-MIL能产生更均匀、分散的注意力分布，避免了ABMIL等方法出现的“注意力汇”现象，且无需任何辅助熵损失。
*   **架构选择与数据规模相关**: 消融实验揭示了一个关键发现：最优架构复杂度与训练集规模（患者队列大小）密切相关。
    *   **小数据集**（如肺癌, APL-AML）：更宽松的变体（如无门控版本 QG-MIL no Gate）往往表现更好，因为严格的约束可能引入过多的方差。
    *   **大数据集**（如乳腺癌, cAItomorph）：完整的门控架构和更深的网络（QG-MIL Deep）能充分发挥优势，取得最佳性能。
*   **设计优越性**: QG-MIL的整体设计在跨域实验中提供了**最一致的高性能和最低的方差**，证明了其优于单独使用某个组件的方案。

### 7. 优点

*   **方法论创新**: 首次将大语言模型中的门控归一化思路系统地引入并适配到MIL聚合器设计中，给出了一个优雅的、结构性的解决方案，而非增加训练技巧。
*   **域无关性强**: 成功地在组织病理和细胞血液学两种尺度、诊断和预后两种任务上验证了有效性，体现了极高的通用性，这是该方法的一大亮点。
*   **实验设计严谨且全面**: 多任务、多数据集、多特征编码器、多基线、细致的组件消融以及稳定性分析，构成了一个强大的证据链，结论非常可靠。
*   **可解释性提升**: 显著改善了注意力的分布质量，使模型关注点更平滑、更具临床意义，有利于建立医生对AI辅助诊断的信任。
*   **实用性强**: 作为即插即用的聚合器，计算开销在可接受范围内（优于RRT和TransMIL），易于集成到现有流程中。代码开源，支持复现。

### 8. 不足与局限

*   **小数据集上的变体选择**: 论文自身发现全门控架构在小数据集上并非最优，需要根据数据规模谨慎选择变体（如no Gate），这降低了模型作为“一键式”通用解决方案的便利性。
*   **训练开销增加**: 尽管推理效率尚可，但细粒度门控和SwiGLU模块会增加训练期间的内存占用和时间成本。
*   **超参数未完全优化**: 论文为公平对比，有意未进行详尽的超参数调优。这意味着所报告的绝对性能可能并非上限。
*   **仅限于图像模态**: 研究仅验证了图像领域的MIL，对于多模态临床数据（如基因组学、临床报告）的泛化能力尚未可知。作者也在“未来工作”中指出了这一点。
*   **研究范围**: 研究限于分类和预后任务，对于目标检测、分割等其他MIL任务形式的适用性有待探索。

（完）
