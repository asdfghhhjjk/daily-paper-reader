---
title: "RaLMPH: Reliability-aware Learning for Multi-Pathologist Harmonization in Whole-Slide Image Classification"
title_zh: RaLMPH：面向全切片图像分类的多病理学家协调的可靠性感知学习
authors: "Sungrae Hong, Jiwon Jeong, Soeun Cheon, Donghee Han, Sol Lee, Jisu Shin, Kyungeun Kim, Mun Yong Yi"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15554v2"
tags: ["query:wsi-mil"]
score: 9.0
evidence: 基于多实例学习的全切片图像多病理学家标注协调
tldr: 全切片图像（WSI）分类通常假设单一金标准，但临床中多位病理学家标注常存在分歧，现有MIL方法难以应对。提出RaLMPH框架，引入可靠性场联合建模局部特征结构与专家不确定性，实现逐样本标注者排序与自适应标签融合。在六位病理学家标注的临床数据集及模拟基准上，RaLMPH一致优于现有方法。该工作为多标注者WSI分析提供了有效的标签协调方案，提升了MIL性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15554v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1261, \"height\": 442}, {\"url\": \"assets/figures/arxiv/2606.15554v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1264, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2606.15554v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1263, \"height\": 239}, {\"url\": \"assets/figures/arxiv/2606.15554v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1266, \"height\": 175}, {\"url\": \"assets/figures/arxiv/2606.15554v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1263, \"height\": 175}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15554v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1259, \"height\": 1014}]"
motivation: 现有MIL方法忽视病理学家间标注变异性，无法有效处理WSI多标注者场景下的局部诊断分歧。
method: 构建可靠性场联合建模WSI局部邻域结构和专家熵，通过逐样本局部标注者排序与自适应门控融合可信意见。
result: 在六位病理学家标注的临床数据和模拟基准上，RaLMPH一致超越现有方法，显著提升标签协调与MIL分类性能。
conclusion: 可靠性感知的局部标签协调机制能有效整合多病理学家标注信息，提高WSI分类准确度和鲁棒性。
---

## 摘要
多实例学习（MIL）是全切片图像（WSI）分析的标准范式，并在计算病理学中取得了显著成果。然而，大多数MIL流程假设每张切片只有一个“黄金”标签，这与临床实践中常见的病理学家间显著差异相矛盾。现有的多标注者学习和标签细化方法通常估计全局标注者可靠性或依赖于单一实例假设，这使得它们不适合MIL以及专家意见不一致的局部诊断情境。我们提出RaLMPH（面向多病理学家协调的可靠性感知学习），一个基于MIL的标签协调框架，用于由多位病理学家标注的WSI。RaLMPH引入了一个可靠性场，该场联合建模了（i）WSI特征空间中的局部邻域结构和（ii）专家不确定性（熵），从而能够针对每个样本识别可信的参考邻域。利用这个场，RaLMPH对每个样本进行局部标注者排序，以选择每张切片的可靠意见，并应用自适应门控机制，根据局部可靠性融合标签。在具有六位病理学家标签的临床WSI数据集以及受控模拟基准上的实验表明，RaLMPH始终优于现有方法。进一步的分析阐明了我们的可靠性感知机制如何改善标签协调和下游MIL性能。

## Abstract
Multiple Instance Learning (MIL) is a standard paradigm for Whole-Slide Image (WSI) analysis and has achieved strong results in computational pathology. However, most MIL pipelines assume a single "gold" label per slide, which conflicts with clinical practice where substantial inter-pathologist variability is common. Existing multi-annotator learning and label-refinement methods typically estimate global annotator reliability or rely on single-instance assumptions, making them poorly suited to MIL and to localized diagnostic contexts where experts disagree. We propose RaLMPH (Reliability-aware Learning for Multi-Pathologist Harmonization), a MIL-based label reconciliation framework for WSIs annotated by multiple pathologists. RaLMPH introduces a reliability field that jointly models (i) local neighborhood structure in WSI feature space and (ii) expert uncertainty (entropy), enabling per-sample identification of trustworthy reference neighborhoods. Leveraging this field, RaLMPH performs sample-wise local annotator ranking to select reliable opinions per slide and applies an adaptive gating mechanism to fuse labels conditioned on local reliability. Experiments on a clinical WSI dataset with labels from six pathologists, as well as controlled simulated benchmarks, show that RaLMPH consistently outperforms existing approaches. Further analyses clarify how our reliability-aware mechanism improves label reconciliation and downstream MIL performance.

---

## 论文详细总结（自动生成）

好的，请查收以下基于您提供的论文内容生成的结构化深度分析总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：在计算病理学中，全切片图像（WSI）分析通常采用多实例学习（MIL）范式，但其前提是每张WSI拥有一个单一的“金标准”标签。这与临床实践中病理学家之间常存在显著诊断分歧的现实严重脱节。如何有效整合和协调多位专家提供的、可能矛盾的标注信息，以训练出健壮的MIL模型，是一个尚未解决的挑战。
*   **研究动机与背景**：
    *   **临床现实的挑战**：WSI诊断的模糊性、病理学家的经验和培训差异导致了标注间的分歧，这不被视为简单的“噪声”，而是疾病复杂性的体现。要求专家们对海量数据进行协商达成共识在临床实践中是不可行的。
    *   **现有方法的局限性**：
        *   **传统方法**：（如多数投票、软标签）虽直观，但对不可靠的标注者不鲁棒。
        *   **全局可靠性方法**：假设特定标注者整体上（不）可靠，但这与病理专家在特定诊断点具有专业权威的现实不符，忽视了局部专业性的波动。
        *   **单实例假设**：许多先进的标签细化方法依赖于单实例数据点的假设，无法直接应用于包含数千个图像块（实例）的WSI“包”。
*   **整体含义**：本文旨在提出一种全新的、符合病理学临床实践的标签协调框架，它不再追求全局统一的标注者可靠性，而是动态地、针对每个样本，在局部邻域内识别出最值得信赖的专家意见并进行融合，从而提升MIL模型的性能和临床实用性。

### 2. 论文提出的方法论

本方法的核心思想是：**标签分歧不仅源于主观差异，也源于样本本身的固有属性**。因此，提案框架`RaLMPH`通过**可靠性场**和**基于局部可靠专家的门控标签融合**两大机制来解决此问题。

*   **核心组件一：可靠性场**
    *   **目标**：联合建模样本（WSI）的模糊性和专家意见的不一致性，以此定义一个更精确的样本间距离度量。
    *   **技术细节**：
        1.  **特征提取**：使用预训练的特征提取器将WSI转换为实例特征包，并进一步通过一个WSI级特征提取器`h(·)`得到每个WSI的全局表征 `˜zₙ`。
        2.  **不确定性度量**：
            *   **标签熵**：计算一张WSI的多位专家软标签的平均值的熵 `H(¯sₙ)`，衡量专家共识度。
            *   **样本方差**：通过对特征包`zₙ`多次应用特征Dropout来模拟并计算方差 `σ²ₙ`，衡量样本自身的非典型性。
        3.  **构建场距离**：定义了一个新的距离函数 `dΦ(·,·)`。该距离通过一个权重矩阵 `Mₙₘ` 进行调制，该矩阵的值与两个样本的标签熵和样本方差之和正相关。
        *   **效果**：在可靠性场`Φ`中，标签越模糊、样本越不典型的WSI，相互之间的距离被“推”得更远。这使得在特征空间中，具有高度共识的典型样本能形成更紧凑、可信的邻域。

*   **核心组件二：基于局部可靠专家的门控标签融合**
    *   **目标**：当目标样本的标签模糊时，从其在`Φ`场中的局部邻域内，识别出可靠的专家意见并进行加权融合，得到一个协调后的标签。
    *   **技术细节**：
        1.  **门控机制**：设计了一个门控值 `Gₙ`，它由目标样本自身的标签熵 `H(¯sₙ)` 和其 `K` 个邻域的平均标签熵 `¯H(¯sₖ)` 共同决定。
            *   当自身共识度高（`H(¯sₙ)` 低）时，`Gₙ` 接近1，更多地保留原始平均标签。
            *   当自身共识度低，但邻域共识度高（`¯H(¯sₖ)` 低）时，`Gₙ` 接近0.5，会更依赖邻域信息。
            *   当自身和邻域共识度都低时，`Gₙ` 约为0.73，偏向于保留原始标签。
        2.  **局部专家排序与融合**：
            *   **专家排序**：对于一个样本`zₙ`，计算其每个邻域样本的每位专家标签与邻域平均标签的余弦相似度并求和，据此对`J`位专家进行局部可靠性排名，得到列表 `Jₖⁿ`。
            *   **邻域意见聚合**：利用该排名，将邻域内每位专家的意见加权聚合成一个单一的邻域标签 `ṡₖⁿ`（排名越靠前，权重越高）。
            *   **自适应邻居选择**：为了避免过度平滑，从一个初始的`K`个邻居中，根据峰值置信度自适应地选择出一个最优子集 `K*`。
            *   **最终协调**：最终的协调标签 `ỹₙ` 由门控值 `Gₙ` 控制，是初始平均标签 `¯sₙ` 和来自 `K*` 个邻居的加权聚合标签 `ṡₖⁿ` 的动态组合。

*   **公式概览** (以文字描述)：
    *   **公式1（场距离）**: 新的距离是L2距离乘以一个权重矩阵，这个权重是两样本的熵和方差之和的指数函数。
    *   **公式2（门控）**: 门控值 `Gₙ` 是一个Sigmoid函数，其输入与 `H(¯sₙ)` 和 `¯H(¯sₖ)` 的倒数有关，实现了上述三种情况的动态切换。
    *   **公式4（标签协调）**: `ỹₙ = Gₙ × ¯sₙ + (1 − Gₙ) × Σ(Wₖ · ṡₖⁿ)`，其中 `Wₖ` 是基于场距离的softmax权重。

### 3. 实验设计

*   **数据集**:
    *   **临床数据集 (In-house)**: 包含698个WSI训练样本，由6位病理学家进行软标签标注（7个结直肠息肉类别）。另有684个由专家共识确定单一诊断的WSI作为测试集。
    *   **公开基准数据集**:
        *   **BRACS**: 547张WSI，用于乳腺癌亚型分类（7分类）。
        *   **BCNB**: 1，058张穿刺活检WSI，用于乳腺癌分子亚型预测（4分类）。
    *   **模拟专家生成**: 由于公开数据集只有单一标签，本文遵循领域惯例，通过将特征空间聚类并为每位专家在不同簇中分配不同可靠性分数（`eⱼᶜ ~ U(8,12)）`，用狄利克雷分布生成模拟的、体现局部专家水平的软标签。

*   **基准对比方法**:
    *   **基础方法**: 多数投票（MV）、软标签平均（SL）、模型集成（Ensemble）。
    *   **基于隐藏状态的方法**: GLAD。
    *   **基于深度学习架构的方法**: DL-CL。
    *   **基于数据修正/邻居的方法**: NWVNC。
    *   **基于特征引用的近期方法**: IWBVT， KFNN。
    *   **主干MIL模型**: TransMIL 和 DTFD-MIL-AFS 被用作下游分类模型，以验证方法的通用性。

### 4. 资源与算力

*   文中明确提到“**utilizing an NVIDIA ® RTX A6000**”进行实验。但未说明使用的GPU数量以及整个训练过程的耗时信息。

### 5. 实验数量与充分性

*   **实验数量**:
    1.  **主实验**: 在3个数据集（1个临床、2个公开模拟）上，分别使用2种不同的SOTA MIL骨干网络，对比了9种方法（含本文方法），总共约60组对比结果。
    2.  **消融实验**: 对RaLMPH的三个关键组件（`Φ`场、局部排序`Jₖⁿ`、自适应邻居`K*`）进行了移除实验，在全部数据集和MIL骨干网络上进行，共约18组结果。
    3.  **深入分析**:
        *   对`Φ`场和L2距离空间进行了可视化对比。
        *   测试了模型对标注专家数量从6逐步减少到2的鲁棒性（5种数量设置）。
        *   分析了不同方法的计算时间效率。
        *   对超参数`K`（邻居数）和`τ`（平滑参数）进行了敏感性分析（15种组合）。
*   **充分性与公平性**:
    *   **充分性**: 实验设计比较全面。覆盖了真实和模拟数据、多种现有方法对比、以及多个维度的消融和参数分析，能够系统地支撑论文的主张。
    *   **公平性**: 公平性较高。实验使用了固定的随机种子，对模拟的公开数据集也采用了领域内标准、同等的设定。所有聚类方法均使用相同的FAISS库实现，确保了对比的公平性。

### 6. 论文的主要结论与发现

*   **性能优越性**: `RaLMPH`在临床和模拟数据集上，针对不同的MIL主干网络，其分类性能（Accuracy/AUC）一致且显著地超越所有对比方法。
*   **组件有效性**: 消融实验证实，可靠性场（`Φ`）、局部专家排序（`Jₖⁿ`）和自适应邻居选择（`K*`）三个组件对最终性能的提升都不可或缺。
*   **可靠性场的优势**: 与传统的L2距离相比，联合建模标签熵和样本方差的可靠性场，能更好地在特征空间中组织样本，将低共识、高非典型性的样本分离，使邻域更具可信度。
*   **鲁棒性与效率**: `RaLMPH`在标注专家数量减少时表现出优异的性能稳定性，优于多数对比方法。同时，它的预处理耗时较短，且不增加模型训练开销，在计算效率上具有竞争力。

### 7. 优点（亮点）

*   **创新的问题视角**: 摒弃了传统的全局标注者可靠性假设，转向更符合病理学实践的**局部、动态的专家可靠性评估**，是该领域的一个重要观念转变。
*   **精巧的技术设计**: 提出“**可靠性场**”这一概念，将数据不确定性和标签不确定性优雅地统一在一个距离度量中，是对MIL场景下标签协调问题的独特贡献。
*   **实用的工程实现**: 门控机制和自适应邻居选择的设计，使得模型能根据具体情况灵活地决定是信任初始标签还是借鉴邻域信息，并可避免过度平滑，提升了方法的实用性。
*   **全面的实验验证**: 实验设计详尽、对比方法丰富、分析维度多元（可视化、鲁棒性、效率、敏感性），论证非常扎实。

### 8. 不足与局限

*   **专家范围的局限性**: 方法假定所有标注者均为“指定专家”，即不存在非专业的、恶意的或完全随机的标注。作者明确指出，该方法目前不适用于众包等场景。
*   **对初始聚类的依赖**: 可靠性场的构建依赖一个WSI级特征提取器（`h(·)`），其质量会影响下游的邻域选择和标签协调效果。文中虽未深入讨论，但如果WSI级特征表征能力不足，会成为一个性能瓶颈。
*   **外部专家有效性的验证不足**: 在公开数据集上模拟的专家，其“可靠性”是通过聚类预定义的，这可能无法完全反映真实世界中病理专家诊断分歧的复杂性（如某些特殊亚型的混淆）。框架在真实噪声标签下的健壮性验证尚不充分。
*   **计算细节的缺失**: 论文未说明训练时长，无法精确评估其完整的时间成本。同时，未提供方法的统计显著性检验细节（仅在表格中以符号标注，未说明具体检验方法），虽然影响不大，但也是一个可以增强严谨性的点。

（完）
