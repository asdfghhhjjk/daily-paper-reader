---
title: Rethinking the Adaptation of Vision Foundation Models for Efficient Cell Segmentation
title_zh: 重新思考用于高效细胞分割的视觉基础模型的适应
authors: "Qing Xu, Xiangjian He, Wenting Duan, Jiebo Luo, Zhen Chen"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21913v1"
tags: ["query:pathology-ai"]
score: 9.0
evidence: 不重新训练编码器的高效细胞分割框架
tldr: 细胞分割依赖视觉基础模型（VFM）但适应成本高。EffiCell-Seg冻结编码器，利用预设VFM的全局显著性与局部形态先验，通过CSP-Encoder合成显式结构先验图，并用SM-Decoder联合预测几何距离场和语义图。仅5M可训练参数（比全微调少130倍），在多种细胞成像模态上超越SOTA。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21913v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1142, \"height\": 329, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21913v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1190, \"height\": 503, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21913v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1142, \"height\": 320, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21913v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1270, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21913v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1267, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21913v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 319, \"label\": \"Table\"}]"
motivation: 解决VFM在细胞分割中资源密集型适应问题，避免大规模标注和计算开销。
method: 冻结VFM编码器，CSP-Encoder从冻结表示合成语义显著性和形态先验，SM-Decoder通过互引导联合预测几何与语义。
result: 仅5M可训练参数，性能优于全微调方法，参数减少超130倍，在多个数据集上达到SOTA。
conclusion: 展示了冻结VFM高效适应细胞分割的潜力，大幅降低计算与标注依赖，支持多种成像模态。
---

## 摘要
细胞分割对于计算病理学和生物医学发现至关重要。尽管最近的视觉基础模型（VFM）展示了卓越的通用特征表示，但受限于资源密集型的适应范式，其在细胞成像中的潜力尚未充分发挥。现有方法通常依赖于微调重型视觉编码器，导致巨大的计算开销和对大规模标注的依赖。为解决这一问题，我们提出了EffiCell-Seg框架，用于在不重新训练视觉编码器的情况下实现高效细胞分割。我们的核心见解是，预训练的VFM内在编码了互补的结构先验：用于定位潜在细胞的全局显著性，以及用于描绘细胞结构的局部形态模式。为了利用这些先验，我们设计了一个细胞结构提示编码器（CSP-Encoder），它从冻结的VFM表示中合成语义感知的显著性和主要形态特征，形成显式结构先验图。此外，我们提出了一种协同掩码解码器（SM-Decoder），通过联合预测几何距离场和语义图并相互交叉指导，强制执行上下文一致性。大量实验表明，EffiCell-Seg在多种细胞成像模态中优于最先进的方法，同时仅需约5M可训练参数，比完全微调的VFM对应方法少130倍以上。代码可在https://github.com/xq141839/EffiCell-Seg获取。

## Abstract
Cell segmentation is critical for computational pathology and biomedical discovery. While recent Vision Foundation Models (VFMs) have demonstrated remarkable universal feature representations, unlocking their full potential for cellular imaging is currently bottlenecked by resource-intensive adaptation paradigms. Existing methods typically rely on fine-tuning heavy visual encoders, leading to extensive computational overhead and a dependency on large-scale annotations. To address this, we propose the EffiCell-Seg framework for highly efficient cell segmentation without re-training the visual encoder. Our core insight is that pretrained VFMs intrinsically encode complementary structural priors: global saliency for localizing potential cells, and local morphological patterns for delineating cellular structures. To harness these priors, we devise a Cell Structure Prompt Encoder (CSP-Encoder) that synthesizes semantic-aware saliency and principal morphological features from frozen VFM representations into explicit structural prior maps. Moreover, we propose a Synergistic Mask Decoder (SM-Decoder) that enforces contextual consistency by jointly predicting geometric distance fields and semantic maps via mutual cross-guidance. Extensive experiments demonstrate that EffiCell-Seg outperforms state-of-the-art methods across diverse cell imaging modalities while requiring only ~5M trainable parameters, over 130x fewer than fully fine-tuned VFM counterparts. The code is available at https://github.com/xq141839/EffiCell-Seg.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：细胞分割是计算病理学和生物医学发现的关键任务，但面临成像模态多样、细胞形态异质等挑战。尽管视觉基础模型（VFM，如DINOv3）在自然图像上预训练后具有强大的通用表示能力，但将其应用于细胞成像时，传统适应范式（如全微调编码器）需要大量计算资源和像素级标注，成本高昂。参数高效微调（如Adapter、LoRA）虽减少了参数量，但仍需重新训练部分编码器特征，依赖大规模标注数据。
- **整体含义**：作者发现预训练的VFM本身已经编码了适用于细胞分割的**互补结构先验**（全局显著性用于定位细胞，局部形态模式用于描绘边界），因此提出**不需要重新训练视觉编码器**，而是通过重新对齐这些先验知识来实现高效、准确的细胞分割。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 冻结VFM编码器（DINOv3），保留其预训练的通用结构知识。通过两个轻量模块（CSP-Encoder和SM-Decoder）提取并融合VFM内部的全局显著性先验和局部形态先验，生成结构先验图，进而联合预测几何距离场和语义图，实现细胞实例分割。

### 关键技术细节
- **CSP-Encoder（细胞结构提示编码器）**：
  - 利用VFM输出的CLS token和空间token分别提取全局和局部先验。
  - **全局显著性**：计算CLS token与每个空间token的余弦相似度，生成显著性图α，突出细胞区域。
  - **局部形态特征**：对空间token进行PCA降维，保留前K个主成分，得到形态特征图m，编码边界、纹理等细节。
  - 融合：逐元素相乘 \( z_i = a_i \cdot m_i \)，再通过轻量卷积得到提示嵌入z，实现空间门控。
- **SM-Decoder（协同掩码解码器）**：
  - 初始化两组可学习查询：几何查询\( q_g \)和语义查询\( q_s \)。
  - 通过两路Transformer将冻结VFM特征与提示嵌入对齐，初始化几何和语义表示。
  - **交叉引导注意力**：在几何和语义表示之间双向交互，确保几何精度与语义置信度相互增强。
  - 最终输出水平/垂直距离场和语义图。
- **优化**：联合损失（交叉熵+Dice损失用于语义，MSE用于距离场），推理时用分水岭算法分离实例。
- **参数量**：仅~5M可训练参数（CSP-Encoder + SM-Decoder），VFM编码器完全冻结。

## 3. 实验设计

- **数据集**：
  - **CellSeg**：多模态显微镜图像1101张，涵盖多种细胞类型（如浆细胞、白细胞）。
  - **DSB**（2018 Data Science Bowl）：670张细胞图像。
  - 均按7:1:2划分训练/验证/测试。
- **Benchmark**：语义分割（Dice, mIoU）和实例分割（PQ, F1）。
- **对比方法**：
  - 经典方法：CPPNet, Swin-UMamba。
  - VFM基方法：CellSAM, PromptNucSeg, CellViT, SAC。
  - 所有基线均在相同设置下微调或使用自动提示模式。
- **公平性**：同一输入分辨率（512×512），统一评估标准。

## 4. 资源与算力

- **文中明确说明**：
  - GPU：NVIDIA H20 ×1。
  - 框架：PyTorch。
  - 训练超参：AdamW优化器，学习率1e-4，CosineAnnealingLR调度，批量大小16，训练100轮。
  - 未明确报告训练总时长或显存占用。
- **提示**：由于VFM编码器冻结，仅训练轻量模块，实际算力需求远低于全微调方法（参数量减少130倍以上）。

## 5. 实验数量与充分性

- **实验组数**：
  - 两组主要数据集（CellSeg、DSB）上的语义分割和实例分割对比（表1、表2）。
  - 一组消融实验（表3），包含5种配置（baseline、仅添加相似性图、仅添加PCA图、CSP-Encoder完整、完整框架），在两个数据集上均报告Dice/mIoU/PQ/F1。
  - 定性结果展示（图3）。
- **充分性评价**：实验设计较为充分——覆盖不同模态、不同细胞类型；对比了3类方法（经典、全微调VFM、高效微调VFM）；消融实验验证了各组件的贡献。但未见跨数据集泛化测试（如仅在一个数据集上训练，在另一个上测试等），也未见与更多最新高效适应方法（如其他LoRA变体）的对比。总体而言，实验数量足够，公平性较好（统一设置）。

## 6. 论文的主要结论与发现

- EffiCell-Seg在两个基准数据集上的语义分割和实例分割均达到最高Dice、mIoU、PQ、F1，显著优于所有对比方法。
- 仅需~5M可训练参数，比完全微调的CellSAM少130倍以上，比参数高效微调的SAC少约10倍。
- 核心发现：**冻结VFM编码器，仅通过轻量模块重新对齐其固有的结构先验，即可高效实现高精度细胞分割**，证明了“重新对齐先验知识”比“重新训练特征表示”更有效。

## 7. 优点

- **方法创新性**：首次提出完全冻结VFM编码器进行细胞分割，避免大规模标注和昂贵计算；基于对CLS token和空间token的深入分析，巧妙利用其互补先验。
- **高效性**：仅5M可训练参数，推理/训练开销极低，适合实际部署。
- **性能优异**：在多种成像模态下超越SOTA，特别是对粘连细胞、低对比度区域的分割更精确。
- **设计清晰**：CSP-Encoder和SM-Decoder模块化，消融充分，贡献明确。
- **代码开源**：提升可复现性。

## 8. 不足与局限

- **实验覆盖有限**：
  - 只测试了两个数据集（CellSeg和DSB），未涉及更多临床或病理数据集（如TCGA等）。
  - 未进行跨数据集泛化实验（如一个数据集训练，另一个直接测试）。
  - 未与其他基于SAM或DINOv3的微调方法（如微调Adapter或LoRA的具体变体）进行综合比较。
- **潜在偏差风险**：
  - CSP-Encoder中PCA的K值选择未说明如何确定，可能对特定数据集敏感。
  - 距离场和语义图的联合预测是否在所有模态下都优于独立预测？
  - 未讨论当VFM预训练数据与细胞图像差异极大时（例如低对比度荧光成像），先验质量是否会下降。
- **应用限制**：
  - 依赖VFM（DINOv3）的固定表示，若VFM版本更新或更换，可能需要重新设计CSP-Encoder。
  - 文中未分析在不同细胞密度下的性能稳定性。
  - 只做了细胞分割，未扩展到其他医学任务（如器官、病变分割）。

（完）
