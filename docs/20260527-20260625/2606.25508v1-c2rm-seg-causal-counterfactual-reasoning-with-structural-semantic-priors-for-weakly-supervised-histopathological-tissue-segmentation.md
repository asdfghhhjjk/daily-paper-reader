---
title: "C2RM-Seg: Causal Counterfactual Reasoning with Structural-Semantic Priors for Weakly Supervised Histopathological Tissue Segmentation"
title_zh: C$^2$RM-Seg：基于结构-语义先验的因果反事实推理用于弱监督组织病理学组织分割
authors: "Hualong Zhang, Siyang Feng, Zihan Huan, Yi Qian, Zhenbing Liu, Rushi Lan, Xipeng Pan"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25508v1"
tags: ["query:wsi-mil"]
score: 9.0
evidence: 通过因果类激活图优化的弱监督组织病理学组织分割，识别判别区域
tldr: 组织病理学图像分割中，弱监督方法常依赖CAM生成伪标签，但CAM易受染色驱动外观线索干扰，忽略真实组织形态，导致定位不准和结构一致性差。为此，本文提出C2RM-Seg，一个两阶段弱监督分割框架。首先在分类阶段引入因果反事实推理模块(C2RM)，通过分解特征为潜在因子并进行反事实干预，抑制混杂背景，生成形态对齐的CAM。然后设计双路径结构-语义分割网络，结合ResNeSt的细粒度结构信息和冻结的DINOV3全局语义先验，利用跨路径门控机制自适应注入语义以保持边界。还提出不确定度门控边界损失(UGM)来应对残留伪标签噪声。在两个公开组织病理数据集上，C2RM-Seg取得最新最优性能，展示了因果推理与结构先验融合的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25508v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1265, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25508v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1263, \"height\": 534, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25508v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1121, \"height\": 507, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25508v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 540, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25508v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1238, \"height\": 494, \"label\": \"Table\"}]"
motivation: 弱监督组织分割中CAM生成的伪标签常受染色外观干扰，忽略因果组织形态，导致定位噪声和结构不一致。
method: 提出C2RM-Seg框架，通过因果反事实推理模块抑制混杂并生成形态对齐CAM，结合双路径结构-语义网络与UGM损失进行分割。
result: 在两个公共组织病理数据集上实验表明，C2RM-Seg性能达到最新最优，有效提升了分割准确性。
conclusion: C2RM-Seg融合因果推理与结构-语义先验，为弱监督组织分割提供了有效新方案，缓解了伪标签噪声问题。
---

## 摘要
组织病理学组织分割对于计算机辅助诊断至关重要，然而弱监督方法常常受到类激活映射（CAM）生成的噪声伪标签影响。现有的CAM方法倾向于关注染色驱动的外观线索，而非真正的因果组织形态，导致定位虚假且结构一致性差。为解决此问题，我们提出了C$^2$RM-Seg，一个两阶段框架，它整合了因果伪标签精炼与结构感知语义增强。在分类阶段，我们引入了因果反事实推理模块（C$^2$RM），该模块将特征分解为潜在因子，并通过学习的因果结构矩阵执行反事实干预，抑制混杂上下文，生成形态对齐的CAM。在分割阶段，我们设计了一种双路径结构-语义架构，结合了来自ResNeSt的细粒度结构特征与来自冻结的DINOV3基础模型的全局语义先验。跨路径门控机制利用局部结构线索自适应调节语义注入，以保持边界保真度。为进一步减轻残留的伪标签噪声，我们提出了不确定性门控边界（UGM）损失，该损失根据预测不确定性动态平衡边界强化与置信度学习。在两个公开的组织病理学组织数据集上的大量实验表明，C$^2$RM-Seg达到了最先进的性能。

## Abstract
Histopathological tissue segmentation is essential for computer-aided diagnosis, yet weakly supervised methods often suffer from noisy pseudo-labels generated by Class Activation Mapping (CAM). Existing CAM approaches tend to focus on staining-driven appearance cues rather than true causal tissue morphology, resulting in spurious localization and poor structural consistency. To address this issue, we propose C$^2$RM-Seg, a two-stage framework that integrates causal pseudo-label refinement with structure-aware semantic enhancement. For classification, we introduce a Causal Counterfactual Reasoning Module (C$^2$RM) that decomposes features into latent factors and performs counterfactual intervention via a learned causal structure matrix, suppressing confounding context and producing morphology-aligned CAMs. For segmentation, we design a Dual-Path Structural-Semantic Architecture that combines fine-grained structural features from ResNeSt with global semantic priors from a frozen DINOV3 foundation model. A cross-path gating mechanism adaptively regulates semantic injection using local structural cues to preserve boundary fidelity. To further mitigate residual pseudo-label noise, we propose an Uncertainty-Gated Margin (UGM) loss, which dynamically balances margin enforcement and confidence learning based on prediction uncertainty. Extensive experiments on two public histopathological tissue datasets show that C$^2$RM-Seg achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：弱监督语义分割（WSSS）在组织病理学中常用类激活图（CAM）生成伪标签，但CAM极易受染色差异、扫描参数等与诊断无关的“风格”因素干扰，激活区域偏向外观纹理而非真实的组织形态，造成定位漂移与结构碎片化。
- **研究动机**：现有方法大多通过后处理或重加权策略抑制噪声，但未从根本上去除这些混杂（confounding）因素的因果影响，导致伪标签系统性地偏离真实形态。
- **整体含义**：论文提出C$^2$RM-Seg，通过引入结构因果模型（SCM）对特征进行反事实干预，去除混杂效应，再结合基础模型的结构‑语义先验，从伪标签生成到分割训练全链条提升弱监督组织分割的准确度与边界质量。

### 2. 论文提出的方法论
C$^2$RM‑Seg 是一个两阶段弱监督分割框架。

- **第一阶段：因果反事实伪标签生成**
  - **因果反事实推理模块（C$^2$RM）**：将编码器特征投射到K个潜在因子（K=8），计算全局因子统计量 $\mathbf{z}_k$。学习一个因果结构矩阵 $\mathbf{A}$（对角置零，行softmax），估计每个因子的父节点贡献 $\mathbf{E}_i = \sum_{j \neq i} A_{i,j} \mathbf{z}_j$。执行反事实干预，得到纯净因子 $\mathbf{z}^{cf} = \mathbf{z} - \mathbf{A}\mathbf{z}$，从而减去由染色、背景等混杂造成的成分。
  - **空间重校正**：将 $\mathbf{Z}^{cf}$ 广播回空间维度，通过门控残差 $\hat{\mathbf{X}} = \mathbf{X} + \gamma \cdot(\mathbf{G} \odot \mathbf{X})$ 重新校准特征，生成形态对齐的CAM种子，进而得到高质量伪掩码。

- **第二阶段：双路径结构‑语义分割网络**
  - **结构分支**：采用ResNeSt‑200e + 金字塔池化提取细粒度局部形态特征 $\mathbf{F}^S$。
  - **语义分支**：使用冻结的DINOV3（ViT‑B/16）提取全局语义特征，经过可学习的轻量适配器和上采样后得到 $\mathbf{F}^V$。
  - **跨路径门控融合**：利用结构特征生成空间门控图 $\mathbf{G} = \sigma(\Psi(\mathbf{F}^S))$，对语义特征进行空间调制：$\hat{\mathbf{F}} = \mathbf{F}^S + \mathbf{G} \odot \mathbf{F}^V$，确保语义信息只在结构显著区域注入，保护组织边界。
  - **不确定性门控边界损失（UGM）**：对每个像素计算边界违反值 $M_i$ 和置信度惩罚 $C_i$，以动态权重 $\alpha_i = 1/(1+e^{-\kappa(u_i-0.5)})$ （$u_i$ 为预测不确定性）将两者结合，在确定区域强制增大边界，在不确定区域退化为保守的置信度学习，有效应对伪标签噪声。总损失为 $\mathcal{L} = \frac{1}{|\Omega|}\sum_{i\in\Omega}[\alpha_i C_i + (1-\alpha_i)M_i]$。

### 3. 实验设计
- **数据集**：
  - **LUAD‑HistoSeg**：肺腺癌，54张全切片图像（WSI），4类组织（TE, TAS, NEC, LYM），训练/验证测试分别为16,678/607个图块（训练仅图像级标签）。
  - **BCSS‑WSSS**：乳腺癌，151张WSI，4类组织（TUM, STR, LYM, NEC），训练/验证测试分别为23,422/8,404个图块。
- **对比方法**：涵盖了全监督基础模型（SAM, SAM2, MedSAM均为提示式）、经典WSSS方法（TPRO, HAMIL, MLPS, SIPE, ARML, OEEM）以及最新的弱监督方法（ESFAN）。
- **评估指标**：mIoU、bIoU（边界IoU）、HD95。结果均报告5次独立运行的标准差。

### 4. 资源与算力
- **硬件**：文中仅提及使用NVIDIA RTX 3090（未指明数量，推测为单卡）。训练总耗时未明确说明，但提供了推理效率：191.47 GFLOPs，速度86.08 FPS。
- **训练配置**：分类阶段使用ResNet38d训练20个epoch，分割阶段ResNeSt用SGD、DINOV3适配器用AdamW。未提供详细的训练时长。

### 5. 实验数量与充分性
- **主要对比实验**：在两个数据集上，与12种以上的方法进行定量对比（表1），包含大量最新SOTA，对比公平、完整。
- **消融实验**：在LUAD‑HistoSeg上对C$^2$RM、双路径架构、UGM损失三模块进行完整的组合消融（8组实验，表2），分析各模块及交互作用。
- **可视化分析**：提供分割结果定性对比（图2）和t‑SNE特征分布（图3），直观支持定量结论。
- 实验覆盖广泛、设计系统，可充分支撑论文主张。

### 6. 论文的主要结论与发现
- **因果去混杂有效**：C$^2$RM通过反事实干预显著抑制染色等混杂，使特征空间更加紧凑、类间分离更明显，生成的伪标签更贴合真实组织形态。
- **结构‑语义协同**：双路径架构以结构引导基础模型语义的注入，既保留了细粒度边界，又增强了整体语义一致性。
- **噪声鲁棒优化**：UGM损失利用不确定性动态调节边界约束，有效抑制残余伪标签噪声，极大改善边界指标（HD95）。
- **整体性能领先**：框架在两个基准上均取得最优mIoU和HD95，超越所有对比的弱监督方法，且效率可接受。

### 7. 优点
- **理论创新**：首次将结构化因果反事实推理引入弱监督组织分割，从根本上解决染色‑外观混杂问题，思路新颖且具有解释性。
- **架构设计精巧**：双路径门控融合冻结基础模型的方式，既利用了大规模预训练的先验，又避免了微调带来的过拟合，边界保持巧妙。
- **损失函数自适应**：UGM损失无需复杂调节，能根据预测不确定性平滑切换优化目标，实用且高效。
- **实验扎实**：对比方法全面（包含大模型提示式分割），消融充分，分析可视化，代码开源，可信度高。

### 8. 不足与局限
- **因果模型简化**：因子交互采用线性SCM近似，现实中的混杂关系可能更复杂非线性，模型容量有限。
- **基础模型适配性**：DINOV3完全冻结，仅通过适配器投影，可能无法充分挖掘其在病理域的潜力，依赖预训练数据的分布。
- **数据广度有限**：仅在两个肿瘤类型（肺腺癌、乳腺癌）上验证，对其他器官、炎症或非肿瘤病变的泛化能力未经验证。
- **训练与部署细节缺失**：未报告训练时长、多卡扩展性以及处理完整WSI的全局上下文整合方式。
- **两阶段解耦**：伪标签生成与分割训练分开，未能实现端到端联合优化，可能限制整体性能上限。

（完）
