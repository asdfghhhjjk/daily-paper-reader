---
title: Aligning Cellular Sheaves with Classifier Attention for Interpretable Weakly-Supervised Pathology Localization
title_zh: 对齐细胞层与分类器注意力以实现可解释的弱监督病理定位
authors: "Devansh Lalwani, Swapnil Bhat, Maulik Shah"
date: 2026-05-24
pdf: "https://arxiv.org/pdf/2606.00092v1"
tags: ["query:wsi-mil"]
score: 9.0
evidence: 基于注意力MIL的弱监督全切片定位
tldr: 弱监督全切片图像分类中，基于注意力的多实例学习虽性能饱和，但注意力图常偏离病灶，降低临床可信性。本文引入细胞层（cellular sheaves）建模图结构局部不一致性，并提出注意力条件一致性训练，联合优化分类器和投影器。在Camelyon16上，注意力定位AUC从0.717提升至0.953，补丁级不一致性场AUC达0.940；无需微调即迁移至Camelyon17。该方法生成注意力图和细胞层不一致性图双重解释，增强模型在病理定位中的可信度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00092v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1596, \"height\": 1682}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00092v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1193, \"height\": 240}]"
motivation: 现有弱监督病理定位中，注意力图无法一致激活病灶区域，导致模型分类正确但定位不可信，临床解释性不足。
method: 提出注意力条件一致性损失，使细胞层不一致性场仅在注意力高的邻域内追求特征一致，联合训练分类器和投影器。
result: 在Camelyon16上，注意力和不一致性场AUC分别达0.953和0.940；二阶段消融表明增益源于联合训练。
conclusion: 细胞层与注意力联合训练产生互补的可解释定位信号，显著提升弱监督病理模型的临床可信度与泛化性。
---

## 摘要
基于注意力机制的多实例学习（ABMIL）在基础特征之上对全切片图像进行弱监督分类，目前在Camelyon16切片级别性能上已接近饱和，但对应的注意力图作为定位信号并不完美：在临床解释中，一个能正确分类但未对实际病灶产生激活的模型难以让人信任。我们通过细胞层来弥补这一差距，细胞层为图的每个顶点和边赋予有限维向量空间以及它们之间一致的线性映射，提供了一种在图结构数据上检测局部不一致性的原则性方法。我们将细胞层应用于全切片图像上的弱监督肿瘤定位，将层不一致场与ABMIL相结合。自然的训练目标鼓励相似特征之间的一致性，会产生一个追踪组织级纹理而非诊断内容的不一致场。我们提出注意力条件一致性，利用分类器的注意力来定义哪些相邻小块应该一致。在该目标下联合训练分类器和层，在Camelyon16上产生的小块级AUC为0.940的不一致场，并将注意力头从仅ABMIL时的0.717提升到0.953。两阶段消融实验将分类器冻结在ABMIL值，不一致场仅达到0.727，注意力保持0.717，证实增益来自投影器在两个目标下的共同适应，而非单独损失变化。训练好的模型无需重新训练即可迁移到Camelyon17的标注切片，保持Delta AUC 0.932±0.083和注意力AUC 0.955±0.099。结果是注意力图和层不一致图在相同的诊断区域激活，为临床医生提供每个切片级别预测的两个互补解释。

## Abstract
Weakly-supervised classification of whole-slide images with attention-based multiple instance learning (ABMIL) on top of foundation features now reaches near-saturation on Camelyon16 slide-level performance, but the corresponding attention maps are an imperfect localization signal: in clinical interpretation, a model that classifies correctly without firing on the actual lesion is hard to trust. We address this gap with cellular sheaves, which equip each vertex and edge of a graph with a finite-dimensional vector space and consistent linear maps between them, providing a principled way to detect local disagreement on graph-structured data. We apply cellular sheaves to weakly-supervised tumour localization on whole-slide images, combining a sheaf disagreement field with ABMIL. The natural training objective, encouraging consistency between similar features, produces a disagreement field that tracks tissue-level texture rather than diagnostic content. We propose attention-conditional consistency, which uses the classifier's attention to define which neighbouring patches should agree. Joint training of the classifier and the sheaf under this objective produces a disagreement field with patch-level AUC 0.940 on Camelyon16 and raises the attention head from its ABMIL-alone level of 0.717 to 0.953. Two-stage ablation with the classifier frozen at its ABMIL values reaches only 0.727 on the disagreement field and leaves attention at 0.717, confirming that the gain comes from the projector co-adapting under both objectives, not from the loss change in isolation. The trained model transfers without retraining to annotated slides from Camelyon17, maintaining Delta AUC 0.932 +/- 0.083 and attention AUC 0.955 +/- 0.099. The result is an attention map and a sheaf-disagreement map that fire on the same diagnostic regions, giving clinicians two complementary explanations for each slide-level prediction.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：在弱监督全切片图像（WSI）分类中，基于注意力机制的多实例学习（ABMIL）在切片级别性能已接近饱和（Camelyon16上约0.95 AUC），但对应的注意力图作为定位信号不完美——模型可能分类正确却未在病灶区域激活，降低了临床可信度。
- **动机**：病理医生需要知道模型为何判定阳性：哪个区域触发了预测、模型局部置信度如何。现有ABMIL注意力仅依赖单个小块的特征，缺乏对空间邻域关系的显式建模。肿瘤区域具有空间连贯性，边界是最具诊断信息的位置，因此需要一种能显式建模邻域一致性的定位信号。
- **切入点**：引入**细胞层（cellular sheaves）**——一种为图顶点和边分配向量空间及线性映射的拓扑框架，可自然检测图结构上的局部不一致性，并可作为可学习的定位信号。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 构建小块图（patch graph），每个顶点对应一个组织小块（128μm物理分辨率），边连接空间最近邻（k=8）。
- 为每个顶点和边分配有限维向量空间（顶点：256维；边：64维），并通过低秩映射（秩r=8）定义限制映射。
- 利用**层拉普拉斯算子**计算每个顶点的**不一致性值**Δv，作为额外的定位信号。
- 关键创新：提出**注意力条件一致性**训练目标，使层不一致性场与分类器的注意力对齐，而非简单的特征相似性。

### 技术细节
1. **图构建与层定义**：
   - 顶点：UNI提取的1024维特征经投影为256维。
   - 边：8个空间最近邻。
   - 限制映射：由超网络（hypernetwork）基于两个端点特征和空间偏移生成，采用低秩因子分解（A∈ℝ^{64×8}, B∈ℝ^{256×8}），减少参数量并强制比较仅沿少数方向进行。
   - 层拉普拉斯：L_F = δᵀδ，Δv = ∥(L_F x)ᵥ∥²。

2. **注意力条件一致性损失**：
   - 将分类器注意力向量a（detached）用于定义边的“一致性”目标：
     - 低差异边（|aᵤ - aᵥ|最低30%）：视为一致，最小化∥δₑ∥²。
     - 高差异边（最高30%）：视为不一致，鼓励∥δₑ∥²超过一个间隔（margin）。
   - 注意力被detached，梯度不反向传播到分类器头，但投影器同时接收分类和目标一致性梯度。

3. **联合训练**：
   - 模型包含共享投影器、分类器头（gated attention + 分类logit）和层头（计算不一致场）。
   - 损失：分类交叉熵 + 注意力条件一致性损失 + 弱正交正则化项。
   - 优化：AdamW，学习率1e-4，权重衰减1e-5，batch size=1 slide，梯度裁剪1.0，40个epoch，前5个epoch为分类器预热。

## 3. 实验设计

### 数据集
- **Camelyon16**：训练用270张切片（110肿瘤，160正常），测试用129张（49肿瘤，80正常）。
- **Camelyon17**：外部验证集，50张带像素级标注的切片（来自5个不同医学中心），用于零样本迁移评估。
- 特征提取：UNI（ViT-L/16, 1024维），128μm分辨率。

### Benchmark与对比方法
- 对比基线：**ABMIL**（单独训练）的注意力图定位性能。
- 本文方法：**Sheaf-MIL-Loc**（联合训练 + 注意力条件一致性）。
- 消融实验：**两阶段消融**——冻结分类器在ABMIL值，仅训练层头；以及**朴素特征相似性一致性**变体。

### 评估指标
- 小块级AUC（per-slide patch-level AUC）。
- Prec@50（top-50小块精确率）。
- 统计：均值±标准差（跨切片）。

## 4. 资源与算力

论文**未明确说明**使用的GPU型号、数量及训练总时长。仅提到优化器超参数和训练epoch数（40个epoch，batch size=1）。因此无法直接评估算力消耗。

## 5. 实验数量与充分性

### 主要实验组
1. **Camelyon16定位性能**：单个种子下的49张肿瘤测试切片，报告Δ AUC、Attention AUC、Prec@50。
2. **两阶段消融**：验证联合训练的必要性。
3. **朴素特征相似性一致性对比**（仅文字描述，未在表中给出具体数值，只提Δ AUC=0.575）。
4. **Camelyon17零样本迁移**：44张可用肿瘤切片（从50张中过滤），报告Δ AUC和Attention AUC均值±标准差。
5. **诊断解释分析**：计算Δ与多种参考量的Spearman相关性（特征模长、邻居余弦相似度、到异类标签的跳数、肿瘤标签）。

### 充分性评价
- **积极面**：包含主要数据集上的性能报告、关键消融、外部验证，验证了方法核心论点。
- **不足**：
  - 仅与ABMIL单一基线对比，未与CLAM、DSMIL、TransMIL等方法在同一UNI特征协议下比较（作者承认此为未来工作）。
  - 缺少多种子（multi-seed）方差估计，仅报告单个种子结果。
  - 对超参数（秩r=8, k=8, 边特征维度d'=64）没有敏感性分析。
  - 朴素特征相似性一致性对比的数值未在正式表格中出现，仅文本提及。
  - 实验虽然逻辑完整，但覆盖范围可进一步扩展。

## 6. 主要结论与发现

1. **注意力条件一致性**有效：使得层不一致性场定位AUC达到0.940。
2. **联合训练提升分类器注意力**：注意力定位AUC从0.717跃升至0.953（提升0.236）。
3. **两阶段消融证明增益源于联合优化**：冻结分类器时Δ AUC仅0.727，注意力无变化。
4. **零样本迁移能力强**：Camelyon17上Δ AUC 0.932±0.083，Attention AUC 0.955±0.099，接近域内结果。
5. **两个定位信号互补**：注意力图和不一致场在相同诊断区域激活，由不同数学原理驱动，可提供双重解释。

## 7. 优点

- **方法论创新**：将细胞层引入WSI弱监督定位，提出注意力条件一致性损失，有效对齐分类器注意力和拓扑不一致性。
- **解释性强**：生成两个互补的定位图，增强临床可信度。
- **训练效率高**：在标准ABMIL+UNI基础上仅增加少量参数（低秩层），可端到端训练。
- **迁移性好**：无需重新训练即可泛化到不同采集协议的Camelyon17数据集。
- **实验设计严谨**：通过两阶段消融明确排除了替代解释，证实联合训练的必要性。

## 8. 不足与局限

- **基线对比不充分**：仅对比ABMIL，缺少与CLAM、DSMIL、TransMIL等流行MIL方法的比较，无法充分证明方法的相对优势。
- **统计稳健性不足**：仅报告单次种子结果，缺乏多种子方差分析；定性结果中的代表性病例图（Fig.1）有主观选择之嫌。
- **超参数敏感性未知**：秩r、邻居数k、边特征维度d'均固定为某一值，未进行敏感性分析。
- **朴素特征相似性一致性对比未正式报告**：其Δ AUC=0.575仅在文本中提到，未列入表格，削弱了消融的透明度。
- **实际应用限制**：方法依赖预训练基础特征（UNI），若更换特征提取器可能需要重新训练；且仅在淋巴结转移癌数据集上验证，需推广至其他组织类型和病灶（如有丝分裂、分级癌等）。
- **计算资源未披露**：难以评估可复现性和资源需求。

（完）
