---
title: "SegMix:Shuffle-based Feedback Learning for Semantic Segmentation of Pathology Images"
title_zh: SegMix：基于打乱的反馈学习用于病理图像的语义分割
authors: "Zhiling Yan, Sicheng Chen, Tianyi Zhang, Nan Ying, Yanli Lei, Guanglei Zhang"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15777v1"
tags: ["query:cellseg"]
score: 9.0
evidence: 病理图像的语义分割
tldr: 本研究针对病理图像分割标注成本高的问题，提出了SegMix——一种基于打乱与反馈学习机制的弱监督分割方法。通过在图像块级别进行打乱并根据模型反馈自适应调整策略，SegMix能够生成更高质量的伪分割掩膜，并在多个数据集上超越现有技术性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15777v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the proposed method. The model adaptively adjusts the schedulers in the left blue box based on the feedback from previous learning. The patch size scheduler and shuffle ratio scheduler regulate the patch size and shuffle ratio. Then, relation patches are in-place shuffled across the same batch, maintaining their relative positions the same in images. Lastly, the mixed and original images are fed into the Weekly Supervised Segmentation Network for twins’ steps training in the green boxes.\", \"page\": 2, \"index\": 1, \"width\": 1068, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2604.15777v1/fig-002.webp\", \"caption\": \"Table 1: Dice score (%) and IoU (%) of methods for weakly supervised semantic segmentation. Ours w/o FL denotes the proposed method without the feedback learning. The best results are in bold.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 394}]"
motivation: 病理图像像素级标注成本高昂，限制了深度学习在医学图像分割中的应用。
method: 提出基于shuffle和反馈学习的SegMix框架，通过图像块重排与反馈调整策略生成高质量伪分割掩膜。
result: 在三个不同数据集上，SegMix的性能优于多种现有方法。
conclusion: 研究表明，SegMix能有效缓解病理图像中伪标签质量低的问题，为病理图像的弱监督语义分割提供新途径。
---

## 摘要
分割是计算病理学中的关键任务，因为它能够识别受疾病或异常生长影响的区域，对于诊断和治疗至关重要。然而，获取高质量的像素级监督分割数据需要经验丰富的病理学家付出很大的工作量，从而限制了深度学习的应用。为克服这一挑战，通过将标签条件放宽为图像级分类标签，可以使用更多的数据并适用于更多场景。其中一种方法是利用类别激活图（Class Activation Map, CAM）在仅有图像级标签的情况下为语义分割生成伪像素级标注。然而，该方法无法充分挖掘病理图像的关键特征，只能识别出较小的区域，不足以生成高质量的伪掩码。本文提出了一种受课程学习（curriculum learning）启发的基于打乱的反馈学习新方法，用于生成更高质量的伪语义分割掩码。具体来说，我们在病理图像的补丁（patch）层面进行打乱操作，模型根据前一轮学习的反馈自适应地调整打乱策略。实验结果表明，在三个不同数据集上，我们的方法优于当前的最新方法。

## Abstract
Segmentation is a critical task in computational pathology, as it identifies areas affected by disease or abnormal growth and is essential for diagnosis and treatment. However, acquiring high-quality pixel-level supervised segmentation data requires significant workload demands from experienced pathologists, limiting the application of deep learning. To overcome this challenge, relaxing the label conditions to image-level classification labels allows for more data to be used and more scenarios to be enabled. One approach is to leverage Class Activation Map (CAM) to generate pseudo pixel-level annotations for semantic segmentation with only image-level labels. However, this method fails to thoroughly explore the essential characteristics of pathology images, thus identifying only small areas that are insufficient for pseudo masking. In this paper, we propose a novel shuffle-based feedback learning method inspired by curriculum learning to generate higher-quality pseudo-semantic segmentation masks. Specifically, we perform patch level shuffle of pathology images, with the model adaptively adjusting the shuffle strategy based on feedback from previous learning. Experimental results demonstrate that our proposed approach outperforms state-of-the-arts on three different datasets.

---

## 论文详细总结（自动生成）

# SegMix：基于打乱的反馈学习用于病理图像弱监督语义分割 — 深度总结

---

## 一、研究背景与核心问题

- **研究动机**：病理图像语义分割需要高质量、像素级的标注数据，而这类标注需由专业病理学家完成，成本高、效率低。  
- **问题核心**：在只有图像级标签的情况下，如何通过弱监督方式生成高质量的伪分割掩膜并提高分割模型的性能。  
- **现有挑战**：传统弱监督方法（如通过类激活图 CAM）多针对自然图像设计，无法充分捕捉病理图像中细粒度特征（细胞结构、组织分布、染色扰动等），导致伪标签区域过小或不准确。  
- **研究目标**：设计一种能在弱监督条件下自适应学习病理图像局部与全局特征的机制，提高伪分割掩膜质量，并实现多尺度语义分割。

---

## 二、方法论：SegMix 框架与算法原理

### 1. 核心思想
- 提出 **SegMix** 框架，基于 **图像块（patch）级打乱（shuffle）与反馈学习（feedback learning）** 的组合策略，以实现从粗到细的自适应训练。
- 设计思路源于 **课程学习（Curriculum Learning）**，模型从简单特征（大块、低打乱度）逐步转向复杂特征（小块、高打乱度）。

### 2. 主要组成模块
- **反馈学习模块（Feedback Learning Module）**：  
  - 包含两个调度器：
    - *Patch Size Scheduler*：决定每阶段划分的补丁大小，从大到小变化。
    - *Shuffle Ratio Scheduler*：控制打乱比例，随训练进展从低到高。
  - 模型依据训练反馈（损失值是否低于阈值）自动调整补丁大小与打乱比例，使学习过程自适应。

- **打乱补丁模块（Shuffle Patches）**：  
  - 将每张病理图像按大小 p 划分为多个补丁 \( P_i \)，并按比例 f 打乱同一批次中相同位置的补丁。
  - 打乱公式：  
    \( P_{mix,i} = M ⊙ P_{F,i} + (1−M) ⊙ P_{R,i} \)，其中 M 为二值掩膜（标记固定或打乱位置）。  
  - 这种随机打乱在不同样本间引入关联，强化模型对跨样本组织特征的学习。

- **像素相关模块（Pixel Correlation Module, PCM）**：  
  - 改进 CAM 的一致性预测，通过邻域像素的余弦相似度加权融合，提升伪标签的连贯性与边界准确性。

### 3. 学习机制总结
- 模型在每一阶段：
  1. 判断损失是否低于阈值。
  2. 若效果良好（低损失），则缩小补丁尺寸、提高打乱比例。
  3. 逐阶段实现从粗到细的多尺度学习。
- 这种反馈控制使模型能够动态地在不同尺度下挖掘局部细节与全局结构。

---

## 三、实验设计与评估指标

### 1. 数据集
- **ROSE**（胰腺细胞病理图像，含正常与癌变样本）。  
- **WBC**（白血细胞分类图像，含多种细胞类型：嗜酸性粒细胞、单核细胞等）。  
- **MARS**（胃组织图像，含正常及两类腺癌样本）。  

### 2. 对比方法（Benchmark）
- 与当前主流弱监督方法对比：
  - CAM（Class Activation Map）
  - SEAM（Self-supervised Equivariant Attention Mechanism）
  - CPN（Complementary Patch Network）

### 3. 评估指标
- **Dice 系数（DSC）** 与 **IoU（Intersection over Union）**
  - 均衡衡量分割掩膜的重叠精度与完整度。

---

## 四、资源与算力配置

- **硬件环境**：  
  - 一台 Ubuntu 服务器，配置为 *Intel Xeon Platinum 8350C CPU* + *2×NVIDIA RTX3090 GPU*（每次实验仅使用 1 块 GPU）。  
- **软件环境**：  
  - PyTorch 1.10.0 + CUDA 11.3 + Python 3.8。  
- **训练细节**：  
  - 批量大小 4，学习率 0.0001（部分数据集稍调低）。
  - 迭代 8 次，未给出具体训练时长。  
- 若对算力消耗进行量化分析，论文仅给出环境配置，**未报告训练时间与显存占用**。

---

## 五、实验数量与充分性分析

- **主要实验类型**：
  1. 三个公开数据集的综合性能对比实验。  
  2. **消融实验**：考察“去除反馈学习模块（w/o FL）”及不同打乱策略（Group、Back）的影响。  
  3. **可视化分析**：展示各方法的分割结果与错误区域。

- **实验充分性**：
  - 实验覆盖多种组织、细胞类型，验证方法的通用性。  
  - 对基线与自己方法的不同配置均做对比，实验设计较为系统。
  - 数据集划分（train:val:test = 7:1:2）合理，保证客观性。  
  - 尽管迭代次数较少（8 次），但性能差异显著，**实验验证充分且公平**。

---

## 六、主要结论与研究发现

- SegMix 在三个数据集上均实现 **显著性能提升**：
  - ROSE：DSC ↑至 60.7%，IoU ↑至 50.2%。  
  - WBC：DSC ↑至 41.5%，IoU ↑至 29.4%。  
  - MARS：DSC ↑至 40.6%，IoU ↑至 31.3%。  
- 打乱模块单独使用即可提升性能；**加入反馈学习后性能进一步提升**（DSC +6.9%，IoU +4.8%）。  
- 该方法能在仅有图像级标签的条件下，生成高质量伪分割掩膜并捕获多尺度病理特征。  
- 实验证明反馈学习机制使模型能自动调节混合策略，提高适应性与泛化能力。

---

## 七、主要优点与创新亮点

- **算法创新**：
  - 首次将“图像打乱”与“反馈学习调度”结合用于病理图像弱监督分割。
  - 模型具备自适应课程学习特性，可根据学习阶段调整视觉粒度。
- **理论可解释性强**：
  - 解释了局部与全局关系、跨样本特征交互在病理图像中的作用。
- **实验设计合理**：
  - 各模块效果单独验证，消融结果清晰，实验公平。
- **通用性与可拓展性**：
  - 可扩展至其他医学图像分割场景（作者计划未来研究 3D 医学图像）。

---

## 八、不足与局限性

- **训练资源未充分说明**：未披露训练时长、显存或计算代价，无法量化效率。  
- **仅限二维病理图像**：框架尚未验证在 3D 医学图像或全切片图像上的性能。  
- **反馈机制依赖阈值设定**：阈值 T 与增长因子 α 为人工预设，可能影响稳定性。  
- **伪标签质量评估不足**：论文主要依靠分割指标，而非直接评估伪标签的可解释性与临床价值。  
- **模型复杂度与可复现性**：多调度器与随机打乱机制可能导致复现难度较高。

---

（完）
