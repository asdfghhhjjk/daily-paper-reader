---
title: Cell Instance Segmentation via Multi-Task Image-to-Image Schrödinger Bridge
title_zh: 通过多任务图像到图像 Schrödinger Bridge 的细胞实例分割
authors: "Hayato Inoue, Shota Harada, Shumpei Takezaki, Ryoma Bise"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12318v1"
tags: ["query:cellseg"]
score: 10.0
evidence: 通过多任务薛定谔桥在PanNuke数据集上进行细胞实例分割
tldr: 本文针对传统细胞实例分割中缺乏全局结构约束的问题，提出了基于薛定谔桥的多任务图像到图像生成框架，将分割任务表述为分布变换过程，并结合反向距离图的边界监督与确定性推理，在PanNuke和MoNuSeg数据集上实现了无须SAM预训练和后处理的优异性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12318v1/fig-001.webp\", \"caption\": \"Fig. 5. Joint distributions of cell size and circularity for the ground truth (GT), the proposed method, and CellViT variants on the PanNuke dataset. The proposed method produces a distribution closer to the GT, suppressing morphologically atypical cell instances.\", \"page\": 6, \"index\": 1, \"width\": 533, \"height\": 232}, {\"url\": \"assets/figures/arxiv/2604.12318v1/fig-002.webp\", \"caption\": \"Fig. 2. Overview of the proposed Multi-Task Image-to-Image SB framework.\", \"page\": 2, \"index\": 2, \"width\": 474, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2604.12318v1/fig-003.webp\", \"caption\": \"Fig. 4. Transformation process at time step.\", \"page\": 5, \"index\": 3, \"width\": 1072, \"height\": 232}]"
motivation: 传统细胞实例分割方法受限于确定性预测和后处理，缺乏对实例掩码全局结构的显式约束。
method: 提出一种多任务图像到图像的薛定谔桥框架，通过反向距离图引入边界感知监督并采用确定性推理。
result: 在PanNuke和MoNuSeg数据集上取得与主流方法相当或更优的性能，尤其在训练数据有限时表现出较强的鲁棒性。
conclusion: 该研究表明基于薛定谔桥的图像到图像生成框架在细胞实例分割中具有有效性和鲁棒性。
---

## 摘要
现有的细胞实例分割流程通常将确定性预测与后处理相结合，这对实例掩膜的全局结构只施加了有限的显式约束。在本文中，我们提出了一种多任务的图像到图像 Schrödinger Bridge 框架，将实例分割表述为基于分布的图像到图像生成问题。通过反向距离图实现了边界感知监督，并采用确定性推理以产生稳定的预测。基于 PanNuke 数据集的实验结果表明，该方法在不依赖 SAM 预训练或额外后处理的情况下，获得了具有竞争力或更优的性能。在 MoNuSeg 数据集上的附加实验结果显示，该方法在训练数据有限的情况下仍具有较强的鲁棒性。这些发现表明，基于 Schrödinger Bridge 的图像到图像生成为细胞实例分割提供了一种有效的框架。

## Abstract
Existing cell instance segmentation pipelines typically combine deterministic predictions with post-processing, which imposes limited explicit constraints on the global structure of instance masks. In this work, we propose a multi-task image-to-image Schrödinger Bridge framework that formulates instance segmentation as a distribution-based image-to-image generation problem. Boundary-aware supervision is integrated through a reverse distance map, and deterministic inference is employed to produce stable predictions. Experimental results on the PanNuke dataset demonstrate that the proposed method achieves competitive or superior performance without relying on SAM pre-training or additional post-processing. Additional results on the MoNuSeg dataset show robustness under limited training data. These findings indicate that Schrödinger Bridge-based image-to-image generation provides an effective framework for cell instance segmentation.

---

## 论文详细总结（自动生成）

# 论文总结：通过多任务图像到图像 Schrödinger Bridge 的细胞实例分割

---

## 一、研究背景与核心问题

- **研究动机**：  
  细胞实例分割是病理图像分析中的基础任务，用于定量病理、药物筛选和表型分析。  
  传统方法（如 U-Net、CellPose、StarDist 等）依赖确定性预测加复杂的后处理（如 watershed、形态学操作）来区分相邻细胞。此类方法存在以下问题：  
  - 对数据集特性和超参数敏感，鲁棒性较差。  
  - 缺乏对生成掩膜的**全局形态结构约束**，易产生不合理的形态（如细胞融合或碎片化）。  

- **研究目标**：  
  作者旨在用**分布建模**思路替代确定性分割，通过学习从输入图像到实例掩膜的“有效映射分布”，以生成**形态学合理**且**无需后处理**的分割结果。

---

## 二、方法论：多任务图像到图像 Schrödinger Bridge（SB）

### 1. 核心思想
- 将细胞实例分割视作**图像到图像的分布变换**任务，而非逐像素分类。
- 利用 **Schrödinger Bridge (SB)** 理论，将输入图像分布（源态）与输出掩膜分布（目标态）通过**概率传输过程**显式连接。
- 通过在训练中引入 **反向距离图（Reverse Distance Map）** 的多任务学习，增强边界感知。

### 2. 关键技术结构
- **输入输出构建：**
  - 输入  \( X_1 = (X_i, X_i) \in \mathbb{R}^{H \times W \times 6} \)
  - 输出  \( X_0 = (M_i, M_i, M_i, R_i, R_i, R_i) \)，其中  
    - \( M_i \) 为实例掩膜  
    - \( R_i \) 为反向距离图  
  - 通过“按通道复制”匹配输入输出维度以适配 SB 定义。

- **中间状态采样公式：**
  \[
  q(X_t|X_0,X_1) = \mathcal{N}(X_t ; \mu_t, \Sigma_t)
  \]
  其中 \(\mu_t\) 和 \(\Sigma_t\) 的解析表达式确保可直接采样。

- **训练目标函数：**
  \[
  L = ||\epsilon_\theta(X_t, t) - \frac{X_t - X_0}{\sigma_t}||^2
  \]
  用以学习去噪得分（score function），实现从输入图像到目标掩膜的**结构保持**生成。

- **反向距离图监督：**  
  利用反向归一化欧式距离，使靠近边界的像素具有更高响应，实现边界敏感的分割。

- **确定性推理策略：**  
  推理阶段不加入随机噪声，而沿 SB 的**期望轨迹**反向传播，确保预测稳定一致。最终根据掩膜通道平均值与阈值 0.5 决定输出，无需任何 heuristic 后处理。

---

## 三、实验设计

### 1. 数据集与场景
| 数据集 | 场景 | 特点 |
| :-- | :-- | :-- |
| **PanNuke** | 大规模 | 7,904 张图像，19 种组织类型，189,744 个细胞 |
| **MoNuSeg** | 小数据 | 30 张训练图像，14 张测试图像，7 个器官 |

目标：验证方法在数据充足与数据稀少两种条件下的表现。

### 2. 对比方法
- **基线模型：**
  - U-Net（单任务）
  - U-Net+R（多任务带反向距离图）
- **最新方法：**
  - CellViT（含 Segment Anything Model 预训练）
  - CellViT+proc（附加后处理版本）
- **本文方法：**
  - Multi-Task Image-to-Image SB（无预训练，无后处理）

### 3. 评价指标
- **bPQ**（binary Panoptic Quality）= SQ × DQ  
- **Precision、Recall、F1-score**（基于细胞质心匹配）  
- 定量和可视化分析结合（生成过程、形态分布分析）。

---

## 四、资源与算力

- **计算平台：** NVIDIA **RTX A6000 GPU**
- **时间消耗：**
  - 推理平均 **3.5 秒/patch（T=50 步）**
  - 对比 CellViT 的 0.35 秒
- **训练细节：**
  - Adam 优化器，初始学习率 \(5\times10^{-5}\)，共 300,000 次迭代  
  - 应用 EMA 平滑参数  
  - 使用 ImageNet 上预训练的 ADM 作为初始化。

---

## 五、实验充分性与公平性

- **实验数量：**
  1. **两种数据规模场景**（PanNuke / MoNuSeg）  
  2. **四个基线方法**对比  
  3. **消融实验 Table II**：单任务（掩膜 / 反向距离图） vs. 多任务联合。  
  4. **形态特征分布分析**（大小与圆度联合分布图）。  

- **实验充分性：**
  - 涵盖了不同数据规模、不同任务设计，并与当前最强方法（CellViT + SAM）直接比较。
  - 采用交叉验证并报告平均结果，具备较全面与客观性。

---

## 六、主要结论与发现

- 通过 SB 分布建模，模型可生成**形态合理且空间一致**的细胞掩膜。
- 在 **PanNuke** 上：
  - 最高 bPQ = **0.6362**，超过所有对比，包括 SAM 预训练 + 后处理的 CellViT+proc。
- 在 **MoNuSeg** 上：
  - 在小数据场景中仍保持 bPQ≈0.60，与 CellViT+proc 持平。
  - 在 F1 和 Precision 上仍为最高，表现出优异鲁棒性。
- 形态分布分析显示：
  - 生成结果的尺寸–圆度分布更贴近真实 GT，异常形态明显减少。
- 因此，该方法在**无需预训练、无需后处理**的前提下实现了性能与稳定性兼顾。

---

## 七、优点与创新

- **创新性方法：**
  - 首次将 **Schrödinger Bridge** 应用于细胞实例分割的**图像到图像生成任务**。
  - 利用 **反向距离图**引入显式边界监督，缓解相邻细胞混并。
- **统一框架：**
  - 训练阶段多任务学习 → 推理阶段单纯生成，无需额外后处理管线。  
- **结构保持：**
  - SB 模型在整个生成过程中维持空间对应性，使细胞边界形成连续演化，结果稳定可复现。
- **性能卓越且鲁棒：**
  - 数据充足和有限两种场景皆优于或匹配 SOTA 模型。
- **形态一致性分析**提供了可信度验证，揭示生成结果的生物学合理性。

---

## 八、不足与局限

- **计算成本高**：推理速度（3.5 s/patch）较传统分割网络慢约十倍，难以直接应用于大规模全切片处理。
- **架构复杂度高**：SB 框架实现与优化门槛高，缺少开源工具链支持。  
- **实验范围有限**：  
  - 仅验证在 H&E 组织切片上的两个公开数据集。  
  - 尚未测试在其他显微成像模态（如荧光、相差显微镜）中的泛化性。
- **缺少多类别细胞类型实验**：主要关注实例分割而非分类任务。
- **推理可解释性待提升**：尽管提供了生成过程可视化，但内部能量传输机制未深入探讨。

---

**（完）**
