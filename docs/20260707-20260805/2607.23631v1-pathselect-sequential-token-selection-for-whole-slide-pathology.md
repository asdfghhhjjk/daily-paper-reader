---
title: "PathSelect: Sequential Token Selection for Whole Slide Pathology"
title_zh: PathSelect：用于全切片病理的序列化令牌选择
authors: "Jingzhi Chen, Landi He, Zehong Chen, Peihang Wu, Lijian Xu"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23631v1"
tags: ["query:path-xai-sel"]
score: 9.0
evidence: 顺序令牌选择以识别WSI中的重要区域
tldr: "千兆像素的全切片图像因空间特征弥散，使视觉语言模型面临序列长度计算瓶颈，现有空间采样或静态剪枝易丢失关键诊断证据。PathSelect将token剪枝转化为序列化选择，作为SlideChat的冻结插件，引入方差保持噪声门与可微分Soft Top-K算子调制信息流，配合对角注意力去噪器无语义泄露地恢复扰动表征。推理阶段去噪器完全剥离，仅凭训练的Scorer以确定性Hard Top-K执行自适应终止，极大降低token数量与推理延迟。在SlideBench上平均仅44.86个token即获74.00%准确率，较未压缩基线减少约36.6倍空间token，并全面超越采样方法，彰显了学习式路由在计算病理学中的潜力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23631v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1709, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23631v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1645, \"height\": 526, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23631v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1623, \"height\": 1209, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23631v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1592, \"height\": 1551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23631v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1685, \"height\": 645, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23631v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1711, \"height\": 777, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23631v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 831, \"height\": 450, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23631v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 514, \"label\": \"Table\"}]"
motivation: 全切片图像gigapixel导致VLM序列过长，现有剪枝易丢失弥散的弱信号，需学习式路由保留关键证据。
method: PathSelect将token选择建模为序列过程，采用方差保持噪声门与可微Soft Top-K调制信息流，通过对角注意力去噪器无泄漏恢复，推理时仅Scorer做Hard Top-K终止。
result: "在SlideBench (TCGA)上仅用平均44.86个token（K=128），准确率74.00%，相对未压缩基线减少约36.6倍空间token，优于采样方法。"
conclusion: 学习式序列化token选择可大幅减少病理图像计算开销，同时保持高性能，为高效全切片图像理解提供新途径。
---

## 摘要
千兆像素的全切片图像（WSI）因其极长的序列长度，给视觉-语言模型（VLM）带来了根本性的计算瓶颈。现有方法主要依赖空间采样或无需训练的剪枝，但这可能稀释微弱但信息丰富的信号，导致由于病理线索的空间弥散特性而丢失关键的诊断证据。我们将WSI令牌剪枝重新定义为一种序列化选择过程，使模型能够自主地学习最优的路由策略，而非依赖静态的启发式方法。我们在此提出一个解耦的路由框架，该框架作为主动插件集成到完全预训练的SlideChat基础模型中，并保持切片编码器和大语言模型冻结不动。为了在训练过程中为非可微的剪枝操作提供连续梯度，我们引入了PathSelect。PathSelect采用方差保持噪声门，通过可微的Soft Top-K算子调制每块图像块的信息流，并配以一个对角注意力去噪器，该去噪器能够在不引入语义泄漏的情况下恢复受扰动的表示。在推理阶段，PathSelect模块被完全移除。仅依赖训练好的评分器，一个确定性的Hard Top-K算子执行自适应的、数据依赖的轨迹终止，从而大幅加速下游生成过程，且顺序令牌选择延迟极低。在最大约束K=128下，仅凭平均44.86个令牌的经验驱动，我们的框架在SlideBench（TCGA）上实现了74.00%的整体准确率，相对于未压缩基线平均实现了约36.6倍的空间令牌缩减，同时始终优于基于采样的对比方法。

## Abstract
Gigapixel Whole-Slide Images (WSIs) present a fundamental computational bottleneck for vision-language models (VLMs) due to extreme sequence lengths. Existing approaches predominantly rely on spatial sampling or training-free pruning, which risk diluting weak but informative signals, leading to the loss of critical diagnostic evidence due to the spatially diffuse nature of pathological cues. We reformulate WSI token pruning as a sequential selection process, enabling the model to autonomously learn an optimal routing strategy rather than relying on static heuristics. We herein propose a decoupled routing framework integrated as an active plugin into the fully pre-trained SlideChat base model, leaving both the slide encoder and large language model frozen. To provide continuous gradients for the non-differentiable pruning operation during training, we introduce PathSelect. PathSelect employs a variance-preserving noise gate to modulate each patch's information flow via a differentiable Soft Top-K operator, paired with a diagonal-attention Denoiser that recovers the perturbed representations without semantic leakage. At inference, the PathSelect module is entirely detached. Relying solely on the trained Scorer, a deterministic Hard Top-K operator executes adaptive, data-dependent trajectory termination, significantly accelerating downstream generative processing with exceptionally low sequential token selection latency. Driven by an empirical average of only 44.86 tokens under a maximum constraint of K = 128, our framework achieves 74.00% overall accuracy on SlideBench (TCGA), representing an approximate 36.6x spatial token reduction relative to the uncompressed baseline average while consistently outperforming sampling-based counterparts.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
*   **核心问题**：千兆像素级别的全切片病理图像（WSI）在输入视觉语言模型（VLM）进行诊断分析时，会生成极长（>10^5级）的视觉令牌序列，导致自注意力计算出现平方级的复杂度爆炸，带来巨大的计算开销和内存瓶颈。
*   **现状痛点**：
    *   **空间采样或启发式剪枝**：传统的空间压缩方法或基于静态指标（如注意力大小）的训练无关剪枝，容易遗漏占全片面积小于1%、但具有决定性诊断意义的微转移灶等稀疏病理信号。
    *   **静态与孤立决策**：现有方法多采用一刀切的静态保留预算，且独立评估每个图像块的去留，忽视了组织空间依赖和序列选择过程中的边际效用变化。
*   **整体含义**：本文旨在将WSI的令牌剪枝问题从静态、物理的丢弃策略，提升为一个可端到端学习、文本条件引导的**序列化自适应选择过程**，通过学习最优路由策略来动态保留稀疏但关键的诊断证据，从而在极大降低计算开销的同时，保持或提升诊断准确性。

### 2. 论文提出的方法论
*   **核心思想**：将令牌剪枝重新定义为一种序列化路由（Sequential Selection）范式。模型不再是简单地“丢弃”背景，而是通过一个可训练的轻量级指针解码器，逐步“挑选”出对回答当前病理问题最有用的信息区域，直到模型自行触发一个“停止”动作，从而实现自适应令牌长度分配。
*   **系统架构**：PathSelect作为一个解耦的插件模块，插入到**完全冻结**的视觉编码器与大语言模型（LLM）之间，仅需在单GPU上对PathSelect模块本身进行轻量级的单阶段训练，不干扰预训练多模态先验。
*   **关键技术细节与流程**：
    *   **序列化选择建模**：将选择过程建模为自回归决策。每一步，一个条件指针解码器结合历史选择轨迹和文本指令，为所有候选令牌打分，并通过一个可训练的“停止令牌”来决定是否终止选择动态保留最佳的K个令牌。
    *   **训练路径 - 可微分噪声门控**：为解决硬选择（Hard Top-K）操作不可导的问题，训练时引入了一条差分路径。
        *   **评分聚合**：聚合每一步的存活加权指针概率，形成一个连续的软评分向量。
        *   **可微Soft Top-K极化**：使用`SoftTopK`算子将软评分极化，逼近一个硬的0/1保留指示器。
        *   **方差保持噪声门**：根据极化得分 `α`，用公式 `˜x = √α * x + √(1-α) * ε` 对原始图像块特征进行加噪。被选中的高分区 `α≈1`，特征几乎无损；而未被选中的背景区 `α≈0`，特征被替换为纯高斯噪声，以此模拟训练中的“剪枝”效果。
        *   **对角注意力去噪器**：使用一个带对角注意力掩码的去噪器修复加噪带来的分布偏移。对角掩码**严格禁止令牌间信息交互**，防止语义信息从未剪枝区泄漏到剪枝区，确保了训练与推理行为的一致性。
    *   **推理路径 - 确定性硬选择**：推理时完全移除噪声门和去噪器。直接使用训练好的评分器，对序列化指针解码器的输出执行确定性`argmax`硬选择，直到触发停止令牌。
*   **优化目标**：联合优化标准的语言建模损失和一个长度惩罚项 `λ * (K/N)`，其中 `K` 是动态选择的令牌数，`N` 是总令牌数，以此鼓励模型在保证性能的前提下做极致的稀疏化。

### 3. 实验设计
*   **数据集/场景**：
    *   **主实验**：在SlideBench（基于TCGA）数据集上进行训练和评估，包含“微观特征”、“诊断推理”和“临床应用”三个子类别。
    *   **泛化性测试**：对SlideBench（基于BCNB）和WSI-VQA*这两个不同的数据集进行零样本泛化评估。
*   **对比基准**：
    *   **无压缩上界**：使用完整WSI令牌的SlideChat模型。
    *   **计算量匹配的专用模型**：MedDr、Quilt-LLaVA、LLaVA-Med。
    *   **通用商业大模型**：GPT-4o。
    *   **随机选择**：作为底线基准（Random Baseline）。

### 4. 资源与算力
*   **硬件配置**：本文明确指出，所有训练均使用**单个NVIDIA A6000 GPU**完成。
*   **训练协议**：采用轻量级的**单阶段训练协议**，仅训练插入的PathSelect模块和线性投影层，不需对冻结的编码器或LLM做微调，极大节省了算力开销。文中未明确提及具体的训练总时长。

### 5. 实验数量与充分性
论文进行了充分和系统性的实验，以支撑其核心主张，主要包括：
*   **主结果对比**：1组大型对比实验，全面比较了PathSelect与多个强基线在多项任务上的性能。
*   **效率分析**：2组实验，分别从“性能-令牌效率”和“动态令牌分配下的精度稳定性”角度进行分析。
*   **定性可视化**：通过UMAP特征降维可视化，对比分析被选令牌与未选令牌在特征空间中的拓扑分布。
*   **消融研究**：约5-6组内部消融实验，严谨验证了核心组件的必要性。
    *   **去噪器注意力机制**：对比对角注意力与全局注意力。
    *   **评分聚合策略**：对比最终步指针、均匀求和、存活加权、完整形态（存活加权+深度衰减）四种策略。
    *   **超参数敏感性**：系统性扫描了长度惩罚系数（0.0到6.0的6个值）和目标最大预算（64、128、256）的影响。

### 6. 论文的主要结论与发现
*   **高效达成最优性能**：在最大保留128个令牌的约束下，模型实际平均仅用44.86个令牌，便在SlideBench (TCGA)上取得74.00%的准确率，超过所有同计算量级模型（如MedDr提升6.3%）。
*   **极致压缩比**：相比未压缩SlideChat基线实现了约**36.6倍**的空间令牌缩减。
*   **性能保真与超越**：在“微观特征”和“诊断”任务上，PathSelect恢复了未压缩模型99.68%和98.05%的性能；在“临床推理”任务上，其准确性（74.49%）甚至**超越了**全量输入的SlideChat上界（71.43%），证明有效的令牌筛选能通过滤除冗余噪声来增强复杂推理。
*   **动态选择的鲁棒性**：模型能根据样本内在的病理复杂性，自适应地调整保留令牌的数量，即便在极端压缩（如只保留4-5个令牌）下，其诊断准确率也能保持高度稳定。

### 7. 优点
*   **范式新颖**：将被动、物理的令牌丢弃转变为主动、智能的序列化选择，更贴合病理诊断中寻找稀疏决策证据的本质。
*   **解耦设计优雅**：作为即插即用的插件，无需更改或微调强大的预训练视觉和语言主干，极大降低了训练成本和对大算力的依赖。
*   **训练-推理桥接精巧**：通过方差保持噪声门和对角注意力去噪器的巧妙设计，完美解决了离散选择不可导的优化难题，同时避免了语义泄漏风险，确保了训练目标与推理行为高度一致。
*   **极致的高效与高性能平衡**：在单块消费级GPU上训练，仅用极少数令牌就实现了持平甚至超越完整模型的效果，实用性很强。

### 8. 不足与局限
*   **训练细节透明度**：论文未明确给出在单个A6000 GPU上的具体训练耗时，难以精确评估其训练成本。
*   **泛化性验证范围**：尽管进行了零样本泛化测试，但其主测试基准仍局限在部分公开病理数据集上。面对真实临床环境中更多样化的制片、染色和扫描仪差异时，其鲁棒性尚待进一步验证。
*   **应用局限性**：该方法高度依赖一个强大的、预训练好的视觉-语言基础模型（如SlideChat），其性能上限受限于该基座模型的能力。若基座模型本身存在偏差，PathSelect模块也无法纠正。
*   **序列选择开销**：文中强调了下游LLM推理的加速，但作为自回归过程的指针解码器本身会引入额外的推理延迟，尽管文中声称其延迟极低，但这一部分开销的定量分析相对LLM预填充的节省量是缺失的。

（完）
