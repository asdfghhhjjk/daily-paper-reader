---
title: "From Patches to Evidence Balls: Class-Conditioned Evidence Retrieval for Few-Shot Whole Slide Image Classification"
title_zh: 从切片小块到证据球：用于少样本全切片图像分类的类别条件证据检索
authors: "Di Zhang, Li Zhang, Jiashuai Liu, Junbo Lu, Zhi Zeng, Jiusong Ge, Chunze Yang, Yi Niu, Jian Chen, Kai He, Zeyu Gao, Chen Li"
date: 2026-08-02
pdf: "https://arxiv.org/pdf/2608.01104v1"
tags: ["query:path-xai-sel"]
score: 9.0
evidence: 类别条件下的证据检索选择诊断相关斑块
tldr: 少样本全切片图像分类面临稀疏线索组织与类证据混杂难题，现有全局聚合方法难以从有限标签学习可靠机制，且共享表示削弱了类别特异性推理。为此，本文设计EviBall框架，将局部patches语义空间分配成紧凑Evidence Balls，通过任务导向的语言或分子查询检索支持证据，产生类别条件表示。在形态学与分子端点等四个WSI任务上，EviBall在多种少样本场景下一致超越传统和视觉语言MIL方法，同时提供空间定位、类别特异的诊断证据，提升了模型可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01104v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 858, \"height\": 421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01104v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1775, \"height\": 895, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01104v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 763, \"height\": 872, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01104v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1608, \"height\": 980, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01104v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1803, \"height\": 1709, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01104v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 892, \"height\": 395, \"label\": \"Table\"}]"
motivation: 现有少样本WSI方法难以有效组织稀疏局部线索为可靠全局聚合，且共享表示压缩不同类别证据，限制类特异性推理和可解释性。
method: 提出EviBall，通过语义空间分配与中心细化将patches组织成Evidence Balls，并利用语言或分子引导的类别查询检索证据，生成类别条件表示。
result: 在四项形态学和分子任务上，EviBall在各类少样本设置下均显著超越现有MIL及视觉语言基线，并提供空间局部化证据。
conclusion: EviBall将少样本WSI分类重构为结构化证据检索与竞争，减少对全局聚合的依赖，提供可解释的类别特定证据。
---

## 摘要
全切片图像（WSI）分类是一项证据驱动的任务，其中诊断线索通常稀疏、空间结构化且依赖于类别。现有的多实例学习（MIL）与视觉-语言方法将大量切片小块特征聚合成一个全局的切片表示。在少样本监督下，有限的切片级标签使得学习一个可靠的聚合机制变得困难，该机制可以将稀疏的局部线索组织成紧凑且连贯的诊断证据。此外，共享的切片表示将支持候选类别及其替代类别的证据压缩到相同的特征中，限制了特定类别的推理和可解释性。为了解决这些问题，我们提出了 EviBall，一种用于少样本 WSI 分类的类别条件证据检索框架。EviBall 通过语义-空间分配和中心细化将局部小块组织成证据球，从而在弱监督下产生紧凑且空间连贯的证据单元。然后，它使用任务特定的类别查询——包括面向形态任务的语言引导查询和面向分子终点预测任务的分子引导查询——来检索支持性的证据球，并生成类别条件的证据表示，以进行直接的逐类预测。通过引入结构化的证据单元和任务相关的语义引导，EviBall 减少了对从稀缺的切片级标签中学习无约束的全局聚合机制的依赖。因此，它将少样本 WSI 分类重新表述为结构化的证据检索与候选类别之间的竞争。在四个面向形态和分子终点的 WSI 任务上进行的大量实验表明，EviBall 在不同的少样本设置下始终优于传统的和视觉-语言的 MIL 基线方法，同时为每个预测提供空间定位且类别特定的证据。

## Abstract
Whole slide image (WSI) classification is an evidence-driven task, where diagnostic cues are often sparse, spatially organized, and class-dependent. Existing MIL and vision-language methods aggregate a large pool of patch features into a single global slide representation. Under few-shot supervision, limited slide-level labels make it difficult to learn a reliable aggregation mechanism that organizes sparse local cues into compact and coherent diagnostic evidence. Moreover, a shared slide representation compresses evidence supporting a candidate class and its alternatives into the same feature, limiting class-specific reasoning and interpretability. To address these issues, we propose EviBall, a class-conditioned evidence retrieval framework for few-shot WSI classification. EviBall organizes local patches into Evidence Balls through semantic-spatial assignment and center refinement, yielding compact and spatially coherent evidence units under weak supervision. It then uses task-specific class queries, including language-guided queries for morphology-oriented tasks and molecular-guided queries for molecular endpoint prediction, to retrieve supporting evidence balls and produce class-conditioned evidence representations for direct class-wise prediction. By introducing structured evidence units and task-relevant semantic guidance, EviBall reduces the reliance on learning an unconstrained global aggregation mechanism from scarce slide-level labels. It therefore reformulates few-shot WSI classification as structured evidence retrieval and competition among candidate classes. Extensive experiments across four morphology-oriented and molecular endpoint WSI tasks demonstrate that EviBall consistently outperforms conventional and vision-language MIL baselines under diverse few-shot settings, while providing spatially localized and class-specific evidence for each prediction.

---

## 论文详细总结（自动生成）

## 论文核心问题与整体含义
- **问题背景**：全切片图像（WSI）分类依赖稀疏、空间组织且类别依赖的诊断证据，但现有 MIL 和视觉‑语言方法将大量小块特征聚合成一个全局的切片表示。
- **少样本困境**：在少样本监督下，有限的切片级标签难以学习可靠的聚合机制，导致稀疏诊断线索无法被有效组织。
- **证据混淆**：共享的全局表示会将支持不同候选类别的证据压缩到同一特征中，削弱了类别特异性推理与可解释性。
- **核心意图**：将少样本 WSI 分类重新表述为**结构化证据检索与竞争**，通过构建紧凑的“证据球”并利用任务相关的类别查询检索支持性证据，缓解对全局聚合的依赖。

## 方法论

### 总体框架
EviBall 包含三个主要部分：证据球构建、任务相关的类别语义查询生成、支持感知的类别条件证据检索与预测。

### 证据球构建
- **动机**：将孤立的补丁实例组织为紧凑、空间连贯的证据单元。
- **初始化**：在每个局部区域（大小为 \(M \times M\)）内选择语义最不相似的两个补丁作为种子，形成语义多样的候选证据球。
- **局部证据绑定**：利用局部语义‑空间亲和力（LSSA）计算证据球与邻近补丁的兼容性得分，通过球级 Softmax 和块级 Softmax 分别得到**支持质量** \(q_k\) 与聚合权重 \(\beta_{ki}\)，从而聚合语义中心 \(c_k^{agg}\) 和空间中心 \(s_k^{agg}\)。
- **空间门控中心更新**：根据空间中心位移 \(\Delta s_k\) 计算一个门控系数 \(\rho_k\)，融合初始中心与聚合中心以稳定语义中心更新。

### 类别语义查询生成
- **语言引导查询（形态任务）**：用 LLM 生成类别病理描述，经冻结的 TITAN 文本编码器提取 `[CLS]` 表征。
- **分子引导查询（分子终点预测）**：使用 scGPT 对目标基因和表达状态（低/高表达）进行编码，再通过适配器投影到统一空间。
- **查询投影**：通过源特定的适配器将不同来源的语义映射到证据球空间，形成最终类别查询 \(e_c\)。

### 类别条件证据检索
- **支持感知交叉注意力**：将标准化后的证据支持质量 \(u_k\) 作为附加偏置加入注意力分数中，使语义相关且支持度更高的证据球获得更大权重。
- **逐类预测**：从交叉注意力得到的类条件表征经前馈网络与类别特定预测头生成 logits，再 softmax 得到最终分类概率。

### 优化
- 交叉熵损失 \(L_{CE}\) 监督分类。
- **相同区域多样性正则化**：对同一局部区域初始化的两个证据球的语义相似度施加边际惩罚，鼓励互补模式。
- 总损失：\(L = (1-\lambda_{div}) L_{CE} + \lambda_{div} L_{div}\)。

## 实验设计
- **数据集与任务**：
  - **RCC‑Subtyping**：CPTAC‑CCRCC + DHMC‑RCC（肾细胞癌亚型）。
  - **BRCA‑Subtyping**：TCGA‑BRCA（乳腺癌亚型）。
  - **IMP‑Grading**：IMP‑CRS 2024（结直肠癌分级）。
  - **BCNB‑PR**：BCNB（乳腺癌分子终点预测，孕激素受体状态）。
- **少样本设置**：每个类别 1、2、4、8 个训练切片（H‑shot），5 折交叉验证，验证和测试集保持完整。
- **对比基准**：ABMIL、CLAM、TransMIL、RRT、DSMIL、ViLaMIL、FOCUS、MUSE。
- **公平性措施**：所有方法使用相同的冻结 CONCH v1.5 补丁编码器，需要文本语义的方法共用 TITAN 文本编码器，采用相同的数据划分和模型选择流程。

## 资源与算力
- 文中明确说明所有实验在**单块 NVIDIA RTX 4090 GPU** 上完成。
- 训练最多 200 个 epoch，采用早停（patience=10），未报告具体每折的训练时间。

## 实验数量与充分性
- **主实验**：4 个任务 × 4 种少样本设置（1/2/4/8‑shot），共 16 组对比，涵盖 8 种基线方法及 EviBall（含 EviBall‑LLM 与 EviBall‑RNA）。
- **消融研究**：在 RCC‑Subtyping 上对 5 个关键组件（证据球结构、类条件检索、空间距离惩罚、支持偏置、空间门控）进行 4 种不同 shot 设置下的消融，共 24 组实验。
- **定性分析**：提供乳腺癌异质性案例的类条件证据可视化。
- **附录额外实验**：文中提到附录包含对 \(\lambda_{div}\)、证据球数量 \(K\)、空间权重等超参数的敏感性分析，以及区域种子策略和 LLM 选择的消融，表明实验较为系统。
- **总体评价**：实验覆盖多任务、多 shot 数、基线和消融，设计公平，消融对比充分，定性分析增强了可解释性。

## 主要结论与发现
- EviBall 在绝大多数任务和少样本设置下取得最优或极具竞争力的性能，尤其在极端的 1‑shot 场景下优势明显。
- 结构化的证据球与类别条件检索是性能提升的核心，消融实验证实去除二者会造成最大降幅。
- 对于分子终点预测任务，分子引导查询（EviBall‑RNA）在多数设置下优于纯文本引导，说明任务相关语义更适配分子标签。
- 空间分布与支持质量的显式建模提升了证据检索的鲁棒性和可解释性（例如在混合型乳腺癌中成功区分 IDC 与 ILC 证据）。

## 优点
- **新颖的证据表示**：提出“证据球”将补丁组织为同时包含语义、空间和支持质量的单元，超越传统碎片化补丁。
- **任务自适应查询**：通过语言和分子两种模式生成类别查询，使框架可灵活适配形态学和分子终点的不同任务需求。
- **类别条件推理**：每个类别独立检索证据，避免了共享表示带来的证据混淆，并直接输出类特定证据，显著提升可解释性。
- **空间与支持感知**：LSSA 和空间门控加强空间连贯性，支持偏置使检索偏好高置信证据。
- **实验设计严谨**：大量公平对比、多 shot 评估和系统性消融，结论可信度高。

## 不足与局限
- **数据集覆盖**：实验仅基于 4 个公开数据集，任务类型有限，未在更多样化的器官、染色、标注场景下验证泛化性。
- **编码器依赖**：视觉编码器固定为 CONCH v1.5，文本编码器为 TITAN，分子模型为 scGPT，性能可能与这些预训练模型的表达力挂钩。
- **计算开销未详述**：证据球构建涉及局部区域划分和多次中心更新，文章未分析该过程的额外计算消耗及缩放性。
- **超参数敏感性**：证据球数量 \(K\)、局部区域大小 \(M\)、多样性边际 \(\delta\) 等仍需调优，且部分敏感性分析仅在附录中，正文未呈现调参成本。
- **分子引导的局限性**：分子查询依赖 scGPT 的固定 bin 划分，对于非二分类或多基因终点场景的扩展性尚不明确。
- **与其他证据方法的深度对比**：未与最近的一些结构化原型或多智能体推理方法（如文中提及的部分工作）进行直接比较。

（完）
