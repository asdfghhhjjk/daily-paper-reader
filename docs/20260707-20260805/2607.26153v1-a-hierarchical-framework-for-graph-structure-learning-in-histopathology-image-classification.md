---
title: A Hierarchical Framework for Graph Structure Learning in Histopathology Image Classification
title_zh: 组织病理学图像分类中图结构学习的层级框架
authors: "Sudipta Paul, Amanda W. Lund, Bülent Yener"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.26153v1"
tags: ["query:tme-evidence"]
score: 9.0
evidence: 通过两级图学习建模组织病理中细胞与组织的空间组织，用于全切片分类
tldr: "组织病理学诊断依赖细胞和组织的空间结构，但现有图方法多采用固定或启发式图结构，难以准确建模组织连接性。本研究提出G2*-Net，采用细胞级与图像级两级层次图表示，将图像级图结构学习建模为二阶双层优化问题，并借鉴DARTS的一步展开近似实现高效求解。在三个组织病理数据集上的实验表明，该方法显著提升了大规模图像分类性能。该框架为学习驱动的图结构优化提供了新路径，对数字病理分析具有重要应用价值。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26153v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 830, \"height\": 220, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26153v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1347, \"height\": 542, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26153v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 912, \"height\": 387, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26153v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 910, \"height\": 485, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26153v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1437, \"height\": 397, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26153v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 568, \"height\": 126, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26153v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 755, \"height\": 262, \"label\": \"Table\"}]"
motivation: 固定或启发式图结构无法准确反映组织连接，需可学习的图结构优化方法以提升组织病理图像分类性能。
method: "提出G2*-Net，通过细胞级图捕捉局部架构，并将图像级图结构学习建模为二阶双层优化，利用DARTS启发近似高效求解。"
result: "在三个不同类型组织病理数据集上，G2*-Net有效提升了大规模图像分类的准确性。"
conclusion: "G2*-Net通过可学习图结构优化，为组织病理图像分类提供了一种准确且高效的层次图建模方案。"
---

## 摘要
细胞和组织的空间组织在组织病理学图像中提供了重要的诊断线索。尽管基于图的方法可以建模这些关系，但许多方法依赖于固定的或启发式的图结构，可能无法准确表示组织连接性。在本工作中，我们提出了$G_2^*$-Net，一个优化的两级图学习框架，用于分类大规模组织病理学图像，如全切片图像（WSIs）或大的感兴趣区域（ROIs）。这里，$G_2$表示两级层次图表示，上标$*$表示由所提框架学习到的优化的图像级图结构。该方法首先将每个WSI或大ROI分割成图像块，在每个块内构建细胞级图以捕获局部组织结构，然后将每个块表示为可学习的图像级图中的一个节点。$G_2^*$-Net将图像级图结构学习建模为一个二阶双层优化问题，将图连接性学习与分类器优化分离，同时通过验证驱动的反馈将它们耦合起来。为了使该公式在计算上可行，我们采用了一种受DARTS启发的单步展开近似，以进行高效的超梯度估计。在三个不同的组织病理学数据集上的实验验证证明了我们所提方法的有效性。

## Abstract
The spatial organization of cells and tissues provides important diagnostic cues in histopathology images. Although graph-based approaches can model these relationships, many rely on fixed or heuristic graph structures that may not accurately represent tissue connectivity. In this work, we propose $G_2^*$-Net, an optimized two-level graph learning framework for classifying large-scale histopathology images, such as whole-slide images (WSIs) or large regions of interest (ROIs). Here, $G_2$ denotes the two-level hierarchical graph representation, and the superscript $*$ indicates the optimized image-level graph structure learned from the proposed framework. The method first divides each WSI or large ROI into image patches, constructs cell-level graphs within each patch to capture local tissue architecture, and then represents each patch as a node in a learnable image-level graph. $G_2^*$-Net formulates image-level graph structure learning as a second-order bilevel optimization problem, separating graph connectivity learning from classifier optimization while coupling them through validation-driven feedback. To make this formulation computationally practical, we adopt a DARTS-inspired one-step unrolled approximation for efficient hypergradient estimation. Experimental validation on three distinct histopathology datasets demonstrates the effectiveness of our proposed method.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：组织病理图像分析中，细胞和组织的空间组织结构具有重要诊断价值。现有基于图神经网络（GCN）的方法大多依赖固定或启发式定义的图连接规则（如距离阈值），这些规则可能无法准确捕捉上下文相关的组织交互模式，从而限制分类性能。
- **整体含义**：本文旨在用数据驱动的方式学习图像级的组织区域连接关系，而不是使用固定图结构。通过将图像级图结构学习形式化为一个二阶双层优化问题，在全局层面自适应地调整图形连接性，以服务于下游分类任务。

### 2. 论文提出的方法论
- **整体架构**：$G_2^*$-Net 采用连续两级层次图：
  - **块级细胞图**：将全切片图像（WSI）或大感兴趣区域（ROI）切分成重叠块，每个块内部构建细胞图（根据欧氏距离阈值连接细胞），并提取69维的固定图结构描述符（包括聚类系数、距离度量、谱特征、Voronoi图、Delaunay三角剖分、最小生成树及最近邻特征等）。
  - **图像级可学习图**：每个图像块作为图节点，节点特征为上述69维描述符。图像级图的邻接矩阵由可训练的MLP（后接Gumbel-Sigmoid重参数化）生成，使得连接性是连续且可微的。
- **双层优化公式**：
  - **下层问题**：给定图结构参数 $\psi$，优化GCN分类器参数 $\theta$，最小化训练损失 $\mathcal{L}_{\text{train}}$。
  - **上层问题**：通过验证损失 $\mathcal{L}_{\text{val}}$ 优化图结构参数 $\psi$，其中分类器参数 $\theta^*$ 是下层问题的解。
  $$ \min_{\psi} \mathcal{L}_{\text{val}}(\theta^*(\psi), \psi) \quad \text{s.t.} \quad \theta^*(\psi) = \arg\min_{\theta} \mathcal{L}_{\text{train}}(\theta, \psi) $$
- **高效近似求解**：直接求解二阶双层优化计算代价高。本文采用DARTS启发的一步展开近似：
  - 首先对分类器参数做一个虚拟更新：$\tilde{\theta}_t = \theta_{t-1} - \xi \nabla_{\theta} \mathcal{L}_{\text{train}}(\theta_{t-1}, \psi_{t-1})$。
  - 计算验证损失对图结构的直接梯度 $d^{(t)}_{\psi}$ 和对临时分类器参数 $\tilde{\theta}_t$ 的梯度 $v_t$。
  - 利用有限差分近似Hessian-向量积 $h^{(t)}_{\psi}$，以捕获图结构通过分类器参数变化产生的间接影响。
  - 引入可靠性系数 $\rho_t$ （基于直接梯度与矫正方向余弦相似度的映射），自适应调节二阶矫正项的权重，得到最终超梯度 $g^{(t)}_{\psi} = d^{(t)}_{\psi} - \rho_t \xi h^{(t)}_{\psi}$，用于更新图结构参数。
  - 随后再对分类器参数执行实际的优化步。
- **分类器设计**：GCN结合跳跃知识（Jumping Knowledge）风格的池化，将多层嵌入全局平均池化后拼接，送入MLP头进行图像级分类。

### 3. 实验设计
- **数据集与场景**：
  - **数据集I（Extended CRC）**：300张结直肠癌H&E染色图像，三类（正常、低级别、高级别），20×放大。
  - **数据集II（Colon Cancer）**：717个ROI，二分类（癌与非癌），40×放大。
  - **数据集III（Melanoma）**：153张黑色素瘤WSI，1072个标注ROI，二分类（淋巴细胞富集区与肿瘤富集区），统一到20×。
- **基准对比方法**：
  - **CNN方法**：ResNet50、MobileNet、InceptionV3、Xception、Vision Transformer（ViT）。
  - **图方法**：CGC-Net、HAT-Net、C2P-GCN。
- **评估协议**：数据集I采用三折交叉验证，数据集II和III均采用五折交叉验证（II按图像，III按患者分离折），报告平均准确率±标准差。

### 4. 资源与算力
- 论文**未明确提及**所用的GPU型号、数量或具体训练时间。
- 仅提及批量大小为20，总可训练参数量约0.86 M（分类器+邻接生成器）。从算法和问题的规模（节点数几百）推断，计算需求不高，但缺乏精确的算力信息。

### 5. 实验数量与充分性
- **实验组数**：
  - 主实验：在三个不同组织、不同分类任务的数据集上，与8种基线方法进行全面对比（表III）。
  - 消融实验（数据集I）：
    - 图像级连接性消融：将学习得到的邻接矩阵替换为基于余弦相似度的固定图（表IV）。
    - 块级特征组消融：分别单独使用细胞图特征、Voronoi特征、Delaunay三角剖分特征、最小生成树特征、核最近邻特征，对比完整特征集（表V）。
- **充分性与公平性**：实验覆盖了二分类和多分类任务、不同染色与组织类型，与主流CNN和图方法公平比较，且消融实验解释了框架各组成部分的贡献。整体设计较为全面和客观。

### 6. 论文的主要结论与发现
- $G_2^*$-Net 在所有三个数据集上均表现出强竞争力，在Extended CRC和Melanoma数据集上取得了最高准确率（96.33%、97.29%），在Colon Cancer数据集上接近最佳（97.03%）。
- 通过学习图像级图连接性（双层优化）明显优于固定相似度图，验证了自适应图结构学习的必要性。
- 块级的多组特征具有互补性，融合全部特征能获得最佳性能，其中核最近邻特征的个体贡献最大。
- 基于DARTS的一步展开近似有效解决了二阶双层优化的计算问题，使整个框架端到端可训练。

### 7. 优点
- **创新性的图学习范式**：首次在组织病理图像分类中将图像级图结构学习定义为二阶双层优化，明确分离了图结构学习和分类器训练，并利用二阶超梯度进行结构更新。
- **层次化与可学习结合的建模**：结合了局部手工特征（固定细胞图描述符）与全局可学习连接，兼顾了细胞水平的结构编码和组织区域间的自适应交互。
- **计算可行性设计**：通过一步展开近似、有限差分Hessian估计和自适应可靠性系数，使原本计算昂贵的二阶双层优化能在中等规模图上实际应用。
- **实验验证充分**：涵盖三种不同器官/疾病的组织病理数据集，与多种CNN及GCN基线对比，并提供了详尽的消融分析。

### 8. 不足与局限
- **局部图仍为固定规则**：块级细胞图仍依赖预定义的距离阈值构建，未与图像级图一同优化，可能限制了框架对局部细胞间关系的学习能力（文中将联合优化列为未来工作）。
- **计算复杂度对更大规模图像的制约**：图像级图以每个块为一个节点，当块数量极大时，邻接矩阵的二次复杂度可能成为瓶颈，论文仅在节点数不超过1400的场景下验证。
- **对比方法覆盖**：未与其他最新的自适应图学习或注意力机制方法（如基于transformer的WSI方法、其他双层图学习变体）进行对比。
- **缺乏资源与效率分析**：文中未提供训练时间、GPU占用等实际部署信息，不利于评估实用性。
- **泛化性局限**：仅在H&E染色图像上验证，且数据集规模相对有限，未探讨在其他染色方式或更大规模临床队列中的表现。

（完）
